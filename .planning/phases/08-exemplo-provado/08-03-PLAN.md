---
phase: 08-exemplo-provado
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - .template-tests/test_08_guia_vazamento.py
autonomous: true
requirements: [PRV-03]

must_haves:
  truths:
    - "Uma cópia recém-nascida (copier copy) NÃO contém apps/diarias nem nenhum arquivo com os bytes do fixture — nas DUAS variantes de incluir_app_exemplo"
    - "A árvore do template (git ls-files) não contém apps/diarias fora de .template-tests/fixtures/"
    - "As asserções são estruturais (diretório/bytes), nunca grep da palavra diarias — o teste sobrevive à Fase 9, quando docs/guia/ citará o app legitimamente"
    - "A suíte é descoberta pelo test_command padrão (python3 -m unittest discover -s .template-tests -p 'test_*.py')"
  artifacts:
    - path: ".template-tests/test_08_guia_vazamento.py"
      provides: "Teste negativo de vazamento de domínio (PRV-03), render leve sem Docker"
      contains: "TemporaryDirectory"
      min_lines: 80
  key_links:
    - from: ".template-tests/test_08_guia_vazamento.py"
      to: "copier (render leve)"
      via: "função render() no padrão de test_04_04/test_07_nav com --vcs-ref=HEAD"
      pattern: "vcs-ref"
    - from: ".template-tests/test_08_guia_vazamento.py"
      to: ".template-tests/fixtures/guia/apps/diarias/"
      via: "sha256 de conteúdo dos arquivos do fixture"
      pattern: "fixtures"
---

<objective>
Criar e deixar verde o teste negativo de vazamento (PRV-03): provar que o domínio do guia (`apps/diarias`) nunca chega ao template renderizado nem à cópia gerada — por asserções estruturais que não apodrecem quando a Fase 9 citar "diarias" em `docs/guia/`.

Purpose: PRV-03 e critério 2 do roadmap. O fixture instalado no banco de ensaio compartilhado é estado legítimo (Pattern 4 da pesquisa) — por isso este teste NUNCA olha o banco compartilhado: usa render leve próprio em tempdir, sem Docker, em segundos.
Output: `.template-tests/test_08_guia_vazamento.py` verde nas duas variantes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-exemplo-provado/08-RESEARCH.md
@.template-tests/test_04_04_optional_exemplo.py
@.template-tests/test_07_nav_extensao.py
@copier.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever test_08_guia_vazamento.py (render leve + asserções estruturais)</name>
  <files>.template-tests/test_08_guia_vazamento.py</files>
  <read_first>
    - .template-tests/test_04_04_optional_exemplo.py (função render() com --defaults e --vcs-ref=HEAD e dados não-default — copiar o padrão verbatim)
    - .template-tests/test_07_nav_extensao.py (impressao_subarvore(): sha256 de caminho relativo + \0 + conteúdo, ignorando __pycache__/.pyc)
    - copier.yml (confirmar `_exclude` cobrindo .template-tests — é a base do não-vazamento)
    - .planning/phases/08-exemplo-provado/08-RESEARCH.md (Pitfall 8 e exemplo "Teste negativo")
  </read_first>
  <action>
    Criar `.template-tests/test_08_guia_vazamento.py` (unittest stdlib, padrão dos módulos vizinhos), com constante `FIXTURE = ROOT / ".template-tests/fixtures/guia"` e três grupos de prova:

    1. **Cópia recém-nascida sem o domínio (2 variantes).** Função `render()` copiada do padrão de `test_04_04_optional_exemplo.py` (obrigatório `--vcs-ref=HEAD` — o repo tem a tag v0.2.0 publicada e sem a flag o Copier renderiza a tag) para `tempfile.TemporaryDirectory()`. Para `incluir_app_exemplo=True`: `(dest/"apps"/"diarias")` não existe E `sorted(p.name for p in (dest/"apps").iterdir())` == `["__init__.py", "exemplo"]` (conjunto EXATO, não só ausência). Para `incluir_app_exemplo=False`: lista == `["__init__.py"]`.
    2. **Nenhum byte do fixture na cópia.** Calcular sha256 SÓ DO CONTEÚDO (bytes puros, sem caminho — diferente de `impressao_subarvore`, que embute o caminho relativo e nunca colidiria entre árvores distintas; a pesquisa aponta essa pegadinha explicitamente) de todos os arquivos sob `FIXTURE`, ignorando `__pycache__`/`.pyc`, e de todos os arquivos da cópia renderizada; assertar interseção vazia. Assertar também que o conjunto de hashes do fixture não está vazio (guarda contra falso verde por fixture ausente). Nota operacional: `--vcs-ref=HEAD` renderiza o estado COMMITADO do repo — os arquivos do fixture da onda 1 (08-01/08-02) precisam estar commitados antes da primeira execução desta suíte, senão o grupo de interseção de bytes passa em vácuo (o GSD commita por plano; em execução manual, confirme com `git status` antes de rodar).
    3. **Template limpo.** `git ls-files` (subprocess, cwd=ROOT) não lista NENHUM caminho começando com `apps/diarias` nem contendo `/diarias/` fora de `.template-tests/fixtures/` — cobre a metade "não chega ao template" do PRV-03.

    Proibições (Pitfall 8): nenhuma asserção por grep textual da palavra "diarias" em conteúdo de arquivo da cópia; nenhum uso do banco de ensaio (`ensaio_django.sh`) — o módulo não pode nem importar/invocar o harness. Docstring do módulo explicando por que as asserções são estruturais e por que o banco compartilhado fica de fora.
  </action>
  <verify>
    <automated>python3 -m py_compile .template-tests/test_08_guia_vazamento.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile .template-tests/test_08_guia_vazamento.py` sai com código 0
    - `grep -F 'vcs-ref' .template-tests/test_08_guia_vazamento.py` encontra a flag HEAD no render
    - `grep -F 'ensaio_django' .template-tests/test_08_guia_vazamento.py` NÃO encontra nada (independência do banco compartilhado)
    - O arquivo contém as duas variantes de `incluir_app_exemplo` (True e False) e a comparação de interseção de hashes de conteúdo
    - `grep -F 'git' .template-tests/test_08_guia_vazamento.py` encontra a chamada a git ls-files
  </acceptance_criteria>
  <done>Suíte escrita no padrão das vizinhas, com render leve, asserções estruturais em 3 grupos e zero acoplamento ao banco de ensaio.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a suíte verde e provar a descoberta pelo test_command</name>
  <files>.template-tests/test_08_guia_vazamento.py</files>
  <read_first>
    - .template-tests/test_08_guia_vazamento.py (estado atual da Task 1)
    - .planning/config.json (test_command exato)
  </read_first>
  <action>
    Executar `python3 -m unittest discover -s .template-tests -p 'test_08_guia_vazamento.py' -v` a partir da raiz do repo e corrigir a suíte até verde (cada render leve leva segundos; sem Docker). Depois provar que a descoberta geral não desce no fixture: rodar o discover só em modo de coleta com o padrão amplo (`python3 -m unittest discover -s .template-tests -p 'test_08_guia*.py' -v`) e confirmar que NENHUM teste de `fixtures/guia/apps/diarias/tests/` foi coletado (os diretórios `fixtures/` e `guia/` não são pacotes — sem `__init__.py` — logo o discover não entra; se entrar, o conserto é remover `__init__.py` indevido criado nos planos de onda 1, nunca adicionar guards de import). Se um render falhar por estado do repositório (ex.: árvore suja), diagnosticar com a saída do copier — não relaxar asserções.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .template-tests -p 'test_08_guia_vazamento.py' -v</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s .template-tests -p 'test_08_guia_vazamento.py'` termina com OK (código 0) e >= 3 testes executados
    - A saída do discover com padrão `test_08_guia*.py` NÃO lista nenhum teste vindo de `fixtures/` (somente os do módulo novo)
    - Nenhuma asserção foi enfraquecida para passar (a lista exata de `apps/` e a interseção de hashes permanecem no código)
  </acceptance_criteria>
  <done>Teste negativo verde nas duas variantes, template provado limpo, e descoberta segura do unittest confirmada — critério 2 do roadmap atendido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture (.template-tests) → template/cópia gerada | a fronteira que ESTA suíte vigia: código de domínio não pode cruzá-la (vazamento = todo sistema gerado nasce contaminado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-P3-01 | Information Disclosure / Tampering | vazamento de apps/diarias para o gerado | mitigate | esta suíte é a mitigação: `_exclude` do copier.yml + prova estrutural por diretório e por bytes, nas 2 variantes |
| T-08-P3-02 | Repudiation (teste que apodrece) | asserções do negativo | mitigate | asserções estruturais, nunca grep de "diarias" (Pitfall 8) — o gate continua honesto na Fase 9 |
| T-08-P3-03 | Tampering (falso verde) | conjunto de hashes do fixture | mitigate | asserção de que o conjunto de hashes do fixture é não-vazio antes da interseção |
</threat_model>

<verification>
- Suíte verde standalone e coletável pelo test_command padrão
- Discover não desce em `fixtures/` (nenhum __init__.py nos níveis fixtures/guia/apps)
</verification>

<success_criteria>
- Critério 2 do roadmap verde: cópia recém-nascida sem `apps/diarias` e sem nenhum byte do fixture, nas duas variantes
- PRV-03 coberto também na direção do template (git ls-files limpo fora do fixture)
</success_criteria>

<output>
Create `.planning/phases/08-exemplo-provado/08-03-SUMMARY.md` when done
</output>
