---
phase: 08-exemplo-provado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .template-tests/fixtures/guia/apps/diarias/__init__.py
  - .template-tests/fixtures/guia/apps/diarias/apps.py
  - .template-tests/fixtures/guia/apps/diarias/models.py
  - .template-tests/fixtures/guia/apps/diarias/admin.py
  - .template-tests/fixtures/guia/apps/diarias/forms.py
  - .template-tests/fixtures/guia/apps/diarias/views.py
  - .template-tests/fixtures/guia/apps/diarias/urls.py
  - .template-tests/fixtures/guia/apps/diarias/migrations/__init__.py
  - .template-tests/fixtures/guia/apps/diarias/migrations/0001_initial.py
  - .template-tests/fixtures/guia/apps/diarias/management/__init__.py
  - .template-tests/fixtures/guia/apps/diarias/management/commands/__init__.py
  - .template-tests/fixtures/guia/apps/diarias/management/commands/seed_diarias.py
autonomous: true
requirements: [PRV-01]

must_haves:
  truths:
    - "O fixture contém um app Django completo `apps/diarias` (modelo, admin, forms, views, urls, migração, seed) espelhando arquivo a arquivo o app exemplo"
    - "O modelo Viagem é único, sem FK para nenhuma entidade, com status por TextChoices e HistoricalRecords() (D-01..D-04)"
    - "Nenhuma view do fixture é acessível sem login e nenhuma ordenação sai de entrada crua do request"
    - "A migração 0001_initial.py vive no fixture (determinística) e cria Viagem e HistoricalViagem"
  artifacts:
    - path: ".template-tests/fixtures/guia/apps/diarias/models.py"
      provides: "Modelo Viagem com StatusChoices e auditoria"
      contains: "HistoricalRecords()"
    - path: ".template-tests/fixtures/guia/apps/diarias/views.py"
      provides: "Listagem paginada com whitelist, modal 422/HX-Trigger, dashboard ORM"
      contains: "COLUNAS_ORDENACAO_PERMITIDAS"
    - path: ".template-tests/fixtures/guia/apps/diarias/urls.py"
      provides: "Rotas do app com namespace diarias"
      contains: 'app_name = "diarias"'
    - path: ".template-tests/fixtures/guia/apps/diarias/migrations/0001_initial.py"
      provides: "Migração inicial incluída no fixture (Pitfall 6)"
      contains: "HistoricalViagem"
    - path: ".template-tests/fixtures/guia/apps/diarias/admin.py"
      provides: "Admin com SimpleHistoryAdmin"
      contains: "SimpleHistoryAdmin"
    - path: ".template-tests/fixtures/guia/apps/diarias/management/commands/seed_diarias.py"
      provides: "Seed idempotente com dados pt-BR de viagem"
      contains: "get_or_create"
  key_links:
    - from: ".template-tests/fixtures/guia/apps/diarias/urls.py"
      to: ".template-tests/fixtures/guia/apps/diarias/views.py"
      via: "path() apontando para as cinco views"
      pattern: "views\\.(viagem_listar|dashboard|viagem_criar|viagem_editar|viagem_excluir)_view"
    - from: ".template-tests/fixtures/guia/apps/diarias/views.py"
      to: "templates/diarias/*.html (plano 08-02)"
      via: "render() com nomes de template do contrato"
      pattern: "diarias/(viagem_listar|_tabela_resultado|_form_modal|_confirmar_exclusao_modal|dashboard)\\.html"
    - from: ".template-tests/fixtures/guia/apps/diarias/migrations/0001_initial.py"
      to: ".template-tests/fixtures/guia/apps/diarias/models.py"
      via: "campos idênticos campo a campo (provado depois por makemigrations --check no 08-04)"
      pattern: "valor_diarias"
---

<objective>
Criar o backend do fixture `apps/diarias` (diárias e passagens) em `.template-tests/fixtures/guia/` — o código que o guia da Fase 9 vai ensinar. É o app exemplo re-instanciado no domínio de viagens: modelo único `Viagem`, admin, forms, views com listagem paginada/whitelist/modal 422/dashboard ORM, urls, migração inicial incluída e seed idempotente.

Purpose: PRV-01 — o código do exemplo completo existe como fixture antes do texto do guia. O fixture é material didático: nada além de D-01..D-04.
Output: 12 arquivos Python em `.template-tests/fixtures/guia/apps/diarias/` (sem templates e sem tests do app — plano 08-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-exemplo-provado/08-CONTEXT.md
@.planning/phases/08-exemplo-provado/08-RESEARCH.md

Fonte viva dos padrões (caminho contém chaves Jinja literais — é o nome real do diretório):
`apps/{% if incluir_app_exemplo %}exemplo{% endif %}/`

<interfaces>
Contrato fixado pelo planejamento (os planos 08-02/08-03/08-04 dependem destes identificadores EXATOS):

- App: pacote `apps.diarias`; `class DiariasConfig(AppConfig)` com `default_auto_field = "django.db.models.BigAutoField"`, `name = "apps.diarias"`, `verbose_name = "Diárias e Passagens"`.
- Modelo `Viagem` (models.py), campos:
  - `servidor = CharField("servidor", max_length=150)` (D-03 — texto simples, zero FK)
  - `destino = CharField("destino", max_length=150)`
  - `data_inicio = DateField("início")`
  - `data_fim = DateField("fim")`
  - `motivo = TextField("motivo")`
  - `valor_diarias = DecimalField("valor de diárias", max_digits=12, decimal_places=2, default=Decimal("0.00"), validators=[MinValueValidator(Decimal("0.00"))])`
  - `valor_passagens = DecimalField("valor de passagens", ...)` idem
  - `status = CharField("status", max_length=20, choices=StatusChoices.choices, default=StatusChoices.SOLICITADA)`
  - `criado_em = DateTimeField("criado em", auto_now_add=True)`; `atualizado_em = DateTimeField("atualizado em", auto_now=True)`
  - `history = HistoricalRecords()` (D-04)
- `class StatusChoices(models.TextChoices)`: `SOLICITADA = "SOLICITADA", "Solicitada"`; `APROVADA = "APROVADA", "Aprovada"`; `PAGA = "PAGA", "Paga"`; `CANCELADA = "CANCELADA", "Cancelada"` (D-02)
- urls.py: `app_name = "diarias"`; rotas: `""` → `viagem_listar_view` (name `viagem_listar`), `"dashboard/"` → `dashboard_view` (name `dashboard`), `"novo/"` → `viagem_criar_view` (name `viagem_criar`), `"<int:pk>/editar/"` → `viagem_editar_view` (name `viagem_editar`), `"<int:pk>/excluir/"` → `viagem_excluir_view` (name `viagem_excluir`)
- views.py: `HX-Trigger` de sucesso = `"viagemSalva"` (criar, editar e excluir); form inválido = `status=422`; `Paginator(qs, 10)`; dicionário `COLUNAS_ORDENACAO_PERMITIDAS`; default de ordenação `"-criado_em"`
- Templates referenciados (criados no 08-02): `diarias/viagem_listar.html`, `diarias/_tabela_resultado.html`, `diarias/_form_modal.html`, `diarias/_confirmar_exclusao_modal.html`, `diarias/dashboard.html`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelo Viagem, apps.py, admin, migração inicial e seed</name>
  <files>.template-tests/fixtures/guia/apps/diarias/__init__.py, .template-tests/fixtures/guia/apps/diarias/apps.py, .template-tests/fixtures/guia/apps/diarias/models.py, .template-tests/fixtures/guia/apps/diarias/admin.py, .template-tests/fixtures/guia/apps/diarias/migrations/__init__.py, .template-tests/fixtures/guia/apps/diarias/migrations/0001_initial.py, .template-tests/fixtures/guia/apps/diarias/management/__init__.py, .template-tests/fixtures/guia/apps/diarias/management/commands/__init__.py, .template-tests/fixtures/guia/apps/diarias/management/commands/seed_diarias.py</files>
  <read_first>
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/models.py (estilo de campo, verbose names minúsculos pt-BR, Decimal, Meta, __str__)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/apps.py
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/admin.py (padrão SimpleHistoryAdmin)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/migrations/0001_initial.py (estrutura EXATA a espelhar: HistoricalItemExemplo + ItemExemplo, opções do simple_history)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/management/commands/seed_exemplo.py (padrão de seed idempotente)
    - core/README.md (convenções 1 e 4: timezone.localdate, HistoricalRecords)
    - .planning/phases/08-exemplo-provado/08-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Criar a árvore `.template-tests/fixtures/guia/apps/diarias/` com o backend do modelo, espelhando o app exemplo no domínio de diárias e passagens do setor público (CFC). Decisões travadas: D-01 (modelo único, sem entidades relacionadas), D-02 (status via TextChoices sem transições), D-03 (servidor como CharField), D-04 (HistoricalRecords()).

    - `models.py`: `StatusChoices` e `Viagem` exatamente como no bloco `<interfaces>` do plano. Acrescentar `clean()` validando `data_fim >= data_inicio` com `ValidationError` em pt-BR no campo `data_fim` (mensagem na linha de "a data de fim não pode ser anterior à data de início") — é a validação didática que o modal 422 vai exibir. `Meta`: `ordering = ["-criado_em"]`, `verbose_name = "viagem"`, `verbose_name_plural = "viagens"`. `__str__` retornando servidor + destino. Decisão a critério do planner, registrada: SEM campo `criado_por` (o exemplo tem FK para AUTH_USER_MODEL; D-01/D-03 vetam entidade relacionada — a auditoria de quem alterou já vem do `history_user` do simple-history).
    - `apps.py`: `DiariasConfig` conforme `<interfaces>`.
    - `admin.py`: `class ViagemAdmin(SimpleHistoryAdmin)` registrado com `@admin.register(Viagem)`; `list_display = ("servidor", "destino", "data_inicio", "data_fim", "status", "valor_diarias", "valor_passagens")`, `list_filter = ("status",)`, `search_fields = ("servidor", "destino", "motivo")`, `ordering = ("-criado_em",)` — mesmo formato do admin do exemplo.
    - `migrations/0001_initial.py`: escrever À MÃO espelhando a estrutura do `0001_initial.py` do exemplo (Pitfall 6 da pesquisa — a migração SHIPA no fixture, nunca é gerada em runtime): `initial = True`, `dependencies = [migrations.swappable_dependency(settings.AUTH_USER_MODEL)]`, `CreateModel` de `HistoricalViagem` (com history_id/history_date/history_change_reason/history_type/history_user, opções ordering/get_latest_by e base `simple_history.models.HistoricalChanges`) e `CreateModel` de `Viagem` — campos IDÊNTICOS aos de models.py (choices por extenso, defaults Decimal, validators). Sem campo criado_por em nenhum dos dois CreateModel. O cabeçalho de comentário "# Generated by Django" pode ser omitido ou fixado — o 08-04 prova a consistência com `makemigrations diarias --check --dry-run`.
    - `management/commands/seed_diarias.py`: comando `seed_diarias` no padrão do `seed_exemplo.py`, porém IDEMPOTENTE por construção: lista fixa (sem random) de 12 a 16 viagens pt-BR do universo CFC (destinos brasileiros, motivos como fiscalização, plenária, capacitação; períodos derivados de `timezone.localdate()` com timedelta; valores Decimal realistas), inseridas via `Viagem.objects.get_or_create()` chaveado por (servidor, destino, data_inicio). Rodar duas vezes não pode duplicar registros (o banco de ensaio é reusado entre execuções — Pattern 4 da pesquisa).
    - `__init__.py` do app, de `migrations/`, de `management/` e de `management/commands/` vazios. NÃO criar `__init__.py` em `.template-tests/fixtures/`, em `fixtures/guia/` nem em `fixtures/guia/apps/` — esses diretórios não podem virar pacote, senão o `unittest discover` do test_command desce neles e tenta importar Django no host.
    - Nenhum arquivo com sufixo `.jinja` (anti-pattern da pesquisa: o fixture nunca passa pelo Copier).
  </action>
  <verify>
    <automated>python3 -m py_compile .template-tests/fixtures/guia/apps/diarias/models.py .template-tests/fixtures/guia/apps/diarias/apps.py .template-tests/fixtures/guia/apps/diarias/admin.py .template-tests/fixtures/guia/apps/diarias/migrations/0001_initial.py .template-tests/fixtures/guia/apps/diarias/management/commands/seed_diarias.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile` dos 5 arquivos .py não vazios sai com código 0
    - `grep -c 'HistoricalRecords()' .template-tests/fixtures/guia/apps/diarias/models.py` retorna 1
    - `grep -F 'name = "apps.diarias"' .template-tests/fixtures/guia/apps/diarias/apps.py` encontra a linha
    - `grep -F 'HistoricalViagem' .template-tests/fixtures/guia/apps/diarias/migrations/0001_initial.py` encontra o CreateModel histórico e `grep -F "name='Viagem'" ...` (aspas simples ou duplas) encontra o modelo concreto
    - `grep -F 'ForeignKey' .template-tests/fixtures/guia/apps/diarias/models.py` NÃO encontra nada (D-01/D-03: zero FK no modelo)
    - `grep -F 'get_or_create' .template-tests/fixtures/guia/apps/diarias/management/commands/seed_diarias.py` encontra ao menos 1 ocorrência e `grep -F 'random' seed_diarias.py` não encontra nada
    - `test ! -f .template-tests/fixtures/__init__.py && test ! -f .template-tests/fixtures/guia/__init__.py && test ! -f .template-tests/fixtures/guia/apps/__init__.py` sai com código 0
    - `find .template-tests/fixtures/guia -name '*.jinja'` não retorna nada
  </acceptance_criteria>
  <done>Backend de modelo completo: models/apps/admin/migração/seed compilam no host, espelham o exemplo e honram D-01..D-04 (modelo único, TextChoices, servidor CharField, HistoricalRecords, zero FK).</done>
</task>

<task type="auto">
  <name>Task 2: Forms, views (listagem/modal 422/dashboard ORM) e urls</name>
  <files>.template-tests/fixtures/guia/apps/diarias/forms.py, .template-tests/fixtures/guia/apps/diarias/views.py, .template-tests/fixtures/guia/apps/diarias/urls.py</files>
  <read_first>
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/views.py (INTEGRAL — 288 linhas: COLUNAS_ORDENACAO_PERMITIDAS, extrair_querystring_filtros, paginação, filtros getlist, busca Q, modal 422/HX-Trigger, detecção de fragmento HTMX, dashboard com annotate/aggregate e paleta)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/forms.py (widgets Tailwind do ModelForm)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/urls.py
    - .template-tests/fixtures/guia/apps/diarias/models.py (criado na Task 1 — campos e choices reais)
  </read_first>
  <action>
    Espelhar `views.py`, `forms.py` e `urls.py` do app exemplo no domínio de viagens, mantendo estrutura, comentários didáticos (traduzidos ao domínio) e TODAS as invariantes de segurança. Não inventar telas nem comportamento novo — cada desvio vira dívida na Fase 9.

    - `forms.py`: `class ViagemForm(forms.ModelForm)` sobre `Viagem` com todos os campos editáveis (servidor, destino, data_inicio, data_fim, motivo, valor_diarias, valor_passagens, status), widgets com as MESMAS classes Tailwind do `ItemExemploForm` e widgets de data no padrão do exemplo (respeitando datas DD/MM/AAAA do projeto).
    - `views.py`, espelho função a função do exemplo:
      - `COLUNAS_ORDENACAO_PERMITIDAS` mapeando chaves públicas → campos reais: servidor, destino, data_inicio, valor_diarias, valor_passagens, status, criado_em (com variantes descendentes, no mesmo formato do exemplo); ordenação SEMPRE via `COLUNAS_ORDENACAO_PERMITIDAS.get(ordem_param, "-criado_em")` — nunca `order_by` de entrada crua (mitigação T-08-P1-01).
      - `extrair_querystring_filtros(params, excluir=("pagina",))` idêntico ao do exemplo.
      - `viagem_listar_view`: `@login_required`; filtros `status` via `request.GET.getlist("status")` restritos a `StatusChoices.values`; busca `q` com `Q(servidor__icontains=...) | Q(destino__icontains=...) | Q(motivo__icontains=...)`; `Paginator(qs, 10)`; fragmento `diarias/_tabela_resultado.html` quando requisição HTMX (mesma detecção de header do exemplo), senão `diarias/viagem_listar.html`.
      - `viagem_criar_view` / `viagem_editar_view`: `@login_required`; GET renderiza `diarias/_form_modal.html`; POST inválido re-renderiza o modal com `status=422`; POST válido responde com `resposta["HX-Trigger"] = "viagemSalva"` no mesmo formato de resposta do exemplo (espelhar o código do exemplo, trocando `itemSalvo` → `viagemSalva`).
      - `viagem_excluir_view`: `@login_required`; GET renderiza `diarias/_confirmar_exclusao_modal.html`; POST exclui e responde com `HX-Trigger = "viagemSalva"`.
      - `dashboard_view`: `@login_required`; agregações 100% via ORM: (a) contagem e soma de `valor_diarias + valor_passagens` por `status` (annotate/aggregate, tratando None como o exemplo trata); (b) série mensal por `TruncMonth("data_inicio")` somando valores. Contexto e `json_script` no MESMO desenho do dashboard do exemplo (mesmos mecanismos de serialização; ids de json_script análogos renomeados ao domínio), incluindo a paleta da marca exatamente como o exemplo a serve (via `core.tema` / `paleta_graficos` com `json_script:"paleta-graficos"` — zero hex hard-coded). Renderiza `diarias/dashboard.html`.
    - `urls.py`: exatamente as 5 rotas do bloco `<interfaces>`, `app_name = "diarias"`.
  </action>
  <verify>
    <automated>python3 -m py_compile .template-tests/fixtures/guia/apps/diarias/forms.py .template-tests/fixtures/guia/apps/diarias/views.py .template-tests/fixtures/guia/apps/diarias/urls.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile` dos 3 arquivos sai com código 0
    - `grep -c '@login_required' .template-tests/fixtures/guia/apps/diarias/views.py` retorna 5 (todas as views protegidas)
    - `grep -F 'COLUNAS_ORDENACAO_PERMITIDAS' views.py` encontra a definição e o uso com `.get(`; `grep -F 'order_by(request' views.py` NÃO encontra nada
    - `grep -c '"viagemSalva"' views.py` retorna 3 (criar, editar, excluir) e `grep -F '422' views.py` encontra ao menos 2 ocorrências
    - `grep -F 'app_name = "diarias"' urls.py` encontra a linha; as 5 rotas nomeadas viagem_listar, dashboard, viagem_criar, viagem_editar, viagem_excluir presentes
    - `grep -F 'paleta' views.py` encontra a paleta servida a partir de core.tema (import de `core.tema` OU do mecanismo idêntico ao do exemplo) e `grep -E '#[0-9a-fA-F]{6}' views.py` não encontra hex de cor
    - `grep -F 'diarias/_tabela_resultado.html' views.py` e `grep -F 'diarias/_form_modal.html' views.py` encontram os nomes de template do contrato
  </acceptance_criteria>
  <done>views/forms/urls compilam, espelham o exemplo função a função, com 5 views protegidas, whitelist de ordenação, modal 422 + HX-Trigger "viagemSalva" e dashboard 100% ORM com paleta da marca sem hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → views do fixture | querystring (ordenação, filtros, busca) e corpo de formulário são entrada não confiável |
| fixture → sistemas reais | o guia propagará este código a sistemas de produção — defeito de segurança aqui multiplica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-P1-01 | Tampering | ordenação/filtros em viagem_listar_view | mitigate | whitelist `COLUNAS_ORDENACAO_PERMITIDAS` + filtros restritos a `StatusChoices.values`; nunca `order_by` de entrada crua (padrão exemplo, ASVS V5) |
| T-08-P1-02 | Spoofing/Elevation | 5 views do app | mitigate | `@login_required` em todas (ASVS V4); provado por grep -c == 5 e pelos testes do 08-02 |
| T-08-P1-03 | Tampering | entrada de formulário (valores/datas) | mitigate | ModelForm + `MinValueValidator` + `clean()` de datas; inválido → 422, nunca persistência parcial |
| T-08-P1-SC | Tampering | supply chain | accept | nenhum pacote instalado nesta fase (stack fechada, auditoria de legitimidade N/A por decisão do marco) |
</threat_model>

<verification>
- Todos os .py do fixture compilam no host (`py_compile`)
- Nenhum `__init__.py` em `fixtures/`, `fixtures/guia/`, `fixtures/guia/apps/` (proteção do unittest discover)
- Zero `.jinja`, zero FK no modelo, zero hex de cor em Python
- Identificadores do contrato presentes byte a byte: `apps.diarias`, `app_name = "diarias"`, `viagemSalva`, `COLUNAS_ORDENACAO_PERMITIDAS`, `HistoricalViagem`
</verification>

<success_criteria>
- Backend do fixture completo em `.template-tests/fixtures/guia/apps/diarias/` (12 arquivos), espelho do app exemplo no domínio de viagens
- D-01..D-04 implementados literalmente; nenhuma "melhoria" de modelagem
- Migração inicial incluída no fixture (consistência com models.py será provada no 08-04 por `makemigrations --check`)
</success_criteria>

<output>
Create `.planning/phases/08-exemplo-provado/08-01-SUMMARY.md` when done
</output>
