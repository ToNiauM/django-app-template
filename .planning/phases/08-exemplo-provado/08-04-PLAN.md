---
phase: 08-exemplo-provado
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - .template-tests/test_08_guia_prova.py
autonomous: true
requirements: [PRV-01]

must_haves:
  truths:
    - "A suíte gera/reusa uma cópia Copier real via ensaio_django.sh, instala o fixture como apps/diarias (os mesmos passos que o leitor do guia fará à mão) e sai verde"
    - "Migração aplicada ([X] 0001_initial em showmigrations), makemigrations --check limpo, e manage.py test apps.diarias verde dentro do container"
    - "Smoke HTTP: as 3 telas respondem 302 → /login/ para anônimo, e listagem + dashboard respondem 200 autenticado (dança real de CSRF), com id=paleta-graficos no dashboard e o link href=\"/diarias/dashboard/\" renderizado na região do <nav> da listagem"
    - "A instalação é idempotente com detecção de drift por sha256 — banco reusado nunca prova código morto (Pitfall 4/5)"
    - "O test_command padrão roda os 9 módulos test_*.py existentes + os 2 novos, tudo verde (os 4 test_*.sh são tracers à parte, fora do discover)"
  artifacts:
    - path: ".template-tests/test_08_guia_prova.py"
      provides: "Prova de ponta a ponta do fixture na cópia real (PRV-01)"
      contains: "setUpClass"
      min_lines: 150
  key_links:
    - from: ".template-tests/test_08_guia_prova.py"
      to: ".template-tests/ensaio_django.sh"
      via: "subcomando subir chamado UMA vez em setUpClass, saída ENSAIO_* parseada"
      pattern: "ENSAIO_DESTINO"
    - from: ".template-tests/test_08_guia_prova.py"
      to: "cópia gerada (DESTINO)"
      via: "cópia do fixture + patches de settings/urls/nav + up -d --build web"
      pattern: "apps.diarias"
    - from: ".template-tests/test_08_guia_prova.py"
      to: "container web"
      via: "docker compose exec -T web python manage.py (migrate/test/showmigrations)"
      pattern: "showmigrations"
---

<objective>
Criar e deixar verde `test_08_guia_prova.py`: a prova de ponta a ponta do PRV-01 — o fixture `apps/diarias` instala numa cópia Copier real (banco de ensaio), a migração aplica, os testes do app passam dentro do container e o smoke HTTP das telas responde, anônimo (302) e autenticado (200 com CSRF real). Fecha com a suíte integral do projeto (9 módulos test_*.py + 2 novos) verde pelo test_command padrão.

Purpose: PRV-01 e critérios 1 e 3 do roadmap. A instalação replica exatamente os passos que o leitor do guia fará à mão (copiar app, editar settings/urls/nav, rebuild) — as constantes de patch desta suíte serão citadas pela Fase 9.
Output: `.template-tests/test_08_guia_prova.py` verde + execução integral do test_command verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-exemplo-provado/08-RESEARCH.md
@.template-tests/ensaio_django.sh
@.template-tests/test_07_cor_runtime.sh
@.planning/config.json

<interfaces>
Contrato do fixture (planos 08-01/08-02): app `apps.diarias`; rotas `/diarias/` (listagem, H1 "Diárias e passagens"), `/diarias/dashboard/` (contém `id="paleta-graficos"`), `/diarias/novo/`; migração `0001_initial`; testes internos em `apps.diarias`.

Âncoras REAIS da cópia gerada (verificadas na pesquisa):
- `config/settings/base.py`: linha `"apps.exemplo.apps.ExemploConfig",` dentro de INSTALLED_APPS (fallback: inserir antes do `]` de fechamento da lista se a âncora não existir)
- `config/urls.py`: linha `    path("", include("core.urls")),` — inserir `    path("diarias/", include("apps.diarias.urls")),` imediatamente antes
- `core/templates/core/_nav_dominio.html`: já carrega `{% load navegacao %}`; acrescentar ao fim as duas linhas: `{% item_nav "diarias:dashboard" "Painel de viagens" "grafico" %}` e `{% item_nav "diarias:viagem_listar" "Diárias e passagens" "lista" "/diarias/" "/diarias/dashboard/" %}` (o 5º argumento evita dois aria-current — padrão do exemplo)
- Form de login (`core/_login_form.html`): campos `email`, `password`, `next`, `csrfmiddlewaretoken`; rota `/login/`; POST exige cookie csrftoken + header Referer

Contratos do harness (cabeçalho de ensaio_django.sh — NORMATIVO):
- `sh .template-tests/ensaio_django.sh subir` imprime `ENSAIO_DESTINO=`, `ENSAIO_PORTA=`, `ENSAIO_PROJETO=`, `ENSAIO_URL=`
- Orçamento: quem invoca usa timeout explícito de 600000 ms; primeira criação em BACKGROUND com polling; estouro de tempo com recriação anunciada em stderr NÃO é reprovação — só reprova comando que TERMINOU com código != 0
- `testar/executar/compor/porta` chamam `garantir_banco()` (1 curl SEM retry): jamais chamar logo após um `up -d --build` — usar laço próprio de healthz (Pitfall 2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Núcleo da suíte — subir, instalar com drift, rebuild, migrar, testar in-container</name>
  <files>.template-tests/test_08_guia_prova.py</files>
  <read_first>
    - .template-tests/ensaio_django.sh (CABEÇALHO INTEIRO + funções garantir_banco e impressao_atual — contrato de reúso e orçamento de tempo)
    - .template-tests/test_07_cor_runtime.sh (captura única de porta, laço próprio de healthz, up -d vs restart)
    - .template-tests/test_07_nav_extensao.py (impressao_subarvore para o drift)
    - .planning/phases/08-exemplo-provado/08-RESEARCH.md (Patterns 2-4, Pitfalls 1-6, Code Examples — as receitas prontas de subprocess/patch/compor)
  </read_first>
  <action>
    Criar `.template-tests/test_08_guia_prova.py` (unittest stdlib) com uma classe cujo `setUpClass` faz TODO o trabalho caro uma única vez, seguindo as receitas da pesquisa:

    1. **Subir/reusar o banco de ensaio:** `subprocess.run(["sh", ".template-tests/ensaio_django.sh", "subir"], cwd=ROOT, timeout=600)` — na prática, invocar com tolerância ao orçamento normativo (600000 ms); parsear `ENSAIO_DESTINO/ENSAIO_PORTA/ENSAIO_PROJETO/ENSAIO_URL` da stdout (receita "Capturar o banco de ensaio" da pesquisa). NUNCA chamar `subir/porta/url/destino` de novo depois disso (Pattern 2).
    2. **Detecção de drift (Pattern 3 / Pitfalls 4-5):** comparar sha256 (caminho relativo + conteúdo, padrão `impressao_subarvore`, ignorando `__pycache__`/`.pyc`) de `.template-tests/fixtures/guia/apps/diarias` vs `DESTINO/apps/diarias`. Três estados: ausente → instalar tudo; idêntico → pular direto para as provas (execução barata); divergente → SE o drift toca `models.py` ou `migrations/`, rodar ANTES de sobrescrever `compor exec -T web python manage.py migrate diarias zero --noinput` (com o código antigo ainda instalado), então sobrescrever e seguir.
    3. **Instalação (os passos do leitor):** `shutil.copytree(FIXTURE/"apps/diarias", DESTINO/"apps/diarias", dirs_exist_ok=True)` (removendo antes a árvore destino em caso de drift); patches idempotentes com guarda `if ... not in texto` nas três âncoras do bloco `<interfaces>` (settings INSTALLED_APPS com fallback antes do `]`, urls antes de core.urls, nav com as duas linhas `{% item_nav %}` exatas). Manter os textos de patch como CONSTANTES DE MÓDULO nomeadas (ex.: `LINHA_SETTINGS`, `LINHA_URLS`, `LINHAS_NAV`) — a Fase 9 citará essas constantes.
    4. **Rebuild só quando algo mudou:** `docker compose --project-name PROJETO --env-file .env up -d --build web` com cwd=DESTINO e timeout generoso (o rebuild refaz assets/collectstatic — minutos); em seguida laço PRÓPRIO de healthz: até 180 tentativas de 1s em `ENSAIO_URL + "/healthz"` via urllib (Pitfall 2 — nunca harness aqui). Helper `compor(*args)` da receita da pesquisa.
    5. **Provas in-container (métodos de teste, ordem-independentes — Pitfall 9):** (a) `compor exec -T web python manage.py migrate --noinput` código 0; (b) `showmigrations diarias` contém `[X] 0001_initial`; (c) `makemigrations diarias --check --dry-run` código 0 (prova que a migração escrita à mão no 08-01 está consistente com models.py); (d) `python manage.py test apps.diarias --noinput` código 0. Mensagens de falha incluem stdout+stderr do compose para diagnóstico.

    Docstring do módulo registrando as convenções: banco de ensaio fica COM o fixture instalado ao final (Pattern 4 — estado correto, nenhum trap de derrubar); suítes futuras não devem assumir banco "puro".
  </action>
  <verify>
    <automated>python3 -m py_compile .template-tests/test_08_guia_prova.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile .template-tests/test_08_guia_prova.py` sai com código 0
    - `grep -F 'ENSAIO_DESTINO' test_08_guia_prova.py` encontra o parse; `grep -c 'ensaio_django.sh' test_08_guia_prova.py` mostra o harness invocado apenas com `subir` (nenhuma chamada a `testar`, `porta`, `url` ou `executar` do harness — `grep -E '"(testar|porta|url|executar)"' test_08_guia_prova.py` vazio)
    - `grep -F 'up' test_08_guia_prova.py` mostra `--build` no up do web; `grep -F 'restart' test_08_guia_prova.py` NÃO encontra nada (anti-pattern)
    - `grep -F 'healthz' test_08_guia_prova.py` encontra o laço próprio (180 tentativas)
    - `grep -F 'migrate diarias zero' test_08_guia_prova.py` (ou equivalente em lista de args) presente no ramo de drift de models/migrations
    - `grep -F 'makemigrations' test_08_guia_prova.py` encontra a chamada com `--check`
    - Constantes de patch nomeadas presentes e as duas linhas de `item_nav` idênticas às do bloco interfaces
  </acceptance_criteria>
  <done>Núcleo da suíte escrito: captura única, drift por sha256 com tratamento de migração, instalação idempotente pelos passos do leitor, rebuild + healthz próprio, e as 4 provas in-container.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke HTTP — 302 anônimo e 200 autenticado com CSRF real</name>
  <files>.template-tests/test_08_guia_prova.py</files>
  <read_first>
    - .template-tests/test_08_guia_prova.py (estado atual da Task 1)
    - core/templates/core/_login_form.html (campos reais do form: email, password, next, csrfmiddlewaretoken)
    - .planning/phases/08-exemplo-provado/08-RESEARCH.md (Pitfall 7 e receitas "Smoke autenticado" e "Criar usuário de smoke")
  </read_first>
  <action>
    Acrescentar à suíte os métodos de smoke HTTP (host → porta publicada), na ordem barato→caro:

    1. **302 anônimo:** GET sem sessão em `/diarias/`, `/diarias/dashboard/` e `/diarias/novo/` → status 302 com `Location` começando com `/login/` (urllib com redirect desabilitado). Prova rota registrada + `@login_required`.
    2. **Usuário de smoke idempotente (Pitfall 7 / Assumption A3 — usar a alternativa robusta):** `compor exec -T web python manage.py shell -c` com um one-liner que faz `get_or_create` do usuário `guia@example.invalid` e SEMPRE `set_password` com senha gerada por `secrets.token_urlsafe(16)` a cada execução (a senha vive só na memória do processo; nada persiste em arquivo/argv do host além do one-liner do exec — mitigação T-08-P4-01). Marcar `is_active=True` (o gate do template é is_active; não precisa ser staff para as telas do app).
    3. **Login com dança real de CSRF (receita da pesquisa):** cookie jar urllib; GET `/login/` → extrair `csrfmiddlewaretoken` do HTML; POST com `email`, `password`, `next=/diarias/` e header `Referer`; seguir o redirect. UMA única tentativa com credencial correta — jamais loop de tentativas (lockout do django-axes, T-08-P4-02).
    4. **200 autenticado:** `/diarias/` → 200 contendo "Diárias e passagens" (H1 do contrato); `/diarias/dashboard/` → 200 contendo `id="paleta-graficos"`. No mesmo HTML autenticado de `/diarias/`, assertar que a REGIÃO do `<nav>` (recorte entre `<nav` e `</nav>`) contém `href="/diarias/dashboard/"` — o `{% item_nav %}` falha em silêncio quando a rota não resolve (o item simplesmente não aparece, sem erro), então só esta asserção HTTP prova que os itens de navegação do critério 4 do roadmap de fato renderizam. Sem dependência de seed — as telas respondem 200 com banco vazio (agregações toleram None); NÃO rodar `seed_diarias` no smoke (dados persistiriam no banco reusado).
  </action>
  <verify>
    <automated>python3 -m py_compile .template-tests/test_08_guia_prova.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile` sai com código 0
    - `grep -F 'csrfmiddlewaretoken' test_08_guia_prova.py` encontra a extração e o POST; `grep -F 'Referer' test_08_guia_prova.py` encontra o header
    - `grep -F 'secrets' test_08_guia_prova.py` encontra a geração da senha; `grep -F 'guia@example.invalid' test_08_guia_prova.py` encontra o usuário; nenhuma senha literal no arquivo (`grep -iE 'password.*=.*"[^"]{4,}"' test_08_guia_prova.py` não encontra credencial fixa)
    - `grep -F 'paleta-graficos' test_08_guia_prova.py` e `grep -F 'Diárias e passagens' test_08_guia_prova.py` encontram as asserções de conteúdo
    - `grep -F 'href="/diarias/dashboard/"' test_08_guia_prova.py` encontra a asserção do link de navegação dentro do recorte do `<nav>` da listagem autenticada
    - `grep -F 'seed_diarias' test_08_guia_prova.py` NÃO encontra nada (smoke livre de seed)
    - As 3 rotas do 302 presentes: `/diarias/`, `/diarias/dashboard/`, `/diarias/novo/`
  </acceptance_criteria>
  <done>Smoke completo na suíte: 302 nas 3 telas, usuário idempotente com senha efêmera, login CSRF real em tentativa única e 200 autenticado na listagem e no dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Executar a prova de ponta a ponta e a suíte integral do projeto</name>
  <files>.template-tests/test_08_guia_prova.py</files>
  <read_first>
    - .template-tests/test_08_guia_prova.py (estado atual)
    - .template-tests/ensaio_django.sh (cabeçalho — orçamento e diagnóstico em caso de estouro)
  </read_first>
  <action>
    Executar e corrigir até verde, respeitando o orçamento normativo do harness:

    1. `python3 -m unittest discover -s .template-tests -p 'test_08_guia_prova.py' -v` com timeout de 600000 ms por invocação. Se a PRIMEIRA criação do banco de ensaio estourar o tempo com "recriando banco de ensaio" anunciado em stderr, isso NÃO é reprovação: relançar em background com polling (regra do cabeçalho do harness) e re-executar a suíte quando o banco existir — só reprova comando que TERMINOU com código != 0.
    2. Depois do primeiro verde, executar DE NOVO a mesma suíte e confirmar a via barata: sem drift, nenhum rebuild acontece (execução em poucos minutos) e tudo segue verde — prova da idempotência (Pattern 3).
    3. Rodar a suíte integral do projeto: `python3 -m unittest discover -s .template-tests -p 'test_*.py'` (test_command exato do config.json), timeout 600000 ms — os 9 módulos test_*.py pré-existentes + test_08_guia_vazamento + test_08_guia_prova, tudo verde (critério 3 do roadmap). Nenhum teste do fixture pode ter sido coletado no host.
    4. Qualquer falha: diagnosticar com `docker compose ps`/`logs` via helper `compor`, corrigir a suíte OU o fixture (se o defeito for do fixture, corrigi-lo em `.template-tests/fixtures/guia/` — a detecção de drift reinstala sozinha na execução seguinte) e repetir. Não relaxar asserções para passar.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .template-tests -p 'test_*.py'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s .template-tests -p 'test_08_guia_prova.py'` termina com OK (código 0)
    - Segunda execução consecutiva também OK e SEM rebuild (saída não contém build do web — via barata do drift)
    - `python3 -m unittest discover -s .template-tests -p 'test_*.py'` termina com OK, com contagem de testes estritamente maior que a dos 9 módulos test_*.py anteriores (as 2 novas coletadas) e zero falha/erro
    - Dentro da cópia: `showmigrations diarias` mostrou `[X] 0001_initial` e `manage.py test apps.diarias` saiu 0 (evidência no log da execução)
  </acceptance_criteria>
  <done>Critérios 1 e 3 do roadmap verdes: fixture provado de ponta a ponta na cópia real (migração, testes in-container, smoke 302/200 com CSRF) e suíte integral do projeto verde pelo test_command padrão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte (host) → cópia gerada/container | a suíte injeta código e credenciais na cópia de ensaio — nunca na árvore do template |
| suíte → HTTP autenticado | credenciais e sessão cruzam a porta publicada localmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-P4-01 | Information Disclosure | credencial do usuário de smoke | mitigate | senha efêmera via `secrets.token_urlsafe` por execução, definida via `shell -c` no exec do compose; nunca literal no repo, nunca em arquivo |
| T-08-P4-02 | DoS (lockout django-axes) | POST de login do smoke | mitigate | tentativa única com credencial correta; jamais loop de tentativas erradas |
| T-08-P4-03 | Spoofing (CSRF) | smoke autenticado | mitigate | dança real: cookie jar + csrfmiddlewaretoken + Referer; nenhuma proteção desligada na cópia |
| T-08-P4-04 | Tampering | instalação do fixture | mitigate | escrita restrita a `ENSAIO_DESTINO` (cópia efêmera); zero escrita na árvore do template — o 08-03 vigia a fronteira |
| T-08-P4-05 | Repudiation (prova de código morto) | banco de ensaio reusado | mitigate | drift por sha256 a cada execução + `migrate diarias zero` antes de trocar models/migrations (Pitfalls 4-5) |
</threat_model>

<verification>
- Suíte de prova verde duas vezes seguidas (segunda pela via barata, sem rebuild)
- test_command integral verde (9 + 2 módulos test_*.py; os 4 test_*.sh são tracers à parte), nenhum teste do fixture coletado no host
- Evidências in-container no log: `[X] 0001_initial`, `makemigrations --check` limpo, `test apps.diarias` código 0
</verification>

<success_criteria>
- Critério 1 do roadmap: cópia Copier real via ensaio_django.sh + fixture instalado como apps/diarias + migração + testes + smoke, tudo verde
- Critério 3 do roadmap: suítes novas rodam junto com os 9 módulos test_*.py existentes pelo test_command padrão
- PRV-01 provado de ponta a ponta
</success_criteria>

<output>
Create `.planning/phases/08-exemplo-provado/08-04-SUMMARY.md` when done
</output>
