---
phase: 08-exemplo-provado
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .template-tests/fixtures/guia/apps/diarias/templates/diarias/viagem_listar.html
  - .template-tests/fixtures/guia/apps/diarias/templates/diarias/_tabela_resultado.html
  - .template-tests/fixtures/guia/apps/diarias/templates/diarias/_filtros.html
  - .template-tests/fixtures/guia/apps/diarias/templates/diarias/_form_modal.html
  - .template-tests/fixtures/guia/apps/diarias/templates/diarias/_confirmar_exclusao_modal.html
  - .template-tests/fixtures/guia/apps/diarias/templates/diarias/dashboard.html
  - .template-tests/fixtures/guia/apps/diarias/tests/__init__.py
  - .template-tests/fixtures/guia/apps/diarias/tests/test_models.py
  - .template-tests/fixtures/guia/apps/diarias/tests/test_crud.py
  - .template-tests/fixtures/guia/apps/diarias/tests/test_dashboard.py
autonomous: true
requirements: [PRV-01]

must_haves:
  truths:
    - "As 6 telas/fragmentos do fixture espelham 1:1 os templates do app exemplo, renomeados ao domínio de viagens — nenhum design novo"
    - "O dashboard consome a paleta da marca via json_script paleta-graficos, com esc() em toda interpolação de formatter e reconstrução no evento tema:alterado"
    - "Os testes do app cobrem 302 anônimo, 200 com force_login, 422 de form inválido, HX-Trigger viagemSalva e agregações do dashboard"
  artifacts:
    - path: ".template-tests/fixtures/guia/apps/diarias/templates/diarias/dashboard.html"
      provides: "Dashboard ECharts com paleta da marca"
      contains: "paleta-graficos"
    - path: ".template-tests/fixtures/guia/apps/diarias/templates/diarias/_form_modal.html"
      provides: "Modal de criar/editar com HTMX (fluxo 422)"
      contains: "hx-post"
    - path: ".template-tests/fixtures/guia/apps/diarias/templates/diarias/_tabela_resultado.html"
      provides: "Tabela server-side com ordenação e paginação preservando filtros"
    - path: ".template-tests/fixtures/guia/apps/diarias/tests/test_crud.py"
      provides: "Prova de auth, 422 e HX-Trigger no padrão do exemplo"
      contains: "viagemSalva"
    - path: ".template-tests/fixtures/guia/apps/diarias/tests/test_models.py"
      provides: "Prova do modelo, validação de datas, auditoria e seed idempotente"
    - path: ".template-tests/fixtures/guia/apps/diarias/tests/test_dashboard.py"
      provides: "Prova das agregações ORM e do json_script da paleta"
  key_links:
    - from: "templates/diarias/dashboard.html"
      to: "core/tema.py (paleta servida pela view)"
      via: "json_script id paleta-graficos"
      pattern: "paleta-graficos"
    - from: "templates/diarias/_form_modal.html"
      to: "views.py viagem_criar/viagem_editar (plano 08-01)"
      via: "hx-post para rotas diarias:*"
      pattern: "diarias:viagem_(criar|editar)"
    - from: "tests/test_crud.py"
      to: "urls.py do fixture"
      via: "reverse com namespace diarias"
      pattern: "reverse\\(\"diarias:"
---

<objective>
Criar a camada visual (6 templates) e os testes internos (3 suítes) do fixture `apps/diarias`, espelhando 1:1 os templates e testes do app exemplo — que são o contrato de design desta fase (não existe UI-SPEC; o exemplo É a especificação visual).

Purpose: PRV-01 e critério 4 do roadmap — listagem paginada com filtros, modal 422/HX-Trigger e dashboard ECharts com a paleta da marca existem no fixture e serão provados dentro da cópia no 08-04.
Output: `templates/diarias/` completo + `tests/` completo no fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-exemplo-provado/08-CONTEXT.md
@.planning/phases/08-exemplo-provado/08-RESEARCH.md

Fonte viva dos padrões (caminho com chaves Jinja literais): `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/` e `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/tests/`.

<interfaces>
Contrato fixado (idêntico ao 08-01 — usar estes identificadores EXATOS mesmo que o 08-01 ainda não tenha rodado):
- Rotas: `diarias:viagem_listar`, `diarias:dashboard`, `diarias:viagem_criar`, `diarias:viagem_editar`, `diarias:viagem_excluir`
- Modelo `Viagem` — campos: servidor, destino, data_inicio, data_fim, motivo, valor_diarias, valor_passagens, status (StatusChoices: SOLICITADA/APROVADA/PAGA/CANCELADA), criado_em, atualizado_em; `history` (simple-history); `clean()` exige data_fim >= data_inicio
- HX-Trigger de sucesso: `viagemSalva`; form inválido: HTTP 422
- Form: `ViagemForm` (todos os campos editáveis)
- Seed: comando `seed_diarias`, idempotente (get_or_create)
- Título H1 da listagem: "Diárias e passagens" (o smoke do 08-04 asserta esta string no HTML autenticado)
- Templates: `diarias/viagem_listar.html`, `_tabela_resultado.html`, `_filtros.html`, `_form_modal.html`, `_confirmar_exclusao_modal.html`, `dashboard.html`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Templates de listagem, filtros e modais</name>
  <files>.template-tests/fixtures/guia/apps/diarias/templates/diarias/viagem_listar.html, .template-tests/fixtures/guia/apps/diarias/templates/diarias/_tabela_resultado.html, .template-tests/fixtures/guia/apps/diarias/templates/diarias/_filtros.html, .template-tests/fixtures/guia/apps/diarias/templates/diarias/_form_modal.html, .template-tests/fixtures/guia/apps/diarias/templates/diarias/_confirmar_exclusao_modal.html</files>
  <read_first>
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/item_listar.html
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_tabela_resultado.html (266 linhas — ordenação clicável, paginação com querystring preservada)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_filtros.html (multi-seleção de status)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_form_modal.html (fluxo 422, foco, fechamento por HX-Trigger)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_confirmar_exclusao_modal.html
    - .planning/phases/08-exemplo-provado/08-RESEARCH.md (seção "Padrões do código do fixture")
  </read_first>
  <action>
    Espelhar os 5 templates de CRUD do exemplo, arquivo a arquivo, renomeando ao domínio: `exemplo:` → `diarias:`, item → viagem, `itemSalvo` → `viagemSalva`, colunas da tabela = servidor, destino, período (data_inicio–data_fim em DD/MM/AAAA), valores em R$ (valor_diarias, valor_passagens), status (badge no padrão visual do exemplo). H1 da listagem: "Diárias e passagens" (string exata — o smoke do 08-04 depende dela). Filtro multi-seleção sobre os 4 status (Solicitada/Aprovada/Paga/Cancelada) e campo de busca `q` como no exemplo.

    Regras invioláveis (decisões da Fase 7, registradas no STATE.md):
    - Manter TODAS as classes Tailwind e a estrutura DOM do exemplo — zero design novo; só troca de rótulo/campo/rota. Não usar `bg-ink/40`, `shadow-xs`, `backdrop-blur-xs` nem `text-white` (classes vetadas/corrigidas na Fase 7 — copiar o estado ATUAL dos templates do exemplo já corrigidos).
    - `hx-post`/`hx-get` para as rotas `diarias:*`; CSRF herdado do `htmx:configRequest` do base.html — nenhum desligamento de proteção, `{% csrf_token %}` presente nos forms como no exemplo.
    - Ordenação da tabela emite somente as chaves do `COLUNAS_ORDENACAO_PERMITIDAS`; paginação preserva a querystring de filtros exatamente como o exemplo faz.
    - Comentários didáticos do exemplo traduzidos ao domínio (o guia da Fase 9 vai citar estes arquivos).
  </action>
  <verify>
    <automated>grep -RFl 'diarias:' .template-tests/fixtures/guia/apps/diarias/templates/diarias/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 arquivos existem em `templates/diarias/` com os nomes exatos do contrato
    - `grep -F 'Diárias e passagens' viagem_listar.html` encontra o H1
    - `grep -RF 'exemplo:' .template-tests/fixtures/guia/apps/diarias/templates/` NÃO encontra nada (nenhuma rota do exemplo vazou)
    - `grep -RF 'itemSalvo' .template-tests/fixtures/guia/apps/diarias/` NÃO encontra nada; `grep -RF 'viagemSalva' templates/diarias/` encontra ao menos 1 ocorrência
    - `grep -F 'hx-post' _form_modal.html` e `grep -F 'csrf' _form_modal.html` encontram ocorrências
    - `grep -RE 'bg-ink/40|shadow-xs|backdrop-blur-xs|text-white' templates/diarias/` NÃO encontra nada
  </acceptance_criteria>
  <done>5 templates de CRUD espelhados, com rotas diarias:*, gatilho viagemSalva, CSRF presente e nenhuma classe vetada pela Fase 7.</done>
</task>

<task type="auto">
  <name>Task 2: dashboard.html com ECharts e paleta da marca</name>
  <files>.template-tests/fixtures/guia/apps/diarias/templates/diarias/dashboard.html</files>
  <read_first>
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/dashboard.html (INTEGRAL — 291 linhas: json_script, lerVarCss/getComputedStyle, esc(), corCard dentro de montarGraficos, evento tema:alterado, drill-down)
    - .planning/STATE.md (decisões 07-06 e 07-12: paleta via json_script, esc() em TODA interpolação, corCard dentro de montarGraficos)
  </read_first>
  <action>
    Espelhar `dashboard.html` do exemplo no domínio de viagens, com dois gráficos: (a) viagens por status (dimensão categórica D-02) e (b) valores mensais (diárias + passagens). Manter TODOS os mecanismos do exemplo byte a byte na estrutura:
    - `{{ ... |json_script:... }}` para os dados dos dois gráficos (ids renomeados ao domínio, mesmos nomes usados pela dashboard_view do 08-01) e `json_script:"paleta-graficos"` para a paleta — id EXATO `paleta-graficos` (contrato do core; o smoke do 08-04 asserta `id="paleta-graficos"`).
    - `<script src="{% static 'vendor/echarts.min.js' %}">`; chrome do gráfico (eixo/grade/tooltip/borda) lido via `getComputedStyle`/`lerVarCss` em runtime; ZERO hex de cor no template/JS.
    - `esc()` aplicada a TODA interpolação de formatter, inclusive numéricas (decisão 07-12 — mitigação T-08-P2-01).
    - `corCard` declarada DENTRO de `montarGraficos()`; `dispose()+init()` e nova chamada de `montarGraficos()` no evento `tema:alterado` (decisões 07-06/07-12).
    - Títulos de seção com `text-lg` (decisão 07-07). Rótulos pt-BR, moeda R$ no formatter (via esc()).
  </action>
  <verify>
    <automated>grep -c 'paleta-graficos' .template-tests/fixtures/guia/apps/diarias/templates/diarias/dashboard.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'json_script:"paleta-graficos"' dashboard.html` encontra a linha (id exato)
    - `grep -F "tema:alterado" dashboard.html` e `grep -F 'montarGraficos' dashboard.html` encontram ocorrências; `corCard` aparece dentro do corpo de `montarGraficos` (declaração após a linha da função — verificável com `grep -n` comparando números de linha)
    - `grep -F 'esc(' dashboard.html` encontra ao menos 3 ocorrências e toda interpolação de formatter usa esc()
    - `grep -E '#[0-9a-fA-F]{6}' dashboard.html` NÃO encontra hex de cor
    - `grep -F "vendor/echarts.min.js" dashboard.html` encontra o script estático
  </acceptance_criteria>
  <done>Dashboard espelhado com paleta da marca via json_script, esc() em todos os formatters, reconstrução em tema:alterado e zero hex.</done>
</task>

<task type="auto">
  <name>Task 3: Testes internos do app (test_models, test_crud, test_dashboard)</name>
  <files>.template-tests/fixtures/guia/apps/diarias/tests/__init__.py, .template-tests/fixtures/guia/apps/diarias/tests/test_models.py, .template-tests/fixtures/guia/apps/diarias/tests/test_crud.py, .template-tests/fixtures/guia/apps/diarias/tests/test_dashboard.py</files>
  <read_first>
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/tests/test_crud.py (padrão integral: force_login, 302 anônimo, fragmento vs shell por header HTMX, 422, HX-Trigger)
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/tests/test_models.py
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/tests/test_dashboard.py
  </read_first>
  <action>
    Espelhar as 3 suítes de teste do exemplo para o domínio de viagens (estes testes rodam DENTRO do container da cópia via `manage.py test apps.diarias` no 08-04 — nunca no host):
    - `test_models.py`: criação de `Viagem` válida; `full_clean()` reprova data_fim < data_inicio e valor negativo; `history` registra alterações (D-04); NOVO em relação ao exemplo: `call_command("seed_diarias")` duas vezes seguidas → contagem estável e > 0 (prova de idempotência exigida pelo banco de ensaio reusado).
    - `test_crud.py`: padrão integral do exemplo com `reverse("diarias:...")`: anônimo → 302 para login nas 5 rotas; `force_login` → 200; listagem com filtro de status e busca; requisição HTMX recebe fragmento, requisição normal recebe shell; POST inválido → 422; POST válido → header `HX-Trigger == "viagemSalva"` em criar, editar e excluir; ordenação por chave fora da whitelist cai no default (não quebra).
    - `test_dashboard.py`: com dados criados no teste, as agregações por status e mensais batem com o esperado; resposta contém `id="paleta-graficos"`; banco vazio → 200 (agregações toleram None, padrão do exemplo).
  </action>
  <verify>
    <automated>python3 -m py_compile .template-tests/fixtures/guia/apps/diarias/tests/test_models.py .template-tests/fixtures/guia/apps/diarias/tests/test_crud.py .template-tests/fixtures/guia/apps/diarias/tests/test_dashboard.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile` dos 3 arquivos sai com código 0 (imports Django não executam em py_compile — compila no host)
    - `grep -F 'force_login' test_crud.py` e `grep -F '422' test_crud.py` encontram ocorrências; `grep -F 'HX-Trigger' test_crud.py` asserta o header com valor `viagemSalva`
    - `grep -c 'reverse("diarias:' test_crud.py` retorna >= 5
    - `grep -F 'seed_diarias' test_models.py` encontra a chamada dupla de call_command
    - `grep -F 'paleta-graficos' test_dashboard.py` encontra a asserção
    - Nomes de arquivo exatamente test_models.py, test_crud.py, test_dashboard.py sob `tests/` com `__init__.py`
  </acceptance_criteria>
  <done>3 suítes internas espelhadas cobrindo auth (302/200), 422, HX-Trigger viagemSalva, whitelist, auditoria, seed idempotente e dashboard com paleta — prontas para rodar in-container no 08-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do banco → formatters ECharts | valores exibidos em tooltip/label são renderizados por JS — vetor de XSS se não escapados |
| navegador → modais HTMX | POSTs de criar/editar/excluir cruzam CSRF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-P2-01 | Tampering (XSS) | formatters do dashboard.html | mitigate | `esc()` em TODA interpolação, inclusive numérica (decisão 07-12); gate por grep e leitura |
| T-08-P2-02 | Spoofing (CSRF) | _form_modal / _confirmar_exclusao_modal | mitigate | `{% csrf_token %}` + `htmx:configRequest` do base.html; jamais desligar proteção |
| T-08-P2-03 | Elevation | rotas exercitadas pelos testes | mitigate | test_crud prova 302 anônimo nas 5 rotas (ASVS V4) |
| T-08-P2-SC | Tampering | supply chain | accept | nenhum pacote novo; ECharts vem do vendor estático já auditado do template |
</threat_model>

<verification>
- 6 templates + 4 arquivos de teste existem com os nomes exatos do contrato
- Nenhum resíduo do exemplo (`exemplo:`, `itemSalvo`), nenhuma classe vetada, nenhum hex
- Testes referenciam somente identificadores do contrato (rotas diarias:*, viagemSalva, paleta-graficos)
</verification>

<success_criteria>
- Camada visual do fixture cobre o critério 4 do roadmap: listagem paginada com filtros, modal 422/HX-Trigger e dashboard ECharts com a paleta da marca
- Testes internos prontos para `manage.py test apps.diarias` dentro da cópia (prova real no 08-04)
</success_criteria>

<output>
Create `.planning/phases/08-exemplo-provado/08-02-SUMMARY.md` when done
</output>
