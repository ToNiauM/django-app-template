---
phase: 05-verifica-o-e-documenta-o
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [QA-01, QA-02]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - '`[login-form/empty]` `GET /login/` renderiza campos de e-mail e senha vazios e o CTA `Entrar`, sem dados de sessão expostos.'
    - '`[login-form/error]` Credencial inválida mantém a tela de login e exibe `E-mail ou senha inválidos.`; bloqueio temporário exibe a mensagem contratada.'
    - '`[responsive-shell-navigation/overflow]` Breadcrumb usa `flex-wrap`, aside mantém 232px no desktop e a gaveta móvel não amplia o viewport.'
    - '`[responsive-shell-navigation/long-text]` Identidade usa `min-w-0`/`truncate`; breadcrumbs quebram linha sem ocultar controles.'
    - '`[crud-list/empty]` Zero resultados exibe `Nenhum item encontrado`, o corpo explicativo e `Limpar filtros`.'
    - '`[crud-list/populated]` Com dados de ensaio, título, `Novo item`, filtros, tabela, ordenação, ações e paginação permanecem navegáveis.'
    - '`[crud-list/partial]` Prazo ou descrição opcionais ausentes usam `—` sem alterar alinhamento nem ações da linha.'
    - '`[crud-list/overflow]` Tabela larga usa `overflow-x-auto`; ações permanecem alcançáveis e breadcrumb usa `flex-wrap`.'
    - '`[crud-list/zero-one-many]` Zero itens mostra o vazio; um item conserva a tabela; muitos itens acionam paginação e quantidade exibida.'
    - '`[crud-list/long-text]` Descrição e identidade truncam; título e cópias auxiliares quebram linha sem deslocar ações.'
    - '`[crud-modal-form/empty]` `Novo item` abre formulário sem dados persistidos e com CTAs de criação/fechamento visíveis.'
    - '`[crud-modal-form/error]` Resposta 422 mantém o modal aberto e apresenta erros inline no campo correspondente.'
    - '`[crud-modal-form/partial]` Campos opcionais podem ficar vazios; obrigatórios ausentes recebem erro sem descartar valores válidos.'
    - '`[crud-modal-form/long-text]` Título interpolado é escapado pelo Django e quebra linha sem sobrepor os CTAs destrutivos.'
    - '`[dashboard/empty]` Banco vazio retorna dashboard sem erro, com KPIs e contêineres de gráfico em estado neutro seguro.'
    - '`[dashboard/populated]` Com dados de ensaio, KPIs antecedem dois gráficos ECharts e barras/setores levam à lista filtrada.'
    - '`[dashboard/zero-one-many]` Zero registros preserva estado seguro; um produz métricas válidas; muitos preservam agregação e interação.'
    - statement: "[login-form/loading] Durante o submit síncrono, o formulário permanece visível até a navegação completar; nenhum spinner ou skeleton novo é introduzido."
      verification: backstop
    - statement: "[login-form/partial] Envio com um campo preenchido mantém ambos os controles e associa validação ao campo ausente sem apagar o valor informado."
      verification: backstop
    - statement: "[login-form/overflow] Em viewport móvel, o formulário fica dentro do canvas sem rolagem horizontal nem recorte do CTA."
      verification: backstop
    - statement: "[login-form/long-text] Mensagens longas de bloqueio quebram linha sem sobrepor campos ou CTA."
      verification: backstop
    - statement: "[responsive-shell-navigation/loading] Navegação full-page mantém o shell anterior até a resposta; nenhum estado assíncrono customizado é criado."
      verification: backstop
    - statement: "[responsive-shell-navigation/error] Falha de destino não deixa gaveta/overlay presos e permite retornar à última página válida."
      verification: backstop
    - statement: "[crud-list/loading] Durante swaps HTMX, a lista preserva geometria e não duplica controles; nenhum spinner novo é exigido."
      verification: backstop
    - statement: "[crud-list/error] Falha HTMX não troca a tabela por vazio silencioso e mantém recuperação por nova tentativa ou recarga."
      verification: backstop
    - statement: "[crud-modal-form/loading] Durante submit HTMX, o modal permanece estável, sem formulário duplicado ou confirmação destrutiva acidental."
      verification: backstop
    - statement: "[crud-modal-form/overflow] Em viewport móvel ou com muitos erros, o modal permanece rolável e ambos os CTAs alcançáveis."
      verification: backstop
    - statement: "[dashboard/loading] Enquanto ECharts inicializa, contêineres mantêm dimensões e não cobrem KPIs nem navegação."
      verification: backstop
    - statement: "[dashboard/error] Falha de um gráfico não impede acesso a KPIs, navegação ou lista filtrada."
      verification: backstop
    - statement: "[dashboard/partial] Com apenas parte das séries, KPIs e gráfico disponível continuam legíveis sem inventar valores."
      verification: backstop
    - statement: "[dashboard/overflow] Em viewport estreito, cards/gráficos refluem sem rolagem horizontal de página nem corte de rótulos críticos."
      verification: backstop
    - statement: "[dashboard/long-text] Rótulos longos truncam ou quebram linha sem sobrepor legenda, KPIs ou alvos clicáveis."
      verification: backstop
  artifacts:
    - path: ".template-tests/test_05_nascimento.sh"
      provides: "Ambiente Copier/Compose comprovado e retível para inspeção"
    - path: ".planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md"
      provides: "Contrato visual de 17 critérios covered e 15 marcadores backstop"
  key_links:
    - from: ".template-tests/test_05_nascimento.sh --keep"
      to: "http://127.0.0.1:<porta>/login/"
      via: "NASCIMENTO_URL de uma cópia já migrada/testada, identidade fixa DJANGO_SUPERUSER_EMAIL=nascimento@example.invalid e senha NASCIMENTO_ADMIN_PASSWORD recebida somente pelo handoff em memória das Tasks 1–2"
      pattern: "nascimento@example.invalid|NASCIMENTO_ADMIN_PASSWORD|NASCIMENTO_URL"
    - from: "05-UI-SPEC.md"
      to: "/, /exemplo/, /exemplo/dashboard/"
      via: "checkpoint manual no mesmo sistema gerado, sem edição de código"
      pattern: "login|shell|CRUD|dashboard"
  prohibitions:
    - requirement_id: "QA-02"
      category: "transparency"
      statement: "O checkpoint visual não deve alterar templates, estilos ou comportamento das telas existentes para produzir aprovação; regressões não causadas pelos artefatos da Fase 5 devem ser registradas como gaps próprios."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** operador do template, **I want to** provar o nascimento de uma cópia limpa, **so that** o sistema gerado fique testado, navegável e publicável sem editar código.

<objective>
Completar a prova com inspeção humana no navegador sobre uma cópia real já aprovada automaticamente e eliminar todos os recursos efêmeros ao final.

Purpose: Confirmar os 32 estados visuais/interativos do UI-SPEC sem adicionar automação de navegador ou alterar a UI entregue pelas fases anteriores.
Output: Evidência humana 32/32 e ambiente Copier/Compose removido de forma confinada após aprovação ou relato de problema.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verifica-o-e-documenta-o/05-RESEARCH.md
@.planning/phases/05-verifica-o-e-documenta-o/05-PATTERNS.md
@.planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/05-verifica-o-e-documenta-o/05-01-SUMMARY.md
@.planning/phases/05-verifica-o-e-documenta-o/05-02-SUMMARY.md
@.template-tests/test_05_nascimento.sh
@README.md
</context>

## Artifacts this phase produces

- **Novos ou modificados arquivos persistentes:** nenhum.
- **Símbolos, scripts, flags, test helpers e campos persistentes:** nenhum.
- **Recursos transitórios:** um destino sob `mktemp` identificado por `NASCIMENTO_DESTINO`, um projeto Compose identificado por `NASCIMENTO_PROJETO_COMPOSE`, sua rede/volume/containers e a URL loopback `NASCIMENTO_URL`.
- **Dados transitórios:** superusuário `nascimento@example.invalid`, senha aleatória somente no processo do executor e 25 itens criados por `seed_exemplo --limpar --quantidade 25` no banco efêmero.
- **Resultado humano:** confirmação ou lista de desvios referente aos 17 strings covered e 15 objetos backstop já serializados em `must_haves.truths`; nenhum novo campo de schema é criado.

<prohibition_probe>
- Mantida em `must_haves.prohibitions`: a inspeção não pode produzir conformidade modificando a UI observada.
- Exposição de credencial, porta pública e recursos Docker órfãos são cânones de segurança: cobertos pelo `<threat_model>` e encaminhados a `$gsd-secure-phase`; não foram cunhados como proibições bespoke.
</prohibition_probe>

<tasks>

<task type="auto">
  <name>Task 1: Preparar uma cópia retida e populada para inspeção</name>
  <precondition>Os Planos 05-01 e 05-02 estão concluídos; `.template-tests/test_05_nascimento.sh` é executável e fixa `DJANGO_SUPERUSER_EMAIL=nascimento@example.invalid`, Docker responde e a porta loopback escolhida pelo script está livre.</precondition>
  <reversibility rating="reversible">O estado existe somente em diretório, containers, rede e volume temporários identificados; a Task 3 os remove integralmente.</reversibility>
  <read_first>
    - .template-tests/test_05_nascimento.sh (artefato consumido; não modificar)
    - .planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md (32 considerações e cinco jornadas)
    - .planning/phases/05-verifica-o-e-documenta-o/05-RESEARCH.md (segredos efêmeros e inspeção complementar)
    - .planning/phases/05-verifica-o-e-documenta-o/05-01-SUMMARY.md (confirmar o contrato não secreto `DJANGO_SUPERUSER_EMAIL=nascimento@example.invalid` e a criação observável sem senha)
    - .planning/phases/05-verifica-o-e-documenta-o/05-02-SUMMARY.md
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/management/commands/seed_exemplo.py
    - README.md
  </read_first>
  <files>None — somente destino `mktemp` e recursos Docker criados pelo ensaio.</files>
  <action>
    Obter `NASCIMENTO_ADMIN_PASSWORD` com a atribuição capturada `NASCIMENTO_ADMIN_PASSWORD=$(python3 -c 'import secrets; print(secrets.token_urlsafe(24))')`, com tracing desabilitado para que a saída do subprocesso vá somente à variável da sessão e não ao terminal/log, e mantê-la somente nesse ambiente efêmero. Consumir o contrato do Plano 05-01 executando uma única vez `NASCIMENTO_ADMIN_PASSWORD="$NASCIMENTO_ADMIN_PASSWORD" .template-tests/test_05_nascimento.sh --keep`; não substituir `nascimento@example.invalid`, não reconstruir a senha e não usar valor fixo. Capturar stdout sem `eval`; aceitar a confirmação não secreta `OK: superusuário nascimento@example.invalid criado` e extrair por prefixo exatamente uma ocorrência de `NASCIMENTO_DESTINO`, `NASCIMENTO_PROJETO_COMPOSE` e `NASCIMENTO_URL`. Validar diretório existente, projeto alfanumérico e URL iniciada por `http://127.0.0.1:`. No destino exato, consultar `get_user_model()` e exigir que `nascimento@example.invalid` exista com `is_staff`/`is_superuser`; depois usar `docker compose --project-name "$NASCIMENTO_PROJETO_COMPOSE" --env-file .env exec -T web python manage.py seed_exemplo --limpar --quantidade 25` para disponibilizar estados populated/many no navegador. Os testes já executados pelo tracer são a prova automatizada de banco vazio/um/muitos.

    Confirmar novamente `/healthz` e `/login/` por curl. Preservar na memória da mesma sessão os três campos, a identidade exata `nascimento@example.invalid` e `NASCIMENTO_ADMIN_PASSWORD` para o handoff direto ao checkpoint da Task 2; não gravar a senha em arquivo, árvore gerada, README, SUMMARY, comando versionado ou logs. Não alterar qualquer arquivo do checkout nem do destino após a renderização, salvo os dados criados pelas interfaces/comandos normais do sistema.
  </action>
  <acceptance_criteria>
    - O comando `--keep` termina zero somente depois do tracer completo e retorna uma única cópia/stack ativa em loopback.
    - Os três campos de saída são extraídos sem avaliação dinâmica; `NASCIMENTO_DESTINO` existe e `NASCIMENTO_URL/healthz` e `NASCIMENTO_URL/login/` respondem 2xx.
    - `seed_exemplo --limpar --quantidade 25` passa no serviço `web` do projeto exato e não toca outra stack.
    - O banco da cópia retida contém exatamente o administrador `nascimento@example.invalid` com `is_staff`/`is_superuser`; a confirmação é observável sem ler nem imprimir sua senha.
    - `NASCIMENTO_ADMIN_PASSWORD` não aparece no stdout do script, na árvore gerada, no `.copier-answers.yml`, no README, no SUMMARY ou em logs; permanece disponível somente no handoff em memória para o checkpoint transitório.
    - Nenhum arquivo versionado é modificado nesta task.
  </acceptance_criteria>
  <verify>
    <automated>test -n "$NASCIMENTO_ADMIN_PASSWORD" &amp;&amp; test -n "$NASCIMENTO_DESTINO" &amp;&amp; test -d "$NASCIMENTO_DESTINO" &amp;&amp; test -n "$NASCIMENTO_PROJETO_COMPOSE" &amp;&amp; curl -fsS "$NASCIMENTO_URL/healthz" &gt;/dev/null &amp;&amp; curl -fsS "$NASCIMENTO_URL/login/" &gt;/dev/null &amp;&amp; docker compose --project-name "$NASCIMENTO_PROJETO_COMPOSE" --env-file "$NASCIMENTO_DESTINO/.env" -f "$NASCIMENTO_DESTINO/compose.yml" exec -T web python manage.py shell -c "from django.contrib.auth import get_user_model; from apps.exemplo.models import ItemExemplo; u=get_user_model().objects.get(email='nascimento@example.invalid'); assert u.is_staff and u.is_superuser; assert ItemExemplo.objects.count() == 25"</automated>
  </verify>
  <done>Uma cópia aprovada, populada e acessível somente em loopback está pronta para inspeção; `nascimento@example.invalid` foi confirmado sem revelar a senha, e `NASCIMENTO_ADMIN_PASSWORD` segue apenas no handoff em memória para a Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar visualmente login, shell, CRUD e dashboard</name>
  <precondition>`NASCIMENTO_URL`, `NASCIMENTO_DESTINO`, `NASCIMENTO_PROJETO_COMPOSE`, a identidade contratada `nascimento@example.invalid` e `NASCIMENTO_ADMIN_PASSWORD` recebida pelo handoff em memória da Task 1 continuam disponíveis, e `/healthz` responde.</precondition>
  <reversibility rating="reversible">A verificação só interage com dados descartáveis da cópia; não publica nem altera contratos persistentes.</reversibility>
  <read_first>
    - .planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md (fonte exata dos 17 covered + 15 backstops)
    - .template-tests/test_05_nascimento.sh (prova já executada; não modificar)
    - README.md (URLs/jornada documentada; não modificar)
    - core/templates/core/login.html
    - core/templates/core/shell.html
    - core/templates/core/_nav.html.jinja
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/item_listar.html
    - apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/dashboard.html
  </read_first>
  <files>None — inspeção no navegador e dados descartáveis via UI.</files>
  <action>
    Consumir sem transformação a identidade `nascimento@example.invalid` fixada pelo Plano 05-01 e a senha aleatória recebida exclusivamente em `NASCIMENTO_ADMIN_PASSWORD` pelo handoff em memória da Task 1; não usar senha presumida, literal ou reconstruída. Apresentar ao usuário somente `NASCIMENTO_URL`, esse e-mail e a senha transitória no checkpoint, deixando explícito que a credencial será destruída na Task 3 e não poderá integrar screenshot pública, log ou SUMMARY. Conduzir os passos de `how-to-verify` no mesmo ambiente retido, sem editar template, CSS, JavaScript ou código gerado. Para backstops de loading/error, usar apenas recursos do navegador (viewport responsivo, throttling/offline e request blocking temporários); restaurar a rede normal antes de prosseguir. Para estados CRUD, usar filtros e os dados de ensaio; erros de formulário devem ser provocados pela própria UI. Se surgir regressão, registrar qual dos 32 itens falhou e se há vínculo causal com `.template-tests/test_05_nascimento.sh` ou `README.md`; não reparar escopo da Fase 4 nesta task.
  </action>
  <what-built>Cópia Copier real com db/web, migrações, superusuário, suíte core/apps.exemplo aprovada, 25 itens de ensaio e runbook completo.</what-built>
  <how-to-verify>
    1. Abra `${NASCIMENTO_URL}/login/` em desktop e viewport móvel. Confirme e-mail/senha vazios, CTA `Entrar`, ausência de rolagem horizontal e quebra correta de mensagens longas. Envie uma credencial inválida e um formulário parcial; confirme os textos/associações contratados e que o formulário permanece visível durante a navegação.
    2. Entre com a credencial efêmera. Confirme shell na raiz, aside de 232px no desktop, gaveta/overlay/`Esc`/`Fechar menu` no móvel, breadcrumbs flexíveis, identidade truncada e logout por formulário POST. Com throttling/offline temporário, confirme que navegação não cria estado assíncrono novo nem deixa gaveta presa; restaure a rede.
    3. Abra `/exemplo/`. Confirme título, `Novo item`, filtros, tabela, ordenação, ações e paginação com 25 itens; use filtro sem correspondência para confirmar o vazio e `Limpar filtros`; confira `—`, truncamento, overflow horizontal e comportamento zero/um/muitos pelos controles existentes.
    4. Abra `Novo item`. Confirme formulário vazio, erros 422 inline sem perder valores válidos, campos opcionais vazios, título longo escapado/quebrado e modal rolável em viewport móvel. Com rede lenta/offline temporária, confirme estabilidade, ausência de duplicação e recuperação da tabela/modal; restaure a rede sem concluir ação destrutiva acidental.
    5. Abra `/exemplo/dashboard/`. Confirme KPIs antes de dois gráficos, layout estreito sem overflow e drill-down de barra/setor para a lista filtrada. Bloqueie temporariamente o asset ECharts ou use offline após o HTML carregar para confirmar que KPIs/navegação permanecem acessíveis; restaure e confirme rótulos/estado parcial sem sobreposição. A cobertura automatizada já comprovou dashboard vazio e cardinalidades zero/um/muitos.
  </how-to-verify>
  <acceptance_criteria>
    - Os 17 itens covered serializados em `must_haves.truths` estão sustentados pela suíte já verde e pelos passos 1–5.
    - Cada um dos 15 objetos `verification: backstop` recebe confirmação visual explícita ou um desvio nomeado; nenhum é omitido.
    - Login, shell, CRUD, modal e dashboard permanecem navegáveis em desktop/móvel sem edição de código e com as cópias contratadas.
    - O login aprovado usa exatamente `nascimento@example.invalid` e a `NASCIMENTO_ADMIN_PASSWORD` entregue pela Task 1 na mesma sessão; nenhuma credencial fixa alternativa é tentada.
    - Nenhuma credencial ou valor de `.env` é copiado para arquivo persistente, screenshot pública ou relatório versionado.
    - Aprovação só ocorre se 32/32 estiverem confirmados; qualquer problema é devolvido com o identificador `[surface/state]` correspondente.
  </acceptance_criteria>
  <verify>
    <automated>test -n "$NASCIMENTO_ADMIN_PASSWORD" &amp;&amp; curl -fsS "$NASCIMENTO_URL/healthz" &gt;/dev/null &amp;&amp; curl -fsS "$NASCIMENTO_URL/login/" &gt;/dev/null</automated>
    <human-check>Executar os cinco passos acima e reconciliar a evidência com os 17 strings covered e 15 objetos backstop em `must_haves.truths`.</human-check>
  </verify>
  <resume-signal>Digite "approved" se 32/32 passaram, ou descreva os identificadores que falharam. Em ambos os casos, o executor deve concluir a Task 3 de cleanup antes de encerrar ou corrigir.</resume-signal>
  <done>Há decisão humana explícita sobre os 32 itens do UI-SPEC no sistema gerado; aprovação exige 32/32, e desvios são identificados sem alteração oportunista da UI.</done>
</task>

<task type="auto">
  <name>Task 3: Desmontar e apagar somente o ambiente inspecionado</name>
  <precondition>O checkpoint devolveu aprovação ou lista de desvios, e os valores exatos `NASCIMENTO_DESTINO`/`NASCIMENTO_PROJETO_COMPOSE` da Task 1 ainda estão disponíveis.</precondition>
  <reversibility rating="reversible">A remoção atinge apenas dados explicitamente descartáveis criados pelo ensaio e deixa o checkout intacto.</reversibility>
  <read_first>
    - .template-tests/test_05_nascimento.sh (contrato de ownership/cleanup; não modificar)
    - .planning/phases/05-verifica-o-e-documenta-o/05-PATTERNS.md (ephemeral-resource ownership)
    - .planning/phases/05-verifica-o-e-documenta-o/05-RESEARCH.md (Armadilha 4 e segurança)
  </read_first>
  <files>None — remove somente destino `mktemp` e recursos Docker registrados.</files>
  <action>
    Validar antes de qualquer remoção que `NASCIMENTO_DESTINO` é o diretório existente retornado pelo ensaio, não é vazio nem raiz/home/workspace, e que `NASCIMENTO_PROJETO_COMPOSE` coincide com o projeto alfanumérico retornado. Dentro desse destino exato, executar `docker compose --project-name "$NASCIMENTO_PROJETO_COMPOSE" --env-file .env down --volumes --remove-orphans`. Somente após status zero, remover o diretório exato com caminho validado e verificar por labels Compose que não restaram container, rede ou volume desse projeto. Preservar os valores somente até o `<verify>` concluir e então limpar da memória `NASCIMENTO_ADMIN_PASSWORD`, `NASCIMENTO_DESTINO`, `NASCIMENTO_PROJETO_COMPOSE` e `NASCIMENTO_URL`. Nunca usar prune, glob, nome inferido, outro compose ou diretório amplo. Executar esta task também quando o checkpoint relatar falhas.
  </action>
  <acceptance_criteria>
    - O comando de down usa exatamente o projeto, compose e `.env` da cópia retida e retorna zero.
    - Nenhum container, rede ou volume com label `com.docker.compose.project=$NASCIMENTO_PROJETO_COMPOSE` permanece.
    - O caminho exato `NASCIMENTO_DESTINO` deixa de existir; checkout, outros projetos Docker e `IDEIA.md` permanecem intocados.
    - A senha e os três campos transitórios são descartados da sessão depois das verificações.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e "$NASCIMENTO_DESTINO" &amp;&amp; test -z "$(docker ps -aq --filter label=com.docker.compose.project="$NASCIMENTO_PROJETO_COMPOSE")" &amp;&amp; test -z "$(docker network ls -q --filter label=com.docker.compose.project="$NASCIMENTO_PROJETO_COMPOSE")" &amp;&amp; test -z "$(docker volume ls -q --filter label=com.docker.compose.project="$NASCIMENTO_PROJETO_COMPOSE")" &amp;&amp; unset NASCIMENTO_ADMIN_PASSWORD NASCIMENTO_DESTINO NASCIMENTO_PROJETO_COMPOSE NASCIMENTO_URL</automated>
  </verify>
  <done>Ambiente, credencial, containers, rede, volume e diretório do checkpoint foram removidos sem atingir recursos alheios ao ensaio.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Provar nascimento completo, suíte verde, navegação sem editar código e README até proxy/DNS | 05-01..03 | COVERED | Tracer → runbook → checkpoint/cleanup. |
| REQ | QA-01 | Suíte de core e app exemplo passa no sistema gerado | 05-01, 05-03 | COVERED | Runner Django dentro da cópia; UI-SPEC reconciliado. |
| REQ | QA-02 | copy + `.env` + Compose + migrate + createsuperuser produz sistema navegável | 05-01, 05-03 | COVERED | Automação real seguida de inspeção manual. |
| REQ | DOC-01 | README do template do `copier copy` ao proxy/DNS | 05-02 | COVERED | Sequência local e publicação externa completas. |
| RESEARCH | — | Ensaio efêmero único em cópia `incluir_app_exemplo=true` | 05-01 | COVERED | Não duplica a matriz estática da Fase 4. |
| RESEARCH | — | Config/boot/prontidão/migrate/admin/testes/smoke/cleanup com diagnóstico | 05-01 | COVERED | Script POSIX e Compose confinado. |
| RESEARCH | — | README em camadas com jornada canônica e handoff a `ops/MIGRACAO.md` | 05-02 | COVERED | Dois slices no mesmo runbook. |
| RESEARCH | A1 | Sem browser harness novo; inspeção manual é complementar | 05-03 | COVERED | Checkpoint bloqueante após automação. |
| RESEARCH | — | Segredos efêmeros, bind loopback, cleanup e shell seguro | 05-01..03 | COVERED | Threat models ASVS L1 em todos os planos. |
| CONTEXT | D-37..D-40 | Template in-place, dois READMEs e zero automação pós-copy | 05-01, 05-02 | COVERED | Fonte não é executada; passos continuam conscientes. |
| CONTEXT | D-41..D-52 | Respostas/validators, segredos fora do Copier, `.env`-first, Compose isolado e ícones | 05-01, 05-02 | COVERED | Consumidos/validados sem reimplementação. |
| CONTEXT | D-53..D-56 | Tags/update e app opcional | 05-01, 05-02 | COVERED | Regressões da Fase 4 continuam obrigatórias; nascimento usa exemplo true. |
| CONTEXT | D-57..D-60 | Backup containerizado/offsite e knobs env | 05-01, 05-02 | COVERED | Boot de QA isola db/web; README encaminha operação de backup existente. |
| CONTEXT | D-61..D-64 | Vhost TLS, migração e restore confinado | 05-02 | COVERED | Runbook raiz resume proxy/DNS e aponta ao detalhe renderizado. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → navegador humano | URL e credencial efêmeras são apresentadas somente durante o checkpoint. |
| Browser → aplicação loopback | Login e interações alteram apenas o banco descartável da cópia retida. |
| Checkpoint → cleanup Docker/filesystem | A retomada deve remover recursos mesmo quando a verificação relata falha. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Information Disclosure | credencial efêmera do checkpoint | high | mitigate | Senha aleatória somente em variável/processo, compartilhada apenas no checkpoint, não versionada/logada e destruída com o ambiente. |
| T-05-12 | Elevation of Privilege / Information Disclosure | aplicação retida | high | mitigate | URL exclusivamente `127.0.0.1`, vida limitada ao checkpoint e cleanup obrigatório após aprovação ou falha. |
| T-05-13 | Tampering / Denial of Service | cleanup pós-checkpoint | high | mitigate | Validar destino/projeto registrados, usar `down --volumes --remove-orphans` nesse projeto e confirmar labels antes de encerrar. |
| T-05-14 | Repudiation | aprovação visual incompleta | medium | mitigate | 17 strings + 15 objetos backstop serializados sem perda; resume exige aprovação 32/32 ou identificadores de falha. |
| T-05-15 | Tampering | DevTools/estados manuais | medium | mitigate | Throttling/request blocking são temporários no navegador; nenhuma edição de código, template, CSS, JS ou `.env` é permitida. |
</threat_model>

<verification>
1. O ambiente retido já passou `.template-tests/test_05_nascimento.sh`, possui 25 itens e responde em `/healthz` e `/login/`.
2. O checkpoint reconcilia exatamente 17 critérios covered e 15 objetos backstop do UI-SPEC — 32/32, sem flattening dos marcadores.
3. Login, shell, CRUD/modal e dashboard são inspecionados na mesma cópia, com desktop/móvel e estados de erro/loading sem editar código.
4. Após qualquer resume, o projeto Compose, seus containers/rede/volume, o destino e a credencial deixam de existir.
</verification>

<success_criteria>
- QA-01/QA-02 têm prova automatizada e evidência humana complementar no produto recém-gerado.
- Todos os 32 UI Considerations permanecem representados exatamente como 17 strings e 15 objetos `verification: backstop`.
- A Fase 5 não cria UI, dependência frontend ou harness de navegador.
- `IDEIA.md` e qualquer recurso Docker externo ao ensaio permanecem intocados.
</success_criteria>

<output>
Create `.planning/phases/05-verifica-o-e-documenta-o/05-03-SUMMARY.md` when done.
</output>
