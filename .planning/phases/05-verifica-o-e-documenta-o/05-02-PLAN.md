---
phase: 05-verifica-o-e-documenta-o
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
autonomous: true
requirements: [DOC-01, QA-01, QA-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um operador percorre no README-raiz a jornada completa, em ordem, do Copier copy ao login/shell/CRUD/dashboard sem executar Django ou Compose na árvore Jinja."
    - "O README separa respostas Copier de segredos locais, documenta a criação do .env e mantém `.copier-answers.yml` versionado sem credenciais."
    - "A publicação conserva o bind em 127.0.0.1, usa o vhost gerado para Nginx/TLS, orienta DNS e valida `/healthz` externamente."
    - "Os comandos de regressão distinguem contratos da fonte, ensaio real de nascimento e inspeção manual complementar, sem alegar automação de navegador."
  artifacts:
    - path: "README.md"
      provides: "Runbook canônico do template, do nascimento local à publicação por proxy/TLS/DNS"
      contains: ".template-tests/test_05_nascimento.sh"
  key_links:
    - from: "README.md"
      to: "README.md.jinja"
      via: "handoff explícito para a operação cotidiana do sistema já renderizado"
      pattern: "README.*gerado|README.*renderizado"
    - from: "README.md"
      to: "ops/MIGRACAO.md"
      via: "link para restore, VM, Nginx, TLS, DNS e recuperação detalhados"
      pattern: "ops/MIGRACAO.md"
    - from: "README.md"
      to: ".template-tests/test_05_nascimento.sh"
      via: "mesma sequência Copier/.env/Compose/Django/HTTP usada pelo runbook"
      pattern: "test_05_nascimento.sh"
  prohibitions:
    - requirement_id: "DOC-01"
      category: "safety"
      statement: "O README do template não deve instruir o operador a executar Django ou Docker Compose diretamente na árvore-fonte que contém Jinja."
      status: unverified
      flagged: true
    - requirement_id: "DOC-01"
      category: "transparency"
      statement: "O README não deve alegar que o ensaio shell automatiza cliques ou regressão visual; a inspeção de navegador deve permanecer identificada como evidência manual complementar."
      status: unverified
      flagged: true
  assumptions:
    - requirement_id: "DOC-01"
      classification: unclassified
      status: unresolved
      statement: "O probe spec-less não classificou DOC-01; o plano assume que o README-raiz é o runbook canônico e compensa a incerteza exigindo ordem executável, comandos concretos e links para os detalhes renderizados e de migração."
---

## Phase Goal

**As a** operador do template, **I want to** provar o nascimento de uma cópia limpa, **so that** o sistema gerado fique testado, navegável e publicável sem editar código.

<objective>
Transformar o README-raiz em uma jornada operacional completa e verificável, sincronizada com o tracer aprovado da Wave 1.

Purpose: Entregar DOC-01 sem duplicar internals perigosos de restore nem delegar etapas essenciais a conhecimento implícito.
Output: `README.md` com nascimento local, navegação, regressão e publicação por proxy/TLS/DNS em ordem operacional.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verifica-o-e-documenta-o/05-RESEARCH.md
@.planning/phases/05-verifica-o-e-documenta-o/05-PATTERNS.md
@.planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-06-SUMMARY.md
@.planning/phases/04-templatiza-o-copier/04-07-SUMMARY.md
@.planning/phases/05-verifica-o-e-documenta-o/05-01-SUMMARY.md
@README.md
@README.md.jinja
@ops/MIGRACAO.md.jinja
</context>

## Artifacts this phase produces

- **Arquivo modificado:** `README.md`.
- **Novos arquivos:** nenhum.
- **Seções/âncoras Markdown criadas:** `## Nascimento local de um sistema` e `## Publicação com proxy, TLS e DNS`.
- **Símbolos, funções, classes, campos e test helpers:** nenhum.
- **Flags de CLI criadas:** nenhuma; o README apenas documenta flags já existentes do Copier, Docker Compose, Django, curl, Certbot e Nginx.
- **Scripts criados neste plano:** nenhum; o runbook referencia `.template-tests/test_05_nascimento.sh` criado no Plano 05-01.

<planner_assumptions>
- DOC-01 permanece `unclassified/unresolved` no probe spec-less; a estrutura linear e os gates de conteúdo abaixo são a compensação concreta.
</planner_assumptions>

<prohibition_probe>
- Mantidas em `must_haves.prohibitions`: a fronteira template-fonte/sistema gerado e a transparência sobre o limite do ensaio shell.
- Segredos em documentação, exposição pública sem TLS e comandos de shell inseguros são cânones de segurança: cobertos pelo `<threat_model>` e encaminhados a `$gsd-secure-phase`; não foram cunhados como proibições bespoke.
</prohibition_probe>

<tasks>

<task type="auto">
  <name>Task 1: Documentar o nascimento local até as telas navegáveis</name>
  <precondition>O Plano 05-01 concluiu com `.template-tests/test_05_nascimento.sh` executável e a Fase 4 continua fornecendo `copier.yml`, `.env.example.jinja`, `compose.yml.jinja` e os READMEs separados.</precondition>
  <reversibility rating="reversible">A ordem e o texto do runbook são locais ao README e podem ser corrigidos sem migração, mudança de schema ou quebra de protocolo de máquina.</reversibility>
  <read_first>
    - README.md (arquivo modificado; preservar fronteira do template e instruções de update/release existentes)
    - .planning/phases/05-verifica-o-e-documenta-o/05-RESEARCH.md (Padrão 2 e conteúdo obrigatório)
    - .planning/phases/05-verifica-o-e-documenta-o/05-PATTERNS.md (analog do README)
    - .planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md (URLs/telas e limite da evidência)
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-39, D-40, D-45, D-48, D-49, D-52)
    - README.md.jinja
    - .env.example.jinja
    - compose.yml.jinja
    - .template-tests/test_05_nascimento.sh
  </read_first>
  <files>README.md</files>
  <action>
    Reorganizar o README-raiz mantendo no início a fronteira inequívoca de que este checkout é o template-fonte e conservando a instalação isolada de `copier==9.17.1`. Criar a seção `## Nascimento local de um sistema` como sequência numerada única: pré-requisitos; seleção de tag estável; `copier copy` fora do destino; explicação das oito respostas; entrada no projeto gerado; `cp .env.example .env`; geração local de `SECRET_KEY`; preenchimento coerente de `POSTGRES_PASSWORD` e `DATABASE_URL`; credenciais R2 separadas da prova inicial; `git init`/primeiro commit incluindo `.copier-answers.yml`; `docker compose --env-file .env config -q`; `docker compose up -d --build db web`; logs; `docker compose exec -T web python manage.py migrate --noinput`; `docker compose exec web python manage.py createsuperuser`; e `curl -fsS` em `http://127.0.0.1:&lt;porta&gt;/healthz`. Deixar explícito que segredos nunca são respostas Copier per D-48 e que não existem `_tasks` per D-40.

    Após o health check, listar as URLs da cópia com app exemplo: `/login/`, `/`, `/exemplo/` e `/exemplo/dashboard/`, explicando login, shell, CRUD e dashboard sem inventar telas. Manter o README renderizado como guia cotidiano do sistema específico e o protocolo de ícones/update já existente; não mover comandos Django/Compose para antes da entrada no destino gerado. Fazer o texto espelhar os marcos realmente executados pelo tracer, distinguindo `exec -T` não interativo do `createsuperuser` interativo do operador.
  </action>
  <acceptance_criteria>
    - O README começa identificando a raiz como template-fonte não executável e mantém Copier 9.17.1 na `.venv-template`.
    - Dentro de `## Nascimento local de um sistema`, os marcadores `copier copy`, `cp .env.example .env`, `docker compose --env-file .env config -q`, `docker compose up -d --build db web`, `migrate --noinput`, `createsuperuser` e `/healthz` aparecem nesta ordem.
    - `SECRET_KEY`, `POSTGRES_PASSWORD`, `DATABASE_URL` e credenciais R2 são descritos como valores locais do `.env`; `.copier-answers.yml` é versionado sem credenciais.
    - O runbook fornece URLs concretas para `/login/`, `/`, `/exemplo/` e `/exemplo/dashboard/` e associa cada uma à tela entregue.
    - Nenhum comando Django/Compose é instruído contra a raiz do template.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from pathlib import Path; t=Path('README.md').read_text(); s=t.split('## Nascimento local de um sistema',1)[1]; m=['copier copy','cp .env.example .env','docker compose --env-file .env config -q','docker compose up -d --build db web','migrate --noinput','createsuperuser','/healthz']; p=[s.index(x) for x in m]; assert p == sorted(p); [s.index(x) for x in ['/login/','/exemplo/','/exemplo/dashboard/']]"</automated>
  </verify>
  <done>Um operador consegue gerar, configurar, iniciar, migrar, criar administrador e localizar login/shell/CRUD/dashboard seguindo somente o README-raiz e sem editar código.</done>
</task>

<task type="auto">
  <name>Task 2: Fechar proxy, TLS, DNS e regressão no runbook canônico</name>
  <reversibility rating="reversible">A camada de orientação referencia artefatos operacionais já gerados; desfazer o texto não altera vhost, DNS, certificados ou sistemas implantados.</reversibility>
  <read_first>
    - README.md (já atualizado pela Task 1; arquivo modificado)
    - .planning/phases/05-verifica-o-e-documenta-o/05-RESEARCH.md (conteúdo obrigatório e armadilha de divergência documental)
    - .planning/phases/05-verifica-o-e-documenta-o/05-PATTERNS.md (layered production handoff e comandos concretos)
    - .planning/phases/05-verifica-o-e-documenta-o/05-UI-SPEC.md (inspeção manual complementar)
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-51, D-57..D-64)
    - README.md.jinja
    - ops/MIGRACAO.md.jinja
    - ops/nginx/{% raw %}{{ sistema_slug }}{% endraw %}.conf.jinja
    - compose.yml.jinja
    - .template-tests/test_copier_copy.sh
    - .template-tests/test_copier_update.sh
    - .template-tests/test_05_nascimento.sh
  </read_first>
  <files>README.md</files>
  <action>
    Criar `## Publicação com proxy, TLS e DNS` após o nascimento local. Resumir a ordem operacional comprovável: preparar VM; manter `WEB_BIND_ADDRESS=127.0.0.1`; criar o registro DNS do hostname; liberar somente 80/443; obter certificado com Certbot; instalar o vhost já gerado em `ops/nginx/&lt;slug&gt;.conf`; habilitá-lo; executar `sudo nginx -t`; reiniciar Nginx; e validar `https://&lt;hostname&gt;/healthz` externamente. Referenciar `ops/MIGRACAO.md` como fonte dos detalhes de restore/recuperação, sem copiar os comandos destrutivos de banco para o roteiro de primeiro nascimento. Explicar que Nginx termina TLS e encaminha à porta loopback per D-61/D-62, e manter o README renderizado como detalhe do sistema específico per D-39/D-63.

    Consolidar `## Regressão do template` com os comandos executáveis `.template-tests/test_copier_copy.sh`, `.template-tests/test_copier_update.sh`, `python3 -m unittest discover -s .template-tests -p 'test_04_*.py'` e `.template-tests/test_05_nascimento.sh`. Descrever com precisão que o último gera uma cópia descartável, valida Compose, sobe somente `db`/`web`, executa `core` + `apps.exemplo`, faz smoke em saúde/login e limpa seus recursos; declarar a inspeção de navegador como checkpoint manual complementar, sem atribuir automação visual ao shell. Preservar as seções existentes de portas, releases, updates serializados e resolução de conflitos.
  </action>
  <acceptance_criteria>
    - `## Publicação com proxy, TLS e DNS` contém loopback, DNS, portas 80/443, Certbot, vhost gerado, `nginx -t`, HTTPS externo e link `ops/MIGRACAO.md` na ordem de publicação.
    - O README afirma que somente Nginx termina TLS/publica o sistema e que a aplicação continua em `127.0.0.1:&lt;porta&gt;`.
    - A seção de regressão lista os dois scripts da Fase 4, os quatro contratos Python e `.template-tests/test_05_nascimento.sh`.
    - A descrição do ensaio de nascimento corresponde ao comportamento implementado e qualifica inspeção visual como manual, sem nova framework de browser.
    - Portas, releases semver, `copier update`, árvore Git limpa e recuperação de conflito já documentados permanecem presentes.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from pathlib import Path; import re; t=Path('README.md').read_text(); s=t.split('## Publicação com proxy, TLS e DNS',1)[1]; required=['WEB_BIND_ADDRESS=127.0.0.1','DNS','certbot','ops/nginx/','nginx -t','https://','ops/MIGRACAO.md','.template-tests/test_copier_copy.sh','.template-tests/test_copier_update.sh',\"python3 -m unittest discover -s .template-tests -p 'test_04_*.py'\",'.template-tests/test_05_nascimento.sh']; [s.index(x) for x in required]; assert re.search(r'\b80\b',s) and re.search(r'\b443\b',s)"</automated>
  </verify>
  <done>O README-raiz cobre nascimento, navegação, regressão e publicação até proxy/TLS/DNS, com handoffs explícitos aos documentos renderizados e ao runbook de migração.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README do template → shell do operador | Comandos copiados do documento criam repositório, `.env`, containers e usuário administrativo. |
| Sistema loopback → Nginx/TLS/DNS público | A documentação conecta o serviço interno à fronteira externa de produção. |
| README-raiz → documentos renderizados | O roteiro canônico delega detalhes ao README do sistema e ao runbook de migração. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Information Disclosure | instruções de `.env` e superusuário | high | mitigate | Gerar segredos localmente, nunca usá-los como respostas Copier/valores versionados e manter o comando interativo de administrador fora de logs automatizados. |
| T-05-07 | Elevation of Privilege / Information Disclosure | proxy e publicação Docker | high | mitigate | Documentar bind obrigatório em 127.0.0.1, Nginx como única borda, TLS antes do vhost ativo e validação externa de `/healthz`. |
| T-05-08 | Tampering | comandos copiados do README | high | mitigate | Usar caminhos explícitos, argumentos entre aspas nos exemplos sensíveis, validação `docker compose config -q` e `nginx -t` antes de iniciar/recarregar. |
| T-05-09 | Repudiation | divergência entre runbook e ensaio | medium | mitigate | Ordenar README pelos mesmos marcos do tracer e listar os comandos de regressão que provam fonte e cópia gerada. |
| T-05-10 | Denial of Service | restore/recuperação no primeiro nascimento | medium | mitigate | Manter procedimentos de restore no `ops/MIGRACAO.md` e não misturá-los ao caminho de criação de banco novo. |
</threat_model>

<verification>
1. As duas verificações de conteúdo/ordem do README passam.
2. `.template-tests/test_copier_copy.sh`, `.template-tests/test_copier_update.sh` e `python3 -m unittest discover -s .template-tests -p 'test_04_*.py'` continuam verdes.
3. `.template-tests/test_05_nascimento.sh` continua executando os mesmos marcos documentados.
4. `git diff -- README.md` mostra somente documentação do template; `README.md.jinja` e `ops/MIGRACAO.md.jinja` não são reimplementados.
</verification>

<success_criteria>
- DOC-01 fica autocontido no README-raiz do `copier copy` ao proxy/DNS.
- QA-01/QA-02 ficam rastreáveis dos comandos documentados ao tracer executável.
- O documento mantém segredos fora do Copier/Git, serviço em loopback e TLS na borda.
</success_criteria>

<output>
Create `.planning/phases/05-verifica-o-e-documenta-o/05-02-SUMMARY.md` when done.
</output>
