---
phase: 02-shell-visual-e-kernel
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - requirements.txt
  - config/settings/base.py
  - core/apps.py
  - core/admin_site.py
  - core/admin.py
  - core/templates/admin/base_site.html
  - core/migrations/0002_historicalusuario.py
  - core/README.md
  - core/tests/test_admin.py
  - core/tests/test_auditoria.py
autonomous: true
requirements: [CORE-03, CORE-06]
must_haves:
  truths:
    - "Administrador acessa /admin/ e vê o nome do sistema no header e a cor primária aplicada (CORE-03)"
    - "core/admin_site.py NÃO contém nenhum register — registros vivem só em core/admin.py (D-13, Pitfall 1)"
    - "Usuario aparece e é editável no admin (UsuarioAdmin por e-mail, sem username)"
    - "Salvar um Usuario gera registro em HistoricalUsuario, e edições via request registram history_user (CORE-06, D-21/D-22)"
    - "core/README.md documenta a convenção de auditoria para modelos de domínio: history = HistoricalRecords() (D-23)"
  artifacts:
    - path: "core/admin_site.py"
      provides: "SistemaAdminSite isolado com identidade via settings e admin_tema_css em each_context"
      contains: "each_context"
    - path: "core/apps.py"
      provides: "SistemaAdminConfig com default_site"
      contains: "default_site"
    - path: "core/admin.py"
      provides: "simple_history.register(Usuario) + UsuarioAdmin sem username"
      contains: "simple_history.register"
    - path: "core/templates/admin/base_site.html"
      provides: "Override cirúrgico do bloco extrastyle"
      contains: "extrastyle"
    - path: "core/migrations/0002_historicalusuario.py"
      provides: "Tabela core_historicalusuario"
    - path: "core/README.md"
      provides: "Convenção D-23 + alerta queryset.update() sem histórico"
      contains: "HistoricalRecords"
  key_links:
    - from: "config/settings/base.py"
      to: "core/apps.py"
      via: "INSTALLED_APPS substitui django.contrib.admin"
      pattern: "core\\.apps\\.SistemaAdminConfig"
    - from: "core/apps.py"
      to: "core/admin_site.py"
      via: "default_site"
      pattern: "core\\.admin_site\\.SistemaAdminSite"
    - from: "core/admin_site.py"
      to: "core/templates/admin/base_site.html"
      via: "admin_tema_css no each_context renderizado no bloco extrastyle"
      pattern: "admin_tema_css"
    - from: "config/settings/base.py"
      to: "simple_history"
      via: "INSTALLED_APPS + HistoryRequestMiddleware"
      pattern: "HistoryRequestMiddleware"
---

<objective>
Entregar o admin com identidade visual do sistema (CORE-03: `AdminSite` isolado + override cirúrgico do `extrastyle`) e a auditoria padrão do template (CORE-06: `django-simple-history` instalado, `Usuario` registrado, convenção documentada). As duas entregas compartilham `core/admin.py` e `config/settings/base.py` — por isso vivem na mesma plan.

Purpose: critérios 2 e 4 da fase; o padrão de auditoria é a convenção que os modelos de domínio da Fase 3 exercitarão.
Output: `/admin/` com nome e cor do sistema, `HistoricalUsuario` migrando e gravando, README com a convenção, testes `test_admin.py` e `test_auditoria.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-visual-e-kernel/02-CONTEXT.md
@.planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md
@.planning/phases/02-shell-visual-e-kernel/02-01-SUMMARY.md

<interfaces>
Disponível desde a plan 02-01:
- `settings.SISTEMA_NOME`, `settings.SISTEMA_SIGLA`, `settings.COR_PRIMARIA` (COR_PRIMARIA já validada como #RRGGBB no boot — mitiga T-02-01 antes do |safe usado aqui)

Contrato que ESTA plan cria:
- `core.admin_site.SistemaAdminSite` — site default do projeto (via AdminConfig; `config/urls.py` NÃO muda — D-13)
- `each_context` injeta `admin_tema_css` = `:root { --primary: <cor>; --header-bg: <cor>; --link-fg: <cor>; }` (3 variáveis confirmadas no base.css do Django 5.2)
- Convenção de auditoria (README): modelos de domínio declaram `history = HistoricalRecords()`; user model é exceção via `register()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependência simple-history + settings + AdminSite isolado</name>
  <files>requirements.txt, config/settings/base.py, core/apps.py, core/admin_site.py</files>
  <read_first>
    - /opt/web/pca/core/admin_site.py (SOMENTE LEITURA — a docstring documenta o bug de reentrância do LazyObject._setup() que obriga o módulo isolado; REMOVER ORDEM_GRUPOS_ADMIN/get_app_list/_slug_ascii — D-15; NÃO copiar has_permission custom — Assumption A1)
    - /opt/web/pca/core/apps.py (SOMENTE LEITURA — padrão AdminConfig com import aliased e default=False)
    - /opt/web/pca/config/settings/base.py (SOMENTE LEITURA — posições exatas de simple_history em INSTALLED_APPS e do HistoryRequestMiddleware)
    - config/settings/base.py e core/apps.py (estado atual — onde inserir)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Pattern 2 — código recomendado completo; Pitfalls 1 e 2)
  </read_first>
  <action>
    Em `requirements.txt`, adicionar `django-simple-history==3.13.0` (única dependência nova permitida na fase — stack fechada; auditoria de legitimidade [OK] no RESEARCH).

    Em `core/apps.py` (Pattern 2 tem o código): manter `CoreConfig`; adicionar `SistemaAdminConfig` herdando de `django.contrib.admin.apps.AdminConfig` via import aliased (`import django.contrib.admin.apps as _django_admin_apps` — nunca `from ... import AdminConfig`, comentário pt-BR explica), SEM redeclarar `name` (herda "django.contrib.admin"), com `default_site = "core.admin_site.SistemaAdminSite"` e `default = False`.

    Criar `core/admin_site.py` com APENAS `SistemaAdminSite(admin.AdminSite)` — zero `register` neste módulo (D-13; docstring pt-BR resume o bug de reentrância do LazyObject que apaga registros em silêncio). `__init__` define `site_header = f"{settings.SISTEMA_NOME} — Administração"`, `site_title = settings.SISTEMA_SIGLA`, `index_title = "Painel do administrador"`. `each_context` adiciona `admin_tema_css = f":root {{ --primary: {cor}; --header-bg: {cor}; --link-fg: {cor}; }}"` com `cor = settings.COR_PRIMARIA` (comentário: as 3 variáveis são as confirmadas no base.css do Django 5.2; o valor é seguro porque COR_PRIMARIA é validada no boot). NÃO sobrescrever `has_permission` — manter o gate padrão do Django (`is_active and is_staff`), comentado como decisão deliberada (A1).

    Em `config/settings/base.py`: SUBSTITUIR a linha `"django.contrib.admin"` por `"core.apps.SistemaAdminConfig"` em `INSTALLED_APPS` (uma entrada só — duas geram `Application labels aren't unique` — Pitfall 2); adicionar `"simple_history"` após `"django_htmx"` e antes de `"axes"`; em `MIDDLEWARE`, inserir `"simple_history.middleware.HistoryRequestMiddleware"` imediatamente após `"django.contrib.auth.middleware.AuthenticationMiddleware"` e antes de `"django_htmx.middleware.HtmxMiddleware"` (ordem da PCA; comentário pt-BR: precisa vir depois do auth para capturar request.user como history_user).

    Rebuild para instalar a dependência: `docker compose build web`.
  </action>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web -c "import simple_history; import django; django.setup(); from django.conf import settings; assert 'simple_history' in settings.INSTALLED_APPS" 2>/dev/null || docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "django-simple-history==3.13.0" requirements.txt` retorna 1
    - `grep -c '"django.contrib.admin"' config/settings/base.py` retorna 0 e `grep -c "core.apps.SistemaAdminConfig" config/settings/base.py` retorna 1
    - `grep -c "HistoryRequestMiddleware" config/settings/base.py` retorna 1, e a linha aparece DEPOIS de AuthenticationMiddleware e ANTES de HtmxMiddleware (verificável com `grep -n`)
    - `grep -c "register" core/admin_site.py` retorna 0 (módulo isolado — D-13)
    - `grep -c "default_site" core/apps.py` retorna 1 e `grep -c "default = False" core/apps.py` retorna 1
    - `docker compose build web` conclui sem erro (simple-history instalado na imagem)
    - `docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py check` sai com código 0
  </acceptance_criteria>
  <done>simple-history instalado, admin config substituído sem duplicata, AdminSite isolado com identidade via settings.</done>
</task>

<task type="auto">
  <name>Task 2: core/admin.py + override do template + migração HistoricalUsuario</name>
  <files>core/admin.py, core/templates/admin/base_site.html, core/migrations/0002_historicalusuario.py</files>
  <read_first>
    - /opt/web/pca/core/admin.py (SOMENTE LEITURA — topo com simple_history.register; UsuarioAdmin adaptado a e-mail; IGNORAR admin_grupo e ações em massa — D-15)
    - /opt/web/pca/core/templates/admin/base_site.html (SOMENTE LEITURA — override do extrastyle, verbatim)
    - core/models.py (campos reais do Usuario da Fase 1 — fieldsets devem referenciar só campos existentes, sem username)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Pattern 8 + Code Examples — esqueleto do admin.py; Pitfalls 10 e 12)
  </read_first>
  <action>
    Criar `core/admin.py`: no topo, `simple_history.register(Usuario)` com comentário pt-BR citando a exigência oficial (user model swappable NÃO pode declarar HistoricalRecords no próprio modelo — a FK de history_user cria dependência circular; `register()` é a forma oficial — D-22). Depois, `@admin.register(Usuario)` com `UsuarioAdmin(UserAdmin)`: `ordering = ("email",)`, `list_display = ("email", "first_name", "last_name", "is_staff", "is_active", "last_login")`, `search_fields = ("email", "first_name", "last_name")`, e `fieldsets`/`add_fieldsets` REESCRITOS sem `username` (o UserAdmin padrão referencia username e quebraria com o model da Fase 1 — usar email + password no primeiro fieldset, dados pessoais, permissões, datas). Sem ações em massa (D-15; se alguma entrar no futuro, deve usar `bulk_update_with_history` — Pitfall 9).

    Criar `core/templates/admin/base_site.html` (nome e caminho EXATOS — Pitfall 12): `{% extends "admin/base_site.html" %}` + bloco `extrastyle` com `{{ block.super }}` e `<style>{{ admin_tema_css|safe }}</style>` — nada mais (override cirúrgico, D-14). Comentário pt-BR: o extends recursivo funciona porque o loader DIRS (core/templates) resolve antes do APP_DIRS (contrib.admin).

    Gerar a migração no host via bind-mount pontual (padrão do Plan 01-03): `docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py makemigrations core` — deve criar `core/migrations/0002_*.py` com `CreateModel` de `HistoricalUsuario` (Pitfall 10). Se o nome gerado diferir de `0002_historicalusuario.py`, manter o nome gerado e registrar no SUMMARY. Em seguida `docker compose up -d --build` (rebuild embute admin.py/migração; o entrypoint aplica o migrate). Aguardar `web` healthy.
  </action>
  <verify>
    <automated>docker compose exec -T web python manage.py showmigrations core | grep -c "\[X\] 0002"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "simple_history.register(Usuario)" core/admin.py` retorna 1
    - `grep -c "username" core/admin.py` retorna 0
    - `ls core/migrations/0002_*.py` encontra exatamente 1 arquivo e `grep -c "HistoricalUsuario" core/migrations/0002_*.py` >= 1
    - `grep -c "admin_tema_css" core/templates/admin/base_site.html` retorna 1 e o arquivo tem <= 10 linhas úteis (override cirúrgico)
    - `docker compose exec -T web python manage.py showmigrations core` mostra `[X]` na migração 0002 (aplicada)
    - `docker compose ps` mostra `web` healthy
  </acceptance_criteria>
  <done>Usuario registrado no admin (por e-mail), override do tema no lugar, tabela core_historicalusuario criada e migrada.</done>
</task>

<task type="auto">
  <name>Task 3: Testes de admin e auditoria + convenção no README</name>
  <files>core/tests/test_admin.py, core/tests/test_auditoria.py, core/README.md</files>
  <read_first>
    - /opt/web/pca/core/tests/test_admin.py (SOMENTE LEITURA — cobertura de referência: só os testes de identidade/registro, sem grupos/ações — D-15; manter o padrão @override_settings de cookies)
    - core/README.md (estado atual — 3 convenções; a de auditoria vira a 4ª, mesmo formato)
    - core/admin_site.py e core/admin.py (estado pós Tasks 1-2 — o que assertar)
  </read_first>
  <action>
    Criar `core/tests/test_admin.py` com `@override_settings(SESSION_COOKIE_SECURE=False, CSRF_COOKIE_SECURE=False)`:
    - `django.contrib.admin.site` é instância de `core.admin_site.SistemaAdminSite` (prova do default_site via AdminConfig).
    - Logado como superuser, GET `/admin/` retorna 200 e o HTML contém `settings.SISTEMA_NOME` (site_header) e `--primary: {settings.COR_PRIMARIA}` (tema injetado via extrastyle).
    - GET `/admin/core/usuario/` retorna 200 (Usuario registrado); usuário staff-mas-não-superuser AINDA acessa `/admin/` (200) — trava a decisão A1 (gate padrão is_staff, sem gate superuser da PCA).
    - Anônimo em `/admin/` é redirecionado para login (302).

    Criar `core/tests/test_auditoria.py`:
    - Criar um `Usuario` via ORM → `Usuario.history.count() == 1` e o registro é do tipo `+` (criação) — prova da tabela HistoricalUsuario viva.
    - Logado como superuser no client, POST no change form do admin (`/admin/core/usuario/<id>/change/`) alterando `first_name` → o registro mais recente de `usuario.history` tem `history_user` igual ao superuser logado — prova do HistoryRequestMiddleware capturando o autor.
    - `"simple_history.middleware.HistoryRequestMiddleware"` presente em `settings.MIDDLEWARE` após o AuthenticationMiddleware (asserta índices relativos).

    Em `core/README.md`, adicionar a convenção nº 4, "Auditoria de modelos com django-simple-history" (D-23), no mesmo formato das existentes: modelos de domínio optam por auditoria declarando `history = HistoricalRecords()` no modelo (o `apps/exemplo` da Fase 3 exercita); o user model é a exceção documentada — `simple_history.register(Usuario)` em `core/admin.py`, porque HistoricalRecords direto num model swappable cria dependência circular; alerta: `queryset.update()` NÃO gera histórico — usar `bulk_update_with_history` (Pitfall 9); toda mudança via request registra `history_user` automaticamente pelo middleware.

    Rodar a suíte completa no container (código de teste precisa estar na imagem: `docker compose up -d --build` antes, se ainda não rebuildado nesta task).
  </action>
  <verify>
    <automated>docker compose exec -T web python manage.py test core.tests.test_admin core.tests.test_auditoria -v 2</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose exec -T web python manage.py test core.tests.test_admin core.tests.test_auditoria -v 2` sai com código 0, sem FAIL/ERROR
    - `grep -c "HistoricalRecords" core/README.md` >= 1 e `grep -c "bulk_update_with_history" core/README.md` >= 1
    - `grep -c "SistemaAdminSite" core/tests/test_admin.py` >= 1
    - `grep -c "history_user" core/tests/test_auditoria.py` >= 1
    - Suíte completa também verde: `docker compose exec -T web python manage.py test core.tests -v 2` código 0
  </acceptance_criteria>
  <done>Admin e auditoria provados por testes de comportamento; convenção D-23 documentada no README no formato da casa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /admin/ | Superfície administrativa autenticada |
| `.env` → admin_tema_css | Valor de operador interpolado em `<style>` com `\|safe` |
| PyPI → imagem web | Dependência nova entra no build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering (CSS injection) | `{{ admin_tema_css\|safe }}` em base_site.html | mitigate | COR_PRIMARIA validada `#RRGGBB` no boot (plan 02-01, T-02-01); nenhum outro valor entra na string |
| T-02-07 | Elevation of Privilege | Acesso ao /admin/ | accept | Gate padrão do Django (`is_active and is_staff`) mantido deliberadamente (A1); teste trava o comportamento para a mudança ser sempre explícita |
| T-02-08 | Repudiation | Alterações de dados sem autor | mitigate | HistoryRequestMiddleware grava `history_user` em toda escrita via request; teste prova o vínculo |
| T-02-09 | Spoofing | Login de admin por força bruta | accept | django-axes (Fase 1) já cobre lockout por usuário+IP em todo authenticate() |
| T-02-SC | Tampering | pip install django-simple-history | mitigate | Versão pinada 3.13.0, auditoria de legitimidade `[OK]` (RESEARCH: PyPI ~12 anos, jazzband, slopcheck OK) — sem checkpoint necessário |
</threat_model>

<verification>
- `docker compose exec -T web python manage.py test core.tests -v 2` — suíte completa sem falhas.
- `grep -rn "PCA\|pca" core/admin.py core/admin_site.py core/apps.py core/templates/admin/` — zero menção a domínio.
- <human-check>No navegador: `/admin/` mostra "Sistema Base — Administração" no header com a cor primária aplicada (header azul #1e40af); lista de Usuários abre e edita normalmente; aba "Histórico" do Usuario mostra as entradas do simple-history.</human-check>
</verification>

<success_criteria>
- Admin customizado exibe nome e cor primária do sistema (critério 2 da fase) sem tocar `config/urls.py`.
- `django-simple-history` instalado, migrado, gravando com autor, e documentado como padrão (critério 4 da fase).
- Nenhum register em `core/admin_site.py`; nenhuma duplicata do app admin; suíte verde.
</success_criteria>

<output>
Create `.planning/phases/02-shell-visual-e-kernel/02-03-SUMMARY.md` when done
</output>
