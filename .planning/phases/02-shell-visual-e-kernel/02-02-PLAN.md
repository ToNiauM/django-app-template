---
phase: 02-shell-visual-e-kernel
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - core/templates/core/_breadcrumbs.html
  - core/templates/core/_nav.html
  - core/templates/core/shell.html
  - core/templates/base.html
  - core/templates/core/login.html
  - core/views.py
  - core/tests/test_shell.py
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - "Usuário logado navega em um shell com aside lateral fixa no desktop e gaveta Alpine no mobile (D-09)"
    - "A navegação vive em _nav.html com item ativo marcado por aria-current=\"page\" comparando request.path (D-10)"
    - "Breadcrumbs renderizam a partir do contrato trilha (lista de dicts rotulo/url montada pela view); o último item nunca tem url (D-12)"
    - "shell.html expõe os blocos cabecalho_pagina, titulo_pagina e conteudo_pagina — contrato que o apps/exemplo da Fase 3 consumirá (D-11)"
    - "A tela de login continua centrada após o body do base.html perder o centering global (Pitfall 11)"
    - "Título das páginas e header da aside usam {{ sistema_nome }} do context processor — nenhum nome hard-coded (D-16)"
  artifacts:
    - path: "core/templates/core/shell.html"
      provides: "Shell completo: aside + gaveta + overlay + blocos de página + rodapé com usuário/Sair"
      contains: "cabecalho_pagina"
      min_lines: 60
    - path: "core/templates/core/_nav.html"
      provides: "Ponto de extensão de navegação com contrato de item documentado"
      contains: "aria-current"
    - path: "core/templates/core/_breadcrumbs.html"
      provides: "Partial do contrato trilha"
      contains: "trilha"
    - path: "core/views.py"
      provides: "shell_view monta trilha no contexto"
      contains: "trilha"
    - path: "core/tests/test_shell.py"
      provides: "Prova de nav/breadcrumbs/blocos/login centrado"
  key_links:
    - from: "core/views.py"
      to: "core/templates/core/_breadcrumbs.html"
      via: "contexto trilha do shell_view"
      pattern: "trilha"
    - from: "core/templates/core/shell.html"
      to: "core/templates/core/_nav.html"
      via: "include dentro do x-data do shell"
      pattern: "include \"core/_nav.html\""
    - from: "core/templates/core/_nav.html"
      to: "tokens de marca (02-01)"
      via: "classes bg-brand-tint/text-brand-ink no item ativo"
      pattern: "bg-brand"
---

<objective>
Substituir a casca mínima da Fase 1 pelo shell visual completo: aside fixa + gaveta mobile Alpine (D-09), navegação como ponto de extensão (`_nav.html`, D-10), blocos de página nomeados (D-11) e breadcrumbs pelo contrato `trilha` (D-12). Corrige o Pitfall 11 (body centrado da Fase 1 quebraria a aside fixa).

Purpose: é a experiência visual central da fase (CORE-04) e a interface pública que o `apps/exemplo` da Fase 3 consumirá (blocos, trilha, nav).
Output: shell navegável por usuário logado, partials de nav/breadcrumbs, login ajustado, testes `test_shell.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-visual-e-kernel/02-CONTEXT.md
@.planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md
@.planning/phases/02-shell-visual-e-kernel/02-01-SUMMARY.md

<interfaces>
Disponível desde a plan 02-01 (usar diretamente, sem exploração):
- Context processor `identidade`: `{{ sistema_nome }}`, `{{ sistema_sigla }}`, `{{ cor_primaria }}` em todo template
- Context processor `usuario_atual` (Fase 1): `{{ usuario_atual.email }}`
- Tokens Tailwind: `bg-page`, `bg-surface`, `bg-surface-2`, `text-ink`, `text-ink-2`, `text-muted`, `border-grid`, `bg-brand`, `bg-brand-hover`, `text-brand-ink`, `bg-brand-tint`
- Regra `[x-cloak]` já no CSS

Contrato que ESTA plan cria (consumido pela Fase 3):
- Blocos do shell: `{% block cabecalho_pagina %}` (contém o include de breadcrumbs + `<h1>{% block titulo_pagina %}`), `{% block conteudo_pagina %}`
- Contrato `trilha`: lista de dicts `{"rotulo": str, "url": str|None}` montada pela view; último item sempre sem url
- Contrato de item de nav (documentado em comentário no _nav.html): classes ativo/inativo, `aria-current="page"`, `@click` que fecha a gaveta no mobile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Partials _breadcrumbs.html e _nav.html + trilha no shell_view</name>
  <files>core/templates/core/_breadcrumbs.html, core/templates/core/_nav.html, core/views.py</files>
  <read_first>
    - /opt/web/pca/core/templates/core/_breadcrumbs.html (SOMENTE LEITURA — transplante quase verbatim; só trocar exemplos do comentário por exemplos neutros)
    - /opt/web/pca/core/templates/core/_nav_visoes.html (SOMENTE LEITURA — padrão aria-current/item ativo; REMOVER hx-swap-oob e querystring, específicos do domínio PCA)
    - core/views.py (estado atual do shell_view — docstring já anuncia a substituição)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Patterns 5 e 6 — inclui o esqueleto recomendado do _nav.html)
  </read_first>
  <action>
    Criar `core/templates/core/_breadcrumbs.html` implementando D-12 (contrato D-308 da PCA): recebe `trilha` (lista de dicts `rotulo`/`url`); itens com `url` viram `<a href>`, o último (sem `url`) vira `<span aria-current="page">`; separador `/` com `aria-hidden="true"`; tipografia `text-xs font-medium uppercase text-ink-2`. Comentário pt-BR no topo documenta o contrato: a trilha é montada PELA VIEW com dados já no contexto — o partial nunca consulta banco nem usa templatetag com ORM.

    Criar `core/templates/core/_nav.html` implementando D-10 (Pattern 5 do RESEARCH tem o esqueleto): `{% url 'core:shell' as url_inicio %}`, `<nav aria-label="Navegação principal" class="flex flex-col gap-1">` com um item "Início"; item ativo quando `request.path == url_inicio` ganha `aria-current="page"`, classes `bg-brand-tint text-brand-ink` e um `<span>` de 2px `bg-brand` à esquerda; inativo usa `text-ink-2 hover:bg-surface-2`; `@click="if (!desktop) sidebarAberta = false"` fecha a gaveta no mobile (depende do x-data do shell — o partial é incluído dentro dele). Comentário pt-BR no topo documenta o arquivo como PONTO DE EXTENSÃO: apps de domínio adicionam seus `<a>` aqui copiando o contrato do item (o apps/exemplo da Fase 3 será o exemplo vivo); sem registry dinâmico.

    Em `core/views.py`, atualizar `shell_view`: passar `trilha = [{"rotulo": "Início", "url": None}]` no contexto como exemplo vivo do contrato, e atualizar a docstring (a casca mínima virou shell completo; explicar o contrato trilha em pt-BR). Sem `hx-boost` em lugar nenhum (D-09).

    Registrar explicitamente (comentário no topo do `_breadcrumbs.html` e nota no SUMMARY desta plan): o kernel desta fase entrega ZERO template tags customizadas por decisão — D-12 veta templatetag com ORM por trás e a trilha vem pronta da view; o item "template tags" de CORE-04 é atendido deliberadamente sem tags (resposta explícita para a verificação pós-execução, não uma lacuna).
  </action>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-current="page"' core/templates/core/_breadcrumbs.html` >= 1 e `grep -c "trilha" core/templates/core/_breadcrumbs.html` >= 2
    - `grep -c "hx-boost" core/templates/core/_nav.html core/templates/core/_breadcrumbs.html` retorna 0 em ambos
    - `grep -c "hx-swap-oob" core/templates/core/_nav.html` retorna 0
    - `grep -c '"rotulo": "Início"' core/views.py` retorna 1
    - `manage.py check` sai com código 0
  </acceptance_criteria>
  <done>Partials criados com contratos documentados em pt-BR; shell_view passa a trilha de exemplo.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever shell.html (aside + gaveta Alpine) e destravar o body do base.html</name>
  <files>core/templates/core/shell.html, core/templates/base.html, core/templates/core/login.html</files>
  <read_first>
    - /opt/web/pca/core/templates/core/shell.html (SOMENTE LEITURA — estrutura canônica: x-data desktop/sidebarAberta, listener de matchMedia, z-30/40/50, blocos, rodapé; REMOVER busca F7, tema claro/escuro, hx-swap-oob e qualquer marca de domínio)
    - core/templates/core/shell.html (casca atual a substituir)
    - core/templates/base.html (body com centering a remover; título a parametrizar)
    - core/templates/core/login.html (ganhará o wrapper de centering)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Pattern 4 — classes e estados exatos; Pitfalls 8 e 11)
  </read_first>
  <action>
    Reescrever `core/templates/core/shell.html` implementando D-09/D-11 (Pattern 4 tem os valores exatos):
    - Raiz `x-data` com `desktop: window.matchMedia('(min-width: 768px)').matches` e `sidebarAberta: false`, com `x-init` registrando listener de `change` do MediaQueryList que atualiza `desktop` — comentário pt-BR explicando o Pitfall 8 (x-show não reavalia sem dependência reativa; ler matchMedia direto congela a gaveta no breakpoint do load).
    - Header mobile `z-30` com botão hambúrguer (`@click="sidebarAberta = true"`); overlay `fixed inset-0 bg-black/40 md:hidden z-40` com `x-show="sidebarAberta && !desktop" x-cloak @click="sidebarAberta = false"`; aside `fixed inset-y-0 left-0 w-[232px] z-50` com `x-show="sidebarAberta || desktop" x-cloak` — empilhamento header < overlay < aside comentado (a gaveta cobre o header ao abrir; D-09).
    - Topo da aside: `{{ sistema_nome }}` e `{{ sistema_sigla }}` (sem logo de domínio); corpo: `{% include "core/_nav.html" %}`; rodapé: avatar com iniciais `usuario_atual.email|slice:":2"|upper`, e-mail truncado, botão "Sair" com `hx-post="{% url 'core:logout' %}" hx-target="body"` e `{% csrf_token %}` (fallback no-JS, como hoje).
    - `<main class="flex-1 md:ml-[232px] px-6 pb-6 pt-20 md:py-6 min-w-0">` contendo `{% block cabecalho_pagina %}` (com `{% include "core/_breadcrumbs.html" with trilha=trilha %}` + `<h1>{% block titulo_pagina %}Início{% endblock %}</h1>`) e `{% block conteudo_pagina %}` (conteúdo default: card de boas-vindas neutro). Bloco `titulo` do base.html preenchido com `{% block titulo_pagina_head %}` ou diretamente com o título + `· {{ sistema_sigla }}`.

    Em `core/templates/base.html`: trocar a classe do `<body>` para `min-h-screen bg-page text-ink font-sans text-base` (REMOVER `flex flex-col items-center justify-center` — Pitfall 11, comentar o porquê: o centering era da tela de login e espremeria a aside fixa) e trocar o default do `{% block titulo %}` para `{{ sistema_nome }}` (D-16 — zero nome hard-coded).

    Em `core/templates/core/login.html`: envolver o card num wrapper `min-h-screen flex flex-col items-center justify-center px-4` (o centering migra do body para cá) e trocar o título do bloco para `Entrar · {{ sistema_nome }}`.

    Manter nomes dos blocos do base.html (`titulo`, `content`, `scripts`) — Assumption A5; o contrato D-11 vale para os blocos internos do shell.
  </action>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cabecalho_pagina\|titulo_pagina\|conteudo_pagina" core/templates/core/shell.html` >= 3
    - `grep -c "matchMedia" core/templates/core/shell.html` >= 1 e `grep -c "x-cloak" core/templates/core/shell.html` >= 2
    - `grep -c "z-30\|z-40\|z-50" core/templates/core/shell.html` >= 3
    - `grep -c "items-center justify-center" core/templates/base.html` retorna 0
    - `grep -c "items-center justify-center" core/templates/core/login.html` >= 1
    - `grep -c "sistema_nome" core/templates/base.html` >= 1 e `grep -c "Sistema Base" core/templates/core/shell.html` retorna 0 (nome só via context processor)
    - `grep -rc "hx-boost" core/templates/` soma 0
  </acceptance_criteria>
  <done>Shell completo com gaveta/aside e blocos de página; base.html destravado; login continua centrado; identidade só via context processor.</done>
</task>

<task type="auto">
  <name>Task 3: Testes do shell + rebuild + suíte completa</name>
  <files>core/tests/test_shell.py</files>
  <read_first>
    - core/tests/test_login_flow.py (estilo dos testes da Fase 1 — override_settings de cookies, criação de usuário, client login)
    - core/templates/core/shell.html e core/templates/core/_breadcrumbs.html (estado pós Tasks 1-2 — o que assertar)
  </read_first>
  <action>
    Criar `core/tests/test_shell.py` com `@override_settings(SESSION_COOKIE_SECURE=False, CSRF_COOKIE_SECURE=False)`:
    - Autenticado, GET `/` retorna 200 e o HTML contém: `aria-label="Navegação principal"`, `aria-current="page"` (item Início ativo na raiz), o e-mail do usuário logado e o nome do sistema (`settings.SISTEMA_NOME`).
    - Não autenticado, GET `/` redireciona para `/login/` (LoginRequiredMiddleware intacto).
    - Contrato trilha: `render_to_string("core/_breadcrumbs.html", {"trilha": [{"rotulo": "Início", "url": "/"}, {"rotulo": "Atual", "url": None}]})` produz `<a href="/">` para o primeiro item e `<span aria-current="page">` (sem `<a>`) para o último.
    - Blocos: resposta de `/` contém `<h1>` com o título default e o include de breadcrumbs renderizado com "Início" como texto puro (sem link).
    - Login: GET `/login/` retorna 200 e contém o wrapper centrado (asserta a classe `justify-center` no corpo da resposta) — regressão do Pitfall 11.

    Rodar `docker compose up -d --build` (rebuild materializa templates novos e o CSS com as classes brand usadas pela primeira vez), aguardar `web` healthy, e rodar a suíte completa no container.
  </action>
  <verify>
    <automated>docker compose exec -T web python manage.py test core.tests -v 2</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose exec -T web python manage.py test core.tests -v 2` sai com código 0, sem FAIL/ERROR (suíte inteira: Fase 1 + identidade + shell)
    - `docker compose ps` mostra `web` healthy
    - `docker compose exec -T web sh -c 'grep -c "brand-tint" core/static/dist/tailwind.css'` >= 1 (tokens de marca agora emitidos — primeira utilização real)
    - `curl -fsS http://127.0.0.1:8000/healthz` retorna `{"status": "ok"}`
  </acceptance_criteria>
  <done>Shell provado por testes de comportamento; sistema rebuildado e saudável com o novo layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → shell_view | Única entrada de request; conteúdo autenticado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Shell autenticado servido a anônimos | mitigate | `LoginRequiredMiddleware` global permanece (nenhum `@login_not_required` novo nesta plan); teste de redirect para /login/ |
| T-02-04 | Spoofing/XSS | Rótulos da trilha renderizados no template | mitigate | Trilha montada exclusivamente pela view (D-12, sem input do usuário); autoescape padrão do Django ativo (nenhum `\|safe` nos partials) |
| T-02-05 | Tampering (CSRF) | Botão Sair via hx-post | mitigate | Token lido do cookie via `htmx:configRequest` (Fase 1, intacto — D-09 proíbe hx-boost) + `{% csrf_token %}` de fallback |
| T-02-SC | Tampering | npm/pip installs | accept | Nenhuma dependência nova nesta plan |
</threat_model>

<verification>
- Suíte completa no container: `docker compose exec -T web python manage.py test core.tests -v 2` — 0 falhas.
- `grep -rn "PCA\|pca" core/templates/ core/views.py` não retorna nenhuma menção a domínio nos arquivos tocados.
- <human-check>No navegador (127.0.0.1:8000): logar, ver aside fixa no desktop com "Início" ativo e breadcrumb; estreitar a janela (<768px) e abrir/fechar a gaveta pelo hambúrguer — a gaveta cobre o header e o overlay fecha ao clicar fora; redimensionar de volta não congela o estado (Pitfall 8).</human-check>
</verification>

<success_criteria>
- Usuário logado navega no shell com navegação e breadcrumbs funcionais (critério 1 da fase).
- Contratos D-10/D-11/D-12 criados e documentados — interface pública pronta para a Fase 3.
- Login continua funcional e centrado; suíte completa verde.
</success_criteria>

<output>
Create `.planning/phases/02-shell-visual-e-kernel/02-02-SUMMARY.md` when done
</output>
