---
phase: 02-shell-visual-e-kernel
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - core/views.py
  - core/urls.py
  - core/static/offline.html
  - core/static/img/icon-192.png
  - core/static/img/icon-512.png
  - core/static/img/icon-512-maskable.png
  - ops/gerar_icones_pwa.py
  - core/templates/base.html
  - core/templates/core/shell.html
  - core/tests/test_pwa.py
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "GET /manifest.json (sem login) retorna 200 com name/short_name/theme_color vindos dos settings e ícones resolvidos via static() (D-18)"
    - "GET /sw.js (sem login) retorna 200 na raiz do site com header Service-Worker-Allowed: / (D-19)"
    - "O service worker cacheia apenas /static/, pré-cacheia offline.html e usa fallback de navegação offline — HTML e fragmentos HTMX nunca são cacheados (D-19)"
    - "O botão Sair limpa Cache Storage e htmx-history-cache ANTES do POST de logout; o body tem hx-history=\"false\" (V3 — nada autenticado persiste no cliente)"
    - "Ícones placeholder neutros existem em core/static/img/ e são regeneráveis pelo script documentado (D-20)"
  artifacts:
    - path: "core/views.py"
      provides: "manifest_view e service_worker_view com @login_not_required"
      exports: ["manifest_view", "service_worker_view"]
    - path: "core/urls.py"
      provides: "Rotas /manifest.json e /sw.js na raiz"
      contains: "sw.js"
    - path: "core/static/offline.html"
      provides: "Fallback offline autocontido com cores neutras"
    - path: "core/static/img/icon-512.png"
      provides: "Ícone PWA placeholder 512x512"
    - path: "ops/gerar_icones_pwa.py"
      provides: "Script de regeneração dos ícones (roda no host com Pillow)"
    - path: "core/tests/test_pwa.py"
      provides: "Prova de manifest/sw/ícones (truque IHDR sem Pillow)"
  key_links:
    - from: "core/templates/base.html"
      to: "/manifest.json e /sw.js"
      via: "link rel=manifest + navigator.serviceWorker.register com scope /"
      pattern: "serviceWorker\\.register"
    - from: "core/views.py"
      to: "core/static/img/icon-*.png"
      via: "static() no manifest (compatível com hashing do WhiteNoise)"
      pattern: "static\\("
    - from: "core/templates/core/shell.html"
      to: "limparCachePwa (base.html)"
      via: "hx-on::before-request no botão Sair"
      pattern: "limparCachePwa"
---

<objective>
Tornar o sistema instalável como PWA (CORE-05): manifest e service worker servidos por views parametrizadas pelos settings de identidade (D-18/D-19), ícones placeholder regeneráveis (D-20), página offline, e o fechamento do ciclo de sessão no cliente (limpeza de cache no logout + `hx-history="false"`).

Purpose: critério 3 da fase — Chrome/Edge oferecem instalação; e fecha a superfície de vazamento de HTML autenticado no cliente (V3/ASVS).
Output: `/manifest.json` e `/sw.js` funcionais, ícones + script `ops/gerar_icones_pwa.py`, `offline.html`, integração no base.html/shell.html, testes `test_pwa.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-visual-e-kernel/02-CONTEXT.md
@.planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md
@.planning/phases/02-shell-visual-e-kernel/02-02-SUMMARY.md

<interfaces>
Disponível das plans anteriores:
- `settings.SISTEMA_NOME` / `SISTEMA_SIGLA` / `COR_PRIMARIA` (02-01)
- Context processor `identidade`: `{{ cor_primaria }}` para o meta theme-color (02-01)
- `base.html` com blocos `titulo`/`content`/`scripts` e listener `htmx:configRequest`; `shell.html` com botão Sair `hx-post` (02-02)
- Rotas existentes em `core/urls.py`: `login/`, `logout/`, `""` (shell) — `core.urls` é incluído na RAIZ em config/urls.py, então `path("manifest.json", ...)` e `path("sw.js", ...)` ficam em `/manifest.json` e `/sw.js` (escopo `/` garantido)

Contrato que ESTA plan cria:
- Rotas nomeadas `core:manifest` e `core:service_worker`
- Função JS global `limparCachePwa()` no base.html (apaga Cache Storage + chave `htmx-history-cache` do localStorage)
- Cache name do SW: `"static-v1"` (bump manual do sufixo documentado em comentário)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: manifest_view + service_worker_view + rotas + offline.html</name>
  <files>core/views.py, core/urls.py, core/static/offline.html</files>
  <read_first>
    - /opt/web/pca/core/views.py (SOMENTE LEITURA — manifest_view e service_worker_view canônicos, pitfalls comentados inline; generalizar identidade via settings)
    - /opt/web/pca/core/static/offline.html (SOMENTE LEITURA — estrutura do fallback; trocar título e REMOVER o hex de marca #003c71 — cores neutras, D-17/A3)
    - core/views.py e core/urls.py (estado atual — onde inserir; padrão @login_not_required do healthz)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Pattern 7 + Pitfalls 3, 4 e 5)
  </read_first>
  <action>
    Em `core/views.py`, adicionar `manifest_view` (D-18): `@login_not_required` (Pitfall 5 — sem isso o LoginRequiredMiddleware devolve 302 e a PWA não instala), retorna `JsonResponse` com `content_type="application/manifest+json"`; campos: `name = settings.SISTEMA_NOME`, `short_name = settings.SISTEMA_SIGLA`, `start_url = "/"`, `display = "standalone"`, `theme_color = settings.COR_PRIMARIA`, `background_color = "#f9f9f7"` (comentário: mesmo hex do token `page` — neutro de template, não identidade, A3), `icons` com os 3 PNGs (192, 512, 512-maskable com `"purpose": "maskable"`) usando `django.templatetags.static.static(...)` — NUNCA caminho literal (Pitfall 3: o hashing do WhiteNoise renomeia os arquivos no collectstatic; caminho literal 404aria só em produção).

    Adicionar `service_worker_view` (D-19): `@login_not_required`, retorna `HttpResponse` com `content_type="application/javascript"` e header `Service-Worker-Allowed: /` (Pitfall 4); corpo JS hand-rolled (~50 linhas, seguir o canônico da PCA) com: cache name `"static-v1"` (comentário: bump manual do sufixo invalida tudo; Cache Storage é escopado por origem), `install` pré-cacheia `static("offline.html")` + `skipWaiting()`, `activate` apaga caches com nome diferente + `clients.claim()`, `fetch` só intercepta GET same-origin: URLs sob `/static/` usam cache-first com preenchimento; navegações (`request.mode === "navigate"`) usam network-first com fallback para `offline.html`; QUALQUER outra requisição (HTML, fragmentos HTMX, JSON) passa direto à rede sem cache — comentário pt-BR explica que cachear HTML autenticado vazaria conteúdo após logout.

    Em `core/urls.py`, adicionar `path("manifest.json", views.manifest_view, name="manifest")` e `path("sw.js", views.service_worker_view, name="service_worker")` — na raiz, nunca sob /static/ (escopo do SW).

    Criar `core/static/offline.html`: página autocontida (zero requisições de rede — CSS inline), texto pt-BR ("Você está offline..."), cores neutras da paleta (fundo `#f9f9f7`, texto `#0b0b0b`, botão "Tentar novamente" com `location.reload()` em cinza-escuro neutro `#52514e` — NENHUM hex de marca, D-17).
  </action>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "login_not_required" core/views.py` >= 3 (healthz + manifest + sw)
    - `grep -c "application/manifest+json" core/views.py` retorna 1 e `grep -c "Service-Worker-Allowed" core/views.py` retorna 1
    - `grep -c "static(" core/views.py` >= 3 (ícones + offline via static())
    - `grep -c "manifest.json\|sw.js" core/urls.py` retorna 2
    - `grep -c "http" core/static/offline.html` retorna 0 além do DOCTYPE/lang (nenhum recurso externo) e `grep -ci "1e40af\|003c71" core/static/offline.html` retorna 0 (sem hex de marca)
    - `manage.py check` código 0
  </acceptance_criteria>
  <done>Views de manifest e SW parametrizadas pelos settings, rotas na raiz, offline.html neutro e autocontido.</done>
</task>

<task type="auto">
  <name>Task 2: Ícones placeholder + integração base.html/shell.html</name>
  <files>ops/gerar_icones_pwa.py, core/static/img/icon-192.png, core/static/img/icon-512.png, core/static/img/icon-512-maskable.png, core/templates/base.html, core/templates/core/shell.html</files>
  <read_first>
    - core/templates/base.html (estado pós 02-02 — onde inserir manifest link, theme-color e scripts)
    - core/templates/core/shell.html (estado pós 02-02 — botão Sair a receber o hx-on)
    - /opt/web/pca/core/templates/base.html (SOMENTE LEITURA — como a PCA registra o SW e limpa cache no logout)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Pattern 7, itens base.html e ícones; Security Domain V3)
  </read_first>
  <action>
    Criar `ops/gerar_icones_pwa.py` (roda no HOST — Pillow 12.1.1 disponível; o container não tem Pillow e não deve ganhar): lê cor e sigla de argumentos CLI com defaults `#1e40af`/`SB` (docstring pt-BR: os defaults espelham os do settings; ao nascer um sistema novo, rodar com os valores do seu `.env` — item de substituição documentado, D-20); gera `core/static/img/icon-192.png` e `icon-512.png` (quadrado chapado na cor com a sigla centrada em branco, fonte default do Pillow escalada) e `icon-512-maskable.png` (mesmo desenho com padding de 20% — safe zone de maskable). Executar o script no host (`python3 ops/gerar_icones_pwa.py`) para commitar os 3 PNGs (Assumption A2: binários gerados uma vez; regeneração é passo documentado, não automático).

    Em `core/templates/base.html`, no `<head>`: `<link rel="manifest" href="{% url 'core:manifest' %}">` e `<meta name="theme-color" content="{{ cor_primaria }}">`. No `<body>`: adicionar atributo `hx-history="false"` (comentário pt-BR: impede snapshot de HTML autenticado no localStorage do htmx — defesa V3). No bloco de scripts inline: função global `limparCachePwa()` — `async`, apaga todas as chaves de `caches` (`caches.keys()` + `caches.delete`) e remove `htmx-history-cache` do `localStorage`, com try/catch silencioso (comentário: chamada no logout ANTES do POST, para nada autenticado sobreviver à sessão); e registro do SW: `if ("serviceWorker" in navigator) navigator.serviceWorker.register("{% url 'core:service_worker' %}", { scope: "/" })`.

    Em `core/templates/core/shell.html`, no botão Sair: adicionar `hx-on::before-request="limparCachePwa()"` (limpeza ANTES do POST de logout — Pattern 4 da PCA).
  </action>
  <verify>
    <automated>python3 -c "import struct; d=open('core/static/img/icon-512.png','rb').read(); assert d[:8]==b'\x89PNG\r\n\x1a\n'; w,h=struct.unpack('>II', d[16:24]); assert (w,h)==(512,512), (w,h); print('icon-512 ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 PNGs existem e têm as dimensões corretas (assinatura PNG + IHDR: 192x192, 512x512, 512x512)
    - `grep -c 'rel="manifest"' core/templates/base.html` retorna 1 e `grep -c "theme-color" core/templates/base.html` retorna 1
    - `grep -c 'hx-history="false"' core/templates/base.html` retorna 1
    - `grep -c "limparCachePwa" core/templates/base.html` >= 1 e `grep -c "limparCachePwa" core/templates/core/shell.html` retorna 1
    - `grep -c "serviceWorker.register" core/templates/base.html` retorna 1 com `scope: "/"` na mesma chamada
    - `grep -ci "pca" ops/gerar_icones_pwa.py` retorna 0
  </acceptance_criteria>
  <done>Ícones commitados e regeneráveis por script; base.html instala/registra a PWA; logout limpa o estado do cliente.</done>
</task>

<task type="auto">
  <name>Task 3: test_pwa.py + rebuild final + suíte completa da fase</name>
  <files>core/tests/test_pwa.py</files>
  <read_first>
    - /opt/web/pca/core/tests/test_pwa.py (SOMENTE LEITURA — cobertura de referência, incluindo o truque de ler IHDR com struct, sem Pillow no container)
    - core/views.py (estado pós Task 1 — assinaturas reais a testar)
  </read_first>
  <action>
    Criar `core/tests/test_pwa.py` (adaptado do canônico; asserts de identidade SEMPRE contra settings, nunca strings literais):
    - GET `/manifest.json` SEM login → 200, content-type `application/manifest+json`; JSON com `name == settings.SISTEMA_NOME`, `short_name == settings.SISTEMA_SIGLA`, `theme_color == settings.COR_PRIMARIA`; cada `icons[*]["src"]` igual ao `static(...)` resolvido do arquivo correspondente (Pitfall 3).
    - GET `/sw.js` SEM login → 200, content-type de JavaScript, header `Service-Worker-Allowed` == `/`; corpo contém `static-v1`, `offline.html` e NÃO contém chamadas de cache para navegações HTML (asserta que a estratégia de navegação usa fallback, não cache.put de HTML — verificar presença de `navigate` + ausência de cache de respostas de navegação conforme implementação).
    - Ícones: abrir cada PNG de `core/static/img/` e validar assinatura + dimensões via `struct` (192/512/512) — sem Pillow.
    - GET `/` autenticado contém `rel="manifest"` e `hx-history="false"` (integração no base.html).

    Rodar `docker compose up -d --build` (rebuild final da fase: views, templates, estáticos e ícones na imagem; collectstatic hasheia os PNGs), aguardar `web` healthy, e rodar a SUÍTE COMPLETA no container. Confirmar manualmente por curl: `curl -fsS http://127.0.0.1:8000/manifest.json` retorna o JSON com o nome do sistema e `curl -fsSI http://127.0.0.1:8000/sw.js | grep -i service-worker-allowed` mostra `/`.
  </action>
  <verify>
    <automated>docker compose exec -T web python manage.py test core.tests -v 2</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose exec -T web python manage.py test core.tests -v 2` sai com código 0, sem FAIL/ERROR (suíte inteira da fase: identidade, shell, admin, auditoria, pwa + Fase 1)
    - `curl -fsS http://127.0.0.1:8000/manifest.json` retorna JSON com `"name"` igual ao SISTEMA_NOME configurado, sem redirect para /login/
    - `curl -fsSI http://127.0.0.1:8000/sw.js` inclui `Service-Worker-Allowed: /`
    - `docker compose ps` mostra `web` healthy
  </acceptance_criteria>
  <done>PWA provada por testes e por curl contra o container em execução; suíte completa da fase verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rotas públicas novas | /manifest.json e /sw.js ganham @login_not_required |
| Browser (SW/Cache Storage) | Código do SW roda no cliente e persiste dados entre sessões |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | SW cacheando HTML autenticado | mitigate | Estratégia só cacheia GET same-origin sob /static/ + offline.html; navegações nunca são gravadas em cache; teste asserta a estratégia |
| T-02-11 | Information Disclosure | Snapshot htmx no localStorage e Cache Storage após logout | mitigate | `hx-history="false"` no body + `limparCachePwa()` ANTES do POST de logout (hx-on::before-request no botão Sair) |
| T-02-12 | Elevation of Privilege | SW com escopo indevido/código remoto | mitigate | SW servido por view same-origin, sem imports remotos; escopo explícito `/` + header Service-Worker-Allowed |
| T-02-13 | Information Disclosure | manifest/sw públicos (@login_not_required) | accept | Conteúdo não sensível: nome do sistema, cor e caminhos estáticos — nada de sessão/dados |
| T-02-SC | Tampering | pip/npm installs | accept | Nenhuma dependência nova nesta plan (SW hand-rolled por D-19; ícones gerados com Pillow já presente no host) |
</threat_model>

<verification>
- Suíte completa: `docker compose exec -T web python manage.py test core.tests -v 2` — 0 falhas.
- `grep -rn "PCA\|pca" core/views.py core/static/offline.html ops/gerar_icones_pwa.py core/tests/test_pwa.py` — zero menção a domínio (case-insensitive, ignorar falsos positivos em palavras pt-BR).
- <human-check>No Chrome/Edge em 127.0.0.1:8000 (logado): DevTools → Application mostra manifest válido com ícones e SW ativo com escopo "/"; o navegador oferece "Instalar" (ícone na omnibox); após logout, Application → Cache Storage está vazio; com o SW ativo, derrubar a rede e navegar mostra a página offline neutra.</human-check>
- Nota: `healthz` (item de discretion do CONTEXT) já foi entregue na Fase 1 — nada a fazer nesta fase (Open Question 1 do RESEARCH, resolvida por fato consumado).
</verification>

<success_criteria>
- Sistema instala como PWA com manifest, ícones e service worker parametrizados pelo nome do sistema (critério 3 da fase).
- Nenhum HTML autenticado persiste no cliente após logout (V3 fechado).
- Ícones placeholder neutros regeneráveis via `ops/gerar_icones_pwa.py`, documentados como item de substituição.
</success_criteria>

<output>
Create `.planning/phases/02-shell-visual-e-kernel/02-04-SUMMARY.md` when done
</output>
