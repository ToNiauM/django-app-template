---
phase: 02-shell-visual-e-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings/base.py
  - .env.example
  - core/context_processors.py
  - tailwind.config.js
  - core/static/src/input.css
  - core/tests/test_identidade.py
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - "SISTEMA_NOME, SISTEMA_SIGLA e COR_PRIMARIA existem como settings lidos do .env (com defaults) e chegam a todo template via context processor (D-16)"
    - "COR_PRIMARIA fora do formato #RRGGBB derruba o boot com erro explícito (mitigação de CSS injection via settings)"
    - "As classes semânticas já usadas pelos templates da Fase 1 (bg-page, bg-surface, text-ink) passam a existir de fato no CSS gerado (Pitfall 6)"
    - "Tokens de marca (brand, brand-hover, brand-ink, brand-tint) derivam de um ÚNICO hex literal no tailwind.config.js (D-17)"
    - "A regra CSS [x-cloak] existe no CSS final (pré-requisito do shell da wave 2 — Pitfall 7)"
  artifacts:
    - path: "config/settings/base.py"
      provides: "SISTEMA_NOME/SISTEMA_SIGLA/COR_PRIMARIA + validação #RRGGBB + context processor registrado"
      contains: "SISTEMA_NOME"
    - path: "core/context_processors.py"
      provides: "Context processor identidade (sistema_nome, sistema_sigla, cor_primaria)"
      contains: "def identidade"
    - path: "tailwind.config.js"
      provides: "Paleta semântica neutra + tokens de marca derivados de COR_PRIMARIA"
      contains: "brand-tint"
    - path: "core/static/src/input.css"
      provides: "Regra [x-cloak]"
      contains: "x-cloak"
    - path: "core/tests/test_identidade.py"
      provides: "Prova do contrato de identidade (settings + context processor)"
  key_links:
    - from: "config/settings/base.py"
      to: "core/context_processors.py"
      via: "TEMPLATES.OPTIONS.context_processors"
      pattern: "core\\.context_processors\\.identidade"
    - from: "tailwind.config.js"
      to: "core/static/dist/tailwind.css"
      via: "build multi-stage do Dockerfile (estágio node)"
      pattern: "bg-page"
---

<objective>
Concentrar a identidade parametrizada do sistema nos dois touchpoints definidos em D-16/D-17 (`.env` → settings → context processor, e `tailwind.config.js`) e corrigir o bug latente da Fase 1 (Pitfall 6: tokens `bg-page`/`bg-surface`/`text-ink` usados nos templates mas inexistentes no config — o JIT os ignora em silêncio).

Purpose: tudo desta fase (shell, admin, PWA) consome esses settings e tokens; sem eles nenhuma outra plan tem fonte de identidade. Fatia vertical: após esta plan, a tela de login renderiza com a paleta real (hoje renderiza sem cores) e o contrato de identidade é testado.
Output: settings de identidade validados, context processor `identidade`, paleta Tailwind completa, regra `[x-cloak]`, teste `test_identidade.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-visual-e-kernel/02-CONTEXT.md
@.planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md

<interfaces>
Contrato que as plans 02-02/02-03/02-04 consumirão (definido AQUI):

De config/settings/base.py (novo):
- `SISTEMA_NOME: str` — default "Sistema Base"
- `SISTEMA_SIGLA: str` — default "SB"
- `COR_PRIMARIA: str` — default "#1e40af", validado como `#RRGGBB` no boot

De core/context_processors.py (novo, ao lado de `usuario_atual`):
- `identidade(request) -> {"sistema_nome": ..., "sistema_sigla": ..., "cor_primaria": ...}`

De tailwind.config.js (tokens disponíveis nos templates):
- Neutros: `page`, `surface`, `surface-2`, `ink`, `ink-2`, `muted`, `grid`
- Marca: `brand`, `brand-hover`, `brand-ink`, `brand-tint` (derivados do único literal `COR_PRIMARIA`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings de identidade + context processor + teste</name>
  <files>config/settings/base.py, .env.example, core/context_processors.py, core/tests/test_identidade.py</files>
  <read_first>
    - config/settings/base.py (estado atual — onde inserir os settings e registrar o context processor)
    - core/context_processors.py (padrão `usuario_atual` a seguir — docstring pt-BR explicando o porquê)
    - .env.example (estilo de comentário por variável a manter)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (seções "Pattern 1" e "Security Domain" — mitigação de CSS injection)
  </read_first>
  <action>
    Implementar D-16. Em `config/settings/base.py`, após o bloco de localização: `SISTEMA_NOME = env("SISTEMA_NOME", default="Sistema Base")`, `SISTEMA_SIGLA = env("SISTEMA_SIGLA", default="SB")`, `COR_PRIMARIA = env("COR_PRIMARIA", default="#1e40af")`. Validar `COR_PRIMARIA` no boot com `re.fullmatch(r"#[0-9a-fA-F]{6}", COR_PRIMARIA)` levantando `django.core.exceptions.ImproperlyConfigured` com mensagem em pt-BR se falhar — comentário explica que o valor é interpolado em CSS (`admin_tema_css` na plan 02-03 usa `|safe`) e a validação de formato é a barreira contra CSS injection via `.env` (T-02-01). Comentar também que estes 3 settings + o literal do `tailwind.config.js` são os DOIS únicos touchpoints de identidade que a Fase 4 (Copier) parametrizará (D-17).

    Em `core/context_processors.py`, adicionar `identidade(request)` retornando `{"sistema_nome": settings.SISTEMA_NOME, "sistema_sigla": settings.SISTEMA_SIGLA, "cor_primaria": settings.COR_PRIMARIA}` (import `from django.conf import settings`), com docstring pt-BR. Registrar `"core.context_processors.identidade"` em `TEMPLATES[0]["OPTIONS"]["context_processors"]` após `usuario_atual`.

    Em `.env.example`, adicionar bloco "Identidade do sistema" documentando `SISTEMA_NOME`, `SISTEMA_SIGLA` e `COR_PRIMARIA` (formato `#RRGGBB` obrigatório; virarão variáveis Copier na Fase 4). Adicionar as mesmas 3 linhas ao `.env` local (não versionado) com os valores default, para exercitar o caminho `.env` → settings.

    Criar `core/tests/test_identidade.py`: (1) `settings.COR_PRIMARIA` casa com `#[0-9a-fA-F]{6}`; (2) GET `/login/` (sem login) tem `sistema_nome`, `sistema_sigla` e `cor_primaria` no `response.context` com os valores dos settings — prova de que o context processor está registrado; (3) `"core.context_processors.identidade"` presente em `settings.TEMPLATES[0]["OPTIONS"]["context_processors"]`. Usar `@override_settings(SESSION_COOKIE_SECURE=False, CSRF_COOKIE_SECURE=False)` como nos testes da Fase 1.
  </action>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py test core.tests.test_identidade -v 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SISTEMA_NOME\|SISTEMA_SIGLA\|COR_PRIMARIA" config/settings/base.py` retorna >= 3
    - `grep -c "ImproperlyConfigured" config/settings/base.py` retorna >= 1
    - `grep -c "def identidade" core/context_processors.py` retorna 1
    - `grep -c "core.context_processors.identidade" config/settings/base.py` retorna 1
    - `grep -c "SISTEMA_NOME" .env.example` retorna >= 1
    - Comando de verify sai com código 0, sem FAIL/ERROR (roda com bind-mount pontual — imagem antiga serve, nenhuma dependência nova)
  </acceptance_criteria>
  <done>Settings de identidade validados no boot, context processor exposto a todos os templates, `.env.example` completo, teste passando no container.</done>
</task>

<task type="auto">
  <name>Task 2: Paleta Tailwind (neutros + marca derivada) e regra [x-cloak]</name>
  <files>tailwind.config.js, core/static/src/input.css</files>
  <read_first>
    - tailwind.config.js (estado atual — extend vazio, comentário já reserva a paleta para esta fase)
    - core/static/src/input.css (só 3 diretivas @tailwind hoje)
    - .planning/phases/02-shell-visual-e-kernel/02-RESEARCH.md (Pattern 1 — config completo recomendado, com a função `misturar` e os hexes neutros; Pitfalls 6 e 7)
    - /opt/web/pca/tailwind.config.js e /opt/web/pca/core/static/src/input.css (SOMENTE LEITURA — fonte canônica dos neutros e da regra [x-cloak], linha ~338 do input.css da PCA)
    - Dockerfile (estágio assets — guarda de 5000 bytes; entender como o CSS é gerado)
  </read_first>
  <action>
    Implementar D-17 seguindo o Pattern 1 do RESEARCH (config pronto lá — usar como base): declarar `const COR_PRIMARIA = "#1e40af";` como ÚNICO literal de identidade do arquivo, com comentário pt-BR dizendo que a Fase 4 (Copier) parametriza exatamente esta linha + o `.env`. Função `misturar(hex, alvo, fator)` em JS puro derivando: `brand` = COR_PRIMARIA, `brand-hover` = clareado 12% (`misturar(COR_PRIMARIA, 255, 0.12)`), `brand-ink` = escurecido 18% (`misturar(COR_PRIMARIA, 0, 0.18)`), `brand-tint` = fundo tênue (`misturar(COR_PRIMARIA, 255, 0.9)`). Neutros de template (NÃO são identidade — literais permitidos): `page: "#f9f9f7"`, `surface: "#fcfcfb"`, `"surface-2": "#f3f2ef"`, `ink: "#0b0b0b"`, `"ink-2": "#52514e"`, `muted: "#77756f"`, `grid: "#e4e2dd"`. Manter `content: ["./core/templates/**/*.html"]` e sintaxe Tailwind 3.4 (stack fechada — NÃO migrar para v4).

    Em `core/static/src/input.css`, após as diretivas `@tailwind`, adicionar `[x-cloak] { display: none !important; }` com comentário pt-BR: `x-cloak` é só um atributo — sem esta regra escrita à mão a gaveta mobile do shell (wave 2) pisca aberta no primeiro paint, antes do Alpine iniciar (Pitfall 7; primeiro uso de x-cloak entra na plan 02-02).

    Rebuild da imagem para materializar o CSS: `docker compose build web` e `docker compose up -d`.
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c 'grep -c "bg-page" core/static/dist/tailwind.css && grep -c "x-cloak" core/static/dist/tailwind.css'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COR_PRIMARIA" tailwind.config.js` retorna >= 2 (declaração + usos) e `grep -c '"#' tailwind.config.js` mostra apenas UM hex de marca (o literal) além dos 7 neutros documentados
    - `grep -c "brand-tint\|brand-hover\|brand-ink" tailwind.config.js` retorna >= 3
    - Dentro do container: `grep -c "bg-page" core/static/dist/tailwind.css` >= 1 e `grep -c "x-cloak" core/static/dist/tailwind.css` >= 1
    - Build da imagem passa na guarda de 5000 bytes do Dockerfile (o `docker compose build web` conclui sem erro)
    - `docker compose ps` mostra `web` como `healthy` após `up -d`
  </acceptance_criteria>
  <done>Paleta semântica e tokens de marca emitidos no CSS final da imagem; regra [x-cloak] presente; tela de login renderiza com as cores reais (bg-page/bg-surface deixam de ser ignorados).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → settings | Valores de operador entram no processo Django e serão interpolados em CSS/manifest nas próximas plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (CSS injection) | `COR_PRIMARIA` interpolada em `<style>` com `\|safe` (plan 02-03) | mitigate | Validação `#RRGGBB` via regex no boot em `config/settings/base.py`, `ImproperlyConfigured` se falhar (Task 1) |
| T-02-02 | Information Disclosure | `.env` com identidade | accept | Identidade não é segredo; segredos reais (SECRET_KEY, senha do banco) já tratados na Fase 1 |
| T-02-SC | Tampering | pip installs | accept | Nenhuma dependência nova nesta plan; simple-history entra na 02-03 com auditoria `[OK]` do RESEARCH |
</threat_model>

<verification>
- `docker compose exec -T web python manage.py test core.tests -v 2` — suíte completa (Fase 1 + test_identidade) sem falhas.
- `docker compose exec -T web python manage.py check` sem erros.
- Visual (opcional): tela de login em `http://127.0.0.1:8000/login/` agora tem fundo `#f9f9f7` (bg-page) e card `#fcfcfb` (bg-surface) — antes desta plan renderizava branco puro.
</verification>

<success_criteria>
- Os 3 settings de identidade existem, vêm do `.env` com defaults, e COR_PRIMARIA inválida derruba o boot com mensagem clara.
- Context processor `identidade` registrado e provado por teste.
- CSS final contém as classes `bg-page`/`text-ink` (Pitfall 6 fechado) e a regra `[x-cloak]` (Pitfall 7 prevenido).
- Tokens de marca derivam de um único literal em `tailwind.config.js` (D-17 — exatamente dois touchpoints).
</success_criteria>

<output>
Create `.planning/phases/02-shell-visual-e-kernel/02-01-SUMMARY.md` when done
</output>
