---
phase: 07-herdar-o-design-system-do-pca
plan: 08
type: execute
wave: 7
depends_on: ["07-07"]
files_modified:
  - README.md
autonomous: false
requirements: [REL-01, QA-03]

must_haves:
  truths:
    - "O template ganha a tag v0.2.0, que entrega Fase 6 e Fase 7 juntas — antes dela nenhum sistema derivado recebeu nada das duas fases, porque o Copier lê a última tag e não o HEAD"
    - "Um derivado preso na v0.1.0 tem um roteiro escrito para resolver os três conflitos previsíveis do copier update (_nav.html, tailwind.config.js, input.css)"
    - "Os 77 testes Django e as 11 suítes de .template-tests/ estão verdes, incluindo o ensaio A→B→C (critério 8)"
    - "As 4 telas (login, shell, CRUD, dashboard) foram inspecionadas nos dois temas numa cópia real gerada do template, e o operador aprovou (ui_safety_gate)"
  artifacts:
    - path: "README.md"
      provides: "Roteiro de atualização dos derivados + regressão atualizada com as suítes novas"
      contains: "v0.2.0"
  key_links:
    - from: "tag v0.2.0"
      to: "sistemas derivados"
      via: "copier update --vcs-ref v0.2.0"
      pattern: "v0\\.2\\.0"
---

<objective>
A fase fecha: a regressão inteira roda verde, o operador inspeciona as 4 telas nos dois
temas numa cópia real, o README ganha o roteiro de atualização para os derivados presos na
v0.1.0, e o repositório recebe a tag `git tag -a v0.2.0`.

Purpose: a tag **é** o entregável. O Copier lê a última tag, não o HEAD — sem `v0.2.0`,
nem a Fase 6 (marca, logos, bind mount) nem a Fase 7 chegam a nenhum sistema. São 39
commits desde a `v0.1.0`. E sem o roteiro de conflito, o primeiro `copier update` real do
derivado vira suporte manual.
Output: README com roteiro e regressão atualizada, inspeção humana aprovada, tag `v0.2.0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-CONTEXT.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md
</context>

<interfaces>
### Os três conflitos previsíveis num derivado que nasceu na v0.1.0

Um derivado que fez reskin à mão editou exatamente os três arquivos que esta fase
reescreve inteiros. O conflito é **certo** e **não viola o critério 7**, que fala de
arquivo que o derivado **não tenha tocado**:

| Arquivo | O que o derivado fez | Resolução |
|---|---|---|
| `core/templates/core/_nav.html` | apagou o item "Início", colou os itens do domínio | `git checkout --theirs` e recriar os itens em `core/templates/core/_nav_dominio.html`, uma linha por item com `{% item_nav %}` |
| `tailwind.config.js` | fixou a cor de marca no arquivo e acrescentou `borderRadius`/`fontSize`/`fontFamily` à mão | `git checkout --theirs`; a cor fixada à mão vai para `COR_PRIMARIA` no `.env`, **não** para o config — o config passa a ser verbatim e é reescrito a cada update |
| `core/static/src/input.css` | acrescentou o `@layer base { :focus-visible }` no topo | `git checkout --theirs`; qualquer token próprio de estado migra para `core/static/src/dominio.css`, que o update nunca toca |

Ordem do roteiro: (0) árvore limpa e branch de atualização; (1) `copier update --vcs-ref v0.2.0`;
(2) resolver os três arquivos ficando com a versão do template; (3) recriar o menu em
`_nav_dominio.html`; (4) mover tokens próprios para `dominio.css`; (5) transportar a cor
para o `.env`; (6) `docker compose up -d --build` (o CSS é artefato de build);
(7) rodar `manage.py test` do próprio sistema.

### Regressão completa da fase

```
bash .template-tests/test_copier_copy.sh
bash .template-tests/test_copier_update.sh
python3 -m unittest discover -s .template-tests -p 'test_*.py'
bash .template-tests/test_05_nascimento.sh --keep
```

O último gera uma cópia real, sobe `db` + `web`, migra, cria administrador de ensaio e roda
`docker compose exec -T web python manage.py test core apps.exemplo --noinput`. O `--keep`
retém a cópia e informa destino, projeto Compose e URL — é ela que o checkpoint inspeciona.

### Pré-condições da tag (README.md §"Criando a tag de release")

Árvore limpa (`git status --short` sem saída) **e** regressão completa verde. Só então:
`git tag -a v0.2.0 -m "..."`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Roteiro de atualização dos derivados e regressão documentada</name>
  <files>README.md</files>
  <read_first>
    - `README.md` linhas 323-368 (seção "Regressão do template" — a lista de comandos e a descrição de cada suíte)
    - `README.md` linhas 368-400 (seção "Releases e atualização do núcleo" e "Criando a tag de release")
    - `.template-tests/` (a lista real de arquivos após os planos 07-01 a 07-07 — inclui `test_07_tokens.py` e `test_07_nav_extensao.py`)
    - `.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md` §"Pitfall 16" (os três conflitos) e §"Pitfall 19" (a cor fixada à mão precisa ir para o `.env`)
    - `copier.yml` (o bloco `_skip_if_exists`, que é o que garante a preservação de `_nav_dominio.html` e `dominio.css` no update)
  </read_first>
  <action>
    Em `README.md`, na seção **"Releases e atualização do núcleo"**, acrescente uma
    subseção nova — "Atualizando um sistema que nasceu na v0.1.0" — com o roteiro de 8
    passos e a tabela dos três conflitos previsíveis, ambos descritos em `<interfaces>`.
    Explique, em uma frase, por que esses conflitos não violam o contrato: o que o derivado
    escreveu à mão é exatamente o que o núcleo passa a entregar, e o critério de
    atualização sem conflito vale para arquivos que o derivado **não** tocou. Mencione
    `_skip_if_exists` e o que ele protege (`core/templates/core/_nav_dominio.html` e
    `core/static/src/dominio.css`).

    Na seção **"Regressão do template"**, atualize a lista de comandos e a descrição das
    suítes para refletir o estado real: acrescente `test_07_tokens.py` (contrato dos tokens
    sobre a fonte, régua tipográfica e classes mortas) e `test_07_nav_extensao.py`
    (contrato do ponto de extensão da navegação). Registre também, em uma frase, que todas
    as suítes que invocam `copier copy` usam `--vcs-ref=HEAD` e por quê.

    Na seção **"Criando a tag de release"**, troque os exemplos `v0.1.0` por `v0.2.0` onde
    fizer sentido como exemplo corrente e acrescente uma frase dizendo o que a `v0.2.0`
    entrega: as Fases 6 e 7 juntas — marca por arquivo, persistência em bind mount, design
    system com tema escuro e o ponto de extensão da navegação.

    Este arquivo é o README **do template** (`README.md`, não `README.md.jinja`): ele não
    chega à árvore gerada, então pode citar contexto de família livremente. Ainda assim,
    não cite entidade nem norma institucional — mantenha a linguagem do produto.
  </action>
  <verify>
    <automated>grep -c 'v0.2.0' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v0.2.0' README.md` retorna ao menos 3
    - `grep -c '_nav_dominio.html' README.md` retorna ao menos 2
    - `grep -c '_skip_if_exists' README.md` retorna ao menos 1
    - `grep -c 'test_07_tokens.py' README.md` retorna ao menos 1 e `grep -c 'test_07_nav_extensao.py' README.md` retorna ao menos 1
    - `grep -c -- '--vcs-ref=HEAD' README.md` retorna ao menos 1
    - `grep -c 'checkout --theirs' README.md` retorna ao menos 1
    - `bash .template-tests/test_copier_update.sh` sai com código 0 (ele assere `grep -Fq 'A → B → C' README.md` — a seção não pode ter sido quebrada)
  </acceptance_criteria>
  <done>Um derivado preso na v0.1.0 tem instruções executáveis; a regressão documentada bate com as suítes que existem.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Inspeção visual das 4 telas, nos dois temas</name>
  <files>(nenhum arquivo é alterado nesta tarefa — é inspeção)</files>
  <action>
    Rode `bash .template-tests/test_05_nascimento.sh --keep`, anote destino, projeto
    Compose e URL que ele imprime, e conduza o operador pelo roteiro de
    `<how-to-verify>` — as 4 telas, nos 2 temas, na ordem descrita. **Não altere nenhum
    arquivo nesta tarefa.** Se o operador reprovar algum ponto, registre exatamente qual
    (tela, tema, elemento), faça a correção como edição explícita no arquivo responsável e
    repita a inspeção inteira antes de seguir para a Task 3. Ao final, derrube a cópia
    retida com `docker compose -p <projeto-informado> down` e apague o diretório.
  </action>
  <verify>
    <human-check>Operador respondeu "aprovado" após conferir as 4 telas nos 2 temas na cópia retida</human-check>
  </verify>
  <what-built>
    Design system inteiro herdado: tokens de cor em variáveis CSS com par claro/escuro,
    família de marca derivada de `COR_PRIMARIA` em runtime, tema escuro com controle de 3
    estados na aside, 3 níveis de elevação, raio único de 2px, régua tipográfica com teto
    de 20px, pilha `system-ui`, focus-ring único, classes de componente com `safelist`,
    gráficos sem hex com paleta servida pelo Django, e o ponto de extensão da navegação.

    A automação já rodou: `.template-tests/test_copier_copy.sh`,
    `.template-tests/test_copier_update.sh`, todas as suítes Python de `.template-tests/`
    e o ensaio real de nascimento `.template-tests/test_05_nascimento.sh --keep`, que
    gerou uma cópia descartável, subiu `db` + `web`, migrou, criou um administrador de
    ensaio e rodou os testes Django de `core` e `apps.exemplo`.
  </what-built>
  <how-to-verify>
    1. Rode `bash .template-tests/test_05_nascimento.sh --keep`. Ao final ele imprime o
       diretório da cópia retida, o nome do projeto Compose e a URL. Anote os três.
    2. Abra a URL no navegador e entre com o administrador de ensaio informado pelo script.
    3. Para **cada uma** das 4 telas — **login**, **shell (raiz `/`)**, **CRUD
       (`/exemplo/`)** e **dashboard (`/exemplo/dashboard/`)** — confira nos **dois temas**
       (use o controle Automático / Claro / Escuro no rodapé da aside; na tela de login,
       alterne o tema do sistema operacional):
       - **Sem flash**: recarregue a página com `Ctrl+Shift+R` no tema escuro. A página
         **não** pode piscar branca antes de escurecer.
       - **Texto legível**: nada de texto de corpo ilegível depois do encolhimento
         (corpo agora em 13px, metadados em 11px). Títulos de página em 20px.
       - **Cantos**: todos os cantos com 2px de raio — sem cantos arredondados de 4px ou
         mais, exceto os avatares circulares.
       - **Elevação**: cards de KPI e cards de gráfico com sombra sutil no claro e com
         superfície mais clara (sem sombra) no escuro; modais visivelmente flutuando.
       - **Foco**: navegue por **Tab** e confirme o anel de foco de 2px em todo elemento
         focável, nos dois temas.
    4. No **CRUD**, abra o modal de criação e o modal de exclusão. O véu de fundo tem que
       estar **opaco** (escurecendo o conteúdo atrás) — se estiver transparente,
       `bg-black/40` não foi aplicado e é falha bloqueante.
    5. No **dashboard**, com os gráficos visíveis, **troque o tema pelo controle da aside**
       e observe **sem recarregar a página**: tooltip, eixos, grade e o donut precisam
       repintar. Passe o mouse sobre uma barra e sobre uma fatia para conferir o tooltip
       nos dois temas.
    6. No **menu**, confirme que "Início", "Dashboard" e "Itens (CRUD)" aparecem, que o
       item da página atual tem o filete vertical de 2px à esquerda e o fundo tênue, e que
       navegar entre eles move o destaque.
    7. Faça **logout** e volte a entrar: o tema escolhido tem que ter sobrevivido.
    8. Quando terminar, derrube a cópia retida com
       `docker compose -p <projeto-informado> down` e apague o diretório.
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" para liberar a tag `v0.2.0`, ou descreva o que está errado
    (tela, tema, elemento) para correção antes da release.
  </resume-signal>
  <read_first>
    - `README.md` §"Regressão do template" (o procedimento do `--keep` e o que ele retém)
    - `core/templates/core/shell.html` (a tabela dos 3 níveis de elevação, para saber o que se espera ver)
  </read_first>
  <acceptance_criteria>
    - `bash .template-tests/test_05_nascimento.sh --keep` sai com código 0 e imprime destino, projeto Compose e URL
    - As 4 telas foram abertas nos 2 temas e o operador respondeu "aprovado" (ou as correções pedidas foram feitas e a inspeção repetida)
    - O véu dos dois modais escurece o conteúdo de fundo
    - A troca de tema no dashboard repinta os gráficos sem reload
    - O tema sobrevive ao logout
    - A cópia retida foi derrubada e removida ao final
  </acceptance_criteria>
  <done>O operador aprovou as 4 telas nos dois temas numa cópia real gerada do template.</done>
</task>

<task type="auto">
  <name>Task 3: Regressão completa verde e a tag v0.2.0</name>
  <files>README.md</files>
  <read_first>
    - `README.md` §"Criando a tag de release" (as pré-condições: árvore limpa + regressão verde)
    - `.planning/ROADMAP.md` §"Phase 7" — "Nota de release" (o que a tag entrega)
    - `.planning/phases/07-herdar-o-design-system-do-pca/07-CONTEXT.md` §"Specifics" (39 commits desde a `v0.1.0`; nada em `copier.yml` nem em CI depende da tag — não há `.github/` neste repositório)
  </read_first>
  <action>
    Rode a regressão completa na ordem da seção "Regressão do template" do README:
    `bash .template-tests/test_copier_copy.sh`, `bash .template-tests/test_copier_update.sh`,
    `python3 -m unittest discover -s .template-tests -p 'test_*.py'` e
    `bash .template-tests/test_05_nascimento.sh` (sem `--keep`, para não deixar resíduo).
    Registre no SUMMARY a contagem de testes Django obtida pelo ensaio de nascimento —
    o piso é 77 (54 em `core` + 23 em `apps.exemplo`), e esta fase acrescenta os testes de
    `test_navegacao`, `test_tema`, `test_tema_escuro` e `test_dashboard`.

    Confirme `git status --short` sem saída. Só então crie a tag anotada:

    `git tag -a v0.2.0` com mensagem em pt-BR descrevendo que a release entrega as Fases 6
    e 7 juntas: marca por arquivo fixo e persistência em bind mount (Fase 6); design system
    com tokens em variáveis CSS, tema escuro, elevação, raio, régua tipográfica, fonte,
    focus-ring, classes de componente, paleta de gráfico servida pelo servidor e o ponto de
    extensão da navegação com `_nav_dominio.html` e `{% item_nav %}` (Fase 7).

    Confira com `git tag -l` (tem que listar `v0.1.0` e `v0.2.0`) e
    `git show v0.2.0 --stat`. **Não** faça `git push` — publicação é decisão do operador.

    Se qualquer suíte falhar, **não crie a tag**: corrija e reexecute a regressão inteira.
  </action>
  <verify>
    <automated>git tag -l | grep -Fx v0.2.0</automated>
  </verify>
  <acceptance_criteria>
    - `bash .template-tests/test_copier_copy.sh` sai com código 0
    - `bash .template-tests/test_copier_update.sh` sai com código 0
    - `python3 -m unittest discover -s .template-tests -p 'test_*.py'` sai com código 0
    - `bash .template-tests/test_05_nascimento.sh` sai com código 0 e o log mostra `OK` da suíte Django com ao menos 77 testes
    - `git status --short` não produz saída antes da criação da tag
    - `git tag -l | grep -Fx v0.2.0` casa
    - `git cat-file -t v0.2.0` retorna `tag` (tag anotada, não leve)
    - `git rev-list v0.1.0..v0.2.0 --count` retorna um número maior que 39
    - Uma geração de conferência com a tag prova o entregável: `copier copy --vcs-ref v0.2.0` (sem `--vcs-ref=HEAD`) produz uma árvore que contém `core/static/img/logo-entidade.svg`, `core/static/src/dominio.css` e `core/templates/core/_nav_dominio.html` — os artefatos das Fases 6 e 7 que a `v0.1.0` não entregava
  </acceptance_criteria>
  <done>A regressão inteira está verde e o repositório tem a tag anotada `v0.2.0`, que entrega Fases 6 e 7 a qualquer sistema derivado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag do template → sistemas derivados | A tag é o que o Copier lê; um estado não verificado publicado como tag chega a todos os sistemas gerados a partir dela |
| cópia de ensaio (`--keep`) → host do operador | O ensaio sobe containers e cria um administrador de ensaio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Tampering | tag criada sobre árvore suja ou regressão vermelha, publicando estado não verificado a todos os derivados | mitigate | Pré-condições explícitas na Task 3: `git status --short` sem saída **e** as 4 camadas de regressão verdes antes do `git tag -a`; instrução de não criar a tag se qualquer suíte falhar |
| T-07-23 | Information disclosure | administrador de ensaio e `.env` de ensaio sobrevivendo à cópia retida com `--keep` | mitigate | O checkpoint termina exigindo `docker compose -p <projeto> down` e a remoção do diretório; o `.env` de ensaio recebe apenas segredos efêmeros, por contrato já existente do `test_05_nascimento.sh`; a Task 3 roda o ensaio **sem** `--keep` |
| T-07-24 | Repudiation | tag leve (sem autor, data ou mensagem) tornando a release não rastreável | mitigate | `git tag -a` obrigatório, com verificação `git cat-file -t v0.2.0` retornando `tag` |
| T-07-SC | Tampering | instalação de pacote npm/pip/cargo | n/a | Nenhum pacote novo em toda a fase; `07-RESEARCH.md` §"Auditoria de Legitimidade de Pacote" registra "não aplicável". O `npx --yes tailwindcss@3.4.17` do `Dockerfile:15` mantém a versão pinada, sem alteração |
</threat_model>

<verification>
- `bash .template-tests/test_copier_copy.sh` — exit 0
- `bash .template-tests/test_copier_update.sh` — exit 0
- `python3 -m unittest discover -s .template-tests -p 'test_*.py'` — exit 0
- `bash .template-tests/test_05_nascimento.sh` — exit 0, suíte Django com ao menos 77 testes
- Inspeção humana das 4 telas nos 2 temas — aprovada
- `git tag -l` — lista `v0.2.0`
</verification>

<success_criteria>
- README com roteiro de atualização dos derivados e regressão documentada corretamente
- 11 suítes de `.template-tests/` e a suíte Django verdes (critério 8)
- Ensaio A→B→C passando com o passo novo do derivado editando o próprio `_nav_dominio.html` (critério 7)
- Operador aprovou a inspeção visual das 4 telas nos dois temas
- Tag anotada `v0.2.0` criada, entregando Fases 6 e 7 juntas
</success_criteria>

<output>
Create `.planning/phases/07-herdar-o-design-system-do-pca/07-08-SUMMARY.md` when done
</output>
