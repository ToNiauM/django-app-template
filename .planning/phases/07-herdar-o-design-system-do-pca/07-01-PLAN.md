---
phase: 07-herdar-o-design-system-do-pca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .template-tests/test_copier_copy.sh
  - .template-tests/test_04_03_identity.py
  - .template-tests/test_04_04_optional_exemplo.py
  - .template-tests/test_04_06_operations.py
autonomous: true
requirements: [QA-03, REL-01]

must_haves:
  truths:
    - "Rodar qualquer suíte de .template-tests/ gera uma árvore a partir do working tree do template, não da tag v0.1.0 (D-95)"
    - "Um artefato entregue depois da v0.1.0 (core/static/img/logo-entidade.svg, Fase 6) aparece na árvore gerada por todas as suítes que invocam copier copy"
    - "Se alguém remover --vcs-ref=HEAD de uma dessas invocações, a regressão falha em vez de passar em silêncio"
  artifacts:
    - path: ".template-tests/test_copier_copy.sh"
      provides: "render() com --vcs-ref=HEAD e guarda de artefato pós-v0.1.0"
      contains: "--vcs-ref=HEAD"
    - path: ".template-tests/test_04_03_identity.py"
      provides: "render() com --vcs-ref=HEAD"
      contains: "--vcs-ref=HEAD"
    - path: ".template-tests/test_04_04_optional_exemplo.py"
      provides: "render() com --vcs-ref=HEAD"
      contains: "--vcs-ref=HEAD"
    - path: ".template-tests/test_04_06_operations.py"
      provides: "render() com --vcs-ref=HEAD"
      contains: "--vcs-ref=HEAD"
  key_links:
    - from: ".template-tests/test_copier_copy.sh"
      to: "working tree do template"
      via: "copier copy --vcs-ref=HEAD"
      pattern: "vcs-ref=HEAD"
---

<objective>
As quatro suítes de `.template-tests/` que hoje geram sistemas a partir da **tag
`v0.1.0`** passam a gerar a partir do **working tree** (`--vcs-ref=HEAD`), e uma guarda
impede a regressão silenciosa.

Purpose: sem isso, todo teste de contrato escrito nas Fases 6 e 7 mede a v0.1.0 — árvore
sem `logo-entidade.svg`, sem bind mount, sem nada desta fase — e dá resultado sem
sentido. É pré-requisito de qualquer verificação dos critérios 5, 6 e 7 (D-95, Pitfall 17).
Output: 4 suítes corrigidas + guarda executável de não-regressão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-CONTEXT.md
</context>

<interfaces>
Forma canônica da flag, já usada pelas três suítes corrigidas
(`test_04_05_backup.py:25`, `test_06_persistencia.py:33`, `test_05_nascimento.sh:132`):

- Em shell: a flag entra na lista de argumentos de `"${COPIER}" copy`, como
  `--vcs-ref=HEAD`.
- Em Python: entra na lista passada a `subprocess.run`, como o item literal
  `"--vcs-ref=HEAD"`, imediatamente depois de `"copy"` e antes de `"--defaults"`.

Sítios exatos a corrigir (um por suíte):

| Arquivo | Função | Invocação |
|---|---|---|
| `.template-tests/test_copier_copy.sh` | `render()` (l. ~36) | `"${COPIER}" copy --defaults …` |
| `.template-tests/test_04_03_identity.py` | `render()` (l. ~36-39) | lista com `"copy", "--defaults", …` |
| `.template-tests/test_04_04_optional_exemplo.py` | `render()` (l. ~18-21) | lista com `"copy", "--defaults", …` |
| `.template-tests/test_04_06_operations.py` | `PortableOperationsTemplateTests.render()` (l. ~17-21) | lista com `"copy", "--defaults", …` |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apontar as quatro suítes para o working tree</name>
  <files>.template-tests/test_copier_copy.sh, .template-tests/test_04_03_identity.py, .template-tests/test_04_04_optional_exemplo.py, .template-tests/test_04_06_operations.py</files>
  <read_first>
    - `.template-tests/test_copier_copy.sh` (função `render`, linhas 25-46)
    - `.template-tests/test_04_03_identity.py` (função `render`, linhas 34-60)
    - `.template-tests/test_04_04_optional_exemplo.py` (função `render`, linhas 16-47)
    - `.template-tests/test_04_06_operations.py` (método `render`, linhas 15-40)
    - `.template-tests/test_04_05_backup.py` linha 25 e `.template-tests/test_06_persistencia.py` linha 33 — a forma correta já aplicada, com o comentário que explica o motivo; copie a redação do comentário para as quatro suítes
    - `.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md` §"Pitfall 17"
  </read_first>
  <action>
    Acrescente `--vcs-ref=HEAD` à invocação de `copier copy` de cada uma das quatro
    suítes listadas em `<files>`, na posição imediatamente após `copy` e antes de
    `--defaults`. Em `test_copier_copy.sh` a mudança é na função `render()`; nas três
    suítes Python é na lista de argumentos de `subprocess.run`.

    Não toque na função `exigir_invalido()` de `test_copier_copy.sh` (l. ~196) nem em
    `test_copier_update.sh`: a primeira testa rejeição de resposta inválida e a segunda
    cria tags temporárias próprias (`v0.1.0`/`v0.1.1`/`v0.1.2`) de propósito — passar
    `--vcs-ref=HEAD` lá destruiria o ensaio A→B→C.

    Em cada sítio corrigido, deixe um comentário de uma linha em pt-BR com a razão:
    sem a flag o Copier renderiza a última tag (PEP 440), não o working tree, e o teste
    mede uma versão antiga do template.
  </action>
  <verify>
    <automated>grep -c -- '--vcs-ref=HEAD' .template-tests/test_copier_copy.sh .template-tests/test_04_03_identity.py .template-tests/test_04_04_optional_exemplo.py .template-tests/test_04_06_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--vcs-ref=HEAD' .template-tests/test_copier_copy.sh` retorna ao menos 1
    - `grep -c -- '"--vcs-ref=HEAD"' .template-tests/test_04_03_identity.py .template-tests/test_04_04_optional_exemplo.py .template-tests/test_04_06_operations.py` retorna ao menos 1 para cada arquivo
    - `grep -c -- 'vcs-ref' .template-tests/test_copier_update.sh` continua mostrando apenas as referências a `v0.1.0`, `v0.1.1` e `v0.1.2` — nenhuma ocorrência de `HEAD`
    - `python3 -m py_compile .template-tests/*.py` sai com código 0
  </acceptance_criteria>
  <done>As quatro suítes invocam `copier copy --vcs-ref=HEAD`; o ensaio A→B→C segue com as tags próprias.</done>
</task>

<task type="auto">
  <name>Task 2: Guarda executável contra a regressão do --vcs-ref</name>
  <files>.template-tests/test_copier_copy.sh</files>
  <read_first>
    - `.template-tests/test_copier_copy.sh` (arquivo inteiro — funções `falhar`, `render`, `verificar_variante` e a chamada final)
    - `core/static/img/` (confirme que `logo-entidade.svg` e `logo-subsistema.svg` existem no working tree — são artefatos da Fase 6, posteriores à tag `v0.1.0`)
    - `.planning/STATE.md` §"Phase 06-02" (contrato dos logos por arquivo fixo, D-65)
  </read_first>
  <action>
    Em `verificar_variante()` de `.template-tests/test_copier_copy.sh`, acrescente duas
    asserções que só passam se a árvore gerada vier do working tree:

    1. `[ -f "${destino}/core/static/img/logo-entidade.svg" ]` — falhar com a mensagem
       "árvore gerada não tem o logo da Fase 6: a suíte está renderizando uma tag antiga,
       não o HEAD".
    2. `! grep -Fq '_commit: v0.1.0' "${destino}/.copier-answers.yml"` — falhar com a
       mensagem "árvore gerada registrou _commit: v0.1.0 — falta --vcs-ref=HEAD".

    Use `falhar` (a função de erro já existente no arquivo) para as duas, mantendo o
    estilo das asserções vizinhas. Ambas valem para as duas variantes
    (`incluir_app_exemplo` true e false) — coloque-as fora do `if [ "${exemplo}" = true ]`.
  </action>
  <verify>
    <automated>bash .template-tests/test_copier_copy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash .template-tests/test_copier_copy.sh` sai com código 0
    - `grep -c 'logo-entidade.svg' .template-tests/test_copier_copy.sh` retorna ao menos 1
    - `grep -c '_commit: v0.1.0' .template-tests/test_copier_copy.sh` retorna ao menos 1
    - Prova negativa manual, registrada no SUMMARY: remover temporariamente `--vcs-ref=HEAD` de `render()` faz `bash .template-tests/test_copier_copy.sh` sair com código diferente de 0 e imprimir uma das duas mensagens novas; a flag é restaurada em seguida
    - `python3 -m unittest discover -s .template-tests -p 'test_04_*.py'` sai com código 0
  </acceptance_criteria>
  <done>A suíte falha ruidosamente se a flag for perdida; hoje passa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree do template → árvore gerada | O que a suíte mede define o que a release entrega; medir a tag errada é um falso verde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | `.template-tests/*` renderizando `v0.1.0` como se fosse o HEAD | mitigate | `--vcs-ref=HEAD` nas 4 suítes + guarda executável (Task 2) que assere um artefato pós-v0.1.0 e a ausência de `_commit: v0.1.0` |
| T-07-SC | Tampering | instalação de pacote npm/pip/cargo | n/a | Este plano não instala nada. `07-RESEARCH.md` §"Auditoria de Legitimidade de Pacote" registra: nenhum pacote novo na fase; `copier==9.17.1` já passou pelo gate humano da Fase 4 e `tailwindcss@3.4.17` está pinado no `Dockerfile:15` |

Superfície de segurança de aplicação neste plano: **vazia**. O plano só toca arquivos de
teste que nunca chegam à árvore gerada (`.template-tests` está no `_exclude` do
`copier.yml`). Não há entrada de usuário, saída renderizada nem dado persistido.
</threat_model>

<verification>
- `bash .template-tests/test_copier_copy.sh` — exit 0
- `python3 -m unittest discover -s .template-tests -p 'test_04_*.py'` — exit 0
- `python3 -m py_compile .template-tests/*.py` — exit 0
</verification>

<success_criteria>
- As 4 suítes listadas em `files_modified` invocam `copier copy --vcs-ref=HEAD`
- `test_copier_update.sh` continua sem `HEAD` (usa tags próprias)
- `test_copier_copy.sh` falha se a flag for removida (prova negativa registrada no SUMMARY)
- Toda a regressão de `.template-tests/` segue verde
</success_criteria>

<output>
Create `.planning/phases/07-herdar-o-design-system-do-pca/07-01-SUMMARY.md` when done
</output>
