---
phase: 07-herdar-o-design-system-do-pca
plan: 14
type: execute
gap_closure: true
wave: 4
subsystem: release-e-verificacao
depends_on: ["07-09", "07-10", "07-11", "07-12", "07-13"]
dependency-graph:
  requires:
    - "item-ativo-unico"
    - "token-brand-tx"
    - "chrome-do-grafico-lendo-o-token-certo"
    - "rampa-sequencial-de-4-degraus"
  provides:
    - "regressao-completa-verde-pos-gaps"
    - "tag-v0.2.0-sobre-commit-corrigido"
  affects:
    - "README.md"
    - ".planning/ROADMAP.md"
tech-stack:
  added: []
  patterns:
    - "Credencial efêmera de inspeção nasce no executor com tracing desabilitado, viaja só pelo ambiente e nunca toca arquivo, log ou commit"
    - "Tag de release é recriada sobre o commit corrigido, nunca movida às pressas nem publicada sem decisão do operador"
key-files:
  created: []
  modified:
    - "README.md"
    - ".planning/ROADMAP.md"
files_modified:
  - README.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [REL-01, QA-03, DS-02, DS-03, DS-05, NAV-02]

must_haves:
  truths:
    - "Os quatro pontos consertados foram olhados por um humano numa cópia real, nos dois temas"
    - "A regressão inteira das 7 etapas do README passa verde depois dos cinco planos de fechamento"
    - "A tag `v0.2.0` aponta para um commit que não contém nenhum dos quatro bloqueadores"
    - "Nada foi publicado: `git push` continua sendo decisão do operador"
  artifacts:
    - path: "README.md"
      provides: "Nota, na seção de release, de que a v0.2.0 foi recriada e por quê"
    - path: ".planning/ROADMAP.md"
      provides: "Os 6 planos de fechamento listados na Phase 7, com a contagem correta"
  key_links:
    - from: "tag v0.2.0"
      to: "HEAD corrigido"
      via: "git tag -d + git tag -a sobre o commit da regressão verde"
      pattern: "v0.2.0"
---

<objective>
Fecho da rodada de gap closure: um humano confere os quatro pontos que foram consertados
(e só eles), a regressão inteira roda verde, e a tag `v0.2.0` — que hoje aponta para
`367dd9a`, o commit que **contém** os quatro bloqueadores — é apagada e recriada sobre o
commit corrigido.

Purpose: a tag existe localmente e **não** foi publicada. Essa é a única razão pela qual
recriá-la é seguro e correto: nenhum sistema derivado recebeu a v0.2.0 ainda, então não há
histórico de terceiro para preservar. Se a tag já tivesse sido publicada, a resposta seria
outra (uma `v0.2.1`), e é isso que o registro no README precisa deixar claro para a próxima
vez.
Output: inspeção humana restrita aprovada, regressão verde, tag correta, nada publicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-VERIFICATION.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-08-SUMMARY.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-09-SUMMARY.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-10-SUMMARY.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-11-SUMMARY.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-12-SUMMARY.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-13-SUMMARY.md
</context>

<interfaces>
### O que NÃO se reinspeciona

A inspeção visual integral das 4 telas × 2 temas (07-08 Task 2, `ui_safety_gate`) foi
executada numa cópia real e **aprovada pelo operador** em 2026-08-23. O
`07-VERIFICATION.md` registra explicitamente: "Já fechado, não repetir". Não reabra o
roteiro completo — nem ausência de flash, nem legibilidade da régua, nem véu dos modais,
nem sobrevivência do tema ao logout. Pedir de novo o que já foi aprovado gasta o único
recurso escasso desta fase, que é a atenção do operador.

O que se inspeciona são os **quatro pontos consertados**, e mais nada.

### Contrato da credencial efêmera (reaproveitado do 07-08 Task 2)

Sem alterações — é contrato, não sugestão:

1. A senha é gerada **pelo executor**, com
   `python3 -c 'import secrets; print(secrets.token_urlsafe(24))'`, com **tracing
   desabilitado** no shell antes da geração.
2. Ela é passada ao tracer **uma única vez**, **só pelo ambiente**
   (`NASCIMENTO_ADMIN_PASSWORD=… bash .template-tests/test_05_nascimento.sh --keep`).
3. Ela **não** entra em arquivo, SUMMARY, README, `.copier-answers.yml`, log, histórico
   de shell nem comando versionado. Nenhuma exceção.
4. Ela é entregue ao operador **só** no retorno do checkpoint.
5. Identidade fixa contratada pelo plano 05-01 (`nascimento@example.invalid`), sem
   `createsuperuser` manual — recriar credencial por fora do tracer é proibido pelo
   contrato de `05-03-PLAN.md:151`.
6. Antes do handoff, checagem de vazamento: `grep -rIF "<senha>"` no repositório do
   template e na árvore gerada (exceto `dados/`) tem que voltar vazio nos dois.
7. Ao fim, cópia derrubada (`down --volumes --remove-orphans`), diretório apagado e
   `unset` da variável.

Lição do 07-08 a não repetir: uma cópia retida ficou órfã por 3 horas e a senha dela morreu
com a sessão, tornando a cópia inútil. Rode `bash .template-tests/ensaio_django.sh derrubar`
**antes** de preparar a cópia e confira `docker compose ls` sem projeto de ensaio ou
nascimento.

### Roteiro da regressão (seção "Regressão do template" do README)

Nesta ordem, com timeout de **600000 ms** em cada etapa:

1. `bash .template-tests/ensaio_django.sh derrubar`
2. `bash .template-tests/test_copier_copy.sh`
3. `bash .template-tests/test_copier_update.sh`
4. `python3 -m unittest discover -s .template-tests -p 'test_*.py'`
5. `bash .template-tests/test_07_cor_runtime.sh`
6. `bash .template-tests/ensaio_django.sh derrubar` (a suíte de cor recria o banco)
7. `bash .template-tests/test_05_nascimento.sh`

### Estado da tag hoje

```
git tag -l              -> v0.1.0, v0.2.0
git rev-parse v0.2.0^{commit}  -> 367dd9a   (contém os 4 bloqueadores)
git log --oneline -1           -> 85a424e   (remoção do **Mode:** mvp; ainda antes dos consertos)
```

A tag **não foi publicada**. `git push` está fora deste plano e de qualquer plano: é
decisão do operador.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Inspeção visual dos quatro pontos consertados, nos dois temas</name>
  <files>(nenhum arquivo é alterado nesta tarefa — é inspeção)</files>
  <read_first>
    - `07-08-SUMMARY.md` §"Task 2" (o contrato da credencial efêmera, executado com sucesso uma vez — reaproveite-o íntegro, não reinvente)
    - `07-VERIFICATION.md` §"Verificação humana necessária" e a nota "Já fechado, não repetir"
    - Os SUMMARYs de 07-10 a 07-13 (o que exatamente foi consertado em cada ponto)
  </read_first>
  <action>
    Prepare a cópia **antes** de chamar o operador, nesta ordem:

    1. `bash .template-tests/ensaio_django.sh derrubar` e confira `docker compose ls -a`
       sem nenhum projeto de ensaio ou nascimento. O 07-08 registra uma cópia órfã de pé
       por 3 horas cuja senha morreu junto com a sessão que a gerou — comece limpo.
    2. Desabilite o tracing do shell e gere a senha com
       `python3 -c 'import secrets; print(secrets.token_urlsafe(24))'`.
    3. Suba a cópia com
       `NASCIMENTO_ADMIN_PASSWORD=… bash .template-tests/test_05_nascimento.sh --keep`
       (timeout de 600000 ms). A identidade é a fixa contratada pelo plano 05-01
       (`nascimento@example.invalid`); **não** rode `createsuperuser` à mão.
    4. Checagem de vazamento antes do handoff: `grep -rIF "<senha>"` no repositório do
       template e na árvore gerada (exceto `dados/`) tem que voltar vazio nos dois.
       Registre que a checagem foi feita — **jamais** a senha.
    5. Smoke rápido: `/healthz` → 200, `/login/` → 200, `/exemplo/dashboard/` sem
       autenticação → 302.

    Só então entregue ao operador a URL, o usuário e a senha, **no próprio retorno do
    checkpoint** — e nada mais. Nenhum arquivo é alterado por esta task.

    Depois do veredito: derrube a cópia com `down --volumes --remove-orphans`, apague o
    diretório temporário, faça `unset` da variável de senha e confirme `docker compose ls -a`
    sem resíduo. Registre no SUMMARY o veredito ponto a ponto (1 a 4), por tema.
  </action>
  <what-built>
    Cinco planos de fechamento de gap, todos com prova executável que falhava antes e passa
    depois:

    - **07-09** — helper WCAG único (`core/tests/contraste.py`), gate da régua enxergando
      `text-[NNpx]`, gate do dourado auditando todos os prefixos, teto de 20px valendo na
      build do Tailwind.
    - **07-10 (G-01)** — `{% item_nav %}` com desempate exato-vence-prefixo; um só
      `aria-current="page"` por página; `{% nav_dominio %}` tolerante à ausência do arquivo
      do derivado; `request` opcional.
    - **07-11 (G-02)** — `--cor-brand-tx`: o texto sobre a marca inverte com o tema
      (branco no claro, tinta escura no escuro), nos 4 sítios que usavam `text-white`.
    - **07-12 (G-04)** — grade do eixo lendo `--cor-grid`; borda das fatias lendo o fundo
      real do card por tema; tooltip do ECharts com escape.
    - **07-13 (G-03)** — quarto degrau real da rampa (`seq-750`), derivado da marca, no
      lugar do token de fundo `brand-tint`.

    A cópia para inspeção foi gerada com `test_05_nascimento.sh --keep`, com credencial
    efêmera segundo o contrato do 07-08 Task 2.
  </what-built>
  <how-to-verify>
    **Antes:** o executor entrega a URL, o usuário (`nascimento@example.invalid`) e a senha
    efêmera no próprio retorno do checkpoint. Faça login e use o controle de tema do canto
    da `<aside>` para alternar claro/escuro sem recarregar.

    A inspeção integral das 4 telas já foi aprovada em 2026-08-23 e **não** é reaberta.
    Confira **apenas** estes quatro pontos:

    1. **Menu ativo em `/exemplo/dashboard/`** — navegue até o Dashboard do exemplo. Só
       **um** item do menu pode estar aceso (fundo tênue + filete vertical de 2px à
       esquerda): o "Dashboard". O "Itens (CRUD)" tem que estar apagado. Depois vá para
       `/exemplo/` — agora só o "Itens (CRUD)" acende. Repita nos dois temas.

    2. **As 4 fatias do donut, nos 2 temas** — no card "Distribuição por Status", conte as
       fatias visíveis: têm que ser quatro, todas distinguíveis do fundo do card e entre
       si, e a separação entre elas tem que parecer do mesmo tom do card (não uma linha
       escura estranha no escuro). Troque o tema com o gráfico na tela e confira que ele
       repinta mantendo as quatro.

    3. **A grade do eixo no escuro** — no card "Valor por Categoria", com o tema escuro
       ativo: as linhas horizontais de grade do eixo Y têm que existir e ser discretas —
       visíveis, não gritantes. Antes do conserto elas eram exatamente a cor do card, ou
       seja, invisíveis.

    4. **Botões primários no escuro** — na tela de listagem `/exemplo/`, o botão "Novo
       item"; e no modal de criação, o botão "Salvar item". Com o tema escuro ativo, o
       texto tem que estar legível sobre o fundo azul claro do botão (tinta escura, não
       branco). Passe o mouse por cima: o estado de hover clareia o fundo e o texto tem que
       continuar legível — o hover era o pior caso antes do conserto.

    Se qualquer ponto reprovar, descreva o que viu e em qual tema. Não é preciso conferir
    mais nada da tela.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que reprovou, indicando o ponto (1 a 4) e o tema</resume-signal>
  <verify>
    <automated>bash -c 'test -z "$(docker compose ls -a --format json | grep -o ensaio || true)" && echo "host limpo antes da preparacao"'</automated>
  </verify>
  <acceptance_criteria>
    - `bash .template-tests/ensaio_django.sh derrubar` rodou **antes** da preparação e `docker compose ls` não listava nenhum projeto de ensaio ou nascimento
    - `NASCIMENTO_ADMIN_PASSWORD=… bash .template-tests/test_05_nascimento.sh --keep` saiu com código 0, com a contagem de testes Django registrada (medida, não copiada)
    - `grep -rIF "<senha>"` no repositório e na árvore gerada (exceto `dados/`) voltou vazio nos dois — registre que a checagem foi feita, **nunca** a senha
    - O operador respondeu ao checkpoint; o veredito está registrado no SUMMARY ponto a ponto (1 a 4), em ambos os temas
    - Ao fim: cópia derrubada com `down --volumes --remove-orphans`, diretório temporário apagado, `unset` da variável de senha, e `docker compose ls -a` sem resíduo
    - Nenhum arquivo foi alterado por esta task
  </acceptance_criteria>
  <done>Os quatro consertos foram vistos funcionando por um humano, numa cópia real gerada do template, nos dois temas — e o host ficou limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Regressão completa das 7 etapas e o registro do que mudou</name>
  <files>README.md, .planning/ROADMAP.md</files>
  <read_first>
    - `README.md` §"Regressão do template" (a ordem exata das 7 etapas) e §"Criando a tag de release"
    - `.planning/ROADMAP.md` §"### Phase 7" (a lista de planos e a linha `**Plans:**`)
    - Os SUMMARYs dos planos 07-09 a 07-13 (as contagens medidas por cada um)
  </read_first>
  <action>
    Rode a regressão inteira, na ordem do README, com host limpo antes e depois, e com
    timeout explícito de 600000 ms em cada etapa. Registre no SUMMARY uma tabela
    etapa-a-etapa com o resultado e as contagens **medidas** (número de testes Django, de
    testes de `.template-tests/`, de suítes) — nunca copiadas de documento, que é a
    disciplina que o 07-08 estabeleceu.

    Se qualquer etapa falhar, **pare**. Não recrie a tag sobre árvore vermelha, não
    "conserte de passagem": relate a falha e devolva ao operador.

    Depois da regressão verde, atualize os dois documentos:

    **`.planning/ROADMAP.md` §"Phase 7"** — a subseção "Fechamento de gaps" já existe com
    os seis planos e suas ondas. Confira que os seis estão marcados `- [x]` e atualize a
    linha `**Plans:**` para a contagem final (`6/6 concluídos`). Não reescreva os 8 success
    criteria — eles continuam sendo o contrato da fase e continuam atendidos. Se algum
    plano tiver ficado sem marcar por causa de falha de execução, **não** marque: relate.

    **`README.md` §"Criando a tag de release"** — acrescente um parágrafo curto explicando
    que a `v0.2.0` foi **recriada** sobre um commit posterior, e por que isso foi
    aceitável: a tag existia apenas localmente e nunca foi publicada, então não havia
    histórico de terceiro para preservar. Escreva também a regra para a próxima vez: **tag
    publicada não se move** — se um defeito for encontrado depois do push, a resposta é uma
    versão nova, não a mesma tag em outro commit. Esse parágrafo é o valor durável desta
    task; sem ele, a próxima pessoa repete o erro na direção perigosa.

    Não invente números em nenhum dos dois documentos. Onde couber contagem, meça na hora
    (`ls .template-tests/ | grep -c '^test_'` e o total reportado pelo runner) e registre o
    comando ao lado do valor no SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'bash .template-tests/ensaio_django.sh derrubar && bash .template-tests/test_copier_copy.sh && bash .template-tests/test_copier_update.sh && python3 -m unittest discover -s .template-tests -p "test_*.py" && bash .template-tests/test_07_cor_runtime.sh && bash .template-tests/ensaio_django.sh derrubar && bash .template-tests/test_05_nascimento.sh'</automated>
  </verify>
  <acceptance_criteria>
    - As 7 etapas saíram com código 0, e a tabela etapa-a-etapa está no SUMMARY com as contagens medidas
    - `docker compose ls -a` não lista nenhum projeto de ensaio ou nascimento ao fim
    - `grep -c "07-09-PLAN.md" .planning/ROADMAP.md` retorna 1, e o mesmo para 07-10 a 07-14
    - A linha `**Plans:**` da Phase 7 bate com a contagem real de arquivos `07-*-PLAN.md` no diretório da fase
    - `grep -ci "tag publicada" README.md` retorna ao menos 1 (a regra para a próxima vez está escrita)
    - Nenhum número novo entrou em README ou ROADMAP sem ter sido medido nesta task
  </acceptance_criteria>
  <done>A árvore inteira está verde depois dos cinco consertos, e o histórico de por que a tag foi recriada está escrito onde alguém vai procurar.</done>
</task>

<task type="auto">
  <name>Task 3: `v0.2.0` recriada sobre o commit corrigido — sem publicar</name>
  <files>(nenhum arquivo de código; operação de git)</files>
  <read_first>
    - O resultado da Task 2 (a regressão precisa estar verde **antes** desta task)
    - `README.md` §"Criando a tag de release" (o formato da mensagem de tag já usado na v0.2.0 anterior)
    - `07-08-SUMMARY.md` §"Task 3" (como a tag foi criada da primeira vez, incluindo a asserção relativa de posição)
  </read_first>
  <action>
    Pré-condições, nesta ordem, e todas obrigatórias:

    1. `git status --short` sem saída — árvore limpa, tudo commitado.
    2. A Task 2 saiu verde nas 7 etapas, nesta sessão.
    3. `git rev-parse v0.2.0^{commit}` ainda devolve `367dd9a` — se já não devolver, pare e
       relate: alguém mexeu na tag e o pressuposto deste plano mudou.
    4. Confirme que a tag **não** foi publicada:
       `git ls-remote --tags origin 2>/dev/null | grep v0.2.0` não pode retornar nada. Se
       retornar, **pare imediatamente** e devolva ao operador — mover uma tag publicada é
       destrutivo para todo derivado que já a consumiu, e a resposta certa passa a ser uma
       `v0.2.1`, que é decisão do operador, não sua.

    Então: `git tag -d v0.2.0` e recrie com `git tag -a v0.2.0` sobre o HEAD verificado. A
    mensagem, em pt-BR, diz o que a release entrega (Fases 6 e 7) **e** acrescenta um
    parágrafo sobre o fechamento dos quatro bloqueadores encontrados na revisão de código —
    item ativo único na navegação, par texto/fundo da marca no tema escuro, quarto degrau
    real da rampa do donut e grade do eixo visível no escuro.

    Verifique a tag nova com asserções **relativas**, no padrão que o 07-08 estabeleceu —
    nada de número mágico:

    - `git cat-file -t v0.2.0` devolve `tag` (anotada, não leve);
    - `test "$(git rev-list v0.2.0..HEAD --count)" -eq 0` — a tag está no HEAD;
    - `test "$(git rev-list v0.1.0..v0.2.0 --count)" -eq "$(git rev-list v0.1.0..HEAD --count)"`;
    - geração de conferência: `copier copy --defaults --vcs-ref v0.2.0` numa pasta
      temporária sai com código 0, o `.copier-answers.yml` grava `_commit: v0.2.0`, e a
      árvore contém `core/static/src/dominio.css`, `core/templates/core/_nav_dominio.html`
      e `core/static/img/logo-entidade.svg`;
    - na mesma árvore de conferência, prova de que os consertos chegaram:
      `grep -c -- "--cor-brand-tx" core/static/src/input.css` retorna 2,
      `grep -c "seq-750" tailwind.config.js` retorna 1,
      `grep -rc "text-white" apps` não acha nenhum acompanhado de `bg-brand`, e
      `grep -c "excecoes" core/templatetags/navegacao.py` retorna ao menos 1.

    **NÃO execute `git push` nem `git push --tags`, em nenhuma hipótese.** Publicar é
    decisão do operador. Ao fim, apague a pasta de conferência e registre no SUMMARY o
    commit para o qual a tag passou a apontar e o número de commits entre `v0.1.0` e ela —
    medido, não estimado.
  </action>
  <verify>
    <automated>bash -c 'test "$(git cat-file -t v0.2.0)" = "tag" && test "$(git rev-list v0.2.0..HEAD --count)" -eq 0 && test -z "$(git status --short)" && test -z "$(git ls-remote --tags origin 2>/dev/null | grep v0.2.0 || true)" && echo "tag anotada no HEAD, arvore limpa, nada publicado"'</automated>
  </verify>
  <acceptance_criteria>
    - `git cat-file -t v0.2.0` devolve `tag`
    - `git rev-list v0.2.0..HEAD --count` devolve 0 e `git rev-parse v0.2.0^{commit}` **não** é mais `367dd9a`
    - `git ls-remote --tags origin | grep v0.2.0` continua vazio — nada foi publicado
    - A geração de conferência com `--vcs-ref v0.2.0` sai com código 0 e contém os quatro sinais dos consertos listados no `<action>`
    - `git status --short` sem saída antes e depois
    - O SUMMARY registra o commit da tag e a contagem medida de commits desde `v0.1.0`
  </acceptance_criteria>
  <done>A `v0.2.0` aponta para uma árvore sem os quatro bloqueadores, e continua sem sair da máquina.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| credencial efêmera → operador | senha de administrador de uma cópia real, publicada em `127.0.0.1:<porta>`, entregue por uma via só (o retorno do checkpoint) |
| tag local → derivados | a tag é o que o Copier lê; movê-la depois de publicada reescreveria a versão sob os pés de quem já atualizou |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | Information disclosure | senha de administrador da cópia de inspeção vazando para arquivo, log ou commit | mitigate | Contrato do 07-08 Task 2 reaproveitado íntegro: geração no executor com tracing desabilitado, passagem só por ambiente, `grep -rIF` de vazamento antes do handoff, entrega só no retorno do checkpoint, `unset` no fim |
| T-07-23b | Denial of service | cópia retida órfã sobrevivendo à sessão com serviço em `127.0.0.1` de pé | mitigate | `ensaio_django.sh derrubar` antes e depois; `docker compose ls -a` sem resíduo é acceptance criteria das Tasks 1 e 2 |
| T-07-42 | Tampering | mover uma tag já publicada — reescreveria a v0.2.0 sob os pés de derivados que já atualizaram | mitigate | `git ls-remote --tags origin \| grep v0.2.0` é pré-condição bloqueante da Task 3; se retornar algo, o plano para e devolve ao operador. `git push` proibido em qualquer hipótese |
| T-07-43 | Repudiation | tag recriada sem registro de por quê, deixando o histórico ininteligível | mitigate | Mensagem anotada descrevendo os quatro bloqueadores fechados + parágrafo no README com a regra "tag publicada não se move" |
| T-07-SC | Tampering | instalação de pacote npm/pip/cargo | n/a | Nenhum pacote novo em nenhum dos seis planos desta rodada |
</threat_model>

<verification>
- As 7 etapas da seção "Regressão do template" do README — todas exit 0, na ordem
- `docker compose ls -a` sem projeto de ensaio ou nascimento, antes e depois
- `git status --short` sem saída
- `git cat-file -t v0.2.0` = `tag`; `git rev-list v0.2.0..HEAD --count` = 0
- `git ls-remote --tags origin | grep v0.2.0` vazio

> **Orçamento de tempo.** Este plano é o mais caro da rodada: as 7 etapas incluem duas
> gerações completas de sistema, um ensaio A→B→C de `copier update` e a suíte de cor em
> runtime. Rode **cada** etapa com timeout explícito de **600000 ms**, conforme o
> §"Orçamento de tempo" do contrato em `07-01-PLAN.md`. Não encadeie as sete num único
> comando de timeout curto.
</verification>

<success_criteria>
- O operador conferiu os quatro pontos consertados, nos dois temas, e aprovou — sem reabrir a inspeção integral já aprovada
- Nenhuma credencial efêmera sobreviveu à sessão, em nenhuma forma persistida
- As 7 etapas da regressão saíram verdes, com contagens medidas
- ROADMAP e README refletem os seis planos de fechamento e o motivo da recriação da tag
- `v0.2.0` é tag anotada apontando para o HEAD corrigido, e `367dd9a` deixou de ser o alvo
- A regra "tag publicada não se move" está escrita no README
- Nada foi publicado
</success_criteria>

<output>
Create `.planning/phases/07-herdar-o-design-system-do-pca/07-14-SUMMARY.md` when done
</output>
