---
phase: 07-herdar-o-design-system-do-pca
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - core/static/src/input.css
  - core/static/src/dominio.css
  - tailwind.config.js
  - tailwind.config.js.jinja
  - Dockerfile
  - .template-tests/test_04_03_identity.py
  - .template-tests/test_07_tokens.py
  - "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_form_modal.html"
  - "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_confirmar_exclusao_modal.html"
  - "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/dashboard.html"
  - "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/item_listar.html"
autonomous: true
requirements: [DS-01, DS-03, DS-04, DS-06]

must_haves:
  truths:
    - "Um sistema gerado tem a fonte física de toda cor em core/static/src/input.css, com hex plano, e o arquivo não passa por Jinja (D-79)"
    - "tailwind.config.js chega ao sistema gerado verbatim, sem sufixo .jinja e sem nenhuma interpolação — o derivado nunca precisa editá-lo (D-83, critério 4)"
    - "O sistema gerado declara raio único de 2px, régua tipográfica de 6 degraus com teto de 20px e pilha system-ui, sem nenhuma pergunta nova no copier.yml (D-82)"
    - "O sistema gerado declara :root e [data-tema=\"escuro\"] com o mesmo conjunto de tokens, mais darkMode por seletor de atributo"
    - "O derivado tem core/static/src/dominio.css como lugar próprio para os tokens de estado do domínio dele, com o contrato do par X/X-tx documentado (D-92)"
    - "O @import de dominio.css é a PRIMEIRA declaração de input.css — depois de @tailwind ele é descartado em silêncio, e ausente faz o build falhar ruidosamente (D-97)"
    - "O véu dos dois modais do app exemplo continua opaco depois da migração para var() — bg-black/40, nunca bg-ink/40 (D-94)"
    - "Nenhuma classe morta de Tailwind v4 sobra nos templates: shadow-xs some (D-96)"
    - "Nenhuma menção a PCA/CFC ou procedência cromática entra no código gerado (D-93)"
  artifacts:
    - path: "core/static/src/input.css"
      provides: "Fonte física dos tokens, focus-ring único, classes de componente"
      contains: "@import \"./dominio.css\";"
      min_lines: 80
    - path: "core/static/src/dominio.css"
      provides: "Stub de tokens de estado do derivado, com comentário-contrato"
    - path: "tailwind.config.js"
      provides: "darkMode, colors por var(--cor-*), safelist, borderRadius, fontSize, fontFamily"
      contains: "darkMode"
    - path: ".template-tests/test_07_tokens.py"
      provides: "Contrato dos tokens sobre a FONTE, não sobre o CSS compilado"
  key_links:
    - from: "tailwind.config.js"
      to: "core/static/src/input.css"
      via: "colors apontando para var(--cor-*)"
      pattern: "var\\(--cor-"
    - from: "core/static/src/input.css"
      to: "core/static/src/dominio.css"
      via: "@import na primeira linha"
      pattern: "^@import \"\\./dominio\\.css\";"
    - from: "Dockerfile"
      to: "core/static/src/"
      via: "COPY do diretório inteiro (senão o @import quebra o build)"
      pattern: "COPY core/static/src"
---

<objective>
A fonte física de toda cor do sistema passa a ser `core/static/src/input.css`; o
`tailwind.config.js` deixa de ser um arquivo Jinja e passa a só apontar para as
variáveis; o derivado ganha `dominio.css` para os tokens de estado dele; e as classes
que a migração para `var()` mata são corrigidas na mesma leva.

Purpose: é a peça que torna possíveis o tema escuro (o mesmo utilitário resolve nos dois
temas), o gráfico sem hex e o critério 4 ("o derivado não edita `tailwind.config.js` para
nada"). Sem `bg-black/40` no mesmo passo, a migração introduziria um véu transparente nos
dois modais — regressão real, verificada por compilação do Tailwind 3.4.17 (D-94).
Output: `input.css` reescrito, `dominio.css` novo, `tailwind.config.js` verbatim,
`Dockerfile` ajustado, contrato de tokens executável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-CONTEXT.md
@.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md
</context>

<interfaces>
### Tokens neutros — valores-alvo (fonte: `/opt/web/pca/core/static/src/input.css:119-205`)

| Variável | `:root` (claro) | `[data-tema="escuro"]` |
|---|---|---|
| `--cor-page` | `#f9f9f7` | `#0f0e0d` |
| `--cor-surface` | `#fcfcfb` | `#181614` |
| `--cor-surface-2` | `#f3f2ef` | `#22211d` |
| `--cor-surface-3` | `#fcfcfb` | `#2e2c28` |
| `--cor-ink` | `#0b0b0b` | `#eeeeee` |
| `--cor-ink-2` | `#52514e` | `#b9b8b5` |
| `--cor-muted` | `#898781` | `#95938e` |
| `--cor-grid` | `#e1e0d9` | `#3a3833` |
| `--cor-baseline` | `#c3c2b7` | (herda) |
| `--cor-destructive` | `#d03b3b` | (herda) |
| `--cor-danger-tint` | `#fbe9e9` | `#2e1616` |
| `--cor-warn-bg` | `#fdf3e0` | `#2b2011` |
| `--cor-warn-tx` | `#7a5000` | `#c98400` |
| `--cor-secundaria` | `#a07400` | (herda) |

`--cor-muted` e `--cor-grid` **mudam** de valor (hoje `#77756f` e `#e4e2dd`): o padrão de
referência é a fonte declarada (A5 da pesquisa, aceito).

### Família de marca — DEFAULT do template, derivado de `#1e40af`

> ⚠ **Os hex de marca do esqueleto de `07-RESEARCH.md` §"Exemplos de Código"
> (linhas 1326-1341) estão SUPERSEDIDOS por esta tabela.** A pesquisa publicou
> `seq-450: #6379c6`, `seq-300: #a5b1e0`, e no escuro `brand: #8ba4ec`,
> `brand-hover: #bcc9f4`, `brand-ink: #6b8ae5`, `brand-tint: #1c2540`,
> `seq-450: #5878cf`, `seq-300: #3f5aa1` — nenhum desses valores é o que as regras
> declaradas na própria pesquisa produzem para `#1e40af`. A tabela abaixo foi recalculada
> chave a chave no planejamento e é a **única** fonte para escrever o `input.css`.
> O plano 07-04 prova a igualdade por teste; um valor do esqueleto entraria em silêncio
> aqui e só quebraria uma onda depois.

Valores calculados e conferidos no planejamento com as mesmas fórmulas que
`core/tema.py` vai implementar no plano 07-04:

| Variável | `:root` | `[data-tema="escuro"]` |
|---|---|---|
| `--cor-brand` | `#1e40af` | `#889feb` |
| `--cor-brand-hover` | `#3957b9` | `#b4c2f2` |
| `--cor-brand-ink` | `#193490` | `#5d7ce3` |
| `--cor-brand-tint` | `#edf0f9` | `#192035` |
| `--cor-seq-600` | `#1e40af` | `#889feb` |
| `--cor-seq-450` | `#6b81ca` | `#5873c9` |
| `--cor-seq-300` | `#aab6e1` | `#435697` |

Regras que os produziram (o plano 07-04 as implementa em Python e um teste prova a
igualdade com este bloco):
`hover = misturar(cor,255,0.12)`, `ink = misturar(cor,0,0.18)`,
`tint = misturar(cor,255,0.92)`, `seq-450 = misturar(cor,255,0.34)`,
`seq-300 = misturar(cor,255,0.62)`; escuro preserva o matiz e ajusta luminosidade
(e saturação, onde indicado): `brand L=0.727`, `hover L=0.827`, `ink L=0.627`,
`tint S×0.50 L=0.153`, `seq-450 S×0.72 L=0.567`, `seq-300 S×0.55 L=0.427`.

### `tailwind.config.js` — chaves-alvo

- `darkMode: ["selector", '[data-tema="escuro"]']`
- `content: ["./core/templates/**/*.html", "./apps/**/*.html"]` (inalterado)
- `safelist: ["results","module","form-row","btn","btn--primaria","btn--secundaria","btn--neutro","btn--destrutiva"]`
- `theme.extend.colors`: uma chave por variável acima, no formato
  `page: "var(--cor-page)"`. Chaves com hífen entre aspas (`"surface-2"`, `"brand-ink"`,
  `"seq-450"`…).
- `theme.extend.borderRadius: { DEFAULT: "2px", sm: "2px", md: "2px", lg: "2px", xl: "2px", "2xl": "2px" }` — `none` e `full` **não** são declaradas
- `theme.extend.fontSize`: `xs 11px/1.4`, `sm 12px/1.4`, `base 13px/1.5`, `md 14px/1.5`
  (chave nova, não existe no Tailwind), `lg 16px/1.4`, `xl 20px/1.3`. `2xl` **não** é
  declarada — o teto é 20px
- `theme.extend.fontFamily: { sans: ["system-ui", "-apple-system", '"Segoe UI"', "sans-serif"] }`
- `misturar()` e `const COR_PRIMARIA` **desaparecem** do arquivo

### Classes de componente (`@layer components` de `input.css`)

`.results`, `.module`, `.form-row`, `.btn`, `.btn--primaria`, `.btn--secundaria`,
`.btn--neutro`, `.btn--destrutiva` — conteúdo em `07-RESEARCH.md` §"Classes de componente
e safelist", que cita o `input.css` real do padrão (use o arquivo, não o UI-SPEC).
`.form-row` diverge do anel único de `@layer base` **de propósito** (Pitfall 20) — não
"consertar".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: input.css vira a fonte física dos tokens, e dominio.css nasce</name>
  <files>core/static/src/input.css, core/static/src/dominio.css</files>
  <read_first>
    - `core/static/src/input.css` (19 linhas — a regra `[x-cloak]` escopada a <768px tem que sobreviver, com o comentário dela)
    - `/opt/web/pca/core/static/src/input.css` linhas 1-30 (`@layer base` do focus-ring), 38-103 (`@layer components`), 119-205 (blocos `:root` e `[data-tema="escuro"]`) — implementação de referência
    - `.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md` §"Padrão 2 — dominio.css" e §"Pitfall 3" (`@import` posicional). O §"Exemplos de Código" serve **só como esqueleto de estrutura**: os hex da família de marca dele (linhas 1326-1341) estão errados e foram SUPERSEDIDOS pela tabela de `<interfaces>` deste plano — use a tabela, nunca a pesquisa, para os 14 valores de marca
    - `.template-tests/test_copier_copy.sh` linhas 48-93 (`auditar_neutralidade` — a lista de 15 tokens proibidos, IGNORECASE)
  </read_first>
  <action>
    Reescreva `core/static/src/input.css` na ordem exata abaixo — a ordem é funcional,
    não estética:

    1. `@import "./dominio.css";` como **primeira declaração do arquivo**. Depois de
       `@tailwind base` o `postcss-import` descarta em silêncio (verificado); arquivo
       ausente faz o build falhar com exit 2, que é o comportamento desejado.
    2. `@tailwind base; @tailwind components; @tailwind utilities;`
    3. `@layer base { :focus-visible { outline: 2px solid theme("colors.brand"); outline-offset: 2px; } }`
    4. `@layer components { … }` com as 8 classes listadas em `<interfaces>`.
    5. **Fora de qualquer `@layer`**: o bloco `:root` e, depois dele, o bloco
       `[data-tema="escuro"]`, com os valores das duas tabelas de `<interfaces>`. Os dois
       seletores têm a mesma especificidade — a ordem de declaração é o que faz o escuro
       vencer, e quem não tem override no bloco escuro herda o claro.
    6. A regra `[x-cloak]` dentro de `@media (max-width: 767px)` que já existe hoje,
       com o comentário original preservado.

    Todo valor é **hex plano**. Nada de `color-mix()`, nada de `rgb(var(--x) / <alpha-value>)`:
    `getComputedStyle` não resolve função de cor em custom property, e o JS do dashboard
    e os testes de contraste leem essas variáveis por regex (Pitfall 7).

    Comentários do arquivo, em pt-BR, devem registrar: (a) que este arquivo é a fonte
    física dos valores e que `tailwind.config.js` só aponta para eles; (b) que a família
    de marca aqui é o **default** do template e que `COR_PRIMARIA` do `.env` a sobrescreve
    em runtime pelo `<style>` de `base.html` (plano 07-04); (c) que `--cor-secundaria` é o
    dourado institucional e é **forma e nunca texto** — 3,99:1 reprova AA de texto (D-86);
    (d) por que os blocos `:root`/`[data-tema]` ficam fora de `@layer` (o JIT poda regra de
    layer cujo seletor não aparece no conteúdo varrido).

    **Proibido** nos comentários: as palavras "PCA", "CFC", "Pantone", "Resolução",
    "541 C", "132 C", ou qualquer procedência institucional — `auditar_neutralidade()` roda
    IGNORECASE sobre a árvore gerada e reprova (D-93). Escreva "padrão de referência da
    família" quando precisar citar a origem.

    Crie `core/static/src/dominio.css` como stub: **zero regra CSS**, só um comentário de
    bloco em pt-BR que é o contrato dos tokens de estado do derivado — (1) todo estado tem
    o par `--cor-<estado>` (matiz: preenchimento, dot) e `--cor-<estado>-tx` (par de texto);
    no claro os dois podem coincidir, no escuro só a variante `-tx` ganha par clareado;
    (2) a ponte `data-*` → cor fica **fora** de `@layer`, no formato
    `.status-dot[data-estado="x"] { background: theme("colors.st-x"); }`, porque dentro de
    `@layer` o JIT poda; (3) piso de contraste 4,5:1 para texto e 3:1 para elemento
    gráfico, **nos dois temas**, e validação para daltonismo antes de fixar matizes.
    Nenhum token `st-*` concreto — nomes de estado são vocabulário de domínio (D-92).
  </action>
  <verify>
    <automated>head -1 core/static/src/input.css | grep -Fq '@import "./dominio.css";' && test -f core/static/src/dominio.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 core/static/src/input.css` é exatamente `@import "./dominio.css";`
    - `grep -c 'data-tema="escuro"' core/static/src/input.css` retorna ao menos 1
    - `grep -cE -- '--cor-(page|surface|surface-2|surface-3|ink|ink-2|muted|grid|baseline|destructive|danger-tint|warn-bg|warn-tx|secundaria|brand|brand-hover|brand-ink|brand-tint|seq-600|seq-450|seq-300):' core/static/src/input.css` retorna ao menos **39** — 21 no claro (14 neutros + 7 de marca) e **18** no escuro (11 neutros com override — `page`, `surface`, `surface-2`, `surface-3`, `ink`, `ink-2`, `muted`, `grid`, `danger-tint`, `warn-bg`, `warn-tx` — mais os 7 de marca; `baseline`, `destructive` e `secundaria` herdam do claro e **não** são redeclarados)
    - Os **14** valores de marca estão fixados um a um. No bloco `:root`: `--cor-brand: #1e40af`, `--cor-brand-hover: #3957b9`, `--cor-brand-ink: #193490`, `--cor-brand-tint: #edf0f9`, `--cor-seq-600: #1e40af`, `--cor-seq-450: #6b81ca`, `--cor-seq-300: #aab6e1`. No bloco `[data-tema="escuro"]`: `--cor-brand: #889feb`, `--cor-brand-hover: #b4c2f2`, `--cor-brand-ink: #5d7ce3`, `--cor-brand-tint: #192035`, `--cor-seq-600: #889feb`, `--cor-seq-450: #5873c9`, `--cor-seq-300: #435697`. Cada um conferível com `grep -F -- '<variável>: <hex>' core/static/src/input.css`, e o conjunto inteiro conferível de uma vez com `grep -cE -- '--cor-(brand|brand-hover|brand-ink|brand-tint|seq-600|seq-450|seq-300): #(1e40af|3957b9|193490|edf0f9|6b81ca|aab6e1|889feb|b4c2f2|5d7ce3|192035|5873c9|435697);' core/static/src/input.css` retornando 14
    - Nenhum dos hex do esqueleto superado aparece no arquivo: `grep -ciE '#(6379c6|a5b1e0|8ba4ec|bcc9f4|6b8ae5|1c2540|5878cf|3f5aa1)' core/static/src/input.css` retorna 0
    - `grep -cE 'color-mix|<alpha-value>' core/static/src/input.css` retorna 0
    - `grep -c 'x-cloak' core/static/src/input.css` retorna ao menos 1
    - `grep -ciE '(pca|cfc|pantone|resolução|541 c|132 c)' core/static/src/input.css core/static/src/dominio.css` retorna 0
    - `grep -cE '^\s*[.#a-z\[]' core/static/src/dominio.css` — o arquivo não declara nenhuma regra CSS (apenas comentário de bloco)
    - `grep -c 'st-' core/static/src/dominio.css` — nenhum nome de estado concreto; se aparecer, é só dentro do exemplo genérico `theme("colors.st-x")` do comentário
  </acceptance_criteria>
  <done>`input.css` declara os 21 tokens claros e os 18 overrides escuros em hex plano, com focus-ring e classes de componente; `dominio.css` existe como stub contratual.</done>
</task>

<task type="auto">
  <name>Task 2: tailwind.config.js verbatim + Dockerfile copiando o diretório de fonte</name>
  <files>tailwind.config.js, tailwind.config.js.jinja, Dockerfile, .template-tests/test_04_03_identity.py</files>
  <read_first>
    - `tailwind.config.js.jinja` (arquivo inteiro — `misturar()`, `const COR_PRIMARIA`, o bloco `colors` atual e os comentários que explicam a decisão D-17)
    - `Dockerfile` linhas 1-25 (estágio `assets`: `COPY tailwind.config.js`, `COPY core/static/src/input.css`, a guarda de 5000 bytes)
    - `copier.yml` (confirmar `_templates_suffix: .jinja` e que `tailwind.config.js` não aparece em `_exclude`)
    - `.template-tests/test_04_03_identity.py` linhas 66-84 (`test_tailwind_color_is_the_only_build_interpolation` — vai quebrar; é este teste que muda)
    - `.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md` §"Exemplos de Código" (`tailwind.config.js` sem Jinja) e §"Pitfall 3" (consequência para o Dockerfile)
  </read_first>
  <action>
    Crie `tailwind.config.js` (sem sufixo `.jinja`) com as chaves listadas em
    `<interfaces>` e **apague** `tailwind.config.js.jinja`. O arquivo passa a ser copiado
    verbatim pelo Copier: nenhuma interpolação, nenhuma `misturar()`, nenhum
    `const COR_PRIMARIA`. Abra com um comentário de cabeçalho dizendo que é arquivo do
    núcleo, que os VALORES vivem em `core/static/src/input.css` e que cores próprias do
    domínio vão em `core/static/src/dominio.css`.

    No `Dockerfile`, troque `COPY core/static/src/input.css ./core/static/src/input.css`
    por `COPY core/static/src ./core/static/src`. Sem isso o `@import "./dominio.css"` da
    Task 1 faz o build falhar com exit 2 no estágio `assets`. Atualize o comentário
    vizinho para registrar o motivo. `COPY tailwind.config.js ./` (linha 4) **não muda** —
    já usa o nome renderizado.

    Reescreva `test_tailwind_color_is_the_only_build_interpolation` em
    `.template-tests/test_04_03_identity.py`. O contrato **inverte**: o teste passa a
    provar o critério 4. Novo corpo, mantendo o `render(color=…)` das duas variantes:
    - `self.assertFalse((ROOT / "tailwind.config.js.jinja").exists())`
    - `self.assertTrue((ROOT / "tailwind.config.js").is_file())`
    - `tailwind.config.js` das variantes teal (`#0f766e`) e amber (`#d97706`) é **idêntico
      byte a byte** (`assertEqual`)
    - `tailwind.config.js` renderizado **não contém** `COR_PRIMARIA` nem nenhum hex
      (`assertNotRegex(conteudo, r"#[0-9a-fA-F]{6}")`)
    - `.env.example` das duas variantes **difere** e contém `COR_PRIMARIA=#0f766e` /
      `COR_PRIMARIA=#d97706` — provando que `cor_primaria` continua sendo pergunta do
      Copier, agora com um único ponto de interpolação
    - `config/settings/base.py` das duas variantes continua idêntico (asserção atual, manter)
    Renomeie o método para algo que descreva o novo contrato (ex.:
    `test_tailwind_config_e_verbatim_e_cor_so_entra_pelo_env`).
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .template-tests -p 'test_04_03*.py'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tailwind.config.js && test ! -e tailwind.config.js.jinja` sai com código 0
    - `grep -cE 'COR_PRIMARIA|misturar|\{\{' tailwind.config.js` retorna 0
    - `grep -c 'var(--cor-' tailwind.config.js` retorna ao menos 21
    - `grep -F 'darkMode: ["selector"' tailwind.config.js` casa e a linha contém `[data-tema="escuro"]`
    - `grep -c 'btn--destrutiva' tailwind.config.js` retorna ao menos 1 (safelist com 8 entradas)
    - `grep -F 'COPY core/static/src ./core/static/src' Dockerfile` casa
    - `python3 -m unittest discover -s .template-tests -p 'test_04_03*.py'` sai com código 0
    - `docker build --target assets -t sistema-base-assets-07 .` sai com código 0 e o log imprime `tailwind.css gerado:` com valor acima de 5000
  </acceptance_criteria>
  <done>O `tailwind.config.js` chega verbatim ao sistema gerado; o build de assets compila com o `@import`; o contrato de identidade prova que a cor só entra pelo `.env`.</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir as classes que a migração para var() mata + contrato de tokens</name>
  <files>apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_form_modal.html, apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_confirmar_exclusao_modal.html, apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/dashboard.html, apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/item_listar.html, .template-tests/test_07_tokens.py</files>
  <read_first>
    - `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_form_modal.html` (linha 5 — o véu; linha 135 — `shadow-xs`)
    - `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/_confirmar_exclusao_modal.html` (linhas 5 e 56)
    - `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/dashboard.html` (linha 17)
    - `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/templates/exemplo/item_listar.html` (linha 20)
    - `.planning/phases/07-herdar-o-design-system-do-pca/07-RESEARCH.md` §"Pitfall 1" (`bg-ink/40` some por completo — prova de compilação) e §"Pitfall 2" (`shadow-xs` não existe no v3)
    - `.template-tests/test_04_07_collectstatic.py` — modelo de suíte Python curta que lê a FONTE sem invocar o Copier
  </read_first>
  <action>
    Três correções mecânicas, todas com causa verificada por compilação real do Tailwind
    3.4.17:

    1. **Véu dos modais (D-94):** troque `bg-ink/40` por `bg-black/40` na linha 5 de
       `_form_modal.html` e de `_confirmar_exclusao_modal.html`. Com
       `ink: "var(--cor-ink)"` o Tailwind não gera regra nenhuma para `bg-ink/40` — o véu
       ficaria totalmente transparente. `black` é cor nativa e aceita o modificador.
    2. **`shadow-xs` (D-96):** as 4 ocorrências (`_form_modal.html:135`,
       `_confirmar_exclusao_modal.html:56`, `dashboard.html:17`, `item_listar.html:20`)
       viram `shadow-sm`. `shadow-xs` só existe no Tailwind v4 e hoje não gera regra.
    3. **`backdrop-blur-xs`:** aparece na linha 5 dos dois modais. Confirme se a chave
       existe no Tailwind 3.4.17 rodando o build de assets e inspecionando a saída
       (`docker build --target assets` já roda na Task 2, ou
       `npx --yes tailwindcss@3.4.17 -i core/static/src/input.css -o /tmp/t.css` seguido
       de `grep -c 'backdrop-blur-xs' /tmp/t.css`). Se não gerar regra, troque por
       `backdrop-blur-sm` e registre a constatação no SUMMARY. Se gerar, não mexa.

    Crie `.template-tests/test_07_tokens.py` como `unittest.TestCase` que lê a **FONTE**
    do template (nunca o CSS compilado — Pitfall 4: `dark:` compila para `:where(...)` e a
    forma varia). Asserções mínimas:
    - `input.css` começa com `@import "./dominio.css";` (primeira linha, exata)
    - `input.css` declara os **21** tokens do bloco `:root` (14 neutros + 7 de marca) e os
      **18** overrides de `[data-tema="escuro"]` (11 neutros + 7 de marca; `baseline`,
      `destructive` e `secundaria` herdam e não são redeclarados), todos casando
      `#[0-9a-fA-F]{6}` — nenhum `color-mix`, nenhum `<alpha-value>`. Os dois números são
      asserção exata (`assertEqual`), não piso
    - `tailwind.config.js` existe, `tailwind.config.js.jinja` não existe, e toda chave de
      `colors` do config tem uma variável correspondente declarada em `:root`
      (compare os conjuntos e falhe listando a diferença nos dois sentidos)
    - `fontSize` do config declara exatamente as chaves `xs sm base md lg xl` e **não**
      declara `2xl`; `borderRadius` declara `DEFAULT sm md lg xl 2xl` todas com `2px` e
      **não** declara `none` nem `full`
    - `safelist` contém as 8 entradas
    - Gate de opacidade sobre token migrado — nenhum resultado para
      `(bg|text|border|ring|from|to|via)-(page|surface|surface-2|surface-3|ink|ink-2|muted|grid|baseline|brand|brand-hover|brand-ink|brand-tint|destructive|danger-tint|warn-bg|warn-tx|secundaria|seq-600|seq-450|seq-300)/[0-9]+`
      varrendo `core/templates/**/*.html` e `apps/**/*.html`
    - Gate de classe morta — zero ocorrências de `shadow-xs` em `core/templates` e `apps`
    - Gate de disciplina do dourado (D-86) — zero ocorrências de `text-secundaria` em
      `core/templates` e `apps`; `secundaria` só pode aparecer em `bg-`/`border-`/`fill-`
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .template-tests -p 'test_07_tokens*.py'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'bg-ink/40' core/templates apps` retorna 0 em todos os arquivos
    - `grep -rc 'bg-black/40' apps | grep -v ':0'` mostra os dois modais
    - `grep -rn 'shadow-xs' core/templates apps` não retorna nada (exit 1)
    - `python3 -m unittest discover -s .template-tests -p 'test_07_tokens*.py'` sai com código 0
    - Prova negativa registrada no SUMMARY: reintroduzir `bg-ink/40` num dos modais faz `test_07_tokens.py` falhar; a mudança é revertida em seguida
    - `python3 -m py_compile .template-tests/*.py` sai com código 0
  </acceptance_criteria>
  <done>Nenhuma classe morta ou incompatível com `var()` sobra nos consumidores; o contrato de tokens roda sobre a fonte e falha ruidosamente se um token sumir do `input.css` ou do config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `core/static/src/*.css` → CSS servido a todo usuário | O arquivo é estático e versionado; nenhuma entrada de usuário chega até ele neste plano |
| template Copier → árvore gerada | `auditar_neutralidade()` proíbe 15 tokens de identidade na árvore renderizada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information disclosure | comentários de procedência em `input.css`/`tailwind.config.js` vazando identidade institucional para a árvore gerada | mitigate | Proibição explícita na Task 1 + gate `grep -ciE '(pca\|cfc\|pantone\|resolução)'` nos critérios de aceite + `auditar_neutralidade()` de `test_copier_copy.sh` roda sobre a árvore inteira com IGNORECASE |
| T-07-03 | Denial of service (visual) | `bg-ink/40` gerando véu transparente — conteúdo de fundo continua visível e clicável sob o modal | mitigate | `bg-black/40` nos dois modais + gate de modificador de opacidade sobre token migrado em `test_07_tokens.py` |
| T-07-04 | Denial of service (build) | `@import "./dominio.css"` sem o arquivo no contexto de build faz o estágio `assets` falhar | accept | Falha **ruidosa** é o comportamento desejado (exit 2 do `postcss-import`); mitigado no caminho feliz por `COPY core/static/src` no Dockerfile e pela guarda de 5000 bytes já existente |
| T-07-SC | Tampering | instalação de pacote npm/pip/cargo | n/a | Nenhum pacote novo. `tailwindcss@3.4.17` continua pinado em `Dockerfile:15` via `npx --yes`, sem mudança de versão; `07-RESEARCH.md` §"Auditoria de Legitimidade de Pacote" registra "não aplicável" |

Não há entrada de usuário, sessão, autenticação ou dado persistido neste plano. Categorias
ASVS V2/V3/V4/V6 não se aplicam; V5 é exercida apenas na saída estática, sem interpolação.
</threat_model>

<verification>
- `python3 -m unittest discover -s .template-tests -p 'test_04_*.py'` — exit 0
- `python3 -m unittest discover -s .template-tests -p 'test_07_*.py'` — exit 0
- `bash .template-tests/test_copier_copy.sh` — exit 0 (a auditoria de neutralidade roda aqui)
- `docker build --target assets .` — exit 0, CSS acima de 5000 bytes
</verification>

<success_criteria>
- `input.css` é a única fonte de valor de cor, em hex plano, com `@import` na primeira linha
- `tailwind.config.js` existe verbatim e `tailwind.config.js.jinja` não existe mais
- Todo `colors` do config aponta para uma variável declarada em `:root` (provado por teste)
- `borderRadius`, `fontSize` e `fontFamily` declarados sem nenhuma pergunta nova no `copier.yml`
- `dominio.css` existe como stub contratual, sem nenhum token `st-*` concreto
- Nenhum `bg-ink/40`, nenhum `shadow-xs`, nenhum `text-secundaria` nos templates
- Nenhuma menção a procedência institucional na árvore gerada
</success_criteria>

<output>
Create `.planning/phases/07-herdar-o-design-system-do-pca/07-02-SUMMARY.md` when done
</output>
