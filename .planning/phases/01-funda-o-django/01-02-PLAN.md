---
phase: 01-funda-o-django
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - core/__init__.py
  - core/apps.py
  - core/models.py
  - core/migrations/__init__.py
  - core/axes_lockout.py
  - core/middleware.py
  - core/context_processors.py
  - core/views.py
  - config/urls.py
  - core/templates/base.html
  - core/static/src/input.css
  - core/static/vendor/htmx.min.js
  - core/static/vendor/alpine.min.js
  - core/README.md
autonomous: true
requirements: [CORE-01, CFG-04]
must_haves:
  truths:
    - "O modelo Usuario usa login por e-mail — USERNAME_FIELD=email, sem campo username, UsuarioManager com use_in_migrations=True (D-02)"
    - "AUTH_USER_MODEL=core.Usuario está definido antes de qualquer migração, garantindo o usuário customizado desde a 0001 (D-03)"
    - "django-axes está configurado com lockout customizado via AXES_LOCKOUT_CALLABLE em core/axes_lockout.py (D-04)"
    - "CSRF_COOKIE_HTTPONLY=False está definido (Plan 01-01) e o script htmx:configRequest que lê o cookie csrftoken existe em base.html, pronto para qualquer POST HTMX futuro"
  artifacts:
    - path: "core/models.py"
      provides: "Usuario (AbstractUser) + UsuarioManager, USERNAME_FIELD=email"
      contains: "USERNAME_FIELD = \"email\""
  key_links:
    - from: "core/models.py"
      to: "config/settings/base.py"
      via: "AUTH_USER_MODEL = \"core.Usuario\""
      pattern: "AUTH_USER_MODEL"
---

<objective>
Construir o kernel do app `core`: `Usuario` customizado + `UsuarioManager` (login por e-mail, `USERNAME_FIELD="email"`), o callable de lockout do django-axes, o middleware HTMX-redirect, o context processor, a rota/`view` de `healthz`, e o template base (`base.html`) com o script `htmx:configRequest` que lê o token CSRF do cookie a cada requisição. Sem views de login/logout ainda — isso é o Plan 01-04.

Purpose: Segunda das três plans sequenciais que compõem o antigo Plan 01 (dividido por limite de arquivos por plan — checker feedback). Consome os settings do Plan 01-01 (`AUTH_USER_MODEL`, `MIDDLEWARE`, `AUTHENTICATION_BACKENDS`, `AXES_*`, `TEMPLATES.context_processors` já apontam para módulos deste plan) e produz o `Usuario`/kernel de segurança que o Plan 01-03 vai migrar e subir em produção real (Docker+Postgres), e que o Plan 01-04 vai consumir para login/logout.

Output: `core/` com `apps.py`, `models.py` (Usuario/UsuarioManager), `axes_lockout.py`, `middleware.py`, `context_processors.py`, `views.py` (só `healthz`), `config/urls.py` (admin + healthz), `core/templates/base.html` (com o script `htmx:configRequest`), assets vendorizados (htmx/Alpine) e `core/README.md` documentando as 3 convenções não-óbvias do kernel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-django/01-CONTEXT.md
@.planning/phases/01-funda-o-django/01-RESEARCH.md
@.planning/phases/01-funda-o-django/01-01-PLAN.md

IMPORTANTE — `/opt/web/pca` é a fonte de extração, SOMENTE LEITURA. Nunca escrever, editar ou rodar comandos destrutivos ali. Todo trabalho desta fase acontece em `/opt/sistema_base`.

Esta plan depende do Plan 01-01 já ter criado `config/settings/base.py` completo (`INSTALLED_APPS` já referencia `core.apps.CoreConfig`, `MIDDLEWARE` já referencia `core.middleware.HtmxRedirectMiddleware`, `AUTHENTICATION_BACKENDS`/`AXES_LOCKOUT_CALLABLE` já apontam para `core.axes_lockout.resposta_bloqueio`, `TEMPLATES.context_processors` já referencia `core.context_processors.usuario_atual`, `AUTH_USER_MODEL = "core.Usuario"`) — os módulos referenciados nessas strings são criados agora, nesta plan.

Todo código novo é neutro de domínio: usar "Sistema Base" como placeholder (vira variável Copier na Fase 4). Zero menção a "PCA", "CFC", "orçamento" ou qualquer termo de domínio no código, comentários ou templates gerados aqui.
</context>

<interfaces>
<!-- Contratos consumidos de config/settings/base.py (Plan 01-01) e produzidos aqui para o Plan 01-03 (migração) e Plan 01-04 (login/logout) consumirem. -->

De `config/settings/base.py` (Plan 01-01, já criado): `AUTH_USER_MODEL = "core.Usuario"`, `TEMPLATES[0]["DIRS"] = [BASE_DIR / "core" / "templates"]`, `STATICFILES_DIRS = [BASE_DIR / "core" / "static"]`, `LOGIN_URL = "/login/"` — esta plan cria os módulos que essas configurações referenciam.

Padrão `Usuario`/`UsuarioManager` (fonte: /opt/web/pca/core/models.py, verbatim, sem nada de domínio):
- `UsuarioManager(BaseUserManager)` com `use_in_migrations = True`, método privado `_create_user(email, password, **extra_fields)` que normaliza o e-mail (`self.normalize_email`) e chama `set_password`; `create_user` seta `is_staff=False, is_superuser=False`; `create_superuser` seta ambos `True` e valida que ambos ficaram `True`.
- `Usuario(AbstractUser)`: `username = None`; `email = models.EmailField("e-mail", unique=True)`; `USERNAME_FIELD = "email"`; `REQUIRED_FIELDS = []`; `objects = UsuarioManager()`; `__str__` retorna `self.email`.

Padrão axes (fonte: /opt/web/pca/core/axes_lockout.py, verbatim): função `resposta_bloqueio(request, original_response, credentials=None)` que só retorna `original_response` sem modificação — referenciada em `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"`.

Padrão middleware HTMX (fonte: /opt/web/pca/core/middleware.py, verbatim): classe `HtmxRedirectMiddleware` com `__init__(self, get_response)` e `__call__(self, request)` que, se `getattr(request, "htmx", False)` e `response.status_code in (301, 302)`, devolve `HttpResponseClientRedirect(response["Location"])` (import de `django_htmx.http`); senão devolve `response` normal.

Padrão context processor (fonte: /opt/web/pca/core/context_processors.py, verbatim): função `usuario_atual(request)` retornando `{"usuario_atual": getattr(request, "user", None)}`.

Padrão `core/apps.py` (fonte: /opt/web/pca/core/apps.py — usar SÓ a classe `CoreConfig`, NÃO replicar `PcaAdminConfig`/`default_site` nesta fase, isso é `CORE-03` da Fase 2): `class CoreConfig(AppConfig): default_auto_field = "django.db.models.BigAutoField"; name = "core"`.

`core/views.py` (produzido aqui, só `healthz`) e `core/urls.py` (ainda não criado — o Plan 01-04 cria `core/urls.py` e estende `core/views.py` com `login_view`/`logout_view`/`shell_view`); `config/urls.py` (criado aqui com admin+healthz) será estendido pelo Plan 01-04 com `include("core.urls")`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Core kernel — Usuario, axes, middleware, htmx CSRF, healthz</name>
  <files>core/__init__.py, core/apps.py, core/models.py, core/migrations/__init__.py, core/axes_lockout.py, core/middleware.py, core/context_processors.py, core/views.py, config/urls.py, core/templates/base.html, core/static/src/input.css, core/static/vendor/htmx.min.js, core/static/vendor/alpine.min.js, core/README.md</files>
  <read_first>
    /opt/web/pca/core/models.py
    /opt/web/pca/core/axes_lockout.py
    /opt/web/pca/core/middleware.py
    /opt/web/pca/core/context_processors.py
    /opt/web/pca/core/apps.py
    /opt/web/pca/core/views.py (só a função `healthz`, ignorar o resto — login/logout é o Plan 01-04)
    /opt/web/pca/config/urls.py
    /opt/web/pca/core/templates/base.html
    .planning/phases/01-funda-o-django/01-RESEARCH.md (seções "Pattern 2", "Pattern 4", "Pitfall 8", "Pitfall 9")
    config/settings/base.py (criado no Plan 01-01 — confirma `AUTH_USER_MODEL`, `MIDDLEWARE`, `AUTHENTICATION_BACKENDS`, `TEMPLATES.context_processors` que este Task cria os módulos correspondentes)
  </read_first>
  <action>
    Criar `core/apps.py` com `class CoreConfig(AppConfig): default_auto_field = "django.db.models.BigAutoField"; name = "core"` — NÃO criar `PcaAdminConfig`/`default_site` (isso é `CORE-03`, Fase 2; `django.contrib.admin` fica com o `AdminSite` padrão do Django nesta fase, cf. RESEARCH Assumption A5).

    Criar `core/models.py` com `UsuarioManager(BaseUserManager)` (`use_in_migrations = True`, `_create_user`/`create_user`/`create_superuser` exatamente como o padrão extraído — ver `<interfaces>`) e `Usuario(AbstractUser)` (`username = None`, `email = models.EmailField("e-mail", unique=True)`, `USERNAME_FIELD = "email"`, `REQUIRED_FIELDS = []`, `objects = UsuarioManager()`, `__str__` retorna `self.email`). Mensagens de erro em pt-BR ("O e-mail é obrigatório.", "Superuser precisa ter is_staff=True.", "Superuser precisa ter is_superuser=True.").

    Criar `core/axes_lockout.py` com `resposta_bloqueio(request, original_response, credentials=None)` retornando `original_response` sem modificação (verbatim do padrão extraído, com o comentário explicando por que existe).

    Criar `core/middleware.py` com `HtmxRedirectMiddleware` (verbatim do padrão extraído — converte 301/302 em `HttpResponseClientRedirect` quando `request.htmx` é truthy).

    Criar `core/context_processors.py` com `usuario_atual(request)` retornando `{"usuario_atual": getattr(request, "user", None)}`.

    Criar `core/views.py` com SÓ a view `healthz`: decorada com `@login_not_required` (import de `django.contrib.auth.decorators`), faz `SELECT 1` via `django.db.connection.cursor()`, retorna `JsonResponse({"status": "ok"})` em sucesso ou `JsonResponse({"status": "error"}, status=503)` se a query levantar exceção. NÃO criar `login_view`/`logout_view`/`shell_view` aqui — isso é o Plan 01-04, que vai estender este arquivo.

    Criar `config/urls.py` com `urlpatterns = [path("admin/", admin.site.urls), path("healthz", healthz, name="healthz")]` (import `from core.views import healthz`). NÃO incluir `core.urls` ainda (não existe até o Plan 01-04 criá-lo).

    Criar `core/templates/base.html`: HTML5 mínimo (`<!DOCTYPE html>`, `<html lang="pt-br">`), `<head>` carregando `{% load static %}`, `<link rel="stylesheet" href="{% static 'dist/tailwind.css' %}">`, `<script src="{% static 'vendor/htmx.min.js' %}" defer></script>`, `<script src="{% static 'vendor/alpine.min.js' %}" defer></script>`. No `<body>`, um `{% block content %}{% endblock %}` dentro de um wrapper com classes Tailwind reais (`min-h-screen bg-page flex flex-col items-center justify-center` — usar `page`/`ink` como nomes de cor genéricos do tema, sem replicar a paleta de marca da PCA). Incluir, ANTES do fechamento de `</body>`, o script `htmx:configRequest` EXATO do Pattern 4 do RESEARCH.md: função `csrfCookie(nome)` que lê `document.cookie` via regex, e `document.body.addEventListener("htmx:configRequest", (evento) => { evento.detail.headers["X-CSRFToken"] = csrfCookie("csrftoken"); })`. Comentário em pt-BR acima do script explicando por que não pode ser `hx-headers` estático (Anti-Pattern do RESEARCH — token rotaciona no login/logout).

    Criar `core/static/src/input.css` com só as 3 diretivas Tailwind: `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;` (sem variáveis CSS de paleta de marca — a identidade visual completa é `CORE-03`/Fase 2).

    Baixar e vendorizar `core/static/vendor/htmx.min.js` (htmx 1.9.12, via `https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js`) e `core/static/vendor/alpine.min.js` (Alpine 3.16.2, via `https://unpkg.com/alpinejs@3.16.2/dist/cdn.min.js`) — arquivos commitados no repo, NUNCA carregados via CDN em runtime (D-07/offline-first). Se o download falhar, documentar a URL exata usada no `core/README.md` para permitir novo download manual.

    Criar `core/README.md` documentando 3 convenções não-óbvias do kernel (uma seção curta por convenção, em pt-BR): (1) toda lógica de "hoje"/data atual DEVE usar `django.utils.timezone.localdate()`, nunca `timezone.now().date()` nem `datetime.date.today()` (Pitfall 8 — dentro do container o SO está em UTC mesmo com `TIME_ZONE="America/Sao_Paulo"` no settings); (2) `AXES_USERNAME_FORM_FIELD = "username"` é proposital mesmo com login por e-mail — não "corrigir" para `"email"` (Pitfall 3); (3) `AUTH_USER_MODEL` nunca muda depois da migração `0001` — troca depois é operação destrutiva (D-03).
  </action>
  <verify>
    <automated>grep -q 'USERNAME_FIELD = "email"' core/models.py && grep -q 'class HtmxRedirectMiddleware' core/middleware.py && grep -q 'def healthz' core/views.py && ! grep -q 'login_view' core/views.py && grep -q 'htmx:configRequest' core/templates/base.html && test -s core/static/vendor/htmx.min.js && test -s core/static/vendor/alpine.min.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `core/models.py` contém `USERNAME_FIELD = "email"` e `use_in_migrations = True`
    - `core/apps.py` contém `class CoreConfig(AppConfig)` e NÃO contém a string `PcaAdminConfig` nem `default_site`
    - `core/axes_lockout.py` define uma função `resposta_bloqueio` com assinatura `(request, original_response, credentials=None)`
    - `core/middleware.py` define `class HtmxRedirectMiddleware` com método `__call__`
    - `core/views.py` contém `@login_not_required` e `def healthz(request)`, e NÃO contém `login_view`, `logout_view` ou `shell_view` (essas ainda não existem)
    - `config/urls.py` contém `path("healthz", healthz, name="healthz")` e `path("admin/", admin.site.urls)`
    - `core/templates/base.html` contém a string literal `htmx:configRequest` e a string `X-CSRFToken`
    - `core/static/vendor/htmx.min.js` e `core/static/vendor/alpine.min.js` existem e cada um tem mais de 5000 bytes (arquivo real baixado, não stub vazio)
    - `core/README.md` contém as strings `localdate()` e `AXES_USERNAME_FORM_FIELD`
    - `grep -ril pca core/ config/urls.py` retorna vazio (sem menção a PCA/domínio)
  </acceptance_criteria>
  <done>core/ contém o kernel completo (model, axes, middleware, context processor, healthz, base.html com CSRF/htmx, assets vendorizados); nada de login/logout/shell ainda (Plan 01-04). O app `core` referenciado desde o Plan 01-01 (`INSTALLED_APPS`, `MIDDLEWARE`, `AUTHENTICATION_BACKENDS`, `TEMPLATES.context_processors`) agora existe de fato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Requisição HTTP → `core.middleware.HtmxRedirectMiddleware`/`core.views.healthz` | Primeira camada de código de aplicação (além do Django framework) a tocar cada requisição depois do `MIDDLEWARE` definido no Plan 01-01 |
| `core/axes_lockout.resposta_bloqueio` → resposta HTTP final | Ponto onde o `AxesMiddleware` decide se substitui ou preserva a resposta original da view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | Troca de `AUTH_USER_MODEL` depois da migração `0001` em fase futura | accept | Operação bloqueada pelo próprio Django (destrutiva); documentado em `core/README.md` como invariante — controle de processo, não de código |
| T-01-04 | Denial of Service | `AxesMiddleware` sobrescrevendo a resposta 200 correta por uma página genérica | mitigate | `core/axes_lockout.py` define `resposta_bloqueio` que devolve `original_response` sem modificação, referenciado por `AXES_LOCKOUT_CALLABLE` (Plan 01-01) — testado de ponta a ponta no Plan 01-04 |
</threat_model>

<verification>
`core/models.py` contém `USERNAME_FIELD = "email"` e `use_in_migrations = True`; `core/templates/base.html` contém `htmx:configRequest`/`X-CSRFToken`; assets vendorizados existem com conteúdo real (>5000 bytes cada); nenhum arquivo desta plan contém a string "PCA" (`grep -ril pca core/ config/urls.py`). O app `core` ainda não tem migração gerada nem roda em Docker real — isso é o Plan 01-03.
</verification>

<success_criteria>
- `core.Usuario` existe com `UsuarioManager` próprio e `USERNAME_FIELD="email"`, pronto para a migração `0001` (Plan 01-03)
- `CSRF_COOKIE_HTTPONLY=False` (Plan 01-01) e `base.html` já carrega o script `htmx:configRequest` (pronto para o Plan 01-04 usar)
- `AXES_LOCKOUT_CALLABLE` aponta para um callable real que preserva a resposta 200 da view
- Nenhuma referência a PCA/domínio em qualquer arquivo criado
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-django/01-02-SUMMARY.md` when done
</output>
