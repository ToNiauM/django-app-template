---
phase: 01-funda-o-django
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - Dockerfile
  - compose.yml
  - entrypoint.sh
  - tailwind.config.js
  - .dockerignore
  - core/migrations/0001_initial.py
autonomous: true
requirements: [INF-01, INF-02]
must_haves:
  truths:
    - "Operador roda `docker compose up -d` e o sistema sobe com PostgreSQL 17 e o serviço web saudável (healthz 200), sem passo manual além de preencher o `.env`"
    - "Operador roda `migrate` (via entrypoint) e a tabela do `Usuario` customizado existe, criada pela migração `0001` do app `core` (D-03)"
    - "O Dockerfile é multi-stage: node:20-alpine compila o Tailwind com guarda de tamanho do CSS, runtime python:3.12-slim (D-07)"
    - "O app roda com Gunicorn, WhiteNoise para estáticos, e publica porta apenas em 127.0.0.1 no host (D-08)"
    - "Operador roda `createsuperuser` (não-interativo) e o usuário fica persistido no PostgreSQL, autenticável por e-mail"
  artifacts:
    - path: "core/migrations/0001_initial.py"
      provides: "Migração inicial do core contendo só Usuario (sem HistoricalUsuario)"
      contains: "CreateModel"
    - path: "Dockerfile"
      provides: "Build multi-stage: node:20-alpine (tailwind) -> python:3.12-slim (runtime), collectstatic no build"
      contains: "FROM python:3.12-slim"
    - path: "compose.yml"
      provides: "Serviços web + db (postgres:17) com healthcheck e collation ICU pt-BR"
      contains: "postgres:17"
  key_links:
    - from: "entrypoint.sh"
      to: "core/migrations/0001_initial.py"
      via: "python manage.py migrate --noinput"
      pattern: "manage\\.py migrate"
    - from: "compose.yml (web)"
      to: "compose.yml (db)"
      via: "DATABASE_URL=postgres://...@db:5432/..."
      pattern: "depends_on"
---

<objective>
Construir a infraestrutura Docker (multi-stage Tailwind + Postgres 17), gerar a migração `0001` do app `core` (só `Usuario`, sem `HistoricalUsuario`) e provar, com uma sequência real de comandos, que `docker compose up -d` sobe app+banco saudáveis, `migrate` cria a tabela do `Usuario` e `createsuperuser` grava um usuário real e autenticável no PostgreSQL.

Purpose: Terceira e última das plans sequenciais que compõem o antigo Plan 01 (dividido por limite de arquivos por plan — checker feedback). Consome os settings do Plan 01-01 e o kernel `core` do Plan 01-02 (Usuario/UsuarioManager, axes, middleware, healthz) e prova o osso completo do Walking Skeleton — projeto real, banco real, container real — antes de o Plan 01-04 completar a fatia vertical de login/logout, e antes de a Fase 2 construir o shell visual em cima. Esta é a base sobre a qual TODO sistema derivado (Orçamento, Financeiro, Dívida Ativa) vai nascer via Copier na Fase 4; qualquer atalho aqui se multiplica.

Output: Repositório em `/opt/sistema_base` com `Dockerfile`, `compose.yml`, `entrypoint.sh` funcionais e `core/migrations/0001_initial.py` gerada. `docker compose up -d` sobe app+banco; `migrate` cria a tabela do `Usuario`; `createsuperuser` grava um usuário real no PostgreSQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-django/01-CONTEXT.md
@.planning/phases/01-funda-o-django/01-RESEARCH.md
@.planning/phases/01-funda-o-django/01-01-PLAN.md
@.planning/phases/01-funda-o-django/01-02-PLAN.md

IMPORTANTE — `/opt/web/pca` é a fonte de extração, SOMENTE LEITURA. Nunca escrever, editar ou rodar comandos destrutivos ali. Todo trabalho desta fase acontece em `/opt/sistema_base`.

Esta plan depende dos Plans 01-01 (settings/scaffold completo: `config/settings/{base,dev,prod}.py`, `requirements.txt`, `.env.example`) e 01-02 (kernel `core` completo: `core/models.py` com `Usuario`/`UsuarioManager`, `core/apps.py`, `core/axes_lockout.py`, `core/middleware.py`, `core/context_processors.py`, `core/views.py` com `healthz`, `config/urls.py` com admin+healthz, `core/templates/base.html`) já terem sido executados. Sem os dois, nem `makemigrations` nem `collectstatic` funcionam.

Todo código novo é neutro de domínio: usar "Sistema Base" como placeholder (vira variável Copier na Fase 4). Zero menção a "PCA", "CFC", "orçamento" ou qualquer termo de domínio no código, comentários ou infraestrutura gerados aqui.
</context>

<interfaces>
<!-- Contratos consumidos dos Plans 01-01 e 01-02, necessários para o build/migração/subida desta plan. -->

De `config/settings/base.py`/`prod.py` (Plan 01-01, já criado): `DATABASES = {"default": env.db("DATABASE_URL")}`, `STATIC_ROOT`, `STORAGES["staticfiles"]` (whitenoise com manifest em prod) — o `RUN collectstatic` do Dockerfile usa `DJANGO_SETTINGS_MODULE=config.settings.prod` com valores dummy de `SECRET_KEY`/`DATABASE_URL` só para o comando não falhar por falta de env.

De `core/models.py`/`core/apps.py` (Plan 01-02, já criado): `Usuario(AbstractUser)` com `USERNAME_FIELD="email"` e `CoreConfig` — `makemigrations core` (rodado nesta plan) gera `core/migrations/0001_initial.py` a partir desses módulos.

De `core/views.py` (Plan 01-02, já criado): `healthz` — usado pelo `healthcheck` do serviço `web` em `compose.yml` (`curl -fsS http://127.0.0.1:8000/healthz`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Docker infra + gerar migração 0001 + subir o sistema (walking skeleton)</name>
  <files>Dockerfile, compose.yml, entrypoint.sh, tailwind.config.js, .dockerignore, core/migrations/0001_initial.py</files>
  <read_first>
    /opt/web/pca/Dockerfile
    /opt/web/pca/compose.yml
    /opt/web/pca/entrypoint.sh
    /opt/web/pca/tailwind.config.js
    .planning/phases/01-funda-o-django/01-RESEARCH.md (seções "Pattern 5", "Pattern 6", "Pitfall 7", "Pitfall 10", "Assumption A3", "Assumption A4")
    config/settings/base.py (criado no Plan 01-01)
    core/models.py, core/apps.py (criados no Plan 01-02)
  </read_first>
  <action>
    Criar `tailwind.config.js`: `darkMode` não é necessário nesta fase (omitir ou deixar `false` — a paleta clara/escura é `CORE-03`/Fase 2); `content: ["./core/templates/**/*.html"]`; `theme: { extend: {} }`; `plugins: []`. NÃO replicar a paleta de marca/tokens de cor da PCA (fora de escopo desta fase).

    Criar `Dockerfile` multi-stage, generalizando o padrão extraído (SEM `COPY apps` — não existe `apps/` ainda nesta fase): estágio `assets` (`FROM node:20-alpine AS assets`, `WORKDIR /build`, copia `tailwind.config.js` e `core/static/src/input.css` e `core/templates`, roda `npx --yes tailwindcss@3.4.17 -i ./core/static/src/input.css -o ./core/static/dist/tailwind.css --minify`, guarda de tamanho: mede `CSS_BYTES` com `wc -c`, ecoa o valor, falha (`exit 1`) se `CSS_BYTES` for menor que `5000` — piso absoluto do Pitfall 10, nunca abaixo de ~5 KB de preflight puro). Estágio `runtime` (`FROM python:3.12-slim AS runtime`), `ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1`, `WORKDIR /app`, instala `curl` via apt (necessário para o healthcheck), copia `requirements.txt` e roda `pip install --no-cache-dir -r requirements.txt`, copia todo o restante do projeto (`COPY . .`), copia o CSS do estágio `assets`, roda `collectstatic --noinput` com `SECRET_KEY=build DATABASE_URL=sqlite:///tmp/build.db DJANGO_SETTINGS_MODULE=config.settings.prod` (valores dummy, nunca reais — só para o comando não falhar por falta de env), `chmod +x /app/entrypoint.sh`, `EXPOSE 8000`, `ENTRYPOINT ["/app/entrypoint.sh"]`.

    Criar `entrypoint.sh` (`#!/bin/sh`, `set -eu`, `python manage.py migrate --noinput`, `exec gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3`) — collectstatic NUNCA aqui (Pitfall 5, já rodou no build).

    Criar `compose.yml` com dois serviços apenas (SEM o serviço `backup` — isso é `INF-03`/Fase 4): `db` (`image: postgres:17`, `restart: unless-stopped`, env `POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD` do `.env`, `POSTGRES_INITDB_ARGS: "--locale-provider=icu --icu-locale=pt-BR --encoding=UTF8"` — decisão de collation que precisa estar certa desde o 1º `initdb`, Pitfall 7 — volume nomeado `pgdata` SEM `external: true` nesta fase — Assumption A4, garante que `docker compose up -d` funciona sozinho no primeiro clone —, `healthcheck` via `pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}` com `interval: 5s`, `timeout: 3s`, `retries: 10`, `start_period: 30s`); `web` (`build: {context: ., dockerfile: Dockerfile}`, `restart: unless-stopped`, `depends_on: {db: {condition: service_healthy}}`, `env_file: [.env]`, `ports: ["${WEB_BIND_ADDRESS}:${WEB_PORT}:8000"]` — D-08, nunca publicar sem `WEB_BIND_ADDRESS`, `healthcheck` via `curl -fsS http://127.0.0.1:8000/healthz` com `interval: 10s`, `timeout: 5s`, `retries: 10`, `start_period: 120s` — cobre o `migrate` rodando contra base vazia/recém-criada). Volume `pgdata:` nomeado, sem `external`.

    Criar `.dockerignore` cobrindo: `.git`, `.env`, `__pycache__/`, `*.pyc`, `.venv/`, `staticfiles/`, `core/static/dist/`, `.planning/`, `*.md` (exceto se algum README for necessário em runtime — não é o caso aqui).

    Rodar a sequência completa de verificação (todos os comandos abaixo, nesta ordem, a partir da raiz do repo):
    1. Criar um `.env` de trabalho local (não commitado, cf. `.gitignore`) copiando `.env.example` e preenchendo `SECRET_KEY` com um valor aleatório real (`python3 -c "import secrets; print(secrets.token_urlsafe(50))"` ou equivalente) e `POSTGRES_PASSWORD`/senha do superusuário com valores de desenvolvimento.
    2. `docker compose build web` — deve completar com `CSS_BYTES` acima de 5000 no log do estágio `assets` (se abaixo, investigar o glob de `content` do `tailwind.config.js` antes de prosseguir — não abaixar o piso).
    3. `docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py makemigrations core` — gera `core/migrations/0001_initial.py` no host (bind-mount só para este comando pontual; `compose.yml` continua sem volume de código para o serviço `web` em runtime). Conferir que a migração gerada contém `CreateModel` para `Usuario` e usa `core.models.UsuarioManager` como manager — NÃO deve conter `HistoricalUsuario` (isso só entra numa migração da Fase 2).
    4. `docker compose up -d` — sobe `db` e `web`; aguardar os dois ficarem `healthy` (`docker compose ps`).
    5. `curl -fsS http://127.0.0.1:8000/healthz` (ou o valor de `WEB_BIND_ADDRESS`/`WEB_PORT` do `.env` usado) — deve responder `{"status": "ok"}`.
    6. `docker compose exec -T db psql -U sistema_base -d sistema_base -c "\d core_usuario"` — confirma que a tabela existe com a coluna `email` `unique`.
    7. `docker compose exec -T -e DJANGO_SUPERUSER_EMAIL=admin@sistemabase.local -e DJANGO_SUPERUSER_PASSWORD=troque-esta-senha web python manage.py createsuperuser --noinput` — cria o primeiro usuário real, gravado no PostgreSQL.
    8. `docker compose exec -T web python manage.py shell -c "from core.models import Usuario; u = Usuario.objects.get(email='admin@sistemabase.local'); print(u.is_superuser, u.check_password('troque-esta-senha'))"` — deve imprimir `True True`, provando a escrita (Passo 7) e a leitura (Passo 8) reais no banco.
  </action>
  <verify>
    <automated>docker compose build web && docker compose up -d && sleep 5 && curl -fsS http://127.0.0.1:8000/healthz</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose build web` exit code 0
    - Log do build contém `tailwind.css gerado:` seguido de um número de bytes maior que 5000
    - `core/migrations/0001_initial.py` existe no host após o passo 3, contém `migrations.CreateModel` com `name='Usuario'` e NÃO contém a string `HistoricalUsuario`
    - `docker compose up -d` seguido de `docker compose ps` mostra os serviços `db` e `web` com status `healthy` (aguardando até 120s de `start_period`)
    - `curl -fsS http://127.0.0.1:8000/healthz` retorna corpo `{"status": "ok"}` com HTTP 200
    - `docker compose exec -T db psql -U sistema_base -d sistema_base -c "\d core_usuario"` lista a coluna `email` com constraint `unique`
    - O comando do passo 8 imprime `True True`
    - `Dockerfile` contém `ENTRYPOINT ["/app/entrypoint.sh"]` e NÃO contém `COPY apps`
    - `compose.yml` contém `POSTGRES_INITDB_ARGS` com `icu-locale=pt-BR` e NÃO contém um serviço chamado `backup`
    - `compose.yml` (serviço `db`) NÃO tem chave `ports:` (banco nunca exposto ao host)
  </acceptance_criteria>
  <done>`docker compose up -d` sobe app+PostgreSQL 17 saudáveis a partir de um clone limpo (só preenchendo `.env`); `migrate` (via entrypoint) já aplicou a migração `0001` do `core`; um superusuário real foi criado e autenticado com sucesso via ORM, provando leitura e escrita reais no banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internet → `web` (porta publicada) | Único ponto de entrada externo antes do proxy (Fase 4); hoje restrito a `WEB_BIND_ADDRESS` |
| `web` → `db` | Rede interna do Docker Compose; `db` nunca publica porta ao host |
| Build da imagem (`docker compose build`) → imagem final | `SECRET_KEY=build`/`DATABASE_URL=sqlite://...` são valores dummy que não podem vazar para o runtime real |
| Instalação de `tailwindcss` via `npx` no estágio `assets` | Superfície de supply-chain do build (só dentro do container, nunca no host) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04b | Tampering | Valores dummy de build (`SECRET_KEY=build`) vazando para runtime | accept | Usados só como `ENV` inline no `RUN collectstatic` do Dockerfile, nunca gravados em arquivo da imagem nem usados fora desse comando; risco residual nulo (valor "build" não é segredo real e não é usado em nenhum settings de produção real) |
| T-01-05 | Denial of Service / Information Disclosure | PostgreSQL exposto diretamente à rede | mitigate | `compose.yml` (serviço `db`) não declara `ports:` — só acessível pela rede interna do Compose, nunca do host |
| T-01-06 | Information Disclosure | `web` exposto direto na internet sem proxy/TLS | mitigate | `ports: ["${WEB_BIND_ADDRESS}:${WEB_PORT}:8000"]` com `WEB_BIND_ADDRESS=127.0.0.1` no `.env.example` — D-08; proxy do host (Fase 4) é quem expõe publicamente |
| T-01-SCb | Tampering | Pacotes maliciosos/typosquat via `npx` no estágio `assets` | mitigate | `npx tailwindcss@3.4.17` com versão pinada explícita (D-07), pacote já auditado no `01-RESEARCH.md`; nenhuma outra dependência npm introduzida |
</threat_model>

<verification>
`docker compose up -d` (a partir de um `.env` preenchido a partir de `.env.example`) sobe `db`+`web` saudáveis; `docker compose exec web python manage.py migrate --check` retorna sem migrações pendentes; um usuário criado via `createsuperuser --noinput` é autenticável via ORM (`check_password` retorna `True`). Nenhum arquivo do repositório contém a string "PCA" (`grep -ril pca . --exclude-dir=.git --exclude-dir=.planning`).
</verification>

<success_criteria>
- `docker compose up -d` funciona a partir de um clone limpo, só preenchendo `.env` (sem passo manual extra, sem `docker volume create`)
- `core.Usuario` existe desde a migração `0001` do app `core`, com `UsuarioManager` próprio e `USERNAME_FIELD="email"`
- Um superusuário criado via `createsuperuser --noinput` é gravado no PostgreSQL e autenticável via ORM
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-django/01-03-SUMMARY.md` when done
</output>
