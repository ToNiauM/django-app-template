---
phase: 01-funda-o-django
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manage.py
  - config/__init__.py
  - config/wsgi.py
  - config/settings/__init__.py
  - config/settings/base.py
  - config/settings/dev.py
  - config/settings/prod.py
  - requirements.txt
  - .env.example
  - .gitignore
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
must_haves:
  truths:
    - "Toda configuração sensível (SECRET_KEY, DATABASE_URL, senhas) vem do `.env`; `.env.example` cobre todas as variáveis usadas"
    - "Settings de produção aplicam Argon2 no topo dos hashers, django-axes, cookies Secure/SameSite=Lax, HSTS/SECURE_PROXY_SSL_HEADER, DEBUG=False e ALLOWED_HOSTS restrito"
  artifacts:
    - path: "config/settings/base.py"
      provides: "Settings comuns: INSTALLED_APPS, MIDDLEWARE, AUTH_USER_MODEL, PASSWORD_HASHERS, AUTHENTICATION_BACKENDS, AXES_*, localização, STORAGES, CSRF cookie flags"
      contains: "AUTH_USER_MODEL = \"core.Usuario\""
    - path: "config/settings/prod.py"
      provides: "DEBUG=False, ALLOWED_HOSTS restrito, SECURE_PROXY_SSL_HEADER, SECURE_SSL_REDIRECT, SECURE_HSTS_SECONDS, AXES_IPWARE_*"
      contains: "DEBUG = False"
    - path: ".env.example"
      provides: "Todas as variáveis necessárias documentadas"
      contains: "DATABASE_URL="
  key_links:
    - from: "config/settings/base.py"
      to: ".env"
      via: "django-environ (environ.Env.read_env)"
      pattern: "environ\\.Env\\(\\)"
---

<objective>
Levantar o scaffold executável do Django 5.2 "plano" (D-01, sem Copier ainda): `manage.py`, `config/wsgi.py` e settings por ambiente (`base.py`/`dev.py`/`prod.py`) via `django-environ`, aplicando desde já as invariantes de segurança e localização da PCA (Argon2, django-axes referenciado, cookies seguros, HSTS/proxy, `DEBUG=False` em produção, pt-br/America-Sao_Paulo). `requirements.txt` fixa as 9 dependências desta fase e `.env.example` cobre 100% das variáveis usadas.

Purpose: Esta é a primeira das três plans sequenciais que compõem o antigo Plan 01 (dividido por limite de arquivos por plan — checker feedback). O `core` (Usuario, axes callable, middleware, kernel HTMX) do Plan 01-02 e a infraestrutura Docker do Plan 01-03 dependem de `AUTH_USER_MODEL`, `MIDDLEWARE`, `AUTHENTICATION_BACKENDS` e `AXES_*` já estarem corretos aqui.

Output: `manage.py`, `config/settings/{base,dev,prod}.py`, `requirements.txt`, `.env.example`, `.gitignore` funcionais. Django ainda não roda de ponta a ponta nesta plan (falta o app `core` — Plan 01-02 — e a infra Docker — Plan 01-03); o objetivo aqui é só a fundação de configuração, auditável isoladamente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-django/01-CONTEXT.md
@.planning/phases/01-funda-o-django/01-RESEARCH.md

IMPORTANTE — `/opt/web/pca` é a fonte de extração, SOMENTE LEITURA. Nunca escrever, editar ou rodar comandos destrutivos ali. Todo trabalho desta fase acontece em `/opt/sistema_base`.

Todo código novo é neutro de domínio: usar "Sistema Base" como placeholder (vira variável Copier na Fase 4). Zero menção a "PCA", "CFC", "orçamento" ou qualquer termo de domínio no código, comentários ou `.env.example` gerados aqui.
</context>

<interfaces>
<!-- Contratos que os Plans 01-02, 01-03 e 01-04 (waves seguintes) vão consumir. Extraídos de /opt/web/pca, generalizados. -->

Padrão de settings por ambiente (fonte: /opt/web/pca/config/settings/base.py, dev.py, prod.py — lidos integralmente na pesquisa desta fase):
- `base.py`: `env = environ.Env()`; `environ.Env.read_env(BASE_DIR / ".env")`; `SECRET_KEY = env("SECRET_KEY")`; `DATABASES = {"default": env.db("DATABASE_URL")}`.
- `dev.py`: `from .base import *` + `DEBUG = env.bool("DEBUG", default=True)` + `ALLOWED_HOSTS = ["*"]` + `STORAGES` sobrescrito para `django.contrib.staticfiles.storage.StaticFilesStorage` (sem manifest) + `WHITENOISE_USE_FINDERS = True`.
- `prod.py`: `from .base import *` + `DEBUG = False` + `ALLOWED_HOSTS = list(set(env.list("ALLOWED_HOSTS", default=[]) + ["127.0.0.1"]))` + `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")` + `SECURE_SSL_REDIRECT = env.bool(...)` + `SECURE_REDIRECT_EXEMPT = [r"^healthz$"]` + `USE_X_FORWARDED_HOST = False` + `CSRF_TRUSTED_ORIGINS = env.list(...)` + `SECURE_HSTS_SECONDS = env.int(...)` + `SECURE_HSTS_INCLUDE_SUBDOMAINS = False` + `SECURE_HSTS_PRELOAD = False` + `SILENCED_SYSTEM_CHECKS = ["security.W005", "security.W021"]` + `AXES_IPWARE_PROXY_COUNT = 2` + `AXES_IPWARE_META_PRECEDENCE_ORDER = ("HTTP_X_FORWARDED_FOR", "REMOTE_ADDR")`.

Downstream (Plan 01-02) vai precisar que `base.py` já declare, mesmo antes do app `core` existir: `INSTALLED_APPS` com `core.apps.CoreConfig` e `core` na lista, `AUTH_USER_MODEL = "core.Usuario"`, `MIDDLEWARE` com `core.middleware.HtmxRedirectMiddleware`, `AUTHENTICATION_BACKENDS` com `axes.backends.AxesStandaloneBackend`, `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"`, `TEMPLATES.context_processors` com `core.context_processors.usuario_atual` — todas essas strings apontam para módulos que só passam a existir no Plan 01-02; isso é esperado (Django não importa essas classes até o `runserver`/`manage.py` real rodar, o que só acontece no Plan 01-03).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Django + settings por ambiente</name>
  <files>manage.py, config/__init__.py, config/wsgi.py, config/settings/__init__.py, config/settings/base.py, config/settings/dev.py, config/settings/prod.py, requirements.txt, .env.example, .gitignore</files>
  <read_first>
    /opt/web/pca/config/settings/base.py
    /opt/web/pca/config/settings/dev.py
    /opt/web/pca/config/settings/prod.py
    /opt/web/pca/manage.py
    /opt/web/pca/config/wsgi.py
    /opt/web/pca/requirements.txt
    .planning/phases/01-funda-o-django/01-RESEARCH.md (seções "Pattern 1", "Pattern 3", "Pattern 4", "Standard Stack", "Code Examples > .env.example completo")
  </read_first>
  <action>
    Criar `manage.py` e `config/wsgi.py` idênticos ao padrão da PCA, trocando o default de `DJANGO_SETTINGS_MODULE` para `"config.settings.dev"` (já é esse o valor na PCA — manter).

    Criar `config/settings/base.py` com: `BASE_DIR = Path(__file__).resolve().parent.parent.parent`; `env = environ.Env()` + `environ.Env.read_env(BASE_DIR / ".env")`; `SECRET_KEY = env("SECRET_KEY")`.
    `INSTALLED_APPS` (nesta ordem): `whitenoise.runserver_nostatic`, `core.apps.CoreConfig`, `django.contrib.auth`, `django.contrib.contenttypes`, `django.contrib.sessions`, `django.contrib.messages`, `django.contrib.staticfiles`, `django.contrib.admin`, `django_htmx`, `axes`, `core`. NÃO incluir `django.contrib.postgres` (a PCA usa por causa de campos ArrayField de domínio — não aplicável aqui), NÃO incluir `simple_history` (CORE-06 é Fase 2).
    `MIDDLEWARE` (nesta ordem exata — a ordem é normativa, cf. RESEARCH "ponto crítico desta fase"): `django.middleware.security.SecurityMiddleware`, `django.middleware.clickjacking.XFrameOptionsMiddleware`, `django.contrib.sessions.middleware.SessionMiddleware`, `whitenoise.middleware.WhiteNoiseMiddleware`, `django.middleware.common.CommonMiddleware`, `django.middleware.csrf.CsrfViewMiddleware`, `django.contrib.auth.middleware.AuthenticationMiddleware`, `django_htmx.middleware.HtmxMiddleware`, `core.middleware.HtmxRedirectMiddleware`, `axes.middleware.AxesMiddleware`, `django.contrib.messages.middleware.MessageMiddleware`, `django.contrib.auth.middleware.LoginRequiredMiddleware`. (Sem a linha `simple_history.middleware.HistoryRequestMiddleware` da PCA — Fase 2.)
    `AUTH_USER_MODEL = "core.Usuario"`.
    `PASSWORD_HASHERS = ["django.contrib.auth.hashers.Argon2PasswordHasher", "django.contrib.auth.hashers.PBKDF2PasswordHasher"]`.
    `AUTHENTICATION_BACKENDS = ["axes.backends.AxesStandaloneBackend", "django.contrib.auth.backends.ModelBackend"]`.
    `AXES_FAILURE_LIMIT = 5`; `AXES_COOLOFF_TIME = timedelta(minutes=env.int("AXES_COOLOFF_MINUTES", default=15))`; `AXES_LOCKOUT_PARAMETERS = [["username", "ip_address"]]`; `AXES_USERNAME_FORM_FIELD = "username"` (com o comentário do RESEARCH Pitfall 3 explicando o motivo — Django sempre usa o kwarg literal `username=` em `authenticate()`); `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"` (com o comentário do Pitfall 4).
    `LOGIN_URL = "/login/"`.
    `SESSION_COOKIE_AGE = env.int("SESSION_COOKIE_AGE", default=28800)`; `SESSION_EXPIRE_AT_BROWSER_CLOSE = True`; `SESSION_SAVE_EVERY_REQUEST = True`.
    `ROOT_URLCONF = "config.urls"`; `WSGI_APPLICATION = "config.wsgi.application"`.
    `TEMPLATES`: `DIRS = [BASE_DIR / "core" / "templates"]`, `APP_DIRS = True`, `context_processors`: `django.template.context_processors.request`, `django.contrib.auth.context_processors.auth`, `django.contrib.messages.context_processors.messages`, `core.context_processors.usuario_atual`.
    `DATABASES = {"default": env.db("DATABASE_URL")}`.
    `AUTH_PASSWORD_VALIDATORS`: os 4 validadores padrão do Django (UserAttributeSimilarityValidator, MinimumLengthValidator, CommonPasswordValidator, NumericPasswordValidator).
    `LANGUAGE_CODE = "pt-br"`; `TIME_ZONE = "America/Sao_Paulo"`; `USE_I18N = True`; `USE_TZ = True`.
    `STATIC_URL = "/static/"`; `STATICFILES_DIRS = [BASE_DIR / "core" / "static"]`; `STATIC_ROOT = BASE_DIR / "staticfiles"`.
    `STORAGES = {"default": {"BACKEND": "django.core.files.storage.FileSystemStorage"}, "staticfiles": {"BACKEND": "whitenoise.storage.CompressedManifestStaticFilesStorage"}}` (nunca `STATICFILES_STORAGE` — removido no Django 5.1, Pitfall 5).
    `DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"`.
    `SESSION_COOKIE_SECURE = True`; `SESSION_COOKIE_SAMESITE = "Lax"`; `CSRF_COOKIE_HTTPONLY = False` (com o comentário: htmx lê o cookie, `True` quebraria toda escrita HTMX — CFG-04); `CSRF_COOKIE_SECURE = True`; `CSRF_COOKIE_SAMESITE = "Lax"`; `CSRF_USE_SESSIONS = False`.

    Criar `config/settings/dev.py`: `from .base import *`; `DEBUG = env.bool("DEBUG", default=True)`; `ALLOWED_HOSTS = ["*"]`; sobrescrever `STORAGES["staticfiles"]` para `django.contrib.staticfiles.storage.StaticFilesStorage` (sem manifest — evita `ValueError: Missing staticfiles manifest entry` sem `collectstatic` prévio); `WHITENOISE_USE_FINDERS = True` (com o comentário do motivo, cf. `/opt/web/pca/config/settings/dev.py`).

    Criar `config/settings/prod.py`: `from .base import *`; `DEBUG = False`; `ALLOWED_HOSTS = list(set(env.list("ALLOWED_HOSTS", default=[]) + ["127.0.0.1"]))` (127.0.0.1 sempre presente — é quem o `healthz` interno do compose usa); `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")`; `SECURE_SSL_REDIRECT = env.bool("SECURE_SSL_REDIRECT", default=True)`; `SECURE_REDIRECT_EXEMPT = [r"^healthz$"]` (Pitfall 6 — sem isto o healthcheck do compose entra em loop de redirect); `USE_X_FORWARDED_HOST = False`; `CSRF_TRUSTED_ORIGINS = env.list("CSRF_TRUSTED_ORIGINS", default=[])`; `SECURE_HSTS_SECONDS = env.int("SECURE_HSTS_SECONDS", default=3600)`; `SECURE_HSTS_INCLUDE_SUBDOMAINS = False`; `SECURE_HSTS_PRELOAD = False`; `SILENCED_SYSTEM_CHECKS = ["security.W005", "security.W021"]`; `AXES_IPWARE_PROXY_COUNT = 2`; `AXES_IPWARE_META_PRECEDENCE_ORDER = ("HTTP_X_FORWARDED_FOR", "REMOTE_ADDR")`.

    Criar `requirements.txt` com exatamente estas 9 linhas fixadas (versões do RESEARCH.md Standard Stack, todas `[VERIFIED: PyPI]` e auditadas via slopcheck `[OK]`): `Django==5.2.17`, `psycopg[binary]==3.3.4`, `django-environ==0.14.0`, `django-axes==8.3.1`, `django-htmx==1.29.0`, `argon2-cffi==25.1.0`, `whitenoise==6.12.0`, `gunicorn==26.0.0`, `django-ipware==7.0.1`. NÃO incluir `django-simple-history`, `openpyxl`, `freezegun` (fora do escopo desta fase, cf. RESEARCH "Installation").

    Criar `.env.example` com exatamente as variáveis do RESEARCH "Code Examples > .env.example completo para esta fase": `SECRET_KEY`, `DEBUG`, `DJANGO_SETTINGS_MODULE=config.settings.dev`, `ALLOWED_HOSTS=localhost,127.0.0.1`, `CSRF_TRUSTED_ORIGINS=http://localhost:8000`, `SESSION_COOKIE_AGE=28800`, `AXES_COOLOFF_MINUTES=15`, `WEB_BIND_ADDRESS=127.0.0.1`, `WEB_PORT=8000`, `SECURE_SSL_REDIRECT=true`, `SECURE_HSTS_SECONDS=3600`, `POSTGRES_DB=sistema_base`, `POSTGRES_USER=sistema_base`, `POSTGRES_PASSWORD=replace-with-a-database-password`, `DATABASE_URL=postgres://sistema_base:replace-with-a-database-password@db:5432/sistema_base`. Comentar cada bloco em pt-BR explicando o propósito (estilo da casa). Usar "Sistema Base" (nunca "PCA") em qualquer comentário.

    Criar `.gitignore` cobrindo: `.env`, `__pycache__/`, `*.pyc`, `/staticfiles/`, `core/static/dist/`, `*.sqlite3`, `.venv/`, `*.log`, `.DS_Store`.
  </action>
  <verify>
    <automated>grep -q 'AUTH_USER_MODEL = "core.Usuario"' config/settings/base.py && grep -q 'CSRF_COOKIE_HTTPONLY = False' config/settings/base.py && grep -q 'DEBUG = False' config/settings/prod.py && test "$(wc -l < requirements.txt)" -eq 9 && grep -q 'DATABASE_URL=' .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `manage.py` contém `os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings.dev")`
    - `config/settings/base.py` contém a string exata `AUTH_USER_MODEL = "core.Usuario"`
    - `config/settings/base.py` contém `CSRF_COOKIE_HTTPONLY = False`
    - `config/settings/base.py` contém `AXES_USERNAME_FORM_FIELD = "username"`
    - `config/settings/base.py` contém `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"`
    - `config/settings/base.py` contém `"django.contrib.auth.hashers.Argon2PasswordHasher"` como PRIMEIRO item de `PASSWORD_HASHERS`
    - `config/settings/base.py` contém `LANGUAGE_CODE = "pt-br"` e `TIME_ZONE = "America/Sao_Paulo"`
    - `config/settings/base.py` NÃO contém a string `STATICFILES_STORAGE` (grep retorna 0 ocorrências)
    - `config/settings/prod.py` contém `DEBUG = False` e `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")`
    - `config/settings/prod.py` contém `SECURE_REDIRECT_EXEMPT`
    - `requirements.txt` contém exatamente 9 linhas, nenhuma menciona `simple-history`, `openpyxl` ou `freezegun`
    - `.env.example` contém `DATABASE_URL=` e `SECRET_KEY=`
    - `.gitignore` contém `.env` como linha própria
    - Nenhum arquivo criado nesta task contém a string `PCA` (case-insensitive) — `grep -ril pca manage.py config/ requirements.txt .env.example .gitignore` retorna vazio
  </acceptance_criteria>
  <done>Todos os arquivos de settings/scaffold existem com o conteúdo acima; nenhuma referência a PCA/domínio; requirements.txt fixa as 9 dependências corretas. `config/settings/base.py` já referencia `core.apps.CoreConfig`, `core.middleware.HtmxRedirectMiddleware`, `core.axes_lockout.resposta_bloqueio` e `core.context_processors.usuario_atual` — módulos que o Plan 01-02 cria em seguida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `.env` (host) → `config/settings/*.py` (processo Django) | Segredos (SECRET_KEY, senha do Postgres) cruzam do arquivo não versionado para o processo via `django-environ` |
| Instalação de dependências (`pip install -r requirements.txt`) | Superfície de supply-chain do build da imagem (Plan 01-03 usa este arquivo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` versionado por engano | mitigate | `.gitignore` exclui `.env`; só `.env.example` (sem segredo real) fica no git — verificado por `git status --ignored` não listar `.env` como tracked |
| T-01-02 | Tampering | `ALLOWED_HOSTS`/`Host` header forjado em produção | mitigate | `prod.py` restringe `ALLOWED_HOSTS` ao que vier do `.env` + `127.0.0.1` fixo; `DisallowedHost` rejeita o resto (Django default) |
| T-01-SC | Tampering | Pacotes Python maliciosos/typosquat via `pip` | mitigate | `requirements.txt` fixa 9 pacotes já auditados no `01-RESEARCH.md` (`slopcheck` 9/9 `[OK]`, nenhum `[SLOP]`/`[SUS]`); nenhuma dependência nova introduzida sem passar pelo Package Legitimacy Audit |
</threat_model>

<verification>
`config/settings/base.py`/`dev.py`/`prod.py` contêm todas as chaves auditadas em `acceptance_criteria`; nenhum arquivo desta plan contém a string "PCA" (`grep -ril pca manage.py config/ requirements.txt .env.example .gitignore`). Django ainda não sobe de ponta a ponta nesta plan (falta `core/` — Plan 01-02 — e infraestrutura Docker — Plan 01-03); a verificação de execução completa acontece no Plan 01-03.
</verification>

<success_criteria>
- Toda configuração sensível vem do `.env`; `.env.example` cobre 100% das variáveis usadas em `base.py`/`dev.py`/`prod.py`
- `prod.py` aplica Argon2 no topo, django-axes, cookies seguros, HSTS/proxy, `DEBUG=False`, `ALLOWED_HOSTS` restrito
- `requirements.txt` fixa exatamente as 9 dependências corretas desta fase
- Nenhuma referência a PCA/domínio em qualquer arquivo criado
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-django/01-01-SUMMARY.md` when done
</output>
