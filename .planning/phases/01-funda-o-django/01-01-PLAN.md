---
phase: 01-funda-o-django
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manage.py
  - config/__init__.py
  - config/wsgi.py
  - config/settings/__init__.py
  - config/settings/base.py
  - config/settings/dev.py
  - config/settings/prod.py
  - requirements.txt
  - .env.example
  - .gitignore
  - core/__init__.py
  - core/apps.py
  - core/models.py
  - core/migrations/__init__.py
  - core/axes_lockout.py
  - core/middleware.py
  - core/context_processors.py
  - core/views.py
  - config/urls.py
  - core/templates/base.html
  - core/static/src/input.css
  - core/static/vendor/htmx.min.js
  - core/static/vendor/alpine.min.js
  - core/README.md
  - Dockerfile
  - compose.yml
  - entrypoint.sh
  - tailwind.config.js
  - .dockerignore
  - core/migrations/0001_initial.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CORE-01, INF-01, INF-02]
must_haves:
  truths:
    - "Operador roda `docker compose up -d` e o sistema sobe com PostgreSQL 17 e o serviço web saudável (healthz 200), sem passo manual além de preencher o `.env`"
    - "Operador roda `migrate` (via entrypoint) e a tabela do `Usuario` customizado existe, criada pela migração `0001` do app `core`"
    - "Operador roda `createsuperuser` (não-interativo) e o usuário fica persistido no PostgreSQL, autenticável por e-mail"
    - "Toda configuração sensível (SECRET_KEY, DATABASE_URL, senhas) vem do `.env`; `.env.example` cobre todas as variáveis usadas"
    - "Settings de produção aplicam Argon2 no topo dos hashers, django-axes, cookies Secure/SameSite=Lax, HSTS/SECURE_PROXY_SSL_HEADER, DEBUG=False e ALLOWED_HOSTS restrito"
    - "CSRF_COOKIE_HTTPONLY=False está definido e o script htmx:configRequest que lê o cookie csrftoken existe em base.html, pronto para qualquer POST HTMX futuro"
  artifacts:
    - path: "config/settings/base.py"
      provides: "Settings comuns: INSTALLED_APPS, MIDDLEWARE, AUTH_USER_MODEL, PASSWORD_HASHERS, AUTHENTICATION_BACKENDS, AXES_*, localização, STORAGES, CSRF cookie flags"
      contains: "AUTH_USER_MODEL = \"core.Usuario\""
    - path: "config/settings/prod.py"
      provides: "DEBUG=False, ALLOWED_HOSTS restrito, SECURE_PROXY_SSL_HEADER, SECURE_SSL_REDIRECT, SECURE_HSTS_SECONDS, AXES_IPWARE_*"
      contains: "DEBUG = False"
    - path: "core/models.py"
      provides: "Usuario (AbstractUser) + UsuarioManager, USERNAME_FIELD=email"
      contains: "USERNAME_FIELD = \"email\""
    - path: "core/migrations/0001_initial.py"
      provides: "Migração inicial do core contendo só Usuario (sem HistoricalUsuario)"
      contains: "CreateModel"
    - path: "Dockerfile"
      provides: "Build multi-stage: node:20-alpine (tailwind) -> python:3.12-slim (runtime), collectstatic no build"
      contains: "FROM python:3.12-slim"
    - path: "compose.yml"
      provides: "Serviços web + db (postgres:17) com healthcheck e collation ICU pt-BR"
      contains: "postgres:17"
    - path: ".env.example"
      provides: "Todas as variáveis necessárias documentadas"
      contains: "DATABASE_URL="
  key_links:
    - from: "config/settings/base.py"
      to: ".env"
      via: "django-environ (environ.Env.read_env)"
      pattern: "environ\\.Env\\(\\)"
    - from: "core/models.py"
      to: "config/settings/base.py"
      via: "AUTH_USER_MODEL = \"core.Usuario\""
      pattern: "AUTH_USER_MODEL"
    - from: "entrypoint.sh"
      to: "core/migrations/0001_initial.py"
      via: "python manage.py migrate --noinput"
      pattern: "manage\\.py migrate"
    - from: "compose.yml (web)"
      to: "compose.yml (db)"
      via: "DATABASE_URL=postgres://...@db:5432/..."
      pattern: "depends_on"
---

<objective>
Levantar a fundação executável do Sistema Base: projeto Django 5.2 "plano" (D-01, sem Copier ainda) com settings por ambiente via `django-environ`, o `Usuario` customizado por e-mail desde a migração `0001` do app `core`, o kernel de segurança (Argon2, django-axes adaptado a `USERNAME_FIELD="email"`, CSRF/HTMX) e a infraestrutura Docker (multi-stage Tailwind + Postgres 17) que sobe com `docker compose up -d`.

Purpose: Provar o osso do Walking Skeleton — projeto real, banco real, container real — antes de a Fase 2 construir o shell visual em cima. Esta é a base sobre a qual TODO sistema derivado (Orçamento, Financeiro, Dívida Ativa) vai nascer via Copier na Fase 4; qualquer atalho aqui se multiplica.

Output: Repositório em `/opt/sistema_base` com `manage.py`, `config/`, `core/` (sem views de login ainda — isso é o Plan 02), `Dockerfile`, `compose.yml`, `entrypoint.sh` funcionais. `docker compose up -d` sobe app+banco; `migrate` cria a tabela do `Usuario`; `createsuperuser` grava um usuário real no PostgreSQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-django/01-CONTEXT.md
@.planning/phases/01-funda-o-django/01-RESEARCH.md

IMPORTANTE — `/opt/web/pca` é a fonte de extração, SOMENTE LEITURA. Nunca escrever, editar ou rodar comandos destrutivos ali. Todo trabalho desta fase acontece em `/opt/sistema_base`.

Todo código novo é neutro de domínio: usar "Sistema Base" como placeholder (vira variável Copier na Fase 4). Zero menção a "PCA", "CFC", "orçamento" ou qualquer termo de domínio no código, comentários ou `.env.example` gerados aqui.
</context>

<interfaces>
<!-- Contratos que as tasks desta plan e o Plan 02 (wave 2) vão consumir. Extraídos de /opt/web/pca, generalizados. -->

Padrão de settings por ambiente (fonte: /opt/web/pca/config/settings/base.py, dev.py, prod.py — lidos integralmente nesta pesquisa):
- `base.py`: `env = environ.Env()`; `environ.Env.read_env(BASE_DIR / ".env")`; `SECRET_KEY = env("SECRET_KEY")`; `DATABASES = {"default": env.db("DATABASE_URL")}`.
- `dev.py`: `from .base import *` + `DEBUG = env.bool("DEBUG", default=True)` + `ALLOWED_HOSTS = ["*"]` + `STORAGES` sobrescrito para `django.contrib.staticfiles.storage.StaticFilesStorage` (sem manifest) + `WHITENOISE_USE_FINDERS = True`.
- `prod.py`: `from .base import *` + `DEBUG = False` + `ALLOWED_HOSTS = list(set(env.list("ALLOWED_HOSTS", default=[]) + ["127.0.0.1"]))` + `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")` + `SECURE_SSL_REDIRECT = env.bool(...)` + `SECURE_REDIRECT_EXEMPT = [r"^healthz$"]` + `USE_X_FORWARDED_HOST = False` + `CSRF_TRUSTED_ORIGINS = env.list(...)` + `SECURE_HSTS_SECONDS = env.int(...)` + `SECURE_HSTS_INCLUDE_SUBDOMAINS = False` + `SECURE_HSTS_PRELOAD = False` + `SILENCED_SYSTEM_CHECKS = ["security.W005", "security.W021"]` + `AXES_IPWARE_PROXY_COUNT = 2` + `AXES_IPWARE_META_PRECEDENCE_ORDER = ("HTTP_X_FORWARDED_FOR", "REMOTE_ADDR")`.

Padrão `Usuario`/`UsuarioManager` (fonte: /opt/web/pca/core/models.py, verbatim, sem nada de domínio):
- `UsuarioManager(BaseUserManager)` com `use_in_migrations = True`, método privado `_create_user(email, password, **extra_fields)` que normaliza o e-mail (`self.normalize_email`) e chama `set_password`; `create_user` seta `is_staff=False, is_superuser=False`; `create_superuser` seta ambos `True` e valida que ambos ficaram `True`.
- `Usuario(AbstractUser)`: `username = None`; `email = models.EmailField("e-mail", unique=True)`; `USERNAME_FIELD = "email"`; `REQUIRED_FIELDS = []`; `objects = UsuarioManager()`; `__str__` retorna `self.email`.

Padrão axes (fonte: /opt/web/pca/core/axes_lockout.py, verbatim): função `resposta_bloqueio(request, original_response, credentials=None)` que só retorna `original_response` sem modificação — referenciada em `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"`.

Padrão middleware HTMX (fonte: /opt/web/pca/core/middleware.py, verbatim): classe `HtmxRedirectMiddleware` com `__init__(self, get_response)` e `__call__(self, request)` que, se `getattr(request, "htmx", False)` e `response.status_code in (301, 302)`, devolve `HttpResponseClientRedirect(response["Location"])` (import de `django_htmx.http`); senão devolve `response` normal.

Padrão context processor (fonte: /opt/web/pca/core/context_processors.py, verbatim): função `usuario_atual(request)` retornando `{"usuario_atual": getattr(request, "user", None)}`.

Padrão `core/apps.py` (fonte: /opt/web/pca/core/apps.py — usar SÓ a classe `CoreConfig`, NÃO replicar `PcaAdminConfig`/`default_site` nesta fase, isso é `CORE-03` da Fase 2): `class CoreConfig(AppConfig): default_auto_field = "django.db.models.BigAutoField"; name = "core"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Django + settings por ambiente</name>
  <files>manage.py, config/__init__.py, config/wsgi.py, config/settings/__init__.py, config/settings/base.py, config/settings/dev.py, config/settings/prod.py, requirements.txt, .env.example, .gitignore</files>
  <read_first>
    /opt/web/pca/config/settings/base.py
    /opt/web/pca/config/settings/dev.py
    /opt/web/pca/config/settings/prod.py
    /opt/web/pca/manage.py
    /opt/web/pca/config/wsgi.py
    /opt/web/pca/requirements.txt
    .planning/phases/01-funda-o-django/01-RESEARCH.md (seções "Pattern 1", "Pattern 3", "Pattern 4", "Standard Stack", "Code Examples > .env.example completo")
  </read_first>
  <action>
    Criar `manage.py` e `config/wsgi.py` idênticos ao padrão da PCA, trocando o default de `DJANGO_SETTINGS_MODULE` para `"config.settings.dev"` (já é esse o valor na PCA — manter).

    Criar `config/settings/base.py` com: `BASE_DIR = Path(__file__).resolve().parent.parent.parent`; `env = environ.Env()` + `environ.Env.read_env(BASE_DIR / ".env")`; `SECRET_KEY = env("SECRET_KEY")`.
    `INSTALLED_APPS` (nesta ordem): `whitenoise.runserver_nostatic`, `core.apps.CoreConfig`, `django.contrib.auth`, `django.contrib.contenttypes`, `django.contrib.sessions`, `django.contrib.messages`, `django.contrib.staticfiles`, `django.contrib.admin`, `django_htmx`, `axes`, `core`. NÃO incluir `django.contrib.postgres` (a PCA usa por causa de campos ArrayField de domínio — não aplicável aqui), NÃO incluir `simple_history` (CORE-06 é Fase 2).
    `MIDDLEWARE` (nesta ordem exata — a ordem é normativa, cf. RESEARCH "ponto crítico desta fase"): `django.middleware.security.SecurityMiddleware`, `django.middleware.clickjacking.XFrameOptionsMiddleware`, `django.contrib.sessions.middleware.SessionMiddleware`, `whitenoise.middleware.WhiteNoiseMiddleware`, `django.middleware.common.CommonMiddleware`, `django.middleware.csrf.CsrfViewMiddleware`, `django.contrib.auth.middleware.AuthenticationMiddleware`, `django_htmx.middleware.HtmxMiddleware`, `core.middleware.HtmxRedirectMiddleware`, `axes.middleware.AxesMiddleware`, `django.contrib.messages.middleware.MessageMiddleware`, `django.contrib.auth.middleware.LoginRequiredMiddleware`. (Sem a linha `simple_history.middleware.HistoryRequestMiddleware` da PCA — Fase 2.)
    `AUTH_USER_MODEL = "core.Usuario"`.
    `PASSWORD_HASHERS = ["django.contrib.auth.hashers.Argon2PasswordHasher", "django.contrib.auth.hashers.PBKDF2PasswordHasher"]`.
    `AUTHENTICATION_BACKENDS = ["axes.backends.AxesStandaloneBackend", "django.contrib.auth.backends.ModelBackend"]`.
    `AXES_FAILURE_LIMIT = 5`; `AXES_COOLOFF_TIME = timedelta(minutes=env.int("AXES_COOLOFF_MINUTES", default=15))`; `AXES_LOCKOUT_PARAMETERS = [["username", "ip_address"]]`; `AXES_USERNAME_FORM_FIELD = "username"` (com o comentário do RESEARCH Pitfall 3 explicando o motivo — Django sempre usa o kwarg literal `username=` em `authenticate()`); `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"` (com o comentário do Pitfall 4).
    `LOGIN_URL = "/login/"`.
    `SESSION_COOKIE_AGE = env.int("SESSION_COOKIE_AGE", default=28800)`; `SESSION_EXPIRE_AT_BROWSER_CLOSE = True`; `SESSION_SAVE_EVERY_REQUEST = True`.
    `ROOT_URLCONF = "config.urls"`; `WSGI_APPLICATION = "config.wsgi.application"`.
    `TEMPLATES`: `DIRS = [BASE_DIR / "core" / "templates"]`, `APP_DIRS = True`, `context_processors`: `django.template.context_processors.request`, `django.contrib.auth.context_processors.auth`, `django.contrib.messages.context_processors.messages`, `core.context_processors.usuario_atual`.
    `DATABASES = {"default": env.db("DATABASE_URL")}`.
    `AUTH_PASSWORD_VALIDATORS`: os 4 validadores padrão do Django (UserAttributeSimilarityValidator, MinimumLengthValidator, CommonPasswordValidator, NumericPasswordValidator).
    `LANGUAGE_CODE = "pt-br"`; `TIME_ZONE = "America/Sao_Paulo"`; `USE_I18N = True`; `USE_TZ = True`.
    `STATIC_URL = "/static/"`; `STATICFILES_DIRS = [BASE_DIR / "core" / "static"]`; `STATIC_ROOT = BASE_DIR / "staticfiles"`.
    `STORAGES = {"default": {"BACKEND": "django.core.files.storage.FileSystemStorage"}, "staticfiles": {"BACKEND": "whitenoise.storage.CompressedManifestStaticFilesStorage"}}` (nunca `STATICFILES_STORAGE` — removido no Django 5.1, Pitfall 5).
    `DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"`.
    `SESSION_COOKIE_SECURE = True`; `SESSION_COOKIE_SAMESITE = "Lax"`; `CSRF_COOKIE_HTTPONLY = False` (com o comentário: htmx lê o cookie, `True` quebraria toda escrita HTMX — CFG-04); `CSRF_COOKIE_SECURE = True`; `CSRF_COOKIE_SAMESITE = "Lax"`; `CSRF_USE_SESSIONS = False`.

    Criar `config/settings/dev.py`: `from .base import *`; `DEBUG = env.bool("DEBUG", default=True)`; `ALLOWED_HOSTS = ["*"]`; sobrescrever `STORAGES["staticfiles"]` para `django.contrib.staticfiles.storage.StaticFilesStorage` (sem manifest — evita `ValueError: Missing staticfiles manifest entry` sem `collectstatic` prévio); `WHITENOISE_USE_FINDERS = True` (com o comentário do motivo, cf. `/opt/web/pca/config/settings/dev.py`).

    Criar `config/settings/prod.py`: `from .base import *`; `DEBUG = False`; `ALLOWED_HOSTS = list(set(env.list("ALLOWED_HOSTS", default=[]) + ["127.0.0.1"]))` (127.0.0.1 sempre presente — é quem o `healthz` interno do compose usa); `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")`; `SECURE_SSL_REDIRECT = env.bool("SECURE_SSL_REDIRECT", default=True)`; `SECURE_REDIRECT_EXEMPT = [r"^healthz$"]` (Pitfall 6 — sem isto o healthcheck do compose entra em loop de redirect); `USE_X_FORWARDED_HOST = False`; `CSRF_TRUSTED_ORIGINS = env.list("CSRF_TRUSTED_ORIGINS", default=[])`; `SECURE_HSTS_SECONDS = env.int("SECURE_HSTS_SECONDS", default=3600)`; `SECURE_HSTS_INCLUDE_SUBDOMAINS = False`; `SECURE_HSTS_PRELOAD = False`; `SILENCED_SYSTEM_CHECKS = ["security.W005", "security.W021"]`; `AXES_IPWARE_PROXY_COUNT = 2`; `AXES_IPWARE_META_PRECEDENCE_ORDER = ("HTTP_X_FORWARDED_FOR", "REMOTE_ADDR")`.

    Criar `requirements.txt` com exatamente estas 9 linhas fixadas (versões do RESEARCH.md Standard Stack, todas `[VERIFIED: PyPI]` e auditadas via slopcheck `[OK]`): `Django==5.2.17`, `psycopg[binary]==3.3.4`, `django-environ==0.14.0`, `django-axes==8.3.1`, `django-htmx==1.29.0`, `argon2-cffi==25.1.0`, `whitenoise==6.12.0`, `gunicorn==26.0.0`, `django-ipware==7.0.1`. NÃO incluir `django-simple-history`, `openpyxl`, `freezegun` (fora do escopo desta fase, cf. RESEARCH "Installation").

    Criar `.env.example` com exatamente as variáveis do RESEARCH "Code Examples > .env.example completo para esta fase": `SECRET_KEY`, `DEBUG`, `DJANGO_SETTINGS_MODULE=config.settings.dev`, `ALLOWED_HOSTS=localhost,127.0.0.1`, `CSRF_TRUSTED_ORIGINS=http://localhost:8000`, `SESSION_COOKIE_AGE=28800`, `AXES_COOLOFF_MINUTES=15`, `WEB_BIND_ADDRESS=127.0.0.1`, `WEB_PORT=8000`, `SECURE_SSL_REDIRECT=true`, `SECURE_HSTS_SECONDS=3600`, `POSTGRES_DB=sistema_base`, `POSTGRES_USER=sistema_base`, `POSTGRES_PASSWORD=replace-with-a-database-password`, `DATABASE_URL=postgres://sistema_base:replace-with-a-database-password@db:5432/sistema_base`. Comentar cada bloco em pt-BR explicando o propósito (estilo da casa). Usar "Sistema Base" (nunca "PCA") em qualquer comentário.

    Criar `.gitignore` cobrindo: `.env`, `__pycache__/`, `*.pyc`, `/staticfiles/`, `core/static/dist/`, `*.sqlite3`, `.venv/`, `*.log`, `.DS_Store`.
  </action>
  <verify>
    <automated>grep -q 'AUTH_USER_MODEL = "core.Usuario"' config/settings/base.py && grep -q 'CSRF_COOKIE_HTTPONLY = False' config/settings/base.py && grep -q 'DEBUG = False' config/settings/prod.py && test "$(wc -l < requirements.txt)" -eq 9 && grep -q 'DATABASE_URL=' .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `manage.py` contém `os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings.dev")`
    - `config/settings/base.py` contém a string exata `AUTH_USER_MODEL = "core.Usuario"`
    - `config/settings/base.py` contém `CSRF_COOKIE_HTTPONLY = False`
    - `config/settings/base.py` contém `AXES_USERNAME_FORM_FIELD = "username"`
    - `config/settings/base.py` contém `AXES_LOCKOUT_CALLABLE = "core.axes_lockout.resposta_bloqueio"`
    - `config/settings/base.py` contém `"django.contrib.auth.hashers.Argon2PasswordHasher"` como PRIMEIRO item de `PASSWORD_HASHERS`
    - `config/settings/base.py` contém `LANGUAGE_CODE = "pt-br"` e `TIME_ZONE = "America/Sao_Paulo"`
    - `config/settings/base.py` NÃO contém a string `STATICFILES_STORAGE` (grep retorna 0 ocorrências)
    - `config/settings/prod.py` contém `DEBUG = False` e `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")`
    - `config/settings/prod.py` contém `SECURE_REDIRECT_EXEMPT`
    - `requirements.txt` contém exatamente 9 linhas, nenhuma menciona `simple-history`, `openpyxl` ou `freezegun`
    - `.env.example` contém `DATABASE_URL=` e `SECRET_KEY=`
    - `.gitignore` contém `.env` como linha própria
    - Nenhum arquivo criado nesta task contém a string `PCA` (case-insensitive) — `grep -ril pca manage.py config/ requirements.txt .env.example .gitignore` retorna vazio
  </acceptance_criteria>
  <done>Todos os arquivos de settings/scaffold existem com o conteúdo acima; nenhuma referência a PCA/domínio; requirements.txt fixa as 9 dependências corretas.</done>
</task>

<task type="auto">
  <name>Task 2: Core kernel — Usuario, axes, middleware, htmx CSRF, healthz</name>
  <files>core/__init__.py, core/apps.py, core/models.py, core/migrations/__init__.py, core/axes_lockout.py, core/middleware.py, core/context_processors.py, core/views.py, config/urls.py, core/templates/base.html, core/static/src/input.css, core/static/vendor/htmx.min.js, core/static/vendor/alpine.min.js, core/README.md</files>
  <read_first>
    /opt/web/pca/core/models.py
    /opt/web/pca/core/axes_lockout.py
    /opt/web/pca/core/middleware.py
    /opt/web/pca/core/context_processors.py
    /opt/web/pca/core/apps.py
    /opt/web/pca/core/views.py (só a função `healthz`, ignorar o resto — login/logout é o Plan 02)
    /opt/web/pca/config/urls.py
    /opt/web/pca/core/templates/base.html
    .planning/phases/01-funda-o-django/01-RESEARCH.md (seções "Pattern 2", "Pattern 4", "Pitfall 8", "Pitfall 9")
  </read_first>
  <action>
    Criar `core/apps.py` com `class CoreConfig(AppConfig): default_auto_field = "django.db.models.BigAutoField"; name = "core"` — NÃO criar `PcaAdminConfig`/`default_site` (isso é `CORE-03`, Fase 2; `django.contrib.admin` fica com o `AdminSite` padrão do Django nesta fase, cf. RESEARCH Assumption A5).

    Criar `core/models.py` com `UsuarioManager(BaseUserManager)` (`use_in_migrations = True`, `_create_user`/`create_user`/`create_superuser` exatamente como o padrão extraído — ver `<interfaces>`) e `Usuario(AbstractUser)` (`username = None`, `email = models.EmailField("e-mail", unique=True)`, `USERNAME_FIELD = "email"`, `REQUIRED_FIELDS = []`, `objects = UsuarioManager()`, `__str__` retorna `self.email`). Mensagens de erro em pt-BR ("O e-mail é obrigatório.", "Superuser precisa ter is_staff=True.", "Superuser precisa ter is_superuser=True.").

    Criar `core/axes_lockout.py` com `resposta_bloqueio(request, original_response, credentials=None)` retornando `original_response` sem modificação (verbatim do padrão extraído, com o comentário explicando por que existe).

    Criar `core/middleware.py` com `HtmxRedirectMiddleware` (verbatim do padrão extraído — converte 301/302 em `HttpResponseClientRedirect` quando `request.htmx` é truthy).

    Criar `core/context_processors.py` com `usuario_atual(request)` retornando `{"usuario_atual": getattr(request, "user", None)}`.

    Criar `core/views.py` com SÓ a view `healthz`: decorada com `@login_not_required` (import de `django.contrib.auth.decorators`), faz `SELECT 1` via `django.db.connection.cursor()`, retorna `JsonResponse({"status": "ok"})` em sucesso ou `JsonResponse({"status": "error"}, status=503)` se a query levantar exceção. NÃO criar `login_view`/`logout_view`/`shell_view` aqui — isso é o Plan 02 (wave 2), que vai estender este arquivo.

    Criar `config/urls.py` com `urlpatterns = [path("admin/", admin.site.urls), path("healthz", healthz, name="healthz")]` (import `from core.views import healthz`). NÃO incluir `core.urls` ainda (não existe até o Plan 02 criá-lo).

    Criar `core/templates/base.html`: HTML5 mínimo (`<!DOCTYPE html>`, `<html lang="pt-br">`), `<head>` carregando `{% load static %}`, `<link rel="stylesheet" href="{% static 'dist/tailwind.css' %}">`, `<script src="{% static 'vendor/htmx.min.js' %}" defer></script>`, `<script src="{% static 'vendor/alpine.min.js' %}" defer></script>`. No `<body>`, um `{% block content %}{% endblock %}` dentro de um wrapper com classes Tailwind reais (`min-h-screen bg-page flex flex-col items-center justify-center` — usar `page`/`ink` como nomes de cor genéricos do tema, sem replicar a paleta de marca da PCA). Incluir, ANTES do fechamento de `</body>`, o script `htmx:configRequest` EXATO do Pattern 4 do RESEARCH.md: função `csrfCookie(nome)` que lê `document.cookie` via regex, e `document.body.addEventListener("htmx:configRequest", (evento) => { evento.detail.headers["X-CSRFToken"] = csrfCookie("csrftoken"); })`. Comentário em pt-BR acima do script explicando por que não pode ser `hx-headers` estático (Anti-Pattern do RESEARCH — token rotaciona no login/logout).

    Criar `core/static/src/input.css` com só as 3 diretivas Tailwind: `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;` (sem variáveis CSS de paleta de marca — a identidade visual completa é `CORE-03`/Fase 2).

    Baixar e vendorizar `core/static/vendor/htmx.min.js` (htmx 1.9.12, via `https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js`) e `core/static/vendor/alpine.min.js` (Alpine 3.16.2, via `https://unpkg.com/alpinejs@3.16.2/dist/cdn.min.js`) — arquivos commitados no repo, NUNCA carregados via CDN em runtime (D-07/offline-first). Se o download falhar, documentar a URL exata usada no `core/README.md` para permitir novo download manual.

    Criar `core/README.md` documentando 3 convenções não-óbvias do kernel (uma seção curta por convenção, em pt-BR): (1) toda lógica de "hoje"/data atual DEVE usar `django.utils.timezone.localdate()`, nunca `timezone.now().date()` nem `datetime.date.today()` (Pitfall 8 — dentro do container o SO está em UTC mesmo com `TIME_ZONE="America/Sao_Paulo"` no settings); (2) `AXES_USERNAME_FORM_FIELD = "username"` é proposital mesmo com login por e-mail — não "corrigir" para `"email"` (Pitfall 3); (3) `AUTH_USER_MODEL` nunca muda depois da migração `0001` — troca depois é operação destrutiva (D-03).
  </action>
  <verify>
    <automated>grep -q 'USERNAME_FIELD = "email"' core/models.py && grep -q 'class HtmxRedirectMiddleware' core/middleware.py && grep -q 'def healthz' core/views.py && ! grep -q 'login_view' core/views.py && grep -q 'htmx:configRequest' core/templates/base.html && test -s core/static/vendor/htmx.min.js && test -s core/static/vendor/alpine.min.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `core/models.py` contém `USERNAME_FIELD = "email"` e `use_in_migrations = True`
    - `core/apps.py` contém `class CoreConfig(AppConfig)` e NÃO contém a string `PcaAdminConfig` nem `default_site`
    - `core/axes_lockout.py` define uma função `resposta_bloqueio` com assinatura `(request, original_response, credentials=None)`
    - `core/middleware.py` define `class HtmxRedirectMiddleware` com método `__call__`
    - `core/views.py` contém `@login_not_required` e `def healthz(request)`, e NÃO contém `login_view`, `logout_view` ou `shell_view` (essas ainda não existem)
    - `config/urls.py` contém `path("healthz", healthz, name="healthz")` e `path("admin/", admin.site.urls)`
    - `core/templates/base.html` contém a string literal `htmx:configRequest` e a string `X-CSRFToken`
    - `core/static/vendor/htmx.min.js` e `core/static/vendor/alpine.min.js` existem e cada um tem mais de 5000 bytes (arquivo real baixado, não stub vazio)
    - `core/README.md` contém as strings `localdate()` e `AXES_USERNAME_FORM_FIELD`
    - `grep -ril pca core/ config/urls.py` retorna vazio (sem menção a PCA/domínio)
  </acceptance_criteria>
  <done>core/ contém o kernel completo (model, axes, middleware, context processor, healthz, base.html com CSRF/htmx, assets vendorizados); nada de login/logout/shell ainda (Plan 02).</done>
</task>

<task type="auto">
  <name>Task 3: Docker infra + gerar migração 0001 + subir o sistema (walking skeleton)</name>
  <files>Dockerfile, compose.yml, entrypoint.sh, tailwind.config.js, .dockerignore, core/migrations/0001_initial.py</files>
  <read_first>
    /opt/web/pca/Dockerfile
    /opt/web/pca/compose.yml
    /opt/web/pca/entrypoint.sh
    /opt/web/pca/tailwind.config.js
    .planning/phases/01-funda-o-django/01-RESEARCH.md (seções "Pattern 5", "Pattern 6", "Pitfall 7", "Pitfall 10", "Assumption A3", "Assumption A4")
    config/settings/base.py (criado na Task 1 desta plan)
    core/models.py, core/apps.py (criados na Task 2 desta plan)
  </read_first>
  <action>
    Criar `tailwind.config.js`: `darkMode` não é necessário nesta fase (omitir ou deixar `false` — a paleta clara/escura é `CORE-03`/Fase 2); `content: ["./core/templates/**/*.html"]`; `theme: { extend: {} }`; `plugins: []`. NÃO replicar a paleta de marca/tokens de cor da PCA (fora de escopo desta fase).

    Criar `Dockerfile` multi-stage, generalizando o padrão extraído (SEM `COPY apps` — não existe `apps/` ainda nesta fase): estágio `assets` (`FROM node:20-alpine AS assets`, `WORKDIR /build`, copia `tailwind.config.js` e `core/static/src/input.css` e `core/templates`, roda `npx --yes tailwindcss@3.4.17 -i ./core/static/src/input.css -o ./core/static/dist/tailwind.css --minify`, guarda de tamanho: mede `CSS_BYTES` com `wc -c`, ecoa o valor, falha (`exit 1`) se `CSS_BYTES` for menor que `5000` — piso absoluto do Pitfall 10, nunca abaixo de ~5 KB de preflight puro). Estágio `runtime` (`FROM python:3.12-slim AS runtime`), `ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1`, `WORKDIR /app`, instala `curl` via apt (necessário para o healthcheck), copia `requirements.txt` e roda `pip install --no-cache-dir -r requirements.txt`, copia todo o restante do projeto (`COPY . .`), copia o CSS do estágio `assets`, roda `collectstatic --noinput` com `SECRET_KEY=build DATABASE_URL=sqlite:///tmp/build.db DJANGO_SETTINGS_MODULE=config.settings.prod` (valores dummy, nunca reais — só para o comando não falhar por falta de env), `chmod +x /app/entrypoint.sh`, `EXPOSE 8000`, `ENTRYPOINT ["/app/entrypoint.sh"]`.

    Criar `entrypoint.sh` (`#!/bin/sh`, `set -eu`, `python manage.py migrate --noinput`, `exec gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3`) — collectstatic NUNCA aqui (Pitfall 5, já rodou no build).

    Criar `compose.yml` com dois serviços apenas (SEM o serviço `backup` — isso é `INF-03`/Fase 4): `db` (`image: postgres:17`, `restart: unless-stopped`, env `POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD` do `.env`, `POSTGRES_INITDB_ARGS: "--locale-provider=icu --icu-locale=pt-BR --encoding=UTF8"` — decisão de collation que precisa estar certa desde o 1º `initdb`, Pitfall 7 — volume nomeado `pgdata` SEM `external: true` nesta fase — Assumption A4, garante que `docker compose up -d` funciona sozinho no primeiro clone —, `healthcheck` via `pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}` com `interval: 5s`, `timeout: 3s`, `retries: 10`, `start_period: 30s`); `web` (`build: {context: ., dockerfile: Dockerfile}`, `restart: unless-stopped`, `depends_on: {db: {condition: service_healthy}}`, `env_file: [.env]`, `ports: ["${WEB_BIND_ADDRESS}:${WEB_PORT}:8000"]` — D-08, nunca publicar sem `WEB_BIND_ADDRESS`, `healthcheck` via `curl -fsS http://127.0.0.1:8000/healthz` com `interval: 10s`, `timeout: 5s`, `retries: 10`, `start_period: 120s` — cobre o `migrate` rodando contra base vazia/recém-criada). Volume `pgdata:` nomeado, sem `external`.

    Criar `.dockerignore` cobrindo: `.git`, `.env`, `__pycache__/`, `*.pyc`, `.venv/`, `staticfiles/`, `core/static/dist/`, `.planning/`, `*.md` (exceto se algum README for necessário em runtime — não é o caso aqui).

    Rodar a sequência completa de verificação (todos os comandos abaixo, nesta ordem, a partir da raiz do repo):
    1. Criar um `.env` de trabalho local (não commitado, cf. `.gitignore`) copiando `.env.example` e preenchendo `SECRET_KEY` com um valor aleatório real (`python3 -c "import secrets; print(secrets.token_urlsafe(50))"` ou equivalente) e `POSTGRES_PASSWORD`/senha do superusuário com valores de desenvolvimento.
    2. `docker compose build web` — deve completar com `CSS_BYTES` acima de 5000 no log do estágio `assets` (se abaixo, investigar o glob de `content` do `tailwind.config.js` antes de prosseguir — não abaixar o piso).
    3. `docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py makemigrations core` — gera `core/migrations/0001_initial.py` no host (bind-mount só para este comando pontual; `compose.yml` continua sem volume de código para o serviço `web` em runtime). Conferir que a migração gerada contém `CreateModel` para `Usuario` e usa `core.models.UsuarioManager` como manager — NÃO deve conter `HistoricalUsuario` (isso só entra numa migração da Fase 2).
    4. `docker compose up -d` — sobe `db` e `web`; aguardar os dois ficarem `healthy` (`docker compose ps`).
    5. `curl -fsS http://127.0.0.1:8000/healthz` (ou o valor de `WEB_BIND_ADDRESS`/`WEB_PORT` do `.env` usado) — deve responder `{"status": "ok"}`.
    6. `docker compose exec -T db psql -U sistema_base -d sistema_base -c "\d core_usuario"` — confirma que a tabela existe com a coluna `email` `unique`.
    7. `docker compose exec -T -e DJANGO_SUPERUSER_EMAIL=admin@sistemabase.local -e DJANGO_SUPERUSER_PASSWORD=troque-esta-senha web python manage.py createsuperuser --noinput` — cria o primeiro usuário real, gravado no PostgreSQL.
    8. `docker compose exec -T web python manage.py shell -c "from core.models import Usuario; u = Usuario.objects.get(email='admin@sistemabase.local'); print(u.is_superuser, u.check_password('troque-esta-senha'))"` — deve imprimir `True True`, provando a escrita (Passo 7) e a leitura (Passo 8) reais no banco.
  </action>
  <verify>
    <automated>docker compose build web && docker compose up -d && sleep 5 && curl -fsS http://127.0.0.1:8000/healthz</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose build web` exit code 0
    - Log do build contém `tailwind.css gerado:` seguido de um número de bytes maior que 5000
    - `core/migrations/0001_initial.py` existe no host após o passo 3, contém `migrations.CreateModel` com `name='Usuario'` e NÃO contém a string `HistoricalUsuario`
    - `docker compose up -d` seguido de `docker compose ps` mostra os serviços `db` e `web` com status `healthy` (aguardando até 120s de `start_period`)
    - `curl -fsS http://127.0.0.1:8000/healthz` retorna corpo `{"status": "ok"}` com HTTP 200
    - `docker compose exec -T db psql -U sistema_base -d sistema_base -c "\d core_usuario"` lista a coluna `email` com constraint `unique`
    - O comando do passo 8 imprime `True True`
    - `Dockerfile` contém `ENTRYPOINT ["/app/entrypoint.sh"]` e NÃO contém `COPY apps`
    - `compose.yml` contém `POSTGRES_INITDB_ARGS` com `icu-locale=pt-BR` e NÃO contém um serviço chamado `backup`
    - `compose.yml` (serviço `db`) NÃO tem chave `ports:` (banco nunca exposto ao host)
  </acceptance_criteria>
  <done>`docker compose up -d` sobe app+PostgreSQL 17 saudáveis a partir de um clone limpo (só preenchendo `.env`); `migrate` (via entrypoint) já aplicou a migração `0001` do `core`; um superusuário real foi criado e autenticado com sucesso via ORM, provando leitura e escrita reais no banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `.env` (host) → `config/settings/*.py` (processo Django) | Segredos (SECRET_KEY, senha do Postgres) cruzam do arquivo não versionado para o processo via `django-environ` |
| Internet → `web` (porta publicada) | Único ponto de entrada externo antes do proxy (Fase 4); hoje restrito a `WEB_BIND_ADDRESS` |
| `web` → `db` | Rede interna do Docker Compose; `db` nunca publica porta ao host |
| Build da imagem (`docker compose build`) → imagem final | `SECRET_KEY=build`/`DATABASE_URL=sqlite://...` são valores dummy que não podem vazar para o runtime real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` versionado por engano | mitigate | `.gitignore` exclui `.env`; só `.env.example` (sem segredo real) fica no git — verificado por `git status --ignored` não listar `.env` como tracked |
| T-01-02 | Tampering | `ALLOWED_HOSTS`/`Host` header forjado em produção | mitigate | `prod.py` restringe `ALLOWED_HOSTS` ao que vier do `.env` + `127.0.0.1` fixo; `DisallowedHost` rejeita o resto (Django default) |
| T-01-03 | Elevation of Privilege | Troca de `AUTH_USER_MODEL` depois da migração `0001` em fase futura | accept | Operação bloqueada pelo próprio Django (destrutiva); documentado em `core/README.md` como invariante — controle de processo, não de código |
| T-01-04 | Tampering | Valores dummy de build (`SECRET_KEY=build`) vazando para runtime | accept | Usados só como `ENV` inline no `RUN collectstatic` do Dockerfile, nunca gravados em arquivo da imagem nem usados fora desse comando; risco residual nulo (valor "build" não é segredo real e não é usado em nenhum settings de produção real) |
| T-01-05 | Denial of Service / Information Disclosure | PostgreSQL exposto diretamente à rede | mitigate | `compose.yml` (serviço `db`) não declara `ports:` — só acessível pela rede interna do Compose, nunca do host |
| T-01-06 | Information Disclosure | `web` exposto direto na internet sem proxy/TLS | mitigate | `ports: ["${WEB_BIND_ADDRESS}:${WEB_PORT}:8000"]` com `WEB_BIND_ADDRESS=127.0.0.1` no `.env.example` — D-08; proxy do host (Fase 4) é quem expõe publicamente |
| T-01-SC | Tampering | Pacotes maliciosos/typosquat via `pip`/`npx` no build | mitigate | `requirements.txt` fixa 9 pacotes já auditados no `01-RESEARCH.md` (`slopcheck` 9/9 `[OK]`, nenhum `[SLOP]`/`[SUS]`); `npx tailwindcss@3.4.17` com versão pinada explícita (D-07) |
</threat_model>

<verification>
Ao final das 3 tasks: `docker compose up -d` (a partir de um `.env` preenchido a partir de `.env.example`) sobe `db`+`web` saudáveis; `docker compose exec web python manage.py migrate --check` retorna sem migrações pendentes; um usuário criado via `createsuperuser --noinput` é autenticável via ORM (`check_password` retorna `True`). Nenhum arquivo do repositório contém a string "PCA" (`grep -ril pca . --exclude-dir=.git --exclude-dir=.planning`).
</verification>

<success_criteria>
- `docker compose up -d` funciona a partir de um clone limpo, só preenchendo `.env` (sem passo manual extra, sem `docker volume create`)
- `core.Usuario` existe desde a migração `0001` do app `core`, com `UsuarioManager` próprio e `USERNAME_FIELD="email"`
- Toda configuração sensível vem do `.env`; `.env.example` cobre 100% das variáveis usadas em `base.py`/`dev.py`/`prod.py`/`compose.yml`
- `prod.py` aplica Argon2 no topo, django-axes, cookies seguros, HSTS/proxy, `DEBUG=False`, `ALLOWED_HOSTS` restrito
- `CSRF_COOKIE_HTTPONLY=False` está setado e `base.html` já carrega o script `htmx:configRequest` (pronto para o Plan 02 usar)
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-django/01-01-SUMMARY.md` when done
</output>
