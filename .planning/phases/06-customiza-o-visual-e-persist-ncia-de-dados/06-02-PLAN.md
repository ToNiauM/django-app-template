---
phase: 06-customiza-o-visual-e-persist-ncia-de-dados
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - core/static/img/logo-entidade.svg
  - core/static/img/logo-subsistema.svg
  - core/templates/core/shell.html
  - core/templates/core/login.html
  - core/templates/base.html
  - core/tests/test_logos.py
autonomous: true
requirements: [C1, C2]
must_haves:
  truths:
    - "Existe um local único no core para trocar o logo principal da entidade: core/static/img/logo-entidade.svg — substituir o arquivo, sem editar código (D-65, critério 1 do roadmap)"
    - "Existe um local único no core para trocar o logo do subsistema: core/static/img/logo-subsistema.svg (D-65, critério 2 do roadmap)"
    - "O logo do subsistema aparece no cabeçalho da aside do shell e no header mobile; o logo da entidade aparece na tela de login e discretamente no rodapé da aside (D-68/D-69)"
    - "Um sistema recém-nascido renderiza os placeholders neutros sem nenhuma referência quebrada e sem marca de domínio (D-66, TPL-04)"
    - "Todo <img> de logo tem alt derivado da identidade do context processor — o logo complementa o texto, não o substitui (D-67)"
    - "O admin NÃO ganhou logo — nenhum arquivo de admin foi alterado (D-70)"
  artifacts:
    - path: "core/static/img/logo-entidade.svg"
      provides: "Placeholder SVG neutro do logo da entidade, com viewBox e sem width/height fixos"
      contains: "viewBox"
    - path: "core/static/img/logo-subsistema.svg"
      provides: "Placeholder SVG neutro do logo do subsistema, forma distinta da entidade"
      contains: "viewBox"
    - path: "core/templates/core/shell.html"
      provides: "Logo do subsistema no cabeçalho da aside e no header mobile; logo da entidade no rodapé da aside"
      contains: "img/logo-subsistema.svg"
    - path: "core/templates/core/login.html"
      provides: "Logo da entidade acima do formulário de login"
      contains: "img/logo-entidade.svg"
    - path: "core/tests/test_logos.py"
      provides: "Regressão Django dos contratos de logo (static() resolvido, alt presente, SVGs válidos, favicon)"
      min_lines: 40
  key_links:
    - from: "core/templates/core/shell.html"
      to: "core/static/img/logo-subsistema.svg"
      via: "{% static %} com hashing WhiteNoise"
      pattern: "static 'img/logo-subsistema\\.svg'"
    - from: "core/templates/core/login.html"
      to: "core/static/img/logo-entidade.svg"
      via: "{% static %}"
      pattern: "static 'img/logo-entidade\\.svg'"
    - from: "core/templates/base.html"
      to: "core/static/img/icon-192.png"
      via: "link rel=icon (favicon por contrato de arquivo existente)"
      pattern: "rel=\"icon\""
---

<objective>
Entregar os pontos de customização de marca do core: placeholders SVG neutros em
caminhos fixos (`core/static/img/logo-entidade.svg` e `logo-subsistema.svg`),
inseridos nos templates via `{% static %}` nos pontos travados por D-68/D-69
(aside/header mobile e login/rodapé), favicon via ícone PWA existente, e a
regressão Django que congela o contrato.

Purpose: critérios 1 e 2 do roadmap — um operador troca cada logo substituindo UM
arquivo de nome fixo, sem editar código (D-65). O contrato "arquivo fixo →
{% static %}" estende o padrão de identidade já provado pelos ícones PWA (D-20).
Output: 2 SVGs novos, 3 templates editados, 1 arquivo de teste novo, tracer verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-CONTEXT.md
@.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md
@core/templates/core/shell.html
@core/templates/core/login.html
@core/templates/base.html
@core/tests/test_shell.py
@core/tests/test_pwa.py
</context>

<interfaces>
Contratos existentes que o executor usa diretamente (extraídos do código):

- Context processor `identidade` (core/context_processors.py) expõe `sistema_nome`,
  `sistema_sigla` e `cor_primaria` a todos os templates — os `alt` dos logos usam
  esses valores, nunca literais.
- `base.html` linha 1: `{% load static %}` — o load NÃO é herdado por templates
  filhos; `shell.html` e `login.html` precisam do próprio `{% load static %}`.
- Padrão de teste: `from django.templatetags.static import static` +
  `self.assertIn(static("img/..."), conteudo)` (test_pwa.py, hashing-safe) e
  `@override_settings(SESSION_COOKIE_SECURE=False, CSRF_COOKIE_SECURE=False)` +
  `Usuario.objects.create_user(email=..., password=...)` (test_shell.py).
- `IconesPWATests.DIRETORIO_IMG = Path(settings.BASE_DIR) / "core" / "static" / "img"`
  — mesmo caminho para os testes de existência dos SVGs.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Placeholders SVG neutros em caminhos fixos</name>
  <files>core/static/img/logo-entidade.svg, core/static/img/logo-subsistema.svg</files>
  <read_first>
    - .planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md (Pattern 4 — requisitos duros do placeholder: neutro, viewBox, sem width/height, renderiza em fundo claro)
    - .template-tests/test_04_03_identity.py (linhas 16-31: tupla FORBIDDEN_IDENTITY — nenhuma dessas strings pode aparecer nos SVGs, que são copiados ao sistema gerado e varridos pelo scan)
    - ops/gerar_icones_pwa.py (espírito do placeholder D-20 que este contrato estende)
  </read_first>
  <action>
    Criar os dois SVGs placeholder (D-65/D-66/D-67), cada um com:
    - `xmlns="http://www.w3.org/2000/svg"`, `viewBox="0 0 64 64"`, `role="img"`, SEM atributos
      `width`/`height` (dimensionamento fica nas classes Tailwind do `<img>`), SEM `<script>`,
      SEM texto de marca (o scan de identidade varre o arquivo gerado — zero strings proibidas).
    - Comentário XML em pt-BR no topo: "Placeholder neutro (D-66): substitua este arquivo pela
      arte oficial mantendo o nome <nome-do-arquivo> — nenhuma edição de código é necessária.
      Em produção, rode docker compose up -d --build após a troca."
    - Paleta neutra que renderiza bem em fundo claro (D-67): traços `#9ca3af` (cinza Tailwind
      gray-400), `fill="none"`, `stroke-width="4"`.
    - Formas geométricas DISTINTAS entre si para o operador diferenciá-los de relance
      (discretion exercida): `logo-entidade.svg` = contorno de círculo com cruz interna
      (identidade institucional "da casa"); `logo-subsistema.svg` = contorno de quadrado com
      cantos arredondados (rx) e losango interno (identidade "deste sistema").
  </action>
  <verify>
    <automated>python3 -c "import xml.etree.ElementTree as ET; [ET.parse(f'core/static/img/{n}') for n in ('logo-entidade.svg','logo-subsistema.svg')]" && grep -l viewBox core/static/img/logo-entidade.svg core/static/img/logo-subsistema.svg | wc -l | grep -qx 2 && ! grep -riE 'cfc|pca|sistema.base|script' core/static/img/logo-entidade.svg core/static/img/logo-subsistema.svg</automated>
  </verify>
  <acceptance_criteria>
    - Os dois arquivos existem em `core/static/img/` e são XML bem-formado (parse sem exceção)
    - Ambos contêm `viewBox` e NÃO contêm atributo `width=` nem `height=` no elemento `<svg>`
    - Nenhum contém `<script`, `<text` com marca, ou qualquer string da FORBIDDEN_IDENTITY (grep case-insensitive por cfc/pca/sistema.base retorna vazio)
    - As formas dos dois SVGs são diferentes entre si (asserção de fonte: os paths/elementos internos não são idênticos)
  </acceptance_criteria>
  <done>Os dois caminhos fixos do contrato D-65 existem com placeholders neutros válidos que passam o scan de identidade</done>
</task>

<task type="auto">
  <name>Task 2: Inserir logos nos templates e favicon no base.html</name>
  <files>core/templates/core/shell.html, core/templates/core/login.html, core/templates/base.html</files>
  <read_first>
    - core/templates/core/shell.html (estado atual completo — header mobile linhas 31-40, cabeçalho da aside linhas 49-65, rodapé da aside linhas 70-98; NÃO tem `{% load static %}` hoje)
    - core/templates/core/login.html (card do form linhas 7-12; NÃO tem `{% load static %}` hoje)
    - core/templates/base.html (head linhas 1-14 — inserção do favicon após a linha do manifest)
    - .planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md (Pattern 3 — exemplos exatos de <img> com classes e alt)
  </read_first>
  <action>
    Todas as inserções usam `<img src="{% static 'img/....svg' %}" alt="...">` — nunca SVG
    inline (quebraria o contrato D-65 de trocar arquivo sem editar código). Comentários pt-BR
    citando o contrato: "Logo por arquivo fixo (D-65): trocar = substituir o arquivo em
    core/static/img/ mantendo o nome". Detalhes:

    1. `shell.html`: adicionar `{% load static %}` na linha 2 (logo após o extends — o load do
       base.html não é herdado). Três inserções:
       a. Cabeçalho da aside (D-68): dentro do div `pb-4 px-2 flex flex-row items-start...`,
          antes do bloco de sigla/nome, `<img>` de `img/logo-subsistema.svg` com classes
          `h-8 w-8 flex-none` e `alt="Logo de {{ sistema_sigla }}"`. A sigla e o nome
          permanecem — o logo complementa o texto (D-67). Como a aside É a gaveta no mobile,
          esta inserção cobre também o header da gaveta.
       b. Header mobile fixo (D-68): no div `fixed inset-x-0 top-0 z-30 ... md:hidden`, antes
          do `<span>{{ sistema_nome }}</span>`, `<img>` de `img/logo-subsistema.svg` com
          classes `h-6 w-6 flex-none` e o mesmo alt.
       c. Rodapé da aside (D-69, "discretamente"): dentro do bloco
          `mt-auto border-t border-grid pt-3 ...`, adicionar `<img>` de `img/logo-entidade.svg`
          com classes `h-5 w-5 flex-none opacity-60` e `alt="Logo institucional"`, como primeiro
          elemento da linha (antes do avatar do usuário) — presença discreta da identidade "da
          casa" sem competir com os dados do usuário.

    2. `login.html`: adicionar `{% load static %}` na linha 2. Dentro do card
       (`w-full max-w-sm p-6 bg-surface rounded shadow`), ANTES do `<h1>Entrar</h1>`, inserir
       `<img>` de `img/logo-entidade.svg` com classes `h-12 w-12 mx-auto mb-4` e
       `alt="Logo institucional"` (D-69: identidade institucional acima do formulário).
       Não tocar em `_login_form.html`.

    3. `base.html` (discretion exercida — recomendação do research): após a linha
       `<link rel="manifest" ...>`, adicionar `<link rel="icon" href="{% static 'img/icon-192.png' %}">`
       com comentário pt-BR: favicon reaproveita o ícone PWA existente — zero arquivo novo,
       mesmo contrato de substituição (D-72), e elimina o 302 ruidoso de /favicon.ico no
       LoginRequiredMiddleware.

    4. NÃO tocar em nenhum arquivo de admin, `core/views.py` (manifest_view fica intacto — D-71)
       nem em `_login_form.html`.
  </action>
  <verify>
    <automated>grep -c "static 'img/logo-subsistema.svg'" core/templates/core/shell.html | grep -qx 2 && grep -c "static 'img/logo-entidade.svg'" core/templates/core/shell.html | grep -qx 1 && grep -c "static 'img/logo-entidade.svg'" core/templates/core/login.html | grep -qx 1 && grep -q 'rel="icon"' core/templates/base.html && grep -q '{% load static %}' core/templates/core/shell.html && grep -q '{% load static %}' core/templates/core/login.html && ! git diff --name-only | grep -qi admin</automated>
  </verify>
  <acceptance_criteria>
    - `shell.html` referencia `img/logo-subsistema.svg` exatamente 2 vezes (aside + header mobile) e `img/logo-entidade.svg` exatamente 1 vez (rodapé), todas via `{% static %}`
    - `login.html` referencia `img/logo-entidade.svg` exatamente 1 vez, acima do `<h1>Entrar</h1>`
    - Todo `<img>` de logo tem atributo `alt` não vazio; os alts do subsistema usam `{{ sistema_sigla }}` (nunca literal de marca)
    - `base.html` tem `<link rel="icon"` apontando para `{% static 'img/icon-192.png' %}`
    - `{% load static %}` presente em `shell.html` e `login.html`
    - `git diff --name-only` NÃO lista `core/admin.py` nem qualquer template de admin (D-70)
    - Os textos `{{ sistema_sigla }}`/`{{ sistema_nome }}` continuam renderizados ao lado dos logos (D-67 — nenhuma remoção de texto identitário)
  </acceptance_criteria>
  <done>Os logos aparecem nos quatro pontos travados por D-68/D-69, o favicon existe, e o admin permanece intocado</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regressão Django dos logos e prova ponta a ponta via tracer</name>
  <files>core/tests/test_logos.py</files>
  <read_first>
    - core/tests/test_shell.py (padrão de setUp com Usuario.create_user, override_settings e asserts de conteúdo renderizado)
    - core/tests/test_pwa.py (padrão static() hashing-safe e DIRETORIO_IMG; docstring que exige asserts contra settings, nunca literais de identidade)
    - core/templates/core/shell.html e core/templates/core/login.html (estado pós Task 2 — os literais que os testes vão procurar)
  </read_first>
  <behavior>
    - Teste 1: GET / autenticado contém `static("img/logo-subsistema.svg")` (D-68) e `static("img/logo-entidade.svg")` (D-69, rodapé)
    - Teste 2: GET /login/ (anônimo) contém `static("img/logo-entidade.svg")` (D-69)
    - Teste 3: GET / autenticado contém `'alt="Logo de ' + settings.SISTEMA_SIGLA` (D-67 — alt via identidade, nunca literal)
    - Teste 4: GET /login/ contém `'rel="icon"'` e `static("img/icon-192.png")` (favicon no base.html)
    - Teste 5: `logo-entidade.svg` e `logo-subsistema.svg` existem em `Path(settings.BASE_DIR)/"core"/"static"/"img"`, contêm `<svg` e `viewBox` (contrato de arquivo fixo D-65/D-66)
  </behavior>
  <action>
    Criar `core/tests/test_logos.py` com docstring pt-BR ("Prova do contrato de logos por
    arquivo fixo (D-65..D-69): referências sempre via static(), alt derivado da identidade,
    placeholders SVG válidos"), no padrão dos vizinhos: imports de `settings`, `static`,
    `Client`, `TestCase`, `override_settings`, `Usuario`; classe(s) com
    `@override_settings(SESSION_COOKIE_SECURE=False, CSRF_COOKIE_SECURE=False)` onde houver
    login. Implementar exatamente os 5 comportamentos do bloco behavior. Nenhuma string
    literal de marca nos asserts — identidade sempre via `settings.SISTEMA_SIGLA`/`SISTEMA_NOME`.

    Verificação de execução: a suíte Django só roda dentro de uma cópia gerada (os settings do
    template são .jinja) — a prova de execução é o tracer de nascimento, que roda
    `manage.py test core apps.exemplo` na cópia. Rodar `.template-tests/test_05_nascimento.sh`
    (já corrigido pelo plano 06-01) e, na sequência, o scan de identidade
    `python3 -m unittest discover -s .template-tests -p 'test_04_03_identity.py'` para provar
    que os SVGs e templates novos não vazam marca no sistema gerado.
  </action>
  <verify>
    <automated>python3 -m compileall -q core/tests/test_logos.py && .template-tests/test_05_nascimento.sh && python3 -m unittest discover -s .template-tests -p 'test_04_03_identity.py'</automated>
  </verify>
  <acceptance_criteria>
    - `core/tests/test_logos.py` compila e contém >= 5 métodos de teste cobrindo os 5 comportamentos
    - O tracer imprime `OK: nascimento completo da cópia Copier passou.` — a suíte da cópia gerada (incluindo test_logos.py) passou dentro do container
    - `test_04_03_identity.py` passa: nenhum arquivo do sistema gerado (SVGs incluídos) contém identidade proibida
    - Nenhum assert do teste novo usa literais de marca (grep por `CFC`/`Sistema Base` em test_logos.py retorna vazio)
  </acceptance_criteria>
  <done>O contrato de logos está congelado por testes que rodam na suíte de todo sistema gerado, e o nascimento completo permanece verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arquivos SVG do operador → navegador | logos são estáticos substituídos pelo operador (confiável) — NÃO há upload de usuário (deliberadamente fora do escopo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | SVG com script servido como estático (XSS se aberto direto na origem) | mitigate | referência sempre via `<img>` (contexto de imagem não executa script); placeholders sem `<script>`; nota "use SVG de fonte confiável" na documentação (plano 06-03); upload via admin permanece fora do escopo |
| T-06-06 | Spoofing | logo trocado sem alt/texto poderia mascarar identidade do sistema | mitigate | alt obrigatório derivado de `sistema_sigla` e texto identitário preservado ao lado do logo (D-67, congelado por teste) |
| T-06-SC | Tampering | supply chain npm/pip/cargo | accept | nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `.template-tests/test_05_nascimento.sh` verde (suíte Django da cópia inclui test_logos.py)
- `python3 -m unittest discover -s .template-tests -p 'test_04_03_identity.py'` verde (SVGs neutros)
- `git diff --name-only` não contém arquivos de admin nem core/views.py (D-70/D-71)
</verification>

<success_criteria>
- Critérios 1 e 2 do roadmap: um arquivo fixo por logo, referenciado via {% static %}, trocável sem editar código
- Sistema recém-nascido renderiza placeholders sem referência quebrada (D-66)
- Regressão automatizada roda em TODO sistema gerado (test_logos.py viaja com o core)
</success_criteria>

<output>
Create `.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-02-SUMMARY.md` when done
</output>
