---
phase: 06-customiza-o-visual-e-persist-ncia-de-dados
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - README.md.jinja
  - README.md
  - core/README.md
  - ops/MIGRACAO.md.jinja
autonomous: true
requirements: [C1, C2, C3, C4]
must_haves:
  truths:
    - "O README do sistema gerado tem UMA seção 'Customização de marca' listando TODOS os pontos: logo da entidade, logo do subsistema, ícones/nome do PWA, cor primária, nome/sigla (D-77, critérios 1-3 do roadmap)"
    - "O nome do PWA está documentado como customizável via SISTEMA_NOME/SISTEMA_SIGLA no .env — mecanismo existente, apenas documentado (D-71, critério 3)"
    - "O logo do PWA está documentado como os 3 PNGs de nome fixo, substituíveis ou regeneráveis por ops/gerar_icones_pwa.py (D-72, critério 3)"
    - "O README do template inclui a etapa opcional 'inserir logos oficiais' no nascimento e a nota de persistência dos dados no host (D-78)"
    - "MIGRACAO.md e o README gerado documentam o novo layout de dados (bind mount) e a migração de sistemas nascidos com named volume, sem nenhum script automatizado (D-75, D-40, Pitfall 4)"
    - "Um operador que nunca viu o projeto encontra em minutos onde trocar cada logo e o nome do PWA (critério operacional do CONTEXT)"
  artifacts:
    - path: "README.md.jinja"
      provides: "Seção '## Customização de marca' + nota de persistência + nota de migração named volume → bind mount"
      contains: "Customização de marca"
    - path: "README.md"
      provides: "Etapa opcional de logos no nascimento + nota de persistência no host"
      contains: "logo-entidade.svg"
    - path: "core/README.md"
      provides: "Convenção 5: pontos de customização de marca por arquivo fixo"
      contains: "logo-subsistema.svg"
    - path: "ops/MIGRACAO.md.jinja"
      provides: "Runbook atualizado para o layout bind mount (sem docker volume create, sem volume externo)"
      contains: "PGDATA_DIR"
  key_links:
    - from: "README.md.jinja"
      to: "core/static/img/logo-entidade.svg e logo-subsistema.svg"
      via: "seção Customização de marca nomeando os caminhos fixos"
      pattern: "core/static/img/logo-(entidade|subsistema)\\.svg"
    - from: "README.md.jinja"
      to: "migração named volume → bind mount"
      via: "nota em Atualizações do template com o one-liner de cópia"
      pattern: "cp -a /de/\\. /para/"
    - from: "ops/MIGRACAO.md.jinja"
      to: "compose.yml (bind mount)"
      via: "explicação do layout ./dados/pg"
      pattern: "dados/pg"
---

<objective>
Documentar em um só lugar todos os pontos de customização de marca (D-77) e o novo
layout de persistência de dados, nos quatro documentos do template: README gerado
(README.md.jinja), README do template (raiz), convenções do core (core/README.md) e
runbook de migração (ops/MIGRACAO.md.jinja).

Purpose: os critérios 1-3 do roadmap exigem locais "documentados" — o mecanismo
nasce nos planos 06-01/06-02, mas o valor operacional é o operador achar tudo em
minutos. O critério 3 (PWA) é 100% documentação: o mecanismo já existe (D-71/D-72).
Output: 4 documentos atualizados, neutros para o scan de identidade, com os testes
de operações (test_04_06) ainda verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-CONTEXT.md
@.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md
@README.md.jinja
@core/README.md
@ops/MIGRACAO.md.jinja
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md.jinja — seção Customização de marca, persistência e migração de dados</name>
  <files>README.md.jinja</files>
  <read_first>
    - README.md.jinja (estado atual completo, 79 linhas — seções 'Operação diária', 'Ícones PWA (opcional)' e 'Atualizações do template' são os pontos de edição)
    - .planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md (Pitfalls 2, 4, 5 e 6 e Code Examples 'Migração named volume → bind mount' — conteúdo exato das notas)
    - .template-tests/test_04_03_identity.py (linhas 16-31: FORBIDDEN_IDENTITY — o README renderizado é varrido; zero menção a CFC/PCA/Sistema Base no texto novo)
  </read_first>
  <action>
    Três edições no README do sistema gerado (prosa pt-BR, tom dos docs existentes):

    1. NOVA seção `## Customização de marca` (D-77), substituindo e absorvendo a atual
       `## Ícones PWA (opcional)` (o conteúdo dela migra para cá — nenhuma informação se perde).
       A seção lista TODOS os pontos de customização, cada um com o local único:
       - Logo da entidade: substituir `core/static/img/logo-entidade.svg` mantendo nome e
         extensão — nenhuma edição de código (D-65).
       - Logo do subsistema: substituir `core/static/img/logo-subsistema.svg`, idem.
       - Ícones do PWA: substituir `core/static/img/icon-192.png`, `icon-512.png` e
         `icon-512-maskable.png` pela arte oficial OU regenerar placeholders com
         `python3 ops/gerar_icones_pwa.py` (lê sigla/cor do `.env`) — nomes de arquivo nunca
         mudam (D-72). O favicon reaproveita `icon-192.png`.
       - Nome do PWA: `SISTEMA_NOME` e `SISTEMA_SIGLA` no `.env` — refletem no manifest e na
         instalação sem tocar código (D-71).
       - Cor primária: `COR_PRIMARIA` no `.env` (formato `#RRGGBB`); a paleta Tailwind deriva
         dela no build.
       E três notas: (a) em produção, qualquer troca de arquivo estático só aparece após
       `docker compose up -d --build` — o collectstatic roda no build da imagem (Pitfall 5);
       (b) os logos são SVG por contrato — se a arte só existe em PNG, exporte/converta para
       SVG a partir do vetor original (formato alternativo NÃO é aceito; o nome+extensão fixos
       são o contrato); (c) use apenas SVG de fonte confiável, exportado pela própria equipe
       (arquivo estático é servido a todos os usuários).

    2. Na seção `## Operação diária`, após o passo 3 (subida dos serviços), acrescentar
       parágrafo de persistência: os dados do PostgreSQL vivem no host em `./dados/pg`
       (bind mount criado automaticamente no primeiro `up`; caminho alternativo via
       `PGDATA_DIR` no `.env`, sempre com `./` inicial ou caminho absoluto e sempre um
       subdiretório — nunca a raiz de um disco montado). `docker compose down -v` NÃO apaga
       esses dados. Troubleshooting: o diretório pertence ao uid 999 (usuário postgres do
       container) com permissão 700 — é esperado não conseguir listá-lo sem sudo (D-74).

    3. Na seção `## Atualizações do template`, acrescentar nota de migração de dados
       (Pitfall 4, D-75): sistemas nascidos ANTES do layout de bind mount guardam os dados no
       named volume `<slug>_pgdata`; após um `copier update` que trouxe o novo compose, migre os
       dados ANTES do primeiro `up` — caminho canônico: dump + restore (runbook
       `ops/MIGRACAO.md`); atalho equivalente com a stack parada (`docker compose down`, sem
       `-v`): `docker run --rm -v <slug>_pgdata:/de -v "$(pwd)/dados/pg:/para" postgres:17 sh -c 'cp -a /de/. /para/'`
       e, depois de validar o sistema, `docker volume rm <slug>_pgdata` opcional. Nenhum script
       automatiza isso (D-40) — o passo é consciente e auditável.
  </action>
  <verify>
    <automated>grep -q '## Customização de marca' README.md.jinja && grep -q 'logo-entidade.svg' README.md.jinja && grep -q 'logo-subsistema.svg' README.md.jinja && grep -q 'SISTEMA_NOME' README.md.jinja && grep -q 'gerar_icones_pwa.py' README.md.jinja && grep -q 'PGDATA_DIR' README.md.jinja && grep -q "cp -a /de/. /para/" README.md.jinja && ! grep -qiE 'pca|cfc|sistema.base' README.md.jinja</automated>
  </verify>
  <acceptance_criteria>
    - Existe exatamente uma seção `## Customização de marca` citando os 5 pontos: logo-entidade.svg, logo-subsistema.svg, os 3 icon-*.png + gerar_icones_pwa.py, SISTEMA_NOME/SISTEMA_SIGLA, COR_PRIMARIA
    - A antiga seção `## Ícones PWA (opcional)` não existe mais como seção separada (conteúdo absorvido)
    - A nota de produção `docker compose up -d --build` aparece na seção de customização
    - A nota SVG-only (converter PNG → SVG) aparece; nenhuma promessa de aceitar PNG como logo
    - `## Operação diária` menciona `./dados/pg`, `PGDATA_DIR`, o prefixo `./` e o uid 999
    - `## Atualizações do template` contém o one-liner `cp -a /de/. /para/` e a referência a `ops/MIGRACAO.md`
    - `grep -iE 'pca|cfc|sistema.base' README.md.jinja` retorna vazio (scan de identidade)
  </acceptance_criteria>
  <done>O operador de um sistema gerado encontra num único lugar como trocar cada logo, ícones/nome do PWA, cor e nome — e como os dados persistem e migram</done>
</task>

<task type="auto">
  <name>Task 2: README do template (nascimento) e convenções do core</name>
  <files>README.md, core/README.md</files>
  <read_first>
    - README.md (seção '## Nascimento local de um sistema', passos 1-15 nas linhas 61-163, e seção '## Resumo: nascimento completo em comandos' na linha 419 — pontos de inserção)
    - core/README.md (4 convenções existentes — o novo conteúdo vira a convenção 5, seguindo o formato numerado)
    - .planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-CONTEXT.md (D-70: admin NÃO tem logo — registrar como convenção; D-77/D-78)
  </read_first>
  <action>
    1. `README.md` (raiz — doc do template, não é copiado ao sistema gerado):
       a. Na seção `## Nascimento local de um sistema`, inserir uma nova etapa opcional entre o
          passo 8 (credenciais R2) e o atual passo 9 (git init), renumerando os passos seguintes:
          "**(Opcional) Insira os logos oficiais.** Substitua
          `core/static/img/logo-entidade.svg` (logo da entidade) e
          `core/static/img/logo-subsistema.svg` (logo deste sistema) mantendo nome e extensão;
          se quiser ícones PWA próprios, substitua os três `core/static/img/icon-*.png` ou rode
          `python3 ops/gerar_icones_pwa.py`. Os placeholders neutros funcionam — o passo pode
          ficar para depois; a referência completa é a seção 'Customização de marca' do README
          do sistema gerado." (D-78). Inserir antes do commit inicial para os logos entrarem no
          primeiro commit quando existirem.
       b. Na mesma seção (após o passo do `docker compose up -d --build db web`), acrescentar a
          nota de persistência: os dados do banco ficam em `./dados/pg` dentro do diretório do
          sistema (bind mount) e sobrevivem a `docker compose down -v`; o diretório é criado
          automaticamente e pertence ao uid 999 (D-78).
       c. Na seção `## Resumo: nascimento completo em comandos`, adicionar uma linha de
          comentário no bloco de comandos, no ponto equivalente (antes do `git init`):
          `# (opcional) substituir core/static/img/logo-*.svg pelos logos oficiais`.

    2. `core/README.md`: nova convenção `## 5. Pontos de customização de marca — arquivos de
       nome fixo`, no formato das 4 existentes (título, regra, porquê): os logos do core são
       arquivos estáticos de caminho fixo (`core/static/img/logo-entidade.svg`,
       `core/static/img/logo-subsistema.svg`) e os ícones PWA idem (`icon-192.png`,
       `icon-512.png`, `icon-512-maskable.png`); customizar = substituir o arquivo mantendo
       nome e extensão — nunca criar variável de caminho no `.env` nem SVG inline em template
       (o nome fixo É o contrato, e `{% static %}` resolve o hashing do WhiteNoise). O nome do
       PWA vem de `SISTEMA_NOME`/`SISTEMA_SIGLA` no `.env`. O admin deliberadamente NÃO exibe
       logo — a identidade dele é nome + cor via override existente; não "completar" o admin
       com logo (D-70). Referenciar a seção "Customização de marca" do README do sistema como
       a lista canônica. ATENÇÃO: `core/README.md` é copiado ao sistema gerado e varrido pelo
       scan de identidade — texto 100% neutro, sem menção a CFC/PCA/Sistema Base.
  </action>
  <verify>
    <automated>grep -q 'logo-entidade.svg' README.md && grep -q 'dados/pg' README.md && grep -q 'logo-subsistema.svg' core/README.md && grep -q '## 5\.' core/README.md && ! grep -qiE 'pca|cfc|sistema.base' core/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md tem a etapa opcional de logos dentro da sequência numerada do nascimento (antes do commit inicial), citando os dois .svg e os icon-*.png
    - README.md menciona `./dados/pg` e a sobrevivência a `docker compose down -v` na seção de nascimento
    - O bloco do 'Resumo: nascimento completo em comandos' contém a linha de comentário opcional dos logos
    - core/README.md tem a convenção `## 5.` citando os 2 SVGs, os 3 PNGs, SISTEMA_NOME/SISTEMA_SIGLA e a regra "admin sem logo" (D-70)
    - `grep -iE 'pca|cfc|sistema.base' core/README.md` retorna vazio (o arquivo viaja ao sistema gerado)
    - A âncora `A → B → C` do README.md permanece intacta (test_copier_update.sh a exige)
  </acceptance_criteria>
  <done>O nascimento documenta a etapa opcional de logos e a persistência; o core registra o contrato de arquivo fixo como convenção</done>
</task>

<task type="auto">
  <name>Task 3: Runbook de migração atualizado para o layout bind mount</name>
  <files>ops/MIGRACAO.md.jinja</files>
  <read_first>
    - ops/MIGRACAO.md.jinja (estado atual — o parágrafo das linhas 41-42 sobre "O volume `pgdata` é gerenciado pelo Compose" é o alvo da substituição; seções 3, 4 e 6 permanecem válidas)
    - .template-tests/test_04_06_operations.py (linhas 63-83: tokens OBRIGATÓRIOS no runbook renderizado — `cp .env.example .env`, `pg_restore --clean --if-exists --no-owner`, `docker compose up -d`, `migrate --noinput`, `/healthz`, `nginx -t`, `certbot`, `DNS`, `ensaio_restore_local.sh` — e proibições: a string exata de volume externo com dois-pontos-true e qualquer menção a pca)
  </read_first>
  <action>
    Substituir o parágrafo "O volume `pgdata` é gerenciado pelo Compose; não há etapa de
    `docker volume create` e não use volume externo para esta migração." pelo novo layout
    (D-75): os dados do PostgreSQL usam bind mount para `./dados/pg` (configurável via
    `PGDATA_DIR` no `.env`, sempre `./` inicial ou caminho absoluto de um subdiretório);
    continua não havendo etapa de `docker volume create` — o Compose cria o diretório
    automaticamente no primeiro `up`, vazio na VM nova, e o restore do dump preenche o banco;
    `docker compose down -v` não remove os dados (bind mounts sobrevivem por construção);
    o diretório pertence ao uid 999 com permissão 700. Manter a recomendação de não usar
    volume externo, SEM escrever a string literal de configuração YAML dela (o teste de
    operações proíbe essa string no runbook).

    Opcional recomendado: na seção `## 6.` (ensaio de restore), uma frase esclarecendo que o
    ensaio usa volume efêmero próprio criado via docker e NÃO toca `./dados/pg` da stack.

    Preservar TODOS os tokens obrigatórios do test_04_06 (não remover nem parafrasear os
    comandos existentes das seções 2, 3, 4, 5 e 6). O dump + `.env` + `compose up` continua o
    caminho canônico de migração — a mudança é só a explicação do layout de dados.
  </action>
  <verify>
    <automated>grep -q 'PGDATA_DIR' ops/MIGRACAO.md.jinja && grep -q 'dados/pg' ops/MIGRACAO.md.jinja && ! grep -q 'docker volume create' ops/MIGRACAO.md.jinja && ! grep -qi 'pca' ops/MIGRACAO.md.jinja && python3 -m unittest discover -s .template-tests -p 'test_04_06_operations.py'</automated>
  </verify>
  <acceptance_criteria>
    - O parágrafo antigo sobre o volume `pgdata` gerenciado pelo Compose não existe mais
    - O runbook explica `./dados/pg`, `PGDATA_DIR`, a criação automática do diretório e o uid 999/permissão 700
    - `python3 -m unittest discover -s .template-tests -p 'test_04_06_operations.py'` passa (tokens obrigatórios preservados, string de volume externo ausente, zero menção a pca)
    - O caminho canônico dump + `.env` + `docker compose up -d` permanece descrito sem alteração de comandos
  </acceptance_criteria>
  <done>O runbook reflete o layout bind mount e a regressão de operações continua verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentação → operador | docs orientam decisões de segurança do operador (segredos, dados, SVGs) — erro de doc vira erro operacional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | operador commitando dados/segredos por desconhecer o layout | mitigate | notas explícitas de `./dados/pg`, `.gitignore` e uid 999 nos READMEs e no runbook (D-74/D-75) |
| T-06-08 | Elevation of Privilege | operador usando SVG de fonte não confiável como logo | mitigate | nota "use SVG limpo exportado pela própria equipe" na seção Customização de marca |
| T-06-09 | Tampering | migração pós-copier-update inicializando banco vazio e induzindo perda percebida de dados | mitigate | nota de migração named volume → bind mount com one-liner manual documentado (Pitfall 4, D-40: nunca script automático) |
| T-06-SC | Tampering | supply chain npm/pip/cargo | accept | plano é documentação — nenhum pacote instalado |
</threat_model>

<verification>
- `python3 -m unittest discover -s .template-tests -p 'test_04_06_operations.py'` OK
- `grep -iE 'pca|cfc|sistema.base'` vazio em README.md.jinja, core/README.md e ops/MIGRACAO.md.jinja (arquivos que chegam ao sistema gerado)
- Os quatro documentos citam os mesmos nomes de arquivo/variável definidos nos planos 06-01/06-02 (logo-entidade.svg, logo-subsistema.svg, icon-*.png, SISTEMA_NOME, SISTEMA_SIGLA, COR_PRIMARIA, PGDATA_DIR, ./dados/pg)
</verification>

<success_criteria>
- Critério 3 do roadmap fechado: logo e nome do PWA documentados como customizáveis a partir do core (D-71/D-72)
- Critérios 1, 2 e 4 ganham o componente "documentado": seção única de marca (D-77), etapa de nascimento (D-78), notas de persistência/migração (D-75)
- Critério operacional do CONTEXT: um operador acha em minutos onde trocar cada logo e o nome do PWA
</success_criteria>

<output>
Create `.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-03-SUMMARY.md` when done
</output>
