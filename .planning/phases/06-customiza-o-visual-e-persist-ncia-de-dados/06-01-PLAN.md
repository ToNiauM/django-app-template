---
phase: 06-customiza-o-visual-e-persist-ncia-de-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yml.jinja
  - .env.example.jinja
  - copier.yml
  - .gitignore
  - .gitignore.jinja
  - .template-tests/test_04_05_backup.py
  - .template-tests/test_06_persistencia.py
  - .template-tests/test_05_nascimento.sh
autonomous: true
requirements: [C4]
must_haves:
  truths:
    - "docker compose down -v seguido de up -d num sistema gerado não perde nenhum dado do banco (D-73, critério 4 do roadmap)"
    - "Os dados do PostgreSQL ficam visíveis no host em ./dados/pg (ou no caminho de PGDATA_DIR do .env), dentro do diretório do sistema gerado (D-73/D-76)"
    - "Um sistema recém-gerado nasce com .gitignore que ignora .env e /dados/ — git add . nunca commita segredos nem o banco (D-74, Pitfall 3)"
    - "O tracer de nascimento continua verde ponta a ponta, inclusive a limpeza do diretório de dados uid 999 (Pitfall 1)"
  artifacts:
    - path: "compose.yml.jinja"
      provides: "Serviço db com bind mount ${PGDATA_DIR:-./dados/pg}:/var/lib/postgresql/data, sem bloco volumes: de topo"
      contains: "PGDATA_DIR:-./dados/pg"
    - path: ".env.example.jinja"
      provides: "Variável PGDATA_DIR documentada com default comentado e exigência do prefixo ./"
      contains: "PGDATA_DIR"
    - path: ".gitignore.jinja"
      provides: "Fonte do .gitignore do sistema gerado (.env, /dados/, staticfiles etc.)"
      contains: "/dados/"
    - path: ".template-tests/test_06_persistencia.py"
      provides: "Asserções de contrato do bind mount, do .env.example e do .gitignore gerado (valida Assumption A4)"
      min_lines: 40
    - path: ".template-tests/test_05_nascimento.sh"
      provides: "Limpeza via container root do diretório uid 999 + prova direta de sobrevivência a down -v"
      contains: "down --volumes"
  key_links:
    - from: "compose.yml.jinja"
      to: ".env (PGDATA_DIR)"
      via: "interpolação Compose com default"
      pattern: "\\$\\{PGDATA_DIR:-\\./dados/pg\\}:/var/lib/postgresql/data"
    - from: "copier.yml"
      to: ".gitignore.jinja"
      via: "_exclude sem a entrada .gitignore — o .jinja renderiza e substitui o .gitignore verbatim do template (precedente README.md.jinja)"
      pattern: "gitignore"
    - from: ".template-tests/test_05_nascimento.sh"
      to: "dados uid 999 no host"
      via: "docker run com postgres:17 removendo /alvo/dados antes do rm -rf do TMP"
      pattern: "postgres:17 rm -rf /alvo/dados"
---

<objective>
Trocar o named volume `pgdata` do serviço `db` por bind mount configurável no host
(`${PGDATA_DIR:-./dados/pg}`), garantir que sistemas gerados nasçam com `.gitignore`
que protege `.env` e `/dados/`, e adaptar a rede de testes do template (asserções de
compose, teste novo de persistência e tracer de nascimento) para provar o critério 4
da fase: `docker compose down -v` não destrói dados.

Purpose: hoje uma operação rotineira de compose (`down -v`) apaga o banco inteiro —
este plano torna a perda de dados impossível por construção (D-73..D-76) e fecha, de
quebra, a lacuna do sistema gerado sem `.gitignore` nenhum (Pitfall 3 do research).
Output: `compose.yml.jinja`, `.env.example.jinja`, `copier.yml`, `.gitignore`,
`.gitignore.jinja` (novo), `test_04_05_backup.py` atualizado,
`test_06_persistencia.py` (novo), `test_05_nascimento.sh` corrigido e verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-CONTEXT.md
@.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md
@compose.yml.jinja
@copier.yml
@.env.example.jinja
@.gitignore
@.template-tests/test_05_nascimento.sh
@.template-tests/test_04_05_backup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind mount no compose, PGDATA_DIR no .env.example e .gitignore do sistema gerado</name>
  <files>compose.yml.jinja, .env.example.jinja, copier.yml, .gitignore, .gitignore.jinja</files>
  <read_first>
    - compose.yml.jinja (serviço db linhas 4-19 e bloco `volumes:` de topo linhas 71-73 — remoção integral do bloco)
    - .env.example.jinja (padrão de comentários pt-BR; bloco PostgreSQL nas linhas 34-38 é o ponto de inserção)
    - copier.yml (linha `  - .gitignore` do `_exclude` e o comentário existente sobre README.md.jinja nas linhas 36-38 — é o precedente a replicar)
    - .gitignore (conteúdo atual do template — base do novo .gitignore.jinja)
    - .planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md (Pattern 1, Pattern 2 e Pitfall 3 — semântica verificada do down -v, do prefixo ./ e da colisão .gitignore/.gitignore.jinja)
  </read_first>
  <action>
    Aplicar D-73/D-74/D-76 nos arquivos-fonte do template (comentários sempre em pt-BR explicando o porquê):

    1. `compose.yml.jinja` — no serviço `db`, substituir a linha `- pgdata:/var/lib/postgresql/data`
       por `- ${PGDATA_DIR:-./dados/pg}:/var/lib/postgresql/data`, precedida de comentário pt-BR
       cobrindo três porquês: (a) `docker compose down -v` remove apenas named volumes e volumes
       anônimos — bind mount sobrevive por construção (motivo: incidente de perda de dados da PCA
       em 2026-07-28 — este nome NÃO pode aparecer no comentário; escrever apenas "incidente de
       perda de dados que motivou esta decisão", pois o arquivo renderizado passa pelo scan de
       identidade proibida); (b) o valor de PGDATA_DIR DEVE começar com `./` ou ser absoluto —
       sem o prefixo o Compose interpreta como nome de volume; (c) o mount point
       `/var/lib/postgresql/data` é específico da imagem postgres:17 — em postgres>=18 o layout
       muda e um upgrade de major é migração deliberada via dump/restore, nunca troca de tag.
       Remover integralmente o bloco `volumes:` de topo (as 3 últimas linhas do arquivo, incluindo
       o comentário "Gerenciado pelo Compose"). Healthcheck, environment e os serviços web/backup
       permanecem intactos (D-76).

    2. `.env.example.jinja` — após o bloco PostgreSQL (depois da linha `DATABASE_URL=...`),
       adicionar bloco comentado: explicação de que os dados do banco vivem num diretório do host
       (bind mount), que o default vive no compose (`${PGDATA_DIR:-./dados/pg}`), que o valor deve
       começar com `./` ou ser caminho absoluto e sempre apontar para um SUBDIRETÓRIO (nunca a raiz
       de um disco montado — `lost+found` faz o initdb recusar), e a linha default comentada
       `# PGDATA_DIR=./dados/pg`.

    3. `.gitignore` (do template) — adicionar a linha `/dados/` (D-74: o diretório de dados nunca
       entra no Git, nem no repositório do template).

    4. `.gitignore.jinja` (NOVO) — fonte do `.gitignore` do sistema gerado. Conteúdo: as entradas
       do `.gitignore` atual do template EXCETO `.venv-template/` (ferramenta exclusiva do
       template), MAIS `/dados/`. Ou seja: `.env`, `__pycache__/`, `*.pyc`, `/staticfiles/`,
       `core/static/dist/`, `*.sqlite3`, `.venv/`, `*.log`, `.DS_Store`, `/dados/`. Incluir
       comentário-cabeçalho pt-BR neutro (sem qualquer marca — o arquivo renderizado passa pelo
       scan de identidade do test_04_03) explicando que `.env` guarda segredos locais e `/dados/`
       é o diretório do banco. O arquivo não usa nenhuma variável Jinja — o sufixo `.jinja` existe
       só para que o Copier o renderize como `.gitignore` no destino.

    5. `copier.yml` — remover a linha `  - .gitignore` do `_exclude` e adicionar comentário pt-BR
       no mesmo estilo do comentário existente sobre `README.md.jinja` (linhas 36-38): o
       `_exclude` aplica ao caminho de DESTINO, então manter `.gitignore` ali bloquearia o
       `.gitignore.jinja` renderizado; com a entrada removida, o `.gitignore.jinja` renderiza e
       substitui o `.gitignore` verbatim do template durante a cópia — mesmo mecanismo do README.
       Nenhuma variável Copier nova é criada (D-50, expectativa explícita do CONTEXT).
  </action>
  <verify>
    <automated>grep -c 'PGDATA_DIR:-./dados/pg}:/var/lib/postgresql/data' compose.yml.jinja | grep -qx 1 && ! grep -q 'pgdata' compose.yml.jinja && grep -q '# PGDATA_DIR=./dados/pg' .env.example.jinja && grep -qx '/dados/' .gitignore && grep -qx '/dados/' .gitignore.jinja && grep -qx '.env' .gitignore.jinja && ! grep -qE '^  - \.gitignore$' copier.yml</automated>
  </verify>
  <acceptance_criteria>
    - `compose.yml.jinja` contém exatamente 1 ocorrência de `${PGDATA_DIR:-./dados/pg}:/var/lib/postgresql/data` no serviço `db`
    - A string `pgdata` não aparece em nenhuma linha de `compose.yml.jinja` (bloco de topo removido junto com o comentário antigo)
    - `.env.example.jinja` contém a linha comentada `# PGDATA_DIR=./dados/pg` e menciona o prefixo `./` obrigatório
    - `.gitignore` (template) contém a linha `/dados/`
    - `.gitignore.jinja` existe, contém as linhas `.env` e `/dados/`, NÃO contém `.venv-template/` e não contém nenhuma string do FORBIDDEN_IDENTITY do test_04_03 (sistema_base, Sistema Base, PCA, CFC...)
    - `copier.yml` não tem mais a entrada `  - .gitignore` no `_exclude`; a entrada `  - .gitattributes` permanece; nenhuma pergunta Copier nova foi adicionada
    - Serviços `web` e `backup` e o healthcheck do `db` estão byte a byte como antes (conferível com `git diff compose.yml.jinja` restrito ao serviço db e ao bloco de topo)
  </acceptance_criteria>
  <done>Compose usa bind mount com default relativo, .env.example documenta PGDATA_DIR, e o sistema gerado passa a nascer com .gitignore protegendo .env e /dados/</done>
</task>

<task type="auto">
  <name>Task 2: Rede de testes do template — atualizar test_04_05 e criar test_06_persistencia</name>
  <files>.template-tests/test_04_05_backup.py, .template-tests/test_06_persistencia.py</files>
  <read_first>
    - .template-tests/test_04_05_backup.py (a asserção da linha 70 `compose["volumes"]["pgdata"]` QUEBRA com o bloco de topo removido — é a linha a substituir; o teste roda `docker compose config --format json` numa cópia gerada)
    - .template-tests/test_04_03_identity.py (linhas 1-65: padrão `ROOT`, `COPIER` e helper `render()` com `copier copy --defaults --data ...` — replicar esse helper no teste novo)
    - compose.yml.jinja e .env.example.jinja (estado pós Task 1 — os literais exatos que as asserções vão procurar)
    - copier.yml (estado pós Task 1)
  </read_first>
  <action>
    1. `.template-tests/test_04_05_backup.py` — substituir a linha
       `self.assertFalse(compose["volumes"]["pgdata"].get("external", False))` por asserções do
       novo contrato sobre o JSON de `docker compose config`: (a) `"volumes" not in compose` (o
       bloco de topo sumiu do modelo resolvido); (b) o primeiro item de
       `compose["services"]["db"]["volumes"]` tem `type == "bind"`,
       `target == "/var/lib/postgresql/data"` e `source` terminando em `/dados/pg` (o config
       resolve o caminho relativo para absoluto dentro do diretório gerado). Comentário pt-BR
       citando D-73. Nada mais muda no arquivo.

    2. `.template-tests/test_06_persistencia.py` (NOVO) — unittest no padrão da suíte
       `.template-tests` (`ROOT = Path(__file__).resolve().parents[1]`, docstring pt-BR, executável
       com `python3 -m unittest discover`). Duas classes:

       a. `ContratoFonteTests` — leitura direta dos fontes: `compose.yml.jinja` contém
          `${PGDATA_DIR:-./dados/pg}:/var/lib/postgresql/data` e NÃO contém a substring `pgdata`;
          `.env.example.jinja` contém `PGDATA_DIR`; `copier.yml` não contém linha `- .gitignore`
          no `_exclude` (regex de linha exata); `.gitignore` do template contém `/dados/`;
          `.gitignore.jinja` contém `.env` e `/dados/`.

       b. `GitignoreGeradoTests` — valida a Assumption A4 com uma cópia real: helper `render()`
          no molde do test_04_03 (mesmos `--data`, slug `aurora`, destino em
          `tempfile.TemporaryDirectory`), assertando que no destino: `.gitignore` EXISTE,
          contém `.env` e `/dados/`, e que `.gitignore.jinja` NÃO existe no destino (o Copier
          renderizou e removeu o sufixo, substituindo o verbatim do template).
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .template-tests -p 'test_06_persistencia.py' -v && python3 -m unittest discover -s .template-tests -p 'test_04_05_backup.py'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s .template-tests -p 'test_06_persistencia.py'` termina com OK e >= 2 testes executados
    - `python3 -m unittest discover -s .template-tests -p 'test_04_05_backup.py'` termina com OK (nenhuma referência a `compose["volumes"]` sobrou no arquivo)
    - O teste de cópia real prova: destino tem `.gitignore` com `.env` e `/dados/`; destino NÃO tem `.gitignore.jinja`
    - Nenhum teste usa strings de identidade proibida (o helper usa `aurora`, como o precedente)
  </acceptance_criteria>
  <done>A regressão do template captura o contrato de bind mount e prova que o sistema gerado nasce com .gitignore correto</done>
</task>

<task type="auto">
  <name>Task 3: Tracer de nascimento — limpeza uid 999 e prova de sobrevivência a down -v</name>
  <files>.template-tests/test_05_nascimento.sh</files>
  <read_first>
    - .template-tests/test_05_nascimento.sh (função `limpar()` linhas 79-98 — o `rm -rf "${TMP}"` da linha 96 falha em arquivos uid 999; o trecho pós-suíte linhas 187-198 é onde entra a prova de down -v, antes de `SUCESSO=true`)
    - .planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-RESEARCH.md (Pitfall 1 e "Atualização do tracer" em Code Examples — comandos exatos recomendados)
  </read_first>
  <action>
    Duas mudanças cirúrgicas no tracer, comentadas em pt-BR:

    1. Limpeza (Pitfall 1): em `limpar()`, imediatamente ANTES do bloco final
       `if [ "${TMP}" = "${TMP_VALIDADO}" ]...rm -rf "${TMP}"`, inserir: se `${DESTINO}/dados`
       existir como diretório, remover via container root com
       `docker run --rm -v "${DESTINO}:/alvo" postgres:17 rm -rf /alvo/dados` redirecionando
       stdout/stderr para /dev/null e com `|| true` (a imagem postgres:17 já está local — o
       próprio tracer acabou de usá-la). Comentário: o initdb grava `dados/pg` como uid 999 e o
       usuário do host não consegue removê-lo — sem isso o `rm -rf` do TMP falha silenciosamente
       e acumula lixo em /tmp.

    2. Prova do critério 4 da fase (C4): após o smoke de `/login/` (linha 190) e antes de
       `SUCESSO=true`, inserir a sequência: (a) `[ -d "${DESTINO}/dados/pg" ] || falhar` — o bind
       mount materializou o diretório de dados no host; (b) `compose down --volumes` (a operação
       historicamente destrutiva); (c) `compose up -d db web` (sem --build — reusa a imagem);
       (d) `aguardar_web`; (e) `compose exec -T web python manage.py shell -c` com
       `from django.contrib.auth import get_user_model; get_user_model().objects.get(email='nascimento@example.invalid')`
       seguido de `|| falhar 'dados não sobreviveram a docker compose down -v && up -d'`.
       Comentário pt-BR: esta é a prova direta do critério de sucesso 4 do roadmap — o
       superusuário criado antes do down -v continua existindo depois.

    Depois das edições, rodar o tracer completo uma vez (`.template-tests/test_05_nascimento.sh`)
    como regressão real do nascimento com bind mount.
  </action>
  <verify>
    <automated>sh -n .template-tests/test_05_nascimento.sh && grep -q 'postgres:17 rm -rf /alvo/dados' .template-tests/test_05_nascimento.sh && grep -q 'down --volumes' .template-tests/test_05_nascimento.sh && .template-tests/test_05_nascimento.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n` passa (sintaxe POSIX válida)
    - A execução completa do tracer imprime `OK: nascimento completo da cópia Copier passou.` e retorna 0
    - A saída inclui a passagem pela prova de down -v (o superusuário `nascimento@example.invalid` é reencontrado após `down --volumes` + `up -d`)
    - Após a execução, não sobra nenhum diretório `nascimento*` em /tmp (`ls -d /tmp/tmp*/nascimento 2>/dev/null` vazio — a limpeza via container funcionou)
    - A prova de down -v acontece ANTES de `SUCESSO=true` (falha derruba o tracer)
  </acceptance_criteria>
  <done>O tracer prova ponta a ponta que o sistema gerado sobe, funciona, sobrevive a down -v sem perder dados, e se limpa por completo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório Git do sistema gerado | dados do banco e segredos do .env não podem cruzar para o Git |
| .env do operador → Compose | PGDATA_DIR é config de infraestrutura controlada pelo operador (confiável), interpolada pelo Compose — nunca chega ao Django |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | ./dados/pg dentro do repo do sistema gerado | mitigate | `/dados/` no `.gitignore.jinja` (gerado) e no `.gitignore` do template (D-74) |
| T-06-02 | Information Disclosure | sistema gerado sem `.gitignore` commitaria `.env` inteiro | mitigate | `.gitignore.jinja` inclui `.env`; correção do `_exclude` do copier.yml (Pitfall 3) |
| T-06-03 | Tampering | PGDATA_DIR malicioso/errado no .env (path traversal para diretório sensível) | accept | config de operador no próprio host — mesmo modelo de confiança de todo o .env; documentação exige `./` ou caminho absoluto de subdiretório |
| T-06-04 | Information Disclosure | diretório de dados legível por outros usuários do host | accept | initdb aplica mode 700 e ownership uid 999 por padrão; documentado como troubleshooting no plano 06-03 |
| T-06-SC | Tampering | supply chain npm/pip/cargo | accept | nenhum pacote novo instalado nesta fase (auditoria do RESEARCH.md: n/a) |
</threat_model>

<verification>
- `python3 -m unittest discover -s .template-tests -p 'test_06_persistencia.py'` OK
- `python3 -m unittest discover -s .template-tests -p 'test_04_05_backup.py'` OK
- `.template-tests/test_05_nascimento.sh` termina com `OK: nascimento completo da cópia Copier passou.`
- `git diff compose.yml.jinja` mostra mudança restrita ao serviço db e à remoção do bloco de topo
</verification>

<success_criteria>
- Critério 4 do roadmap provado por teste automatizado: down -v + up -d não perde o superusuário criado antes
- Sistema gerado nasce com `.gitignore` protegendo `.env` e `/dados/`
- Nenhuma variável Copier nova; healthcheck e serviços web/backup intactos (D-76)
- Tracer limpa /tmp por completo mesmo com arquivos uid 999
</success_criteria>

<output>
Create `.planning/phases/06-customiza-o-visual-e-persist-ncia-de-dados/06-01-SUMMARY.md` when done
</output>
