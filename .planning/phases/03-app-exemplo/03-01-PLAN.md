---
phase: 03-app-exemplo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/static/vendor/echarts.min.js
  - core/templatetags/__init__.py
  - core/templatetags/formatos.py
  - tailwind.config.js
  - Dockerfile
  - config/settings/base.py
  - apps/exemplo/__init__.py
  - apps/exemplo/apps.py
  - apps/exemplo/models.py
  - apps/exemplo/admin.py
  - apps/exemplo/migrations/__init__.py
  - apps/exemplo/migrations/0001_initial.py
  - apps/exemplo/management/__init__.py
  - apps/exemplo/management/commands/__init__.py
  - apps/exemplo/management/commands/seed_exemplo.py
  - apps/exemplo/tests/__init__.py
  - apps/exemplo/tests/test_models.py
autonomous: true
requirements:
  - EX-01
  - EX-04

must_haves:
  truths:
    - "Modelo ItemExemplo está registrado e integrado com simple-history para auditoria automática (HistoricalRecords)"
    - "Vendor asset Apache ECharts 5.x está instalado localmente em core/static/vendor/echarts.min.js sem dependência externa"
    - "Filtros de formatação de moeda pt-BR (moeda e moeda_curta) estão disponíveis em core/templatetags/formatos.py"
    - "Configuração do Tailwind e Dockerfile incluem templates sob apps/ para compilação JIT de estilos no build"
    - "Comando seed_exemplo popula o banco de dados com itens realistas e suporta flag --limpar"
  artifacts:
    - path: "apps/exemplo/models.py"
      provides: "Modelo ItemExemplo com escolhas CategoriaChoices e StatusChoices, campos monetários e HistoricalRecords"
      contains: "class ItemExemplo"
    - path: "apps/exemplo/admin.py"
      provides: "Registro do modelo no admin via SimpleHistoryAdmin"
      contains: "SimpleHistoryAdmin"
    - path: "apps/exemplo/migrations/0001_initial.py"
      provides: "Migração inicial do app exemplo e da tabela histórica"
    - path: "core/templatetags/formatos.py"
      provides: "Filtros de formatação monetária pt-BR"
      contains: "def moeda"
    - path: "core/static/vendor/echarts.min.js"
      provides: "Bundle local minificado do Apache ECharts 5.x"
    - path: "apps/exemplo/management/commands/seed_exemplo.py"
      provides: "Comando de seed para popular dados de demonstração"
      contains: "class Command"
  key_links:
    - from: "config/settings/base.py"
      to: "apps.exemplo.apps.ExemploConfig"
      via: "INSTALLED_APPS list"
      pattern: "apps\\.exemplo\\.apps\\.ExemploConfig"
    - from: "apps/exemplo/models.py"
      to: "simple_history.models.HistoricalRecords"
      via: "history field"
      pattern: "history = HistoricalRecords"
---

<objective>
Configurar a fundação de dados e infraestrutura do app de referência `apps/exemplo`:
1. Instalar os assets locais (Apache ECharts) e tags de formatação monetária pt-BR (`moeda`, `moeda_curta`).
2. Atualizar a compilação JIT do Tailwind e o Dockerfile para incluir os templates do diretório `apps/`.
3. Criar a estrutura modular de `apps/exemplo`, o modelo `ItemExemplo` com escolhas tipadas e auditoria `HistoricalRecords`, registrar no Django Admin com `SimpleHistoryAdmin` e adicionar aos `INSTALLED_APPS`.
4. Gerar e aplicar a migração inicial `0001_initial` e implementar o comando de seed idempotente `seed_exemplo`.

Purpose: Estabelecer a camada de dados persistente, schema auditado e assets necessários para o CRUD e o Dashboard das etapas seguintes, mantendo o isolamento estrito de domínio (D-24, D-32, D-34, D-36).
Output: Modelo `ItemExemplo` persistido no PostgreSQL 17, migrations geradas, comando de seed operacional e vendor assets prontos.
</objective>

<execution_context>
@/home/ToNiauM/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ToNiauM/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-app-exemplo/03-CONTEXT.md
@.planning/phases/03-app-exemplo/03-RESEARCH.md
@.planning/phases/03-app-exemplo/03-PATTERNS.md
@config/settings/base.py
@tailwind.config.js
@Dockerfile
</context>

<interfaces>
<!-- Interfaces e contratos que este plano define ou utiliza -->

From core/models.py:
```python
class Usuario(AbstractUser):
    email = models.EmailField(_("endereço de e-mail"), unique=True)
```

From simple_history/models.py:
```python
class HistoricalRecords:
    def __init__(self, verbose_name=None, bases=(models.Model,), ...): ...
```

From simple_history/admin.py:
```python
class SimpleHistoryAdmin(admin.ModelAdmin): ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar assets vendor ECharts, filtros de moeda e configurar build do Tailwind</name>
  <read_first>
    - .planning/phases/03-app-exemplo/03-RESEARCH.md (Seções "D-32", "Pitfall 1", "Code Examples 3")
    - .planning/phases/03-app-exemplo/03-PATTERNS.md (Seções 7 e 8)
    - tailwind.config.js
    - Dockerfile
  </read_first>
  <files>
    core/static/vendor/echarts.min.js,
    core/templatetags/__init__.py,
    core/templatetags/formatos.py,
    tailwind.config.js,
    Dockerfile
  </files>
  <action>
    1. Copiar o arquivo vendor `echarts.min.js` de `/opt/web/pca/core/static/vendor/echarts.min.js` para `core/static/vendor/echarts.min.js` (eliminando dependência externa de CDN per D-32).
    2. Criar `core/templatetags/__init__.py` e `core/templatetags/formatos.py` com os filtros `moeda` e `moeda_curta` para formatação monetária no padrão pt-BR (ex.: `1.234,56` e `1,5 mi`), tratando nulos, tipos numéricos inválidos e valores negativos per D-30 e Research Code Example 3.
    3. Atualizar `tailwind.config.js` na chave `content` para incluir `./apps/**/*.html` além de `./core/templates/**/*.html`, garantindo que classes utilitárias usadas nos templates de `apps/exemplo` sejam compiladas pelo JIT do Tailwind per Pitfall 1.
    4. Atualizar o estágio `assets` do `Dockerfile` adicionando a cópia dos templates do diretório `apps/` (`COPY apps ./apps` ou cópia similar dos templates) antes da execução do CLI do Tailwind, para evitar preflight CSS vazio em builds de produção Docker.
  </action>
  <acceptance_criteria>
    - `core/static/vendor/echarts.min.js` existe e possui tamanho superior a 500 KB.
    - `core/templatetags/formatos.py` exporta os filtros `@register.filter(name="moeda")` e `@register.filter(name="moeda_curta")`.
    - `tailwind.config.js` contém `"./apps/**/*.html"` no array `content`.
    - `Dockerfile` no estágio `assets` copia os templates de `apps`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web -c "import os; assert os.path.exists('core/static/vendor/echarts.min.js'); assert os.path.getsize('core/static/vendor/echarts.min.js') > 500000; from core.templatetags.formatos import moeda, moeda_curta; assert moeda('1234.56') == '1.234,56'; assert moeda_curta('1500000') == '1,5 mi'; print('OK')"</automated>
  </verify>
  <done>
    Assets locais do ECharts instalados, filtros de moeda pt-BR testados com sucesso e configuração JIT do Tailwind expandida para o diretório apps/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar modelo ItemExemplo com auditoria simple-history e registro no admin</name>
  <read_first>
    - .planning/phases/03-app-exemplo/03-CONTEXT.md (Decisão D-24)
    - .planning/phases/03-app-exemplo/03-RESEARCH.md (Seções "D-24", "D-33", "Code Examples 1")
    - .planning/phases/03-app-exemplo/03-PATTERNS.md (Seções 1 e 4)
    - config/settings/base.py
    - core/admin.py
  </read_first>
  <files>
    apps/exemplo/__init__.py,
    apps/exemplo/apps.py,
    apps/exemplo/models.py,
    apps/exemplo/admin.py,
    config/settings/base.py
  </files>
  <action>
    1. Criar o diretório `apps/exemplo/` e os arquivos `apps/exemplo/__init__.py` e `apps/exemplo/apps.py` definindo `ExemploConfig` com `name = "apps.exemplo"`, `verbose_name = "Exemplo"`, `default_auto_field = "django.db.models.BigAutoField"`.
    2. Em `config/settings/base.py`, adicionar `"apps.exemplo.apps.ExemploConfig"` à lista `INSTALLED_APPS` (ponto de integração 1 de 3 per D-34).
    3. Em `apps/exemplo/models.py`, implementar o modelo `ItemExemplo` com:
       - `CategoriaChoices` (TextChoices: `OPERACIONAL="OPERACIONAL"`, `ESTRATEGICO="ESTRATEGICO"`, `ADMINISTRATIVO="ADMINISTRATIVO"`, `FINANCEIRO="FINANCEIRO"`).
       - `StatusChoices` (TextChoices: `RASCUNHO="RASCUNHO"`, `EM_ANDAMENTO="EM_ANDAMENTO"`, `CONCLUIDO="CONCLUIDO"`, `CANCELADO="CANCELADO"`).
       - Campos: `titulo` (CharField 200), `descricao` (TextField blank), `categoria` (CharField 30 choices default OPERACIONAL), `status` (CharField 20 choices default RASCUNHO), `valor` (DecimalField 12,2 default 0.00 com MinValueValidator 0.00), `prazo` (DateField null blank), `ativo` (BooleanField default True), `criado_em` (DateTimeField auto_now_add), `atualizado_em` (DateTimeField auto_now), `criado_por` (ForeignKey settings.AUTH_USER_MODEL on_delete SET_NULL null blank related_name "itens_exemplo").
       - `history = HistoricalRecords()` para rastreamento de auditoria automático per D-23/D-24.
       - `Meta` com `ordering = ["-criado_em"]`, `verbose_name = "item de exemplo"`, `verbose_name_plural = "itens de exemplo"`.
       - Método `__str__` retornando `f"{self.titulo} ({self.get_status_display()})"`.
    4. Em `apps/exemplo/admin.py`, registrar `ItemExemplo` usando `SimpleHistoryAdmin` com `list_display = ("titulo", "categoria", "status", "valor", "prazo", "ativo", "criado_por", "criado_em")`, `list_filter = ("categoria", "status", "ativo")`, `search_fields = ("titulo", "descricao")`, `ordering = ("-criado_em",)`.
  </action>
  <acceptance_criteria>
    - `apps/exemplo/apps.py` define `ExemploConfig` com `name = "apps.exemplo"`.
    - `config/settings/base.py` contém `"apps.exemplo.apps.ExemploConfig"` em `INSTALLED_APPS`.
    - `apps/exemplo/models.py` define `ItemExemplo` com `history = HistoricalRecords()` e escolhas `CategoriaChoices` e `StatusChoices`.
    - `apps/exemplo/admin.py` herda de `SimpleHistoryAdmin`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py check</automated>
  </verify>
  <done>
    Modelo ItemExemplo declarado com choices, validações, auditoria HistoricalRecords e registrado no AdminSite customizado do projeto.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gerar e executar migrações, criar comando seed_exemplo e suite de testes de modelo</name>
  <read_first>
    - .planning/phases/03-app-exemplo/03-CONTEXT.md (Decisão D-36)
    - .planning/phases/03-app-exemplo/03-RESEARCH.md (Seções "D-36", "Schema Migrations Required")
    - .planning/phases/03-app-exemplo/03-PATTERNS.md (Seções 1 e 5)
    - core/tests/test_auditoria.py
  </read_first>
  <files>
    apps/exemplo/migrations/__init__.py,
    apps/exemplo/migrations/0001_initial.py,
    apps/exemplo/management/__init__.py,
    apps/exemplo/management/commands/__init__.py,
    apps/exemplo/management/commands/seed_exemplo.py,
    apps/exemplo/tests/__init__.py,
    apps/exemplo/tests/test_models.py
  </files>
  <action>
    1. Executar `docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py makemigrations exemplo` para gerar `apps/exemplo/migrations/0001_initial.py` no host (criando as tabelas `exemplo_itemexemplo` e `exemplo_historicalitemexemplo`).
    2. Reconstruir a imagem Docker e reiniciar containers: `docker compose up -d --build`.
    3. Executar `docker compose exec -T web python manage.py migrate` para aplicar a migração no PostgreSQL.
    4. Criar `apps/exemplo/management/commands/seed_exemplo.py` implementando comando de management com suporte aos argumentos `--limpar` (remove registros de exemplo anteriores) e `--quantidade` (padrão 25). O comando deve criar dados variados e coerentes abrangendo todas as 4 categorias (`OPERACIONAL`, `ESTRATEGICO`, `ADMINISTRATIVO`, `FINANCEIRO`) e todos os 4 status (`RASCUNHO`, `EM_ANDAMENTO`, `CONCLUIDO`, `CANCELADO`), com valores monetários e prazos distribuídos, associando `criado_por` ao primeiro superusuário/usuário existente per D-36.
    5. Criar `apps/exemplo/tests/test_models.py` contendo testes unitários que provam:
       - Criação de `ItemExemplo` grava registro histórico em `HistoricalItemExemplo` com `history_type="+"`.
       - Atualização de `ItemExemplo` grava snapshot histórico com `history_type="~"`.
       - Choices de categoria e status funcionam e exibem labels em português corretamente.
       - Validação do campo `valor` rejeita números negativos.
       - Execução do comando `seed_exemplo` com `--limpar` e `--quantidade` popula o banco sem erros.
  </action>
  <acceptance_criteria>
    - `apps/exemplo/migrations/0001_initial.py` existe e contém a criação de `ItemExemplo` e `HistoricalItemExemplo`.
    - `python manage.py migrate` é executado com sucesso.
    - `python manage.py seed_exemplo --limpar --quantidade 15` executa e popula 15 itens.
    - `apps/exemplo/tests/test_models.py` passa 100% no runner de testes do Django.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm -v "$PWD:/app" --entrypoint python web manage.py makemigrations exemplo && docker compose up -d --build && docker compose exec -T web python manage.py migrate && docker compose exec -T web python manage.py seed_exemplo --limpar --quantidade 20 && docker compose exec -T web python manage.py test apps.exemplo.tests.test_models</automated>
  </verify>
  <done>
    Migrações geradas e aplicadas no PostgreSQL, comando seed_exemplo populando dados de demonstração com sucesso e testes de modelo/auditoria passando.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Admin / Shell | Usuário autenticado enviando dados via formulários ou executando comandos de seed |
| Backend → PostgreSQL | Execução de migrações DDL e inserções DML em tabelas de domínio e auditoria |
| Webpack/Tailwind Build → Static CSS | Compilação JIT de classes de estilo a partir dos arquivos de template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | `core/templatetags/formatos.py` | mitigate | Tratar explicitamente tipos de entrada com conversão via `Decimal(str(valor))` e capturar exceções (`InvalidOperation`, `TypeError`) para evitar injeção ou falhas de formatação |
| T-03-02 | Tampering | `apps/exemplo/models.py` | mitigate | `valor` protegido por `MinValueValidator(Decimal("0.00"))` e validações nos choices para impedir estados numéricos corrompidos no banco |
| T-03-03 | Repudiation | `apps/exemplo/models.py` | mitigate | Integração com `HistoricalRecords()` captura snapshots automáticos e chave de rastreabilidade `criado_por` em cada mutação (D-24, D-23) |
| T-03-04 | Information Disclosure | `apps/exemplo/management/commands/seed_exemplo.py` | mitigate | Comando gera apenas dados fictícios de demonstração e associa a usuários locais sem expor segredos ou credenciais no stdout |
| T-03-SC | Supply Chain | `core/static/vendor/echarts.min.js` | mitigate | Cópia local de vendor verificado da PCA sem chamadas de rede externas ou dependência de CDN em runtime (D-32) |
</threat_model>

<verification>
Executar a verificação integrada de infraestrutura e dados:
1. `docker compose exec -T web python manage.py check`
2. `docker compose exec -T web python manage.py test apps.exemplo.tests.test_models`
</verification>

<success_criteria>
1. `ItemExemplo` criado, migrado e auditado via `django-simple-history`.
2. Assets do ECharts e tags de formatação monetária instalados e validados.
3. Comando `seed_exemplo` funcional e testado.
4. Suíte `test_models.py` passando com 0 erros.
</success_criteria>

<output>
Criar `.planning/phases/03-app-exemplo/03-01-SUMMARY.md` ao concluir a execução.
</output>
