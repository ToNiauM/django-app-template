---
phase: 04-templatiza-o-copier
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - copier.yml
  - .copier-answers.yml.jinja
  - .env.example
  - .env.example.jinja
  - .gitignore
  - README.md
  - README.md.jinja
autonomous: true
requirements:
  - TPL-01
  - TPL-02

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Após a aprovação do Plano 04-01, o operador executa copier copy, responde oito perguntas e recebe um projeto Django completo com .copier-answers.yml."
    - "Respostas textuais obrigatórias vazias são rejeitadas antes da renderização; slug, banco, hostname, porta e cor obedecem aos formatos definidos em D-42..D-47."
    - "O destino recebe valores de identidade e conexão em .env.example sem gravar SECRET_KEY, senha PostgreSQL ou credenciais R2 nas respostas Copier (D-48, D-49)."
    - "O projeto gerado não contém .planning, documentos de desenvolvimento, README do template, caches ou a venv usada pelo CLI (D-38, D-39)."
  artifacts:
    - path: "copier.yml"
      provides: "Contrato in-place do template, perguntas, validators, StrictUndefined e exclusões"
      contains: "_min_copier_version"
    - path: ".copier-answers.yml.jinja"
      provides: "Persistência oficial de _copier_answers para updates"
      contains: "_copier_answers"
    - path: ".env.example.jinja"
      provides: "Defaults renderizados de identidade, banco, hostname e porta; segredos permanecem placeholders"
    - path: "README.md"
      provides: "Manual do repositório-template e ritual Git/Copier"
    - path: "README.md.jinja"
      provides: "README operacional do sistema derivado"
  key_links:
    - from: "copier.yml"
      to: ".env.example.jinja"
      via: "sistema_nome, sistema_sigla, cor_primaria, sistema_slug, sistema_banco, sistema_hostname e sistema_porta"
      pattern: "sistema_(nome|slug|hostname|porta|banco|sigla)"
    - from: ".copier-answers.yml.jinja"
      to: "copier update"
      via: "metadados _src_path e _commit mantidos pelo Copier"
      pattern: "_copier_answers"
---

## Phase Goal

**As a** operador de sistemas, **I want to** gerar e atualizar sistemas Django parametrizados com Copier, **so that** cada sistema derivado permaneça autocontido, portátil e sincronizável com o núcleo.

<objective>
Entregar o primeiro caminho vertical de produção do template: instalação isolada do CLI previamente aprovado, configuração Copier in-place, arquivo oficial de respostas, defaults renderizados sem segredos e dois READMEs com papéis separados.

Purpose: Provar cedo a arquitetura `template raiz → perguntas/validators → projeto gerado → respostas versionáveis`, antes de expandir opcionalidade e operação (TPL-01, TPL-02, D-37..D-50).
Output: Um `copier copy` real gera um projeto completo, autocontido e livre de artefatos de planejamento/desenvolvimento.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-RESEARCH.md
@.planning/phases/04-templatiza-o-copier/04-PATTERNS.md
@.planning/phases/04-templatiza-o-copier/04-01-SUMMARY.md
@.env.example
@.gitignore
</context>

## Artifacts this phase produces

- Arquivos novos: `copier.yml`, `.copier-answers.yml.jinja`, `.env.example.jinja`, `README.md`, `README.md.jinja`.
- Arquivo removido após migração: `.env.example` (substituído pelo par `.jinja`).
- Ambiente local não versionado: `.venv-template/`, contendo o comando `.venv-template/bin/copier` versão 9.17.1.
- Chaves Copier: `_min_copier_version`, `_envops.undefined`, `_templates_suffix`, `_exclude` e `_answers_file` se necessário para fixar `.copier-answers.yml`.
- Variáveis de template: `sistema_nome`, `sistema_slug`, `sistema_hostname`, `sistema_porta`, `sistema_banco`, `sistema_sigla`, `cor_primaria`, `incluir_app_exemplo`.
- Campos gerados em `.env.example`: `SISTEMA_NOME`, `SISTEMA_SIGLA`, `COR_PRIMARIA`, `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `DATABASE_URL`, `WEB_BIND_ADDRESS`, `WEB_PORT`, `ALLOWED_HOSTS`, `CSRF_TRUSTED_ORIGINS`.
- Comandos documentados: `copier copy`, `git init`, primeiro `git add/commit`, `copier update`, resolução de conflitos e ritual de tag semver.

<must_haves>
- O tracer usa o Copier real aprovado e deixa a configuração como parte permanente do template, não como experimento descartável.
- `.jinja` aparece somente nos arquivos que interpolam respostas; arquivos estáticos continuam byte a byte estáveis para reduzir conflitos de update (D-37, D-50).
- A cópia persiste `_copier_answers` sem escritor YAML próprio e sem pós-geração automatizada (D-40).
- O README do template é excluído; o README renderizado do sistema leva a identidade respondida (D-39).
</must_haves>

<tasks>

<task type="tracer">
  <name>Task 1: Provar copier copy da pergunta ao projeto gerado</name>
  <reversibility rating="costly">Os nomes das respostas entram em repositórios derivados; renomeá-los depois exige atualização coordenada dos arquivos de respostas.</reversibility>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-01-SUMMARY.md (aprovação explícita de copier==9.17.1)
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-37, D-38, D-40, D-41, D-42, D-43, D-44, D-46, D-47, D-48, D-49, D-50)
    - .planning/phases/04-templatiza-o-copier/04-RESEARCH.md (seções "copier.yml prescritivo", "Auditoria de Legitimidade de Pacotes" e "Fluxo de geração e atualização")
    - .planning/phases/04-templatiza-o-copier/04-PATTERNS.md (seções `copier.yml`, `.copier-answers.yml.jinja` e `.env.example.jinja`)
    - .env.example
    - .gitignore
    - copier.yml (novo; confirmar que não existe antes de criar)
    - .copier-answers.yml.jinja (novo; confirmar que não existe antes de criar)
    - .env.example.jinja (novo; destino da migração de `.env.example`)
  </read_first>
  <files>
    copier.yml,
    .copier-answers.yml.jinja,
    .env.example,
    .env.example.jinja,
    .gitignore
  </files>
  <action>
    1. Confirmar que `04-01-SUMMARY.md` registra `aprovado copier==9.17.1`; então criar `.venv-template` com `python -m venv`, instalar exatamente `copier==9.17.1` e registrar `.venv-template/` no `.gitignore`. Não tocar `requirements.txt` nem o Python global.
    2. Criar `copier.yml` na raiz per D-37 com `_min_copier_version: "9.17.1"`, `jinja2.StrictUndefined`, sufixo `.jinja` e lista `_exclude` completa. Repetir exclusões padrão necessárias e incluir `.git`, `copier.yml`, `.planning`, `CLAUDE.md`, `IDEIA.md`, `REVIEW.md`, o `README.md` do template, `.env`, `.venv*`, `.template-tests`, caches Python, `staticfiles`, `core/static/dist`, artefatos de build/editor. Não declarar tasks, migrations ou extensões Jinja per D-40.
    3. Declarar exatamente oito respostas: `sistema_nome` (string não vazia), `sistema_slug` (default derivado do nome e validator `^[a-z0-9]+$` per D-41/D-43), `sistema_hostname` (default baseado no slug, DNS completo sem esquema/caminho/porta per D-44), `sistema_porta` (int, default 8000, intervalo 1024–65535), `sistema_banco` (default slug e formato PostgreSQL seguro), `sistema_sigla` (default das iniciais, não vazia per D-46), `cor_primaria` (default `#1e40af`, validator `^#[0-9a-fA-F]{6}$` per D-47) e `incluir_app_exemplo` (bool, default true per D-54). Validators devem emitir mensagem apenas no caso inválido e rejeitar vazio/nulo quando a resposta for obrigatória, per D-42.
    4. Criar `.copier-answers.yml.jinja` usando `_copier_answers` para que `_src_path`, `_commit` e respostas sejam mantidos pelo próprio Copier. Não criar um script escritor de YAML e não orientar edição manual desse arquivo.
    5. Migrar `.env.example` para `.env.example.jinja`. Renderizar os defaults não sensíveis per D-49: identidade, banco/usuário/URL pelo slug/banco, porta, hostname em `ALLOWED_HOSTS` e origem HTTPS em `CSRF_TRUSTED_ORIGINS`; manter `WEB_BIND_ADDRESS=127.0.0.1`. Preservar textos substituíveis para `SECRET_KEY`, `POSTGRES_PASSWORD` e credenciais R2, sem criar perguntas Copier para eles per D-48.
    6. Executar um `copier copy` real para diretório criado por `mktemp -d`, fornecendo dados explícitos que diferem do sistema-modelo. Provar a travessia completa: perguntas → renderização → `.copier-answers.yml` → `.env.example` parametrizado → projeto Django completo. Limpar somente o diretório temporário criado pelo teste.
  </action>
  <acceptance_criteria>
    - `.venv-template/bin/copier --version` informa 9.17.1 e `requirements.txt` permanece sem Copier.
    - `copier.yml` contém as oito perguntas, seus defaults/validators, StrictUndefined e exclusões; não contém chave de automação pós-geração.
    - A cópia de teste contém `manage.py`, `config/`, `core/`, `Dockerfile`, `compose.yml`, `.copier-answers.yml` e `.env.example`.
    - `.copier-answers.yml` não contém nomes de variáveis de segredo nem seus valores; `.env.example` conserva somente textos substituíveis para segredos.
    - Respostas vazias para campos obrigatórios, slug com separador, porta fora do intervalo e cor fora de `#RRGGBB` falham antes de criar um destino utilizável.
    - `.planning`, `IDEIA.md`, `REVIEW.md`, `CLAUDE.md`, `.venv-template` e o README do template não aparecem no destino.
  </acceptance_criteria>
  <verify>
    <automated>.venv-template/bin/copier --version &amp;&amp; DESTINO_TESTE=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DESTINO_TESTE"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Aurora' --data sistema_slug=aurora --data sistema_hostname=aurora.exemplo.gov.br --data sistema_porta=8123 --data sistema_banco=aurora --data sistema_sigla=SA --data cor_primaria='#0f766e' --data incluir_app_exemplo=true . "$DESTINO_TESTE/gerado" &amp;&amp; test -f "$DESTINO_TESTE/gerado/.copier-answers.yml" &amp;&amp; test -f "$DESTINO_TESTE/gerado/.env.example" &amp;&amp; test -f "$DESTINO_TESTE/gerado/manage.py" &amp;&amp; test ! -e "$DESTINO_TESTE/gerado/.planning"</automated>
  </verify>
  <done>Um projeto Django completo é gerado de ponta a ponta pelo Copier real, com respostas persistidas, defaults renderizados e exclusões aplicadas.</done>
</task>

<task type="auto">
  <name>Task 2: Separar documentação do template e do sistema gerado</name>
  <reversibility rating="reversible">Os READMEs são documentação versionada e podem evoluir sem migrar dados ou contratos de runtime.</reversibility>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-39, D-40, D-45, D-48, D-52, D-53, D-55, D-56)
    - .planning/phases/04-templatiza-o-copier/04-RESEARCH.md (seções "copier update e arquivos opcionais", "Não Construir Manualmente" e "Validação da Fase")
    - .planning/phases/04-templatiza-o-copier/04-PATTERNS.md (seção sem análogo local para os READMEs)
    - copier.yml
    - README.md (novo; confirmar ausência antes de criar)
    - README.md.jinja (novo; confirmar ausência antes de criar)
    - apps/exemplo/README.md (fonte de convenções para a topologia condicional do Plano 04-04)
    - ops/gerar_icones_pwa.py (comando opcional documentado per D-52)
  </read_first>
  <files>
    copier.yml,
    README.md,
    README.md.jinja
  </files>
  <action>
    1. Criar o `README.md` do repositório-template per D-39 e mantê-lo em `_exclude`. Documentar instalação isolada da versão aprovada, `copier copy`, preenchimento de `.env`, `git init`, primeiro commit incluindo `.copier-answers.yml`, tags semver e release consciente per D-40/D-53. Incluir a tabela de alocação de portas da família como convenção informativa, mantendo 8000 como default livremente sobrescrevível per D-45.
    2. Documentar que respostas não guardam segredos; ensinar a gerar `SECRET_KEY` com `python -c "import secrets; print(secrets.token_urlsafe(50))"` e a preencher senha PostgreSQL/R2 diretamente no `.env`, per D-48.
    3. Documentar `copier update` somente sobre Git limpo, mudança de respostas via `--data`, resolução dos conflitos inline, testes e commit; incluir o roteiro A→B e referenciar `.template-tests/test_copier_update.sh` como comando contratual do Plano 04-07, per D-55/D-56.
    4. Criar `README.md.jinja` para o sistema gerado, interpolando `sistema_nome`, `sistema_sigla`, `sistema_hostname` e `sistema_porta`. Cobrir operação diária, `.env`, Docker Compose, convenções em `apps/`, caminho de remoção do exemplo por `copier update --data incluir_app_exemplo=false` e regeneração opcional de ícones com `ops/gerar_icones_pwa.py`, per D-39/D-52/D-54. Não transportar nomes de sistemas reais nem documentação de planejamento.
    5. Repetir a cópia tracer e confirmar que o destino tem somente o README renderizado do sistema, com o nome respondido, enquanto o README de instruções do template não atravessa a fronteira.
  </action>
  <acceptance_criteria>
    - `README.md` diferencia template-fonte de projeto gerado e contém os passos Git necessários ao update.
    - `README.md` documenta versão/tag, conflitos, geração de segredo, tabela de portas e o roteiro de prova A→B.
    - `README.md.jinja` usa as respostas de identidade e não inclui identidade de um sistema real.
    - Uma cópia com `sistema_nome='Sistema Aurora'` gera `README.md` contendo esse nome e nenhuma instrução interna de desenvolvimento do template.
    - O fluxo documentado não executa tarefa automática depois da cópia.
  </acceptance_criteria>
  <verify>
    <automated>DESTINO_DOC=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DESTINO_DOC"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Aurora' --data sistema_slug=aurora --data sistema_hostname=aurora.exemplo.gov.br --data sistema_porta=8123 --data sistema_banco=aurora --data sistema_sigla=SA --data cor_primaria='#0f766e' --data incluir_app_exemplo=true . "$DESTINO_DOC/gerado" &amp;&amp; test -f "$DESTINO_DOC/gerado/README.md" &amp;&amp; grep -Fq 'Sistema Aurora' "$DESTINO_DOC/gerado/README.md"</automated>
  </verify>
  <done>Template e sistema gerado possuem READMEs distintos, e o fluxo manual de nascimento/update fica explícito sem segredo ou automação oculta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Respostas do operador → renderização Jinja | Entrada não confiável passa a nomes, configuração e documentação geradas |
| Repositório-template → diretório gerado | Arquivos internos podem vazar para um projeto derivado |
| PyPI → `.venv-template` | Código externo aprovado é instalado e executado localmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | validators de `copier.yml` | high | mitigate | StrictUndefined e validators bloqueiam slug/banco/hostname/porta/cor inválidos antes da renderização; nome e sigla rejeitam vazio |
| T-04-03 | Information Disclosure | `.copier-answers.yml` | high | mitigate | D-48 exclui todas as credenciais das perguntas; respostas guardam somente identidade/configuração pública e o teste inspeciona o arquivo gerado |
| T-04-04 | Information Disclosure | `_exclude` | medium | mitigate | Lista explícita impede `.planning`, documentos internos, `.env`, caches e venv no destino; cópia tracer comprova a fronteira |
| T-04-05 | Repudiation | `copier update` | medium | mitigate | README exige `.copier-answers.yml` e Git limpo/commitado, preservando origem, tag e diff auditável |
| T-04-SC | Tampering | instalação pip | high | mitigate | Instalar somente `copier==9.17.1` aprovado no Plano 04-01, em `.venv-template`, sem dependência global ou de runtime |

## Cobertura OWASP ASVS L1 aplicável

| Controle aplicável | Evidência executável obrigatória |
|---|---|
| Segredos fora das respostas Copier e valores não utilizáveis no `.env.example` (D-48) | Na cópia real da Task 1, `! grep -Eq 'SECRET_KEY|POSTGRES_PASSWORD|R2_ACCESS_KEY_ID|R2_SECRET_ACCESS_KEY' "$DESTINO_TESTE/gerado/.copier-answers.yml"` deve passar; `grep -Fxq 'SECRET_KEY=replace-with-a-long-random-secret' "$DESTINO_TESTE/gerado/.env.example" && grep -Fxq 'POSTGRES_PASSWORD=replace-with-a-database-password' "$DESTINO_TESTE/gerado/.env.example"` prova os sentinelas, sem comparar ou imprimir credenciais. |
| Validação estrita de entrada antes da renderização (D-42, D-43, D-47) | A Task 1 deve executar as rejeições já declaradas para slug com separador, porta fora de 1024–65535 e cor fora de `#RRGGBB`; a definição é auditada por `grep -Fq '^[a-z0-9]+$' copier.yml && grep -Fq '1024' copier.yml && grep -Fq '65535' copier.yml && grep -Fq '^#[0-9a-fA-F]{6}$' copier.yml`, e cada caso inválido deve retornar não zero sem destino utilizável. |
| Configuração segura do renderizador e minimização da superfície executável | `grep -Fq 'jinja2.StrictUndefined' copier.yml` deve passar e `! rg -n '^_tasks:|^_migrations:|^_jinja_extensions:' copier.yml` comprova que o template não introduz automação/extensão executável pós-cópia. |
</threat_model>

<verification>
1. `.venv-template/bin/copier --version` retorna 9.17.1.
2. Uma cópia com valores não-default produz projeto completo e `.copier-answers.yml` versionável.
3. Casos inválidos de vazio, slug, porta e cor falham no prompt.
4. Artefatos internos e segredos não atravessam para o destino.
5. O README do destino é o renderizado, não o README do template.
6. Executar as evidências da matriz ASVS L1 sobre a mesma cópia temporária da Task 1, antes de o trap removê-la.
</verification>

<success_criteria>
1. `copier copy` real entrega um sistema completo com perguntas e defaults derivados.
2. Identidade, conexão e hostname aparecem em `.env.example`; segredos continuam fora das respostas.
3. `_exclude` preserva a separação entre desenvolvimento do template e repositório gerado.
4. Operador dispõe do fluxo Git necessário para updates futuros, sem `_tasks`.
</success_criteria>

<output>
Criar `.planning/phases/04-templatiza-o-copier/04-02-SUMMARY.md` ao concluir a execução.
</output>
