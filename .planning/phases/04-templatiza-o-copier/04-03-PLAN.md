---
phase: 04-templatiza-o-copier
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tailwind.config.js
  - tailwind.config.js.jinja
  - config/settings/base.py
  - config/settings/base.py.jinja
  - apps/exemplo/templates/exemplo/dashboard.html
  - ops/gerar_icones_pwa.py
  - entrypoint.sh
  - apps/exemplo/management/commands/seed_exemplo.py
  - apps/exemplo/README.md
autonomous: true
requirements: [TPL-02, TPL-04]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Nome, sigla e cor consumidos em runtime vêm obrigatoriamente do .env; a cor interpolada por Jinja existe somente no build Tailwind (D-47, D-50)."
    - "entrypoint.sh mantém 0.0.0.0:8000 como contrato interno neutro do container; WEB_PORT configura exclusivamente a publicação loopback e o proxy no host (D-50)."
    - "O seed do app exemplo permanece código Python estático, genérico e sem fallback de identidade; qualquer nome exibido em runtime deriva do setting obrigatório (D-50)."
    - "Uma cópia real não contém as formas literal snake_case nem legível da identidade antiga em nenhum arquivo textual da árvore gerada (TPL-02, TPL-04, D-50)."
  artifacts:
    - path: "tailwind.config.js.jinja"
      provides: "Cor de marca resolvida em build sem duplicar identidade no runtime"
    - path: "config/settings/base.py.jinja"
      provides: "SISTEMA_NOME, SISTEMA_SIGLA e COR_PRIMARIA obrigatórios via .env"
    - path: "entrypoint.sh"
      provides: "Gunicorn no contrato interno neutro, desacoplado de WEB_PORT"
    - path: "apps/exemplo/management/commands/seed_exemplo.py"
      provides: "Dados de demonstração neutros e reutilizáveis"
  key_links:
    - from: ".env.example.jinja"
      to: "config/settings/base.py.jinja"
      via: "SISTEMA_NOME, SISTEMA_SIGLA e COR_PRIMARIA obrigatórios"
      pattern: "env\\(\"SISTEMA_(NOME|SIGLA)\"\\)"
    - from: ".env.example.jinja"
      to: "ops/gerar_icones_pwa.py"
      via: "COR_PRIMARIA e SISTEMA_SIGLA carregados do ambiente sem defaults de marca"
      pattern: "COR_PRIMARIA|SISTEMA_SIGLA"
---

## Phase Goal

**As a** operador de sistemas, **I want to** gerar e atualizar sistemas Django parametrizados com Copier, **so that** cada sistema derivado permaneça autocontido, portátil e sincronizável com o núcleo.

<objective>
Neutralizar toda identidade de runtime pelo princípio `.env primeiro, .jinja mínimo`, incluindo os pontos obrigatórios `entrypoint.sh` e `seed_exemplo.py` (D-47..D-50, D-52).

Purpose: Cumprir TPL-02/TPL-04 sobre uma cópia real antes de introduzir a topologia condicional do app exemplo.
Output: Runtime/build sem identidade antiga, scripts neutros e uma prova de árvore gerada que rejeita as duas formas literais proibidas.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-RESEARCH.md
@.planning/phases/04-templatiza-o-copier/04-PATTERNS.md
@.planning/phases/04-templatiza-o-copier/04-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `tailwind.config.js.jinja` com uma única interpolação de cor de build.
- `config/settings/base.py.jinja` com identidade obrigatória via `.env` e a validação runtime de `#RRGGBB` preservada.
- `entrypoint.sh` estático com porta interna neutra e sem consumo incorreto de `WEB_PORT`.
- `seed_exemplo.py`, dashboard, gerador de ícones e README do app sem identidade antiga ou fallback de marca.
- Evidência de `copier copy` cujo destino textual inteiro passa pela auditoria de identidade.

<must_haves>
- Arquivos de código permanecem estáticos quando o `.env` alcança o valor; Jinja fica restrito aos touchpoints inevitáveis (D-50).
- A porta respondida não muda a porta interna do container: Compose publica `127.0.0.1:${WEB_PORT}:8000` e Nginx aponta para a porta publicada.
- A auditoria inspeciona somente o destino gerado; `.planning`, README do template e demais fontes permitidas não atravessam `_exclude` (D-38).
- Nenhum controle de auth, middleware, CSRF ou validação de cor é enfraquecido.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrizar identidade obrigatória e cor de build</name>
  <reversibility rating="costly">Retirar defaults afeta boot e build de assets, mas é reversível sem migração de dados.</reversibility>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-47, D-48, D-49, D-50)
    - .planning/phases/04-templatiza-o-copier/04-PATTERNS.md (seções Tailwind, settings e dashboard)
    - tailwind.config.js
    - config/settings/base.py
    - apps/exemplo/templates/exemplo/dashboard.html
    - .env.example.jinja
  </read_first>
  <files>tailwind.config.js, tailwind.config.js.jinja, config/settings/base.py, config/settings/base.py.jinja, apps/exemplo/templates/exemplo/dashboard.html</files>
  <behavior>
    - Cópias com cores distintas geram constantes Tailwind distintas sem alterar Python.
    - Ausência de SISTEMA_NOME, SISTEMA_SIGLA ou COR_PRIMARIA falha explicitamente no boot.
    - COR_PRIMARIA inválida continua levantando ImproperlyConfigured.
  </behavior>
  <action>
    Migrar `tailwind.config.js` para `.jinja` e interpolar somente `const COR_PRIMARIA` per D-47/D-50; manter `misturar`, cores neutras e semânticas byte a byte estáveis. Migrar `config/settings/base.py` para `.jinja`, trocar os três settings de identidade por `env(...)` obrigatório e conservar `re.fullmatch`/`ImproperlyConfigured`; neutralizar comentários/exemplos de identidade sem tocar middleware, auth, CSRF ou segurança. Remover do dashboard o fallback literal da cor e consumir exclusivamente `cor_primaria` fornecida pelo context processor obrigatório.
  </action>
  <acceptance_criteria>
    - Somente `tailwind.config.js.jinja` interpola cor de build.
    - Settings renderizado exige as três variáveis e conserva a barreira `#RRGGBB`.
    - Dashboard não carrega cor default.
    - Duas respostas de cor produzem saídas Tailwind distintas sem diferença no Python renderizado.
  </acceptance_criteria>
  <verify><automated>DEST_IDENT=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DEST_IDENT"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Aurora' --data sistema_slug=aurora --data sistema_hostname=aurora.exemplo.gov.br --data sistema_porta=8123 --data sistema_banco=aurora --data sistema_sigla=SA --data cor_primaria='#0f766e' . "$DEST_IDENT/a" &amp;&amp; grep -Fq '#0f766e' "$DEST_IDENT/a/tailwind.config.js" &amp;&amp; python -m compileall -q "$DEST_IDENT/a/config"</automated></verify>
  <done>Identidade do runtime vem apenas do `.env`, e a única interpolação de marca em código é a constante Tailwind de build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Neutralizar entrypoint, seed, ícones e árvore gerada</name>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-38, D-48, D-50, D-52)
    - .planning/phases/04-templatiza-o-copier/04-PATTERNS.md (seções entrypoint, seed_exemplo e gerar_icones_pwa)
    - entrypoint.sh
    - apps/exemplo/management/commands/seed_exemplo.py
    - apps/exemplo/README.md
    - ops/gerar_icones_pwa.py
    - compose.yml
    - .env.example.jinja
  </read_first>
  <files>entrypoint.sh, apps/exemplo/management/commands/seed_exemplo.py, apps/exemplo/README.md, ops/gerar_icones_pwa.py</files>
  <behavior>
    - WEB_PORT altera somente bind/proxy do host; Gunicorn continua no endpoint interno 0.0.0.0:8000.
    - O seed é executável sem nome de sistema embutido e suas mensagens descrevem somente o app removível.
    - O gerador exige COR_PRIMARIA/SISTEMA_SIGLA do .env quando não recebe argumentos explícitos.
    - A árvore copiada não contém `sistema_base` nem `Sistema Base` em arquivos textuais.
  </behavior>
  <action>
    Aplicar D-50 explicitamente em `entrypoint.sh`: mantê-lo estático, documentar `0.0.0.0:8000` como contrato interno do container e não ler `WEB_PORT`, porque essa resposta controla apenas a publicação loopback do Compose e o `proxy_pass` no host. Neutralizar `seed_exemplo.py` como Python estático: ajustar help/saída para descrever somente dados do app removível; não adicionar default, slug ou nome de sistema e, se uma mensagem precisar do nome em runtime, obtê-lo de `settings.SISTEMA_NOME` sem fallback. Fazer `ops/gerar_icones_pwa.py` carregar `COR_PRIMARIA` e `SISTEMA_SIGLA` do `.env` com biblioteca padrão, preservando argumentos explícitos como override e sem acoplá-lo ao build per D-52. Neutralizar o README do app para que nenhuma identidade de sistema atravesse a cópia. Executar `copier copy` com respostas não-default e varrer todos os arquivos textuais do destino, sem isentar caminhos gerados; os únicos caminhos-fonte que podem conter documentação interna ficam fora do destino por `_exclude` per D-38.
  </action>
  <acceptance_criteria>
    - `entrypoint.sh` usa a porta interna 8000 e não expande `WEB_PORT`.
    - Seed, README do app e gerador não carregam identidade antiga nem defaults de marca.
    - A cópia contém os valores respondidos no `.env.example`, não nos scripts estáticos.
    - A auditoria textual do destino inteiro retorna lista vazia para `sistema_base` e `Sistema Base`.
  </acceptance_criteria>
  <verify><automated>DEST_NEUTRO=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DEST_NEUTRO"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Aurora' --data sistema_slug=aurora --data sistema_hostname=aurora.exemplo.gov.br --data sistema_porta=8123 --data sistema_banco=aurora --data sistema_sigla=SA --data cor_primaria='#0f766e' --data incluir_app_exemplo=true . "$DEST_NEUTRO/gerado" &amp;&amp; python -c 'from pathlib import Path; import sys; r=Path(sys.argv[1]); bad=("sistema_base", "Sistema Base"); hits=[str(p.relative_to(r)) for p in r.rglob("*") if p.is_file() and ".git" not in p.parts and any(x in p.read_text("utf-8", errors="ignore") for x in bad)]; assert not hits, hits' "$DEST_NEUTRO/gerado" &amp;&amp; python -m compileall -q "$DEST_NEUTRO/gerado/config" "$DEST_NEUTRO/gerado/apps" "$DEST_NEUTRO/gerado/ops/gerar_icones_pwa.py"</automated></verify>
  <done>entrypoint e seed estão cobertos por D-50, e uma cópia real rejeita integralmente a identidade antiga na árvore gerada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` gerado → Django/Tailwind/scripts | Identidade configurável chega ao runtime e ao build |
| Porta host → porta interna do container | Compose traduz WEB_PORT para o endpoint fixo do Gunicorn |
| Template-fonte → destino Copier | Exclusões devem impedir que identidade/documentação interna atravessem a geração |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | `COR_PRIMARIA` | high | mitigate | Validator Copier + `re.fullmatch` no boot preservam duas barreiras contra CSS injection |
| T-04-07 | Denial of Service | `entrypoint.sh` | medium | mitigate | Porta interna permanece fixa e desacoplada de WEB_PORT; Compose/healthcheck continuam no mesmo contrato |
| T-04-08 | Information Disclosure | scripts e documentação gerados | high | mitigate | Auditoria textual sobre `copier copy` falha se qualquer forma da identidade antiga existir no destino |
| T-04-SC | Tampering | CLI Copier | high | mitigate | Somente o binário 9.17.1 aprovado no Plano 04-01 e isolado na venv é usado |
</threat_model>

<verification>
1. Gerar sistema com valores não-default e compilar Python renderizado.
2. Confirmar settings obrigatórios, Tailwind interpolado e porta interna fixa.
3. Inspecionar recursivamente todos os arquivos textuais do destino por `sistema_base` e `Sistema Base`.
4. Confirmar que `.planning`, README do template e demais fontes internas não atravessaram `_exclude`.
</verification>

<success_criteria>
1. TPL-02 não deixa identidade hard-coded no runtime.
2. TPL-04 não deixa a identidade do sistema-modelo no projeto gerado.
3. D-50 cobre explicitamente settings, entrypoint e seed com comportamento `.env`-first ou neutralização comprovada.
4. A porta externa continua configurável sem tornar a porta interna ou o código Python específicos por sistema.
</success_criteria>

<output>Criar `.planning/phases/04-templatiza-o-copier/04-03-SUMMARY.md` ao concluir.</output>
