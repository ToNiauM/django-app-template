---
phase: 04-templatiza-o-copier
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - config/settings/base.py.jinja
  - config/urls.py
  - config/urls.py.jinja
  - core/templates/core/_nav.html
  - core/templates/core/_nav.html.jinja
  - apps/exemplo/README.md
  - "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/"
autonomous: true
requirements: [TPL-03, TPL-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Com incluir_app_exemplo=false, apps/exemplo e somente seus três acoplamentos — settings, urls e nav — ficam ausentes (D-34, D-54)."
    - "Com incluir_app_exemplo=true, app, ExemploConfig, rota e links são renderizados completos e continuam estruturalmente válidos."
    - "Um update que muda a resposta para false pode remover o app porque nenhum caminho opcional usa _skip_if_exists (D-54)."
    - "O README do app descreve o opt-out por Copier e não reintroduz identidade, domínio real ou remoção manual como fluxo principal."
  artifacts:
    - path: "config/settings/base.py.jinja"
      provides: "ExemploConfig condicionado pela resposta Copier"
    - path: "config/urls.py.jinja"
      provides: "Include condicional do app exemplo"
    - path: "core/templates/core/_nav.html.jinja"
      provides: "Resoluções e links condicionais do app exemplo"
    - path: "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/"
      provides: "Pacote inteiro condicionado sem skip_if_exists"
  key_links:
    - from: "incluir_app_exemplo"
      to: "settings + urls + nav + apps/exemplo"
      via: "uma única condição cobre exatamente os quatro destinos previstos"
      pattern: "incluir_app_exemplo"
    - from: "apps/exemplo/README.md.jinja"
      to: "copier update"
      via: "comando --data incluir_app_exemplo=false"
      pattern: "incluir_app_exemplo=false"
---

## Phase Goal

**As a** operador de sistemas, **I want to** gerar e atualizar sistemas Django parametrizados com Copier, **so that** cada sistema derivado permaneça autocontido, portátil e sincronizável com o núcleo.

<objective>
Tornar `apps/exemplo/` verdadeiramente opcional nos quatro destinos exatos definidos por D-34/D-54, sem afetar o núcleo.

Purpose: Preparar TPL-03 para a prova de não-ressurreição e manter TPL-04 na variante com e sem exemplo.
Output: Duas topologias Copier válidas, controladas por uma resposta booleana e documentadas pelo mesmo comando de update.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-app-exemplo/03-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-RESEARCH.md
@.planning/phases/04-templatiza-o-copier/04-PATTERNS.md
@.planning/phases/04-templatiza-o-copier/04-03-SUMMARY.md
</context>

## Artifacts this phase produces

- Caminho condicional `apps/{% if incluir_app_exemplo %}exemplo{% endif %}/` sem `_skip_if_exists`.
- Três acoplamentos condicionais: ExemploConfig, include `/exemplo/` e bloco de navegação.
- README do app renderizado e neutro, com `copier update --data incluir_app_exemplo=false` como comando operacional.
- Provas de geração true/false, parsing Python e ausência exata dos quatro destinos.

<must_haves>
- A condição remove exatamente diretório + settings + urls + nav; core, auth, healthz, shell e infraestrutura ficam intocados.
- As resoluções de URL e os dois links do exemplo formam um único bloco condicional, evitando namespace ausente.
- O caminho opcional não usa `_skip_if_exists`, pois updates precisam preservar a remoção.
- As duas variantes são verificadas no projeto gerado, não apenas por leitura do template-fonte.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Condicionar diretório, settings e URLs do app exemplo</name>
  <reversibility rating="costly">A topologia condicional entra no histórico de update dos derivados; mudar seus caminhos exige teste coordenado.</reversibility>
  <read_first>
    - .planning/phases/03-app-exemplo/03-CONTEXT.md (D-33, D-34, D-35)
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-54)
    - .planning/phases/04-templatiza-o-copier/04-RESEARCH.md (seção "copier update e arquivos opcionais")
    - .planning/phases/04-templatiza-o-copier/04-PATTERNS.md (seção opcionalidade do app exemplo)
    - config/settings/base.py.jinja
    - config/urls.py
    - apps/exemplo/
    - apps/exemplo/tests/test_isolamento.py
  </read_first>
  <files>config/settings/base.py.jinja, config/urls.py, config/urls.py.jinja, "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/"</files>
  <behavior>
    - true gera diretório, ExemploConfig e include de URL.
    - false omite exatamente esses três elementos e preserva admin, healthz e core.
  </behavior>
  <action>
    Renomear o diretório para o caminho Jinja condicional definido pela pesquisa, mantendo sufixos `.jinja` somente nos arquivos que interpolam e sem `_skip_if_exists`. Envolver apenas `apps.exemplo.apps.ExemploConfig` em `config/settings/base.py.jinja` per D-34/D-54. Migrar `config/urls.py` para `.jinja` e envolver somente o include `/exemplo/`; manter admin, healthz e core idênticos. Renderizar true/false e verificar por parsing que settings/urls sem exemplo continuam Python válido e não importam o pacote ausente.
  </action>
  <acceptance_criteria>
    - true gera `apps/exemplo`, ExemploConfig e rota `/exemplo/`.
    - false não gera diretório nem os dois acoplamentos; admin, healthz e core permanecem.
    - Nenhum outro arquivo de config passa a referenciar o app.
  </acceptance_criteria>
  <verify><automated>DEST_APP=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DEST_APP"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Núcleo' --data sistema_slug=nucleo --data sistema_hostname=nucleo.exemplo.gov.br --data sistema_banco=nucleo --data sistema_sigla=SN --data incluir_app_exemplo=false . "$DEST_APP/sem" &amp;&amp; test ! -e "$DEST_APP/sem/apps/exemplo" &amp;&amp; python -m compileall -q "$DEST_APP/sem/config"</automated></verify>
  <done>Diretório, ExemploConfig e include obedecem à mesma resposta booleana sem afetar o núcleo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Condicionar navegação e documentar opt-out persistente</name>
  <read_first>
    - .planning/phases/03-app-exemplo/03-CONTEXT.md (D-33, D-34, D-35)
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-39, D-54, D-55)
    - core/templates/core/_nav.html
    - apps/exemplo/README.md
    - config/settings/base.py.jinja
    - config/urls.py.jinja
  </read_first>
  <files>core/templates/core/_nav.html, core/templates/core/_nav.html.jinja, apps/exemplo/README.md, "apps/{% if incluir_app_exemplo %}exemplo{% endif %}/README.md.jinja"</files>
  <behavior>
    - false renderiza somente o link Início e nunca resolve namespaces ausentes.
    - true preserva Dashboard e Itens (CRUD) completos.
    - O README orienta opt-out via update e preserva a disciplina de banco/check/testes.
  </behavior>
  <action>
    Migrar `_nav.html` para `.jinja` e condicionar, como um único bloco, as duas resoluções de URL e os dois links completos do exemplo, deixando o link Início e o contrato visual intocados. Migrar o README do app para `.jinja`, interpolar `sistema_nome`, remover exemplos de sistemas/domínios reais e declarar `copier update --data incluir_app_exemplo=false` como caminho primário; documentar diretório, settings, urls e nav como os únicos pontos removidos. Preservar o protocolo de banco/check/teste per D-33..D-35/D-54.
  </action>
  <acceptance_criteria>
    - false omite as duas URLs e os dois links, sem alterar o item Início.
    - true preserva navegação, app e README interpolado.
    - O destino não contém identificadores PCA/CFC nem nomes dos sistemas reais citados no projeto.
  </acceptance_criteria>
  <verify><automated>DEST_NAV=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DEST_NAV"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Núcleo' --data sistema_slug=nucleo --data sistema_hostname=nucleo.exemplo.gov.br --data sistema_banco=nucleo --data sistema_sigla=SN --data incluir_app_exemplo=false . "$DEST_NAV/sem" &amp;&amp; grep -Fq "core:shell" "$DEST_NAV/sem/core/templates/core/_nav.html" &amp;&amp; test ! -e "$DEST_NAV/sem/apps/exemplo" &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Núcleo' --data sistema_slug=nucleo --data sistema_hostname=nucleo.exemplo.gov.br --data sistema_banco=nucleo --data sistema_sigla=SN --data incluir_app_exemplo=true . "$DEST_NAV/com" &amp;&amp; test -f "$DEST_NAV/com/apps/exemplo/README.md" &amp;&amp; grep -Fq 'exemplo:dashboard' "$DEST_NAV/com/core/templates/core/_nav.html"</automated></verify>
  <done>Os quatro destinos da opcionalidade ficam sincronizados e documentados para update persistente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resposta booleana → sistema de arquivos/configuração | Omissão do app precisa remover todas e somente suas referências |
| Jinja Copier → template Django | Delimitadores sobrepostos precisam renderizar sem corromper o partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Denial of Service | cópia sem app exemplo | high | mitigate | Matriz true/false verifica diretório e três acoplamentos como unidade e compila config renderizada |
| T-04-10 | Tampering | updates de caminho opcional | high | mitigate | `_skip_if_exists` é proibido; a prova A→B→C do Plano 04-07 valida a não-ressurreição |
| T-04-11 | Information Disclosure | README gerado | medium | mitigate | Texto interpolado e varredura do destino impedem nomes do sistema-modelo e domínios reais |
| T-04-SC | Tampering | CLI Copier | high | mitigate | Somente o binário 9.17.1 aprovado e instalado na venv isolada é usado |
</threat_model>

<verification>
1. Gerar destinos true e false com os mesmos dados não-default.
2. Compilar settings/urls e validar exatamente os quatro destinos condicionais.
3. Confirmar link Início em ambas as variantes e links do exemplo apenas na variante true.
4. Confirmar ausência de `_skip_if_exists` no contrato do app opcional.
</verification>

<success_criteria>
1. D-54 cobre exatamente diretório + três acoplamentos.
2. As variantes true/false são estruturalmente válidas e prontas para update.
3. Opt-out não quebra core, auth, shell, healthz ou infraestrutura.
4. TPL-04 permanece satisfeito na documentação e nos arquivos renderizados.
</success_criteria>

<output>Criar `.planning/phases/04-templatiza-o-copier/04-04-SUMMARY.md` ao concluir.</output>
