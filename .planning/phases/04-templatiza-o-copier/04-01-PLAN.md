---
phase: 04-templatiza-o-copier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - TPL-01
  - TPL-03

estimate:
  tokens: 3000
  raw_tokens: 3000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Nenhuma instalação nem execução do Copier ocorre antes de uma pessoa confirmar a procedência e a versão copier==9.17.1."
    - "A aprovação identifica explicitamente o projeto Copier oficial e a distribuição PyPI que será instalada em ambiente virtual isolado."
  artifacts:
    - path: ".planning/phases/04-templatiza-o-copier/04-01-SUMMARY.md"
      provides: "Registro da aprovação humana da proveniência de copier==9.17.1"
  key_links:
    - from: "04-01-SUMMARY.md"
      to: "04-02-PLAN.md"
      via: "pré-requisito de instalação do CLI Copier"
      pattern: "copier==9.17.1"
---

## Phase Goal

**As a** operador de sistemas, **I want to** gerar e atualizar sistemas Django parametrizados com Copier, **so that** cada sistema derivado permaneça autocontido, portátil e sincronizável com o núcleo.

<objective>
Interromper a execução antes de qualquer instalação ou uso do Copier e obter confirmação humana explícita da legitimidade da distribuição `copier==9.17.1`, classificada como `SUS` pela auditoria da pesquisa.

Purpose: Fechar o risco de cadeia de suprimentos obrigatório antes do tracer de `copier copy` e da prova de `copier update` (TPL-01, TPL-03).
Output: Sinal humano inequívoco autorizando a instalação pinada em ambiente virtual local e isolado.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-RESEARCH.md
</context>

## Artifacts this phase produces

- Sinal de retomada `aprovado copier==9.17.1`, registrado em `04-01-SUMMARY.md`.
- Contrato operacional para o plano seguinte: instalar somente `copier==9.17.1` em `.venv-template/`, nunca globalmente e nunca como dependência Django.

<must_haves>
- A auditoria humana antecede instalação e execução do CLI.
- A aprovação vincula nome do pacote, versão, registro PyPI e projeto oficial; uma aprovação genérica não satisfaz o gate.
- A rejeição ou dúvida mantém a fase parada, sem tentativa de fallback por pacote homônimo.
</must_haves>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirmar procedência de copier==9.17.1 antes de instalar ou executar</name>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-RESEARCH.md (seções "Auditoria de Legitimidade de Pacotes", "Stack Padrão" e "Ambiente Disponível")
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-37, D-40, D-53 e D-56)
    - .planning/PROJECT.md (mecanismo Copier como restrição fechada)
  </read_first>
  <action>
    Apresentar à pessoa a distribuição PyPI `copier`, versão exata `9.17.1`, e o vínculo com o projeto oficial documentado em `https://copier.readthedocs.io/`. Não instalar pacote, não criar ambiente virtual e não executar binário neste plano. Solicitar que a pessoa confira a página `https://pypi.org/project/copier/9.17.1/`, o histórico/maintainers e o destino oficial da documentação; só o sinal de retomada exato autoriza o Plano 04-02. Esta separação é obrigatória porque a auditoria marcou o pacote como `SUS` e o CLI ainda não existe na máquina.
  </action>
  <what-built>Gate de cadeia de suprimentos preparado; nenhuma mutação de ambiente foi realizada.</what-built>
  <how-to-verify>
    1. Confirmar que a página PyPI corresponde ao gerador de projetos Copier documentado em copier.readthedocs.io.
    2. Confirmar que a versão a instalar é exatamente 9.17.1.
    3. Confirmar que a instalação será feita apenas em `.venv-template/`, fora de `requirements.txt` e fora do Python global.
  </how-to-verify>
  <acceptance_criteria>
    - A pessoa responde literalmente `aprovado copier==9.17.1` após conferir registro e projeto oficial.
    - Antes desse sinal, `copier` continua ausente do PATH e `.venv-template/bin/copier` não existe.
    - Nenhum arquivo de dependências da aplicação é alterado.
  </acceptance_criteria>
  <verify>
    <automated>test ! -x .venv-template/bin/copier &amp;&amp; ! command -v copier</automated>
    <human-check>Conferência de procedência e versão concluída nos endereços acima.</human-check>
  </verify>
  <done>Aprovação explícita registrada; o executor pode retomar no Plano 04-02 e instalar apenas a versão pinada em ambiente isolado.</done>
  <resume-signal>Digite `aprovado copier==9.17.1` para continuar, ou descreva a divergência encontrada.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registro PyPI → ambiente de desenvolvimento | Código de pacote externo passará a executar localmente somente após aprovação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | pacote `copier` no PyPI | high | mitigate | Checkpoint humano bloqueante confirma projeto, registro e versão antes da instalação; o plano seguinte instala `copier==9.17.1` em venv isolada |
| T-04-SC | Tampering | instalação pip | high | mitigate | Auditoria de legitimidade + versão pinada + proibição de instalação global ou inclusão em `requirements.txt` |
</threat_model>

<verification>
1. `test ! -x .venv-template/bin/copier` passa antes do sinal humano.
2. `command -v copier` continua sem resultado.
3. O resumo registra exatamente o pacote/versão aprovados e o sinal de retomada.
</verification>

<success_criteria>
1. Procedência de `copier==9.17.1` confirmada por uma pessoa.
2. Nenhuma instalação ou execução ocorreu antes da confirmação.
3. O Plano 04-02 dispõe de autorização específica, auditável e não genérica.
</success_criteria>

<output>
Criar `.planning/phases/04-templatiza-o-copier/04-01-SUMMARY.md` ao concluir a execução.
</output>
