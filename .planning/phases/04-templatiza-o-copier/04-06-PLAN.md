---
phase: 04-templatiza-o-copier
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - ops/backup/ensaio_restore_local.sh
  - ops/backup/testar_ensaio_restore.sh
  - "ops/nginx/{{ sistema_slug }}.conf.jinja"
  - ops/MIGRACAO.md.jinja
  - README.md.jinja
autonomous: true
requirements: [TPL-04, INF-03, INF-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O ensaio restaura dump custom em recursos efêmeros únicos e remove somente container, rede e volume que criou (D-64)."
    - "O vhost renderizado termina TLS, redireciona HTTP e encaminha ao loopback/porta respondidos com headers confiáveis (D-61, D-62)."
    - "O runbook cobre dump + .env + compose up + migrate + healthz + proxy/TLS/DNS sem volume externo ou dependência da aplicação no host (D-63, INF-04)."
    - "Restore, Nginx e documentação não transportam identificadores, incidentes, buckets, volumes ou lógica PCA (TPL-04)."
  artifacts:
    - path: "ops/backup/ensaio_restore_local.sh"
      provides: "Restore isolado, migrations e Django check"
    - path: "ops/backup/testar_ensaio_restore.sh"
      provides: "Prova simulada de cleanup confinado"
    - path: "ops/nginx/{{ sistema_slug }}.conf.jinja"
      provides: "Vhost TLS interpolado"
    - path: "ops/MIGRACAO.md.jinja"
      provides: "Runbook portátil de migração"
  key_links:
    - from: "ops/backup/ensaio_restore_local.sh"
      to: "compose.yml.jinja"
      via: "dump mais recente, imagem web e rede/volume isolados pelo slug"
      pattern: "pg_restore.*--clean.*--if-exists.*--no-owner"
    - from: "ops/nginx/{{ sistema_slug }}.conf.jinja"
      to: "compose.yml.jinja"
      via: "127.0.0.1 + sistema_porta/WEB_PORT"
      pattern: "proxy_pass.*127.0.0.1"
---

## Phase Goal

**As a** operador de sistemas, **I want to** gerar e atualizar sistemas Django parametrizados com Copier, **so that** cada sistema derivado permaneça autocontido, portátil e sincronizável com o núcleo.

<objective>
Completar a operação portátil com ensaio de restore confinado, vhost Nginx TLS e runbook de migração generalizado (D-61..D-64).

Purpose: Fechar INF-03/INF-04 separando os riscos de restauração/proxy/documentação do ciclo de backup/Compose já provado.
Output: Restore testável, vhost pronto para copiar e MIGRACAO.md executável sem dependências da aplicação no host.
</objective>

<execution_context>
@/home/ToNiauM/.codex/gsd-core/workflows/execute-plan.md
@/home/ToNiauM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-templatiza-o-copier/04-CONTEXT.md
@.planning/phases/04-templatiza-o-copier/04-RESEARCH.md
@.planning/phases/04-templatiza-o-copier/04-PATTERNS.md
@.planning/phases/04-templatiza-o-copier/04-05-SUMMARY.md
@/opt/web/pca/ops/backup/ensaio_restore_local.sh
@/opt/web/pca/ops/nginx/pca.conf
@/opt/web/pca/ops/MIGRACAO.md
</context>

## Artifacts this phase produces

- `ops/backup/{ensaio_restore_local.sh,testar_ensaio_restore.sh}`.
- `ops/nginx/{{ sistema_slug }}.conf.jinja`, renderizado como `ops/nginx/<slug>.conf`.
- `ops/MIGRACAO.md.jinja`, renderizado como `ops/MIGRACAO.md`.
- README gerado ligado ao runbook e à recomendação periódica de restore.

<must_haves>
- `/opt/web/pca/**` permanece somente leitura; extrair a orquestração e remover todo contexto específico.
- Restore nunca usa prune, glob destrutivo, nomes de produção ou Compose de produção.
- Nginx sobrescreve headers de proxy, termina TLS e encaminha somente ao loopback.
- O runbook não exige Python/pip/rclone/cron da aplicação no host nem volume externo pré-criado.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalizar ensaio de restore com cleanup confinado</name>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-57, D-59, D-64)
    - /opt/web/pca/ops/backup/ensaio_restore_local.sh (somente leitura)
    - ops/backup/backup.sh
    - compose.yml.jinja
    - ops/backup/ensaio_restore_local.sh (novo)
    - ops/backup/testar_ensaio_restore.sh (novo)
  </read_first>
  <files>ops/backup/ensaio_restore_local.sh, ops/backup/testar_ensaio_restore.sh</files>
  <behavior>
    - Restore obtém o dump mais recente, restaura em DB isolado e roda migrate --plan/noinput + check.
    - Sucesso, erro e interrupção removem somente recursos marcados como criados pelo ensaio.
  </behavior>
  <action>
    Reescrever o ensaio sem consulta/modelo/volume/imagem/identificador PCA: gerar nomes `${SISTEMA_SLUG}_rehearsal_&lt;PID&gt;`, registrar flags somente após criar container/rede/volume, descobrir o dump custom mais recente, restaurar com `pg_restore --clean --if-exists --no-owner` e executar `migrate --plan`, `migrate --noinput` e `manage.py check` na imagem do sistema. A função cleanup remove explicitamente apenas recursos com flag registrada; proibir `docker system prune`, `docker volume prune`, globs e Compose de produção. Criar teste com shims de docker/rclone que simule sucesso, falha e interrupção, provando a lista exata de recursos criados/removidos sem tocar Docker real per D-64.
  </action>
  <acceptance_criteria>
    - Scripts usam `set -eu`, trap e nomes únicos por slug/PID.
    - Restore inclui dump custom, migrations e Django check.
    - Teste prova que recursos preexistentes e produção não recebem comando de remoção.
  </acceptance_criteria>
  <verify><automated>sh -n ops/backup/ensaio_restore_local.sh ops/backup/testar_ensaio_restore.sh &amp;&amp; sh ops/backup/testar_ensaio_restore.sh</automated></verify>
  <done>Restore é portátil e sua limpeza fica confinada aos recursos efêmeros registrados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Renderizar vhost TLS e runbook completo de migração</name>
  <read_first>
    - .planning/phases/04-templatiza-o-copier/04-CONTEXT.md (D-61, D-62, D-63, D-64)
    - /opt/web/pca/ops/nginx/pca.conf (somente leitura)
    - /opt/web/pca/ops/MIGRACAO.md (somente leitura)
    - compose.yml.jinja
    - .env.example.jinja
    - README.md.jinja
  </read_first>
  <files>"ops/nginx/{{ sistema_slug }}.conf.jinja", ops/MIGRACAO.md.jinja, README.md.jinja</files>
  <behavior>
    - Hostname/porta aparecem no arquivo, server_name, certificados e proxy_pass.
    - Nginx define Host, X-Real-IP, X-Forwarded-For e X-Forwarded-Proto.
    - Runbook não exige volume externo nem dependência da aplicação no host.
  </behavior>
  <action>
    Criar vhost per D-61/D-62 com HTTPS 443, certificados Let's Encrypt do hostname, redirect 80→HTTPS, `proxy_pass http://127.0.0.1:{{ sistema_porta }}`, HTTP/1.1, timeouts e os quatro `proxy_set_header` definidos por variáveis do Nginx. Generalizar `MIGRACAO.md` per D-63: preparar Docker/Compose e proxy TLS, transferir dump e `.env`, restaurar, subir Compose, rodar migrate, testar `/healthz`, instalar/testar vhost, certificado e DNS. Documentar ensaio periódico e cleanup per D-64; manter rclone no container backup e não adicionar volume externo ou dependência da aplicação no host. Remover todo nome, incidente, imagem, bucket, volume e lógica de negócio da fonte PCA; ligar o README gerado ao runbook.
  </action>
  <acceptance_criteria>
    - Cópia aurora gera `ops/nginx/aurora.conf` com hostname e porta 8123.
    - TLS, redirect e quatro headers estão presentes.
    - Runbook contém a cadeia dump + `.env` + compose up + migrate + proxy/TLS/DNS.
  </acceptance_criteria>
  <verify><automated>DEST_RUNBOOK=$(mktemp -d) &amp;&amp; trap 'rm -rf "$DEST_RUNBOOK"' EXIT &amp;&amp; .venv-template/bin/copier copy --defaults --data sistema_nome='Sistema Aurora' --data sistema_slug=aurora --data sistema_hostname=aurora.exemplo.gov.br --data sistema_porta=8123 --data sistema_banco=aurora --data sistema_sigla=SA . "$DEST_RUNBOOK/gerado" &amp;&amp; test -f "$DEST_RUNBOOK/gerado/ops/nginx/aurora.conf" &amp;&amp; grep -Fq 'http://127.0.0.1:8123' "$DEST_RUNBOOK/gerado/ops/nginx/aurora.conf" &amp;&amp; grep -Fq 'pg_restore --clean --if-exists --no-owner' "$DEST_RUNBOOK/gerado/ops/MIGRACAO.md"</automated></verify>
  <done>O gerado traz restore disciplinado, vhost TLS e migração completa sem conteúdo PCA ou dependência da aplicação no host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ensaio restore → Docker host | Script cria e destrói recursos locais efêmeros |
| Internet → Nginx → web loopback | Proxy termina TLS e estabelece headers confiáveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Tampering | ensaio restore | high | mitigate | Nomes slug+PID, flags de criação, cleanup explícito e teste com shims impedem remoção fora do escopo |
| T-04-18 | Spoofing | headers proxy | high | mitigate | Nginx sobrescreve Host, IP e protocolo com variáveis da conexão |
| T-04-19 | Information Disclosure | runbook/vhost | medium | mitigate | Varredura do gerado elimina nomes, domínios, buckets e incidentes da referência PCA |
| T-04-20 | Elevation of Privilege | exposição web | high | mitigate | Proxy aponta ao loopback e TLS é a única entrada pública documentada |
</threat_model>

<verification>
1. Executar sintaxe e teste simulado do restore.
2. Renderizar sistema não-default e validar vhost/headers/TLS.
3. Validar cadeia completa do runbook e ausência de dependência da aplicação no host.
4. Varrer artefatos gerados por identidade e detalhes PCA.
</verification>

<success_criteria>
1. INF-03 inclui ensaio de restore e vhost Nginx completos.
2. INF-04 prova migração portátil, loopback e cleanup confinado.
3. D-61..D-64 estão cobertas por artefatos e verificações específicas.
4. Nenhum arquivo gerado contém contexto operacional da PCA.
</success_criteria>

<output>Criar `.planning/phases/04-templatiza-o-copier/04-06-SUMMARY.md` ao concluir.</output>
