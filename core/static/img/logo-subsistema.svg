<!-- Placeholder neutro (D-66): substitua este arquivo pela arte oficial
     mantendo o nome logo-subsistema.svg — nenhuma edição de código é
     necessária. Em produção, após a troca, rode:
     docker compose build && docker compose up -d
     (comentário XML não admite hífen duplo, por isso a forma expandida). -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img">
  <!-- Identidade "deste sistema": quadrado de cantos arredondados com
       losango interno — forma distinta do logo da entidade de propósito,
       para o operador diferenciá-los de relance. -->
  <rect x="6" y="6" width="52" height="52" rx="10" fill="none" stroke="#9ca3af" stroke-width="4"/>
  <path d="M32 18 46 32 32 46 18 32Z" fill="none" stroke="#9ca3af" stroke-width="4"/>
</svg>
